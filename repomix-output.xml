This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.agents/skills/Dev/SKILL.md
.gitignore
latex/border.jpeg
latex/logo.jpeg
latex/main.aux
latex/main.fdb_latexmk
latex/main.fls
latex/main.out
latex/main.pdf
latex/main.tex
latex/main.toc
latex/sections/project_overview.tex
latex/titlepage.tex
src/configs/avenue.yaml
src/configs/ucsd_ped1.yaml
src/configs/ucsd_ped2.yaml
src/csv_to_arff.py
src/doc/Kmean.md
src/doc/prd.md
src/doc/spec_1_processed.md
src/doc/spec_1.md
src/doc/spec_2_processed.md
src/doc/spec_2.md
src/doc/spec_3_processed.md
src/doc/spec_3.md
src/doc/spec_4_processed.md
src/doc/spec_4.md
src/doc/spec_5_processed.md
src/doc/spec_5.md
src/kpdl_anomaly/__init__.py
src/kpdl_anomaly/alerts.py
src/kpdl_anomaly/association.py
src/kpdl_anomaly/config.py
src/kpdl_anomaly/feature_selection.py
src/kpdl_anomaly/frames.py
src/kpdl_anomaly/io.py
src/kpdl_anomaly/modeling.py
src/kpdl_anomaly/qualitative.py
src/kpdl_anomaly/rule_model.py
src/kpdl_anomaly/rule_scoring.py
src/kpdl_anomaly/schema.py
src/kpdl_anomaly/scoring.py
src/kpdl_anomaly/smoothing.py
src/kpdl_anomaly/test.py
src/kpdl_anomaly/thresholds.py
src/kpdl_anomaly/tokenization.py
src/kpdl_anomaly/train.py
src/kpdl_anomaly/visualization.py
src/kpdl_preprocess/__init__.py
src/kpdl_preprocess/arff_cli.py
src/kpdl_preprocess/arff.py
src/kpdl_preprocess/cli.py
src/kpdl_preprocess/config.py
src/kpdl_preprocess/datasets.py
src/kpdl_preprocess/features.py
src/kpdl_preprocess/grid.py
src/kpdl_preprocess/pipeline.py
src/kpdl_preprocess/readers.py
src/kpdl_preprocess/records.py
src/kpdl_preprocess/schema.py
src/kpdl_preprocess/utils.py
src/preprocess.py
src/repomix-output.xml
src/requirements.txt
src/rules.py
src/test.py
src/tool/__init__.py
src/tool/_common.py
src/tool/preprocess_avenue.py
src/tool/preprocess_ucsd.py
src/train.py
src/visualize.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/doc/spec_5_processed.md">
# SPEC 5 Processed - Tokenization, rare token score and association rules

Original spec path: `src/doc/spec_5.md`

Date/time processed: 2026-05-27 11:28:44 +07:00

## Summary

Implemented SPEC 5 in the Python pipeline. The project now trains a token/rule artifact from SPEC 3 features and models, scores test rows with `rare_token_score` and `rule_violation_score`, appends token/rule columns only when rule scoring is active, and preserves the old SPEC 3 `cell_scores.csv` schema when rules are disabled.

Primary verified dataset: `ucsd_ped2`.

## Checklist

- [Done] Read PRD, root `repomix-output.xml`, and `src/doc/spec_5.md`.
- [Done] Add `rules` config to dataset YAML files.
- [Done] Update `AnomalyConfig` with rule settings and score weights.
- [Done] Add `src/kpdl_anomaly/tokenization.py`.
- [Done] Fit token bin thresholds from train normal rows.
- [Done] Tokenize feature rows into stable transactions.
- [Done] Assign `cluster=Cx` to train rows from SPEC 3 scaler/model artifacts.
- [Done] Add `src/kpdl_anomaly/association.py`.
- [Done] Count itemset support for bounded itemsets of size `1..3`.
- [Done] Generate bounded Apriori association rules by support/confidence/lift.
- [Done] Add `src/kpdl_anomaly/rule_model.py`.
- [Done] Write `rule_manifest.json`, `token_bins.json`, `itemsets.json`, `rules.json`, `token_stats.json`, and `selected_rules.md`.
- [Done] Add CLI `src/rules.py`.
- [Done] Add `src/kpdl_anomaly/rule_scoring.py`.
- [Done] Compute `rare_token_score`.
- [Done] Compute `rule_violation_score`.
- [Done] Extend `src/kpdl_anomaly/scoring.py` to use rules when active.
- [Done] Extend score CSV writer with token/rule columns when active.
- [Done] Extend alert reasons with token/rule reasons.
- [Done] Add `src/test.py` support for `--rules` and `--no-rules`.
- [Done] Smoke train token/rule artifact.
- [Done] Smoke scoring with token/rule artifact.
- [Done] Smoke fallback scoring with `--no-rules`.
- [Done] Full UCSD Ped2 token/rule training.
- [Done] Full UCSD Ped2 scoring with rules.
- [Done] Visualization smoke for SPEC 4 compatibility.
- [Done] Refresh `repomix-output.xml` after final checks.

## Files Changed

- `src/configs/ucsd_ped2.yaml`
- `src/configs/ucsd_ped1.yaml`
- `src/configs/avenue.yaml`
- `src/kpdl_anomaly/config.py`
- `src/kpdl_anomaly/schema.py`
- `src/kpdl_anomaly/tokenization.py`
- `src/kpdl_anomaly/association.py`
- `src/kpdl_anomaly/rule_model.py`
- `src/kpdl_anomaly/rule_scoring.py`
- `src/kpdl_anomaly/scoring.py`
- `src/kpdl_anomaly/alerts.py`
- `src/kpdl_anomaly/test.py`
- `src/rules.py`
- `src/doc/spec_5_processed.md`

## Verification Evidence

- [Done] `python -m compileall src\kpdl_anomaly src\rules.py src\test.py`
  - Result: compile completed successfully.
- [Done] `python src\rules.py --config src\configs\ucsd_ped2.yaml --model src\outputs\models\ucsd_ped2 --output-dir src\outputs\rules\ucsd_ped2_smoke --limit-rows 50000`
  - Result: `50000` transactions, `12877` itemsets, `200` rules.
- [Done] `python src\test.py --config src\configs\ucsd_ped2.yaml --model src\outputs\models\ucsd_ped2 --rules src\outputs\rules\ucsd_ped2_smoke --result-root src\outputs\results_smoke_rules --limit-rows 50000`
  - Result: `50000` rows scored, `261` frames, `1` alert, rules active.
- [Done] `python src\test.py --config src\configs\ucsd_ped2.yaml --model src\outputs\models\ucsd_ped2 --no-rules --result-root src\outputs\results_smoke_no_rules --limit-rows 50000`
  - Result: `50000` rows scored, SPEC 3 score columns only, first row score remains `0.39380409`.
- [Done] `python src\rules.py --config src\configs\ucsd_ped2.yaml --model src\outputs\models\ucsd_ped2`
  - Result: `477312` transactions, `13932` itemsets, `200` rules, no warnings.
- [Done] `python src\test.py --config src\configs\ucsd_ped2.yaml --model src\outputs\models\ucsd_ped2 --rules src\outputs\rules\ucsd_ped2`
  - Result: `376704` rows scored, `1962` frames, severity `none=844`, `medium=1118`, `high=0`, alerts `17`, rules active.
- [Done] Range check on full `cell_scores.csv`
  - Result: `376704` rows, `0` out-of-range values; `cell_score=[0.0897185, 0.9734689]`, `rare_token_score=[0.83176622, 1.0]`, `rule_violation_score=[0.0, 1.0]`.
- [Done] Alert reason check
  - Result: `alerts.json` includes token support reasons and rule violation reasons.
- [Done] Support/confidence sweep smoke
  - `min_support=0.005`, `min_confidence=0.60`, `limit_rows=50000`: `12877` itemsets, `200` rules.
  - `min_support=0.010`, `min_confidence=0.60`, `limit_rows=50000`: `12877` itemsets, `200` rules.
  - `min_support=0.020`, `min_confidence=0.70`, `limit_rows=50000`: `12877` itemsets, `200` rules.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --top-frames 5 --output-dir src\outputs\visualizations\ucsd_ped2_spec5_top`
  - Result: 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --alerts --limit-frames 5 --output-dir src\outputs\visualizations\ucsd_ped2_spec5_alerts`
  - Result: 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] Automated OpenCV visualization validation
  - Result: 10 images checked, 0 missing, 0 blank.
- [Done] `repomix.cmd`
  - Result: Repomix v1.14.0 packed 69 files successfully and wrote `repomix-output.xml`.

## Generated Artifacts

- `src/outputs/rules/ucsd_ped2/rule_manifest.json`
- `src/outputs/rules/ucsd_ped2/token_bins.json`
- `src/outputs/rules/ucsd_ped2/itemsets.json`
- `src/outputs/rules/ucsd_ped2/rules.json`
- `src/outputs/rules/ucsd_ped2/token_stats.json`
- `src/outputs/rules/ucsd_ped2/selected_rules.md`
- `src/outputs/results/ucsd_ped2/cell_scores.csv`
- `src/outputs/results/ucsd_ped2/frame_scores.csv`
- `src/outputs/results/ucsd_ped2/alerts.json`
- `src/outputs/results/ucsd_ped2/scoring_stats.json`

## Remaining Risks

- Full support/confidence sweep was not run for all three settings; the sweep evidence is smoke-sized at `50000` rows to keep runtime controlled.
- Direction tokens remain disabled because current preprocessing uses frame differencing, as requested by SPEC 5.
- Rare token score is intentionally a weak weighted signal, but many UCSD Ped2 test cell/cube combinations are rare under the chosen cell-specific itemsets; future metric work should tune support thresholds and weights.
</file>

<file path="src/doc/spec_5.md">
# SPEC 5 - Tokenization, rare token score và association rules trong Python

## 1. Mục tiêu

SPEC 5 mô tả kế hoạch triển khai lớp token hóa và khai phá luật kết hợp trong Python sau khi SPEC 3 đã có anomaly score baseline và SPEC 4 đã có heatmap overlay.

Mục tiêu chính:

- biến feature liên tục và kết quả cụm `nearest_cluster` thành token dễ diễn giải;
- học phân phối token/itemset từ dữ liệu train normal;
- khai phá association rules từ train normal;
- tính `rare_token_score` và `rule_violation_score` cho từng cell/cube test;
- mở rộng công thức anomaly score theo PRD:

```text
score = 0.65 * cluster_distance_score
      + 0.20 * temporal_change_score
      + 0.10 * rare_token_score
      + 0.05 * rule_violation_score
```

SPEC 5 không thay thế cluster/distance baseline của SPEC 3. Token/rule chỉ là tín hiệu phụ để tăng khả năng giải thích và hỗ trợ phát hiện tổ hợp hành vi hiếm.

## 2. Liên hệ với PRD và các spec trước

Theo PRD, hệ thống có hai trụ cột:

- phân cụm hành vi-không gian-thời gian;
- khai phá luật kết hợp để giải thích quan hệ giữa vùng, chuyển động, mật độ, độ sáng và cụm hành vi.

SPEC 1 đã sinh feature CSV/ARFF. SPEC 2 mô tả thực hành WEKA với KMeans K=5 và Apriori. SPEC 3 đã triển khai Python per-cell KMeans K=5, sinh `cell_scores.csv`, `frame_scores.csv` và `alerts.json`. SPEC 4 đã hiển thị score bằng heatmap overlay.

SPEC 5 chuyển phần token/rule từ ý tưởng và thực hành thủ công sang pipeline Python chạy lại được bằng config.

Ghi chú trạng thái repo hiện tại:

- `src/doc/spec_5.md` chưa tồn tại trước tài liệu này.
- SPEC 2 processed hiện là tracker thực hành trống, chưa có output WEKA hoàn chỉnh.
- SPEC 5 vẫn có thể triển khai vì cluster assignment có thể lấy trực tiếp từ model Python của SPEC 3.

## 3. Trạng thái đầu vào hiện có

Dataset ưu tiên:

```text
ucsd_ped2
```

Artifact đầu vào chính:

```text
src/outputs/preprocessed/ucsd_ped2/
  features_train.csv
  features_test.csv
  grid.json
  preprocess_stats.json

src/outputs/models/ucsd_ped2/
  model_manifest.json
  cell_models.joblib
  cell_scalers.joblib
  thresholds.json
  feature_stats.json

src/outputs/results/ucsd_ped2/
  cell_scores.csv
  frame_scores.csv
  alerts.json
  scoring_stats.json
```

Feature baseline đang dùng:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Các cột `direction_hist_0..7` chưa dùng trong SPEC 5 MVP vì pipeline tiền xử lý hiện dùng `frame_diff`, direction histogram đang không mang thông tin hướng đáng tin cậy. Hướng chuyển động thật nên để SPEC sau cùng optical flow/Farneback.

## 4. Phạm vi

### 4.1. Nằm trong phạm vi

- Đọc feature train/test và model SPEC 3.
- Gán `cluster_id=C0..C4` cho train bằng model/scaler đã lưu.
- Dùng `nearest_cluster` từ scoring test hoặc tự gán khi cần.
- Fit bin/token thresholds từ train normal.
- Token hóa motion, density, brightness, brightness delta, cell và cluster.
- Tạo transaction cho từng feature row.
- Đếm support của item và itemset trong train normal.
- Sinh association rules giới hạn kích thước để chạy được trên UCSD Ped2.
- Tính rare-token score và rule-violation score cho test.
- Ghi output token/rule artifact.
- Mở rộng scoring để tạo `cell_scores.csv` có thêm token/rule columns khi `rules.enabled=true`.
- Mở rộng `alerts.json` reason bằng token/rule reason.
- Bảo toàn khả năng chạy SPEC 3 cũ khi rules disabled.

### 4.2. Ngoài phạm vi

- Không triển khai FP-Growth tối ưu cho dữ liệu rất lớn trong MVP đầu tiên.
- Không dùng WEKA làm runtime production.
- Không dùng direction token thật khi chưa có optical flow.
- Không đánh giá ROC-AUC/EER, phần đó để SPEC 6.
- Không làm dashboard.
- Không thay đổi heatmap rendering của SPEC 4, chỉ bảo đảm schema mới không làm hỏng visualization.

## 5. Nguyên tắc thiết kế

- Token/rule là tín hiệu phụ, không quyết định toàn bộ alert.
- Binning phải học từ train normal, sau đó áp dụng nguyên trạng cho test.
- Không tạo token quá mịn khiến luật bị vụn.
- Các rule phải có support/confidence/lift đủ rõ và có thể diễn giải.
- Nếu rule artifact thiếu hoặc disabled, pipeline scoring phải quay về công thức SPEC 3.
- Output phải truy vết được về `dataset`, `video_id`, `cube_id`, `center_frame_id`, `cell_id`.
- Giữ dependency nhẹ. MVP nên dùng Apriori giới hạn tự triển khai thay vì thêm dependency nặng nếu không cần.

## 6. Kiến trúc module đề xuất

Bổ sung các file:

```text
src/kpdl_anomaly/
  tokenization.py
  association.py
  rule_model.py
  rule_scoring.py

src/rules.py
```

Vai trò:

```text
tokenization.py:
  fit ngưỡng rời rạc hóa từ train
  chuyển feature row + cluster_id thành token
  tạo transaction ổn định

association.py:
  đếm support itemset
  sinh association rules dạng bounded Apriori
  lọc rule theo support/confidence/lift

rule_model.py:
  train token/rule model từ features_train.csv và model SPEC 3
  lưu token bins, itemset support, rules, manifest

rule_scoring.py:
  load rule artifact
  tính rare_token_score
  tính rule_violation_score
  sinh reason text cho token/rule

src/rules.py:
  CLI entrypoint cho train token/rule artifact
```

Các file cần cập nhật:

```text
src/kpdl_anomaly/config.py
src/kpdl_anomaly/schema.py
src/kpdl_anomaly/scoring.py
src/kpdl_anomaly/alerts.py
src/kpdl_anomaly/test.py
src/configs/ucsd_ped2.yaml
src/configs/ucsd_ped1.yaml
src/configs/avenue.yaml
src/requirements.txt nếu cần
```

## 7. Cấu hình đề xuất

Bổ sung mục `rules` vào YAML dataset:

```yaml
rules:
  enabled: true
  output_root: "src/outputs/rules"
  model_dir: null
  algorithm: "bounded_apriori"
  min_support: 0.01
  min_confidence: 0.60
  min_lift: 1.05
  max_itemset_size: 3
  max_rules: 200
  include_cell_token: true
  include_cluster_token: true
  include_brightness_token: true
  include_direction_token: false
  rare_itemset_size: 3
  rare_support_floor: 0.001
  rare_score_cap: 1.0
```

Cập nhật `scoring` khi rules enabled:

```yaml
scoring:
  cluster_weight: 0.65
  temporal_weight: 0.20
  rare_token_weight: 0.10
  rule_weight: 0.05
```

Khi `rules.enabled=false` hoặc không có rule artifact, giữ công thức SPEC 3:

```yaml
scoring:
  cluster_weight: 0.80
  temporal_weight: 0.20
```

## 8. Token schema MVP

Mỗi feature row test/train sinh một transaction gồm các token:

```text
cell=08_05
cell_row=08
cell_col=05
motion=still|slow|medium|fast|very_fast
density=low|medium|high
brightness=dark|normal|bright
brightness_delta=stable|changing
cluster=C0|C1|C2|C3|C4
```

Token có thể thêm sau:

```text
foreground=low|medium|high
motion_std=low|medium|high
time_bucket=morning|day|evening|night
direction=...
```

Không đưa các token sau vào MVP:

```text
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
```

Lý do: các token định danh/thứ tự dễ tạo luật giả, không mô tả hành vi bình thường tổng quát.

## 9. Fit bin từ train normal

### 9.1. Motion magnitude

Dùng quantile train của `motion_magnitude_mean`:

```text
still      <= q20
slow       <= q40
medium     <= q60
fast       <= q80
very_fast  >  q80
```

Nếu phân phối quá nhiều zero, dùng fallback:

```text
still nếu motion_magnitude_mean <= epsilon
slow/medium/fast/very_fast theo quantile của phần > epsilon
```

### 9.2. Motion density

Dùng 3 bin:

```text
low     <= q33
medium  <= q66
high    >  q66
```

### 9.3. Brightness

Dùng 3 bin:

```text
dark    <= q33
normal  <= q66
bright  >  q66
```

### 9.4. Brightness delta

Dùng absolute value:

```text
stable    nếu abs(brightness_delta) <= q80_abs_delta
changing  nếu abs(brightness_delta) > q80_abs_delta
```

### 9.5. Scope của bin

MVP fit bin theo global train normal để token dễ ổn định và ít vụn. Sau đó có thể mở rộng:

- bin theo từng dataset;
- bin theo cell;
- bin theo zone semantic nếu có.

## 10. Train token/rule model

### Bước 1 - Load artifact

Đọc:

```text
features_train.csv
model_manifest.json
cell_models.joblib
cell_scalers.joblib
thresholds.json
```

Kiểm tra:

- dataset khớp config;
- feature columns khớp model;
- model KMeans có đủ cell;
- train feature có `split=train`.

### Bước 2 - Gán cluster cho train rows

Với mỗi train row:

1. lấy vector feature giống SPEC 3;
2. dùng scaler/model theo `cell_id`;
3. tìm centroid gần nhất;
4. tạo `cluster=Cx`.

Nếu cell fallback không có model:

```text
cluster=unknown
```

và không dùng row đó để sinh rule có cluster nếu số lượng fallback đáng kể.

### Bước 3 - Fit token bins

Fit bin thresholds từ train normal và lưu:

```text
token_bins.json
```

Payload:

```json
{
  "motion_magnitude_mean": {"q20": 0.0, "q40": 0.0, "q60": 0.0, "q80": 0.0},
  "motion_density": {"q33": 0.0, "q66": 0.0},
  "brightness_mean": {"q33": 0.0, "q66": 0.0},
  "brightness_delta_abs": {"q80": 0.0}
}
```

### Bước 4 - Tạo transactions

Mỗi transaction:

```json
{
  "dataset": "ucsd_ped2",
  "split": "train",
  "video_id": "Train001",
  "cube_id": "Train001_000001",
  "center_frame_id": 3,
  "cell_id": "08_05",
  "tokens": [
    "cell=08_05",
    "motion=slow",
    "density=low",
    "brightness=normal",
    "brightness_delta=stable",
    "cluster=C3"
  ]
}
```

MVP không nhất thiết phải lưu toàn bộ transaction JSONL nếu muốn tiết kiệm dung lượng, nhưng nên có tùy chọn `--write-transactions` để debug.

### Bước 5 - Count itemsets

Đếm support cho itemset kích thước `1..max_itemset_size`.

Mặc định:

```text
max_itemset_size = 3
min_support = 0.01
```

Với mỗi transaction có khoảng 6-8 token, số combination nhỏ và bounded Apriori có thể chạy được trên UCSD Ped2.

### Bước 6 - Sinh rules

Từ frequent itemsets kích thước >= 2:

```text
antecedent -> consequent
confidence = support(antecedent U consequent) / support(antecedent)
lift = confidence / support(consequent)
```

Lọc:

```text
support >= min_support
confidence >= min_confidence
lift >= min_lift
```

Ưu tiên rules có consequent thuộc:

```text
motion=...
density=...
cluster=...
brightness=...
```

Không chọn rule chỉ toàn vị trí:

```text
cell_row=08 -> cell_col=05
```

## 11. Output token/rule artifact

Thư mục:

```text
src/outputs/rules/{dataset}/
  rule_manifest.json
  token_bins.json
  itemsets.json
  rules.json
  selected_rules.md
  token_stats.json
  train_tokens_sample.jsonl
```

`rule_manifest.json`:

```text
schema_version
dataset
created_at
config_path
source_model_dir
train_feature_path
feature_columns
token_schema
num_transactions
num_itemsets
num_rules
min_support
min_confidence
min_lift
max_itemset_size
warnings
```

`itemsets.json`:

```json
[
  {
    "items": ["cell=08_05", "motion=slow", "density=low"],
    "count": 1234,
    "support": 0.0123
  }
]
```

`rules.json`:

```json
[
  {
    "rule_id": "R0001",
    "antecedent": ["cell=08_05", "brightness=normal"],
    "consequent": ["motion=slow"],
    "support": 0.0123,
    "confidence": 0.74,
    "lift": 1.18
  }
]
```

`selected_rules.md` chứa 20 rule tốt nhất kèm diễn giải để dùng trong báo cáo.

## 12. Tính rare token score

Với transaction test, chọn itemset quan trọng:

```text
{cell, motion, density}
{cell, motion, cluster}
{cell, density, cluster}
{cell, brightness, motion}
```

Tra support trong train:

```text
support = itemset_support.get(items, 0.0)
```

Công thức MVP:

```text
rare_token_score = 1.0 - min(support / min_support, 1.0)
```

Nếu itemset không đủ thông tin hoặc support không có:

```text
rare_token_score = 1.0
```

Nhưng để tránh false positive quá mạnh:

```text
rare_token_score = min(rare_token_score, rare_score_cap)
```

Khuyến nghị mặc định:

```text
rare_score_cap = 1.0
rare_token_weight = 0.10
```

Reason ví dụ:

```text
token combination {cell=08_05, motion=very_fast, density=high} has support=0.002 below min_support=0.010
```

## 13. Tính rule violation score

Với mỗi rule:

```text
antecedent -> consequent
```

Nếu tất cả token trong antecedent có trong transaction nhưng một hoặc nhiều token consequent không có, xem là rule bị vi phạm.

Điểm từng vi phạm:

```text
violation_strength = confidence * min(lift / 2.0, 1.0)
```

Điểm tổng:

```text
rule_violation_score = max(violation_strength của các rule bị vi phạm)
```

Hoặc lấy top-k mean nếu rule quá nhiễu:

```text
rule_violation_score = mean(top 3 violation_strength)
```

MVP dùng `max` để đơn giản và dễ giải thích.

Reason ví dụ:

```text
rule R0012 expected motion=slow for {cell=08_05, brightness=normal}, but transaction has motion=very_fast
```

## 14. Mở rộng scoring pipeline

Khi `rules.enabled=false`, `cell_scores.csv` giữ schema SPEC 3.

Khi `rules.enabled=true`, thêm cột:

```text
tokens
rare_token_score
rare_itemset
rare_itemset_support
rule_violation_score
violated_rules
token_rule_reasons
```

Schema mới đề xuất:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cell_row
cell_col
nearest_cluster
cluster_distance
cluster_threshold
cluster_distance_score
temporal_change_score
rare_token_score
rule_violation_score
cell_score
tokens
violated_rules
token_rule_reasons
```

`frame_scores.csv` có thể giữ nguyên:

```text
dataset
split
video_id
frame_id
frame_score
smoothed_frame_score
severity
top_cells
```

`alerts.json` cần thêm reason từ token/rule nếu có:

```json
[
  "cell=08_05 has cluster_distance=1.42 above train threshold=0.87",
  "token combination {cell=08_05, motion=very_fast, density=high} has low support",
  "rule R0012 was violated: expected motion=slow"
]
```

## 15. CLI đề xuất

Train token/rule artifact:

```bash
python src/rules.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2
```

Smoke train:

```bash
python src/rules.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --limit-rows 50000
```

Score test với rule artifact:

```bash
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --rules src/outputs/rules/ucsd_ped2
```

Score test fallback không rules:

```bash
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --no-rules
```

Xuất visualization để kiểm tra tương thích:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --top-frames 5
```

## 16. Thí nghiệm bắt buộc

### Experiment 1 - Train token/rule UCSD Ped2

Input:

```text
features_train.csv
SPEC 3 model artifact
```

Yêu cầu:

- sinh `rule_manifest.json`;
- sinh `token_bins.json`;
- sinh `itemsets.json`;
- sinh `rules.json`;
- chọn được ít nhất 20 rule thô hoặc ghi rõ nếu rule quá ít;
- `selected_rules.md` có diễn giải rule dễ đọc.

### Experiment 2 - Score test có token/rule

Input:

```text
features_test.csv
SPEC 3 model artifact
SPEC 5 rule artifact
```

Yêu cầu:

- `cell_scores.csv` có `rare_token_score` và `rule_violation_score`;
- score nằm trong `[0, 1]`;
- `alerts.json` có reason token/rule khi có vi phạm;
- không làm hỏng `frame_scores.csv`.

### Experiment 3 - So sánh baseline không rule và có rule

Chạy:

```text
SPEC 3 score: cluster + temporal
SPEC 5 score: cluster + temporal + rare + rule
```

Ghi:

- số frame medium/high;
- số alert;
- top 20 frame thay đổi nhiều nhất;
- số alert có thêm reason token/rule;
- nhận xét rule có làm score nhiễu hơn không.

### Experiment 4 - Support/confidence sweep

Chạy ít nhất 3 cấu hình:

```text
min_support=0.005, min_confidence=0.60
min_support=0.010, min_confidence=0.60
min_support=0.020, min_confidence=0.70
```

Ghi:

- số itemsets;
- số rules;
- số violated rules trên test;
- số alert có reason rule;
- cấu hình nên dùng mặc định.

### Experiment 5 - Visualization compatibility

Sau khi scoring có schema mới:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --top-frames 5
python src/visualize.py --config src/configs/ucsd_ped2.yaml --alerts --limit-frames 5
```

Yêu cầu:

- ảnh không blank;
- top cells vẫn đọc đúng;
- report hiển thị reason mới nếu có.

## 17. Kiểm tra chất lượng

Điều kiện chấp nhận:

- Rule artifact load lại được bằng JSON.
- Token bins chỉ fit từ train, không fit từ test.
- Không có token rỗng hoặc token chứa dấu phân tách khó parse.
- Itemset support nằm trong `[0, 1]`.
- Confidence và lift tính đúng từ support.
- `rare_token_score`, `rule_violation_score`, `cell_score` nằm trong `[0, 1]`.
- Scoring vẫn chạy khi không có rules.
- Visualization SPEC 4 vẫn chạy với `cell_scores.csv` mới.
- Output có manifest ghi rõ config và số transaction/rule.

Kiểm tra tự động đề xuất:

```bash
python -m compileall src/kpdl_anomaly src/rules.py src/test.py
python src/rules.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --limit-rows 50000
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --rules src/outputs/rules/ucsd_ped2 --limit-rows 50000
python src/visualize.py --config src/configs/ucsd_ped2.yaml --top-frames 5
```

Full verification sau smoke:

```bash
python src/rules.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --rules src/outputs/rules/ucsd_ped2
```

## 18. Checklist triển khai

- [ ] Bổ sung config `rules` vào YAML dataset.
- [ ] Cập nhật `AnomalyConfig` để đọc rules config và score weights mới.
- [ ] Tạo `src/kpdl_anomaly/tokenization.py`.
- [ ] Fit bin thresholds từ train normal.
- [ ] Token hóa row thành transaction.
- [ ] Gán `cluster=Cx` cho train bằng model SPEC 3.
- [ ] Tạo `src/kpdl_anomaly/association.py`.
- [ ] Đếm itemset support kích thước `1..3`.
- [ ] Sinh association rules theo support/confidence/lift.
- [ ] Tạo `src/kpdl_anomaly/rule_model.py`.
- [ ] Lưu `rule_manifest.json`, `token_bins.json`, `itemsets.json`, `rules.json`.
- [ ] Tạo `selected_rules.md`.
- [ ] Tạo CLI `src/rules.py`.
- [ ] Tạo `src/kpdl_anomaly/rule_scoring.py`.
- [ ] Tính `rare_token_score`.
- [ ] Tính `rule_violation_score`.
- [ ] Mở rộng `src/kpdl_anomaly/scoring.py` để dùng rules khi enabled.
- [ ] Mở rộng `CELL_SCORE_COLUMNS` hoặc writer để ghi token/rule columns.
- [ ] Mở rộng reason trong `alerts.json`.
- [ ] Thêm CLI `src/test.py --rules` và `--no-rules`.
- [ ] Chạy smoke train token/rule.
- [ ] Chạy smoke scoring có rules.
- [ ] Chạy full UCSD Ped2 token/rule.
- [ ] Chạy full UCSD Ped2 scoring có rules.
- [ ] Chạy visualization smoke để kiểm tra tương thích.
- [ ] Cập nhật `src/doc/spec_5_processed.md` sau khi triển khai.

## 19. Tiêu chí hoàn thành SPEC 5

SPEC 5 được xem là hoàn thành khi:

- train được token/rule artifact từ UCSD Ped2 train normal;
- sinh được bin thresholds, itemsets, rules và selected rule report;
- test scoring chạy được với công thức đầy đủ `cluster + temporal + rare + rule`;
- output cell score có rare/rule score và reason;
- alert JSON có ít nhất một reason token/rule khi dữ liệu kích hoạt;
- scoring cũ không rules vẫn chạy được;
- visualization SPEC 4 không bị hỏng bởi schema mới;
- có processed file ghi rõ file thay đổi, lệnh verification và kết quả;
- `repomix-output.xml` được refresh sau khi verification pass.

## 20. Rủi ro và giảm thiểu

### 20.1. Token quá vụn

Rủi ro: thêm `cell_id`, `cluster`, nhiều bin làm support thấp.

Giảm thiểu:

- giới hạn bin thô;
- mặc định itemset size tối đa 3;
- thử support sweep;
- không dùng direction token khi chưa có hướng thật.

### 20.2. Rare token không đồng nghĩa bất thường

Rủi ro: tổ hợp hợp lệ nhưng ít gặp bị cộng điểm.

Giảm thiểu:

- `rare_token_weight` chỉ 0.10;
- giữ cluster distance và temporal score làm thành phần chính;
- yêu cầu smoothing/consecutive frame như SPEC 3.

### 20.3. Rule gây hiểu sai

Rủi ro: rule có confidence cao nhưng support thấp hoặc chỉ mô tả độ sáng.

Giảm thiểu:

- lọc theo support/confidence/lift;
- chọn rule có motion/density/cluster;
- ghi warning trong selected rules;
- không dùng rule làm quyết định độc lập.

### 20.4. Bộ đếm itemset tốn RAM

Rủi ro: dữ liệu Avenue lớn hơn UCSD Ped2.

Giảm thiểu:

- ưu tiên UCSD Ped2;
- stream CSV;
- giới hạn max itemset size;
- chỉ lưu itemset đạt support ngưỡng;
- mở rộng FP-Growth sau nếu cần.

### 20.5. Schema mới làm hỏng visualization

Rủi ro: `cell_scores.csv` thêm cột làm code cũ parse sai.

Giảm thiểu:

- giữ nguyên các cột SPEC 3 bắt buộc;
- chỉ thêm cột phía sau;
- chạy smoke SPEC 4 sau scoring.

## 21. Hướng sau SPEC 5

Sau SPEC 5:

- SPEC 6: đánh giá ROC-AUC/EER với ground truth UCSD và so sánh có/không có rule.
- SPEC 7: thêm optical flow hoặc direction feature thật để token hướng chuyển động có ý nghĩa.
- SPEC 8: tinh chỉnh threshold, smoothing và weight theo metric định lượng.
</file>

<file path="src/kpdl_anomaly/association.py">
from __future__ import annotations

from collections import Counter
from itertools import combinations
from typing import Any

POSITION_PREFIXES = ("cell=", "cell_row=", "cell_col=")
PREFERRED_CONSEQUENT_PREFIXES = (
    "motion=",
    "density=",
    "cluster=",
    "brightness=",
    "brightness_delta=",
)


def update_itemset_counts(counter: Counter[tuple[str, ...]], tokens: list[str], max_size: int) -> None:
    unique_tokens = tuple(sorted(set(tokens)))
    upper = min(max_size, len(unique_tokens))
    for size in range(1, upper + 1):
        counter.update(combinations(unique_tokens, size))


def itemset_records(
    counter: Counter[tuple[str, ...]],
    num_transactions: int,
    min_support: float = 0.0,
) -> list[dict[str, Any]]:
    records: list[dict[str, Any]] = []
    if num_transactions <= 0:
        return records
    for items, count in counter.items():
        support = count / num_transactions
        if support < min_support:
            continue
        records.append(
            {
                "items": list(items),
                "count": int(count),
                "support": float(support),
            }
        )
    records.sort(key=lambda item: (-float(item["support"]), len(item["items"]), item["items"]))
    return records


def generate_association_rules(
    counter: Counter[tuple[str, ...]],
    num_transactions: int,
    min_support: float,
    min_confidence: float,
    min_lift: float,
    max_rules: int,
) -> list[dict[str, Any]]:
    if num_transactions <= 0:
        return []

    supports = {items: count / num_transactions for items, count in counter.items()}
    candidates: list[dict[str, Any]] = []
    for itemset, support in supports.items():
        if len(itemset) < 2 or support < min_support:
            continue
        if _position_only(itemset):
            continue

        itemset_set = set(itemset)
        for antecedent_size in range(1, len(itemset)):
            for antecedent in combinations(itemset, antecedent_size):
                consequent = tuple(sorted(itemset_set - set(antecedent)))
                if not consequent or _position_only(antecedent + consequent):
                    continue
                if not _preferred_consequent(consequent):
                    continue

                antecedent_support = supports.get(tuple(sorted(antecedent)), 0.0)
                consequent_support = supports.get(tuple(sorted(consequent)), 0.0)
                if antecedent_support <= 0.0 or consequent_support <= 0.0:
                    continue
                confidence = support / antecedent_support
                lift = confidence / consequent_support
                if confidence < min_confidence or lift < min_lift:
                    continue

                candidates.append(
                    {
                        "antecedent": list(sorted(antecedent)),
                        "consequent": list(consequent),
                        "support": float(support),
                        "confidence": float(confidence),
                        "lift": float(lift),
                    }
                )

    candidates.sort(
        key=lambda item: (
            -float(item["confidence"]),
            -float(item["lift"]),
            -float(item["support"]),
            len(item["antecedent"]),
            item["antecedent"],
            item["consequent"],
        )
    )
    selected = candidates[:max_rules]
    for index, rule in enumerate(selected, start=1):
        rule["rule_id"] = f"R{index:04d}"
    return selected


def _position_only(items: tuple[str, ...]) -> bool:
    return all(any(item.startswith(prefix) for prefix in POSITION_PREFIXES) for item in items)


def _preferred_consequent(consequent: tuple[str, ...]) -> bool:
    return any(any(item.startswith(prefix) for prefix in PREFERRED_CONSEQUENT_PREFIXES) for item in consequent)
</file>

<file path="src/kpdl_anomaly/rule_model.py">
from __future__ import annotations

import csv
import json
from collections import Counter
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import joblib
import numpy as np

from kpdl_preprocess.config import ConfigError

from .association import generate_association_rules, itemset_records, update_itemset_counts
from .config import AnomalyConfig
from .feature_selection import row_to_vector
from .io import output_dir, read_json, require_files, validate_feature_header, write_json
from .schema import METADATA_COLUMNS, RULE_SCHEMA_VERSION
from .tokenization import fit_token_bins, row_to_tokens

SAMPLE_TRANSACTION_LIMIT = 200


def train_rule_model(
    config: AnomalyConfig,
    model_dir: str | Path,
    output_path: str | Path | None = None,
    limit_rows: int | None = None,
    write_transactions: bool = False,
) -> dict[str, Any]:
    model_path = Path(model_dir)
    rule_path = output_dir(output_path or (config.rules.output_root / config.dataset))
    require_files(
        [
            config.train_feature_path,
            model_path / "model_manifest.json",
            model_path / "cell_models.joblib",
            model_path / "cell_scalers.joblib",
            model_path / "thresholds.json",
        ]
    )
    validate_feature_header(config.train_feature_path, METADATA_COLUMNS + config.feature_columns)

    model_manifest = read_json(model_path / "model_manifest.json")
    _validate_model_manifest(config, model_manifest)
    models = joblib.load(model_path / "cell_models.joblib")
    scalers = joblib.load(model_path / "cell_scalers.joblib")

    token_bins = fit_token_bins(config.train_feature_path, expected_split="train", limit_rows=limit_rows)
    itemset_counter: Counter[tuple[str, ...]] = Counter()
    token_counter: Counter[str] = Counter()
    rows_read = 0
    rows_loaded = 0
    invalid_rows = 0
    split_mismatches = 0
    fallback_cluster_rows = 0
    empty_token_rows = 0

    sample_path = rule_path / "train_tokens_sample.jsonl"
    transaction_path = rule_path / "train_transactions.jsonl"
    with config.train_feature_path.open("r", newline="", encoding="utf-8") as input_handle, sample_path.open(
        "w", encoding="utf-8"
    ) as sample_handle:
        transaction_handle = transaction_path.open("w", encoding="utf-8") if write_transactions else None
        try:
            reader = csv.DictReader(input_handle)
            for row in reader:
                if limit_rows is not None and rows_read >= limit_rows:
                    break
                rows_read += 1
                if row.get("split") != "train":
                    split_mismatches += 1
                    continue

                vector = row_to_vector(row, config.feature_columns)
                if vector is None:
                    invalid_rows += 1
                    continue

                cluster_id = _nearest_cluster(str(row["cell_id"]), vector, models, scalers)
                if cluster_id < 0:
                    fallback_cluster_rows += 1

                tokens = row_to_tokens(
                    row,
                    token_bins,
                    cluster_id,
                    include_cell_token=config.rules.include_cell_token,
                    include_cluster_token=config.rules.include_cluster_token,
                    include_brightness_token=config.rules.include_brightness_token,
                    include_direction_token=config.rules.include_direction_token,
                )
                if not tokens:
                    empty_token_rows += 1
                    continue

                rows_loaded += 1
                token_counter.update(tokens)
                update_itemset_counts(itemset_counter, tokens, config.rules.max_itemset_size)

                record = _transaction_record(row, tokens)
                if rows_loaded <= SAMPLE_TRANSACTION_LIMIT:
                    sample_handle.write(json.dumps(record, ensure_ascii=False) + "\n")
                if transaction_handle is not None:
                    transaction_handle.write(json.dumps(record, ensure_ascii=False) + "\n")
        finally:
            if transaction_handle is not None:
                transaction_handle.close()

    if split_mismatches:
        raise ConfigError(f"{config.train_feature_path} contains {split_mismatches} row(s) outside split='train'")
    if rows_loaded == 0:
        raise ConfigError("No valid train transactions were generated")

    itemsets = itemset_records(
        itemset_counter,
        rows_loaded,
        min_support=config.rules.rare_support_floor,
    )
    rules = generate_association_rules(
        itemset_counter,
        rows_loaded,
        min_support=config.rules.min_support,
        min_confidence=config.rules.min_confidence,
        min_lift=config.rules.min_lift,
        max_rules=config.rules.max_rules,
    )
    token_stats = _token_stats(token_counter, rows_loaded)
    warnings = _warnings(
        limit_rows=limit_rows,
        invalid_rows=invalid_rows,
        fallback_cluster_rows=fallback_cluster_rows,
        empty_token_rows=empty_token_rows,
        num_rules=len(rules),
    )
    created_at = datetime.now(timezone.utc).isoformat()
    manifest = {
        "schema_version": RULE_SCHEMA_VERSION,
        "dataset": config.dataset,
        "created_at": created_at,
        "config_path": str(config.config_path),
        "source_model_dir": str(model_path),
        "train_feature_path": str(config.train_feature_path),
        "feature_columns": config.feature_columns,
        "token_schema": _token_schema(config),
        "num_transactions": rows_loaded,
        "rows_read": rows_read,
        "invalid_rows": invalid_rows,
        "fallback_cluster_rows": fallback_cluster_rows,
        "num_itemsets": len(itemsets),
        "num_rules": len(rules),
        "min_support": config.rules.min_support,
        "min_confidence": config.rules.min_confidence,
        "min_lift": config.rules.min_lift,
        "max_itemset_size": config.rules.max_itemset_size,
        "rare_support_floor": config.rules.rare_support_floor,
        "limit_rows": limit_rows,
        "warnings": warnings,
    }

    write_json(rule_path / "rule_manifest.json", manifest)
    write_json(rule_path / "token_bins.json", token_bins)
    write_json(rule_path / "itemsets.json", itemsets)
    write_json(rule_path / "rules.json", rules)
    write_json(rule_path / "token_stats.json", token_stats)
    _write_selected_rules(rule_path / "selected_rules.md", rules, manifest)

    return {
        "dataset": config.dataset,
        "rule_dir": str(rule_path),
        "manifest": manifest,
        "token_bins_path": str(rule_path / "token_bins.json"),
        "itemsets_path": str(rule_path / "itemsets.json"),
        "rules_path": str(rule_path / "rules.json"),
        "selected_rules_path": str(rule_path / "selected_rules.md"),
    }


def _validate_model_manifest(config: AnomalyConfig, manifest: dict[str, Any]) -> None:
    if manifest.get("dataset") != config.dataset:
        raise ConfigError(f"model dataset={manifest.get('dataset')!r} does not match config dataset={config.dataset!r}")
    if list(manifest.get("feature_columns", [])) != config.feature_columns:
        raise ConfigError("model feature_columns do not match config scoring.feature_columns")


def _nearest_cluster(cell_id: str, vector: np.ndarray, models: dict[str, Any], scalers: dict[str, Any]) -> int:
    model = models.get(cell_id)
    scaler = scalers.get(cell_id)
    if model is None or scaler is None:
        return -1
    scaled = scaler.transform(vector.reshape(1, -1))
    distances = model.transform(scaled)[0]
    return int(np.argmin(distances))


def _transaction_record(row: dict[str, str], tokens: list[str]) -> dict[str, Any]:
    return {
        "dataset": row["dataset"],
        "split": row["split"],
        "video_id": row["video_id"],
        "cube_id": row["cube_id"],
        "center_frame_id": int(row["center_frame_id"]),
        "cell_id": row["cell_id"],
        "tokens": tokens,
    }


def _token_schema(config: AnomalyConfig) -> dict[str, Any]:
    return {
        "tokens": [
            "cell",
            "cell_row",
            "cell_col",
            "motion",
            "density",
            "brightness",
            "brightness_delta",
            "cluster",
        ],
        "include_cell_token": config.rules.include_cell_token,
        "include_cluster_token": config.rules.include_cluster_token,
        "include_brightness_token": config.rules.include_brightness_token,
        "include_direction_token": config.rules.include_direction_token,
    }


def _token_stats(token_counter: Counter[str], num_transactions: int) -> dict[str, Any]:
    records = [
        {
            "token": token,
            "count": int(count),
            "support": float(count / num_transactions),
        }
        for token, count in token_counter.items()
    ]
    records.sort(key=lambda item: (-float(item["support"]), item["token"]))
    return {
        "num_transactions": num_transactions,
        "num_tokens": len(records),
        "tokens": records,
    }


def _warnings(
    limit_rows: int | None,
    invalid_rows: int,
    fallback_cluster_rows: int,
    empty_token_rows: int,
    num_rules: int,
) -> list[str]:
    warnings: list[str] = []
    if limit_rows is not None:
        warnings.append(f"limit_rows={limit_rows} was applied; artifact is for smoke/debug use")
    if invalid_rows:
        warnings.append(f"skipped {invalid_rows} row(s) with invalid numeric feature values")
    if fallback_cluster_rows:
        warnings.append(f"{fallback_cluster_rows} row(s) used cluster=unknown because no cell model/scaler was found")
    if empty_token_rows:
        warnings.append(f"skipped {empty_token_rows} row(s) that produced no tokens")
    if num_rules < 20:
        warnings.append(f"only {num_rules} rule(s) passed filters; selected_rules.md may be sparse")
    return warnings


def _write_selected_rules(path: Path, rules: list[dict[str, Any]], manifest: dict[str, Any]) -> None:
    lines = [
        "# Selected Association Rules",
        "",
        f"Dataset: `{manifest['dataset']}`",
        f"Transactions: `{manifest['num_transactions']}`",
        f"Filters: support >= `{manifest['min_support']}`, confidence >= `{manifest['min_confidence']}`, lift >= `{manifest['min_lift']}`",
        "",
    ]
    if not rules:
        lines.append("No association rules passed the configured filters.")
    for rule in rules[:20]:
        lines.append(
            "- `{rule_id}`: {{{antecedent}}} -> {{{consequent}}} "
            "support={support:.4f}, confidence={confidence:.4f}, lift={lift:.4f}".format(
                rule_id=rule["rule_id"],
                antecedent=", ".join(rule["antecedent"]),
                consequent=", ".join(rule["consequent"]),
                support=float(rule["support"]),
                confidence=float(rule["confidence"]),
                lift=float(rule["lift"]),
            )
        )
    path.write_text("\n".join(lines) + "\n", encoding="utf-8")
</file>

<file path="src/kpdl_anomaly/rule_scoring.py">
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Any, Mapping

import numpy as np

from .config import AnomalyConfig
from .io import read_json
from .tokenization import row_to_tokens


@dataclass(frozen=True)
class RuleScore:
    tokens: list[str]
    rare_token_score: float
    rare_itemset: list[str]
    rare_itemset_support: float
    rule_violation_score: float
    violated_rules: list[str]
    reasons: list[str]


@dataclass(frozen=True)
class RuleLoadResult:
    scorer: "RuleScorer | None"
    rule_dir: Path | None
    warnings: list[str]


class RuleScorer:
    def __init__(
        self,
        rule_dir: Path,
        manifest: dict[str, Any],
        token_bins: dict[str, Any],
        itemsets: list[dict[str, Any]],
        rules: list[dict[str, Any]],
        config: AnomalyConfig,
    ) -> None:
        self.rule_dir = rule_dir
        self.manifest = manifest
        self.token_bins = token_bins
        self.rules = rules
        self.min_support = float(manifest.get("min_support", config.rules.min_support))
        self.rare_itemset_size = config.rules.rare_itemset_size
        self.rare_score_cap = config.rules.rare_score_cap
        self.token_schema = dict(manifest.get("token_schema", {}))
        self.itemset_support = {
            tuple(sorted(str(item) for item in record.get("items", []))): float(record.get("support", 0.0))
            for record in itemsets
        }

    def score(self, row: Mapping[str, Any], nearest_cluster: int | str | None) -> RuleScore:
        tokens = row_to_tokens(
            row,
            self.token_bins,
            nearest_cluster,
            include_cell_token=bool(self.token_schema.get("include_cell_token", True)),
            include_cluster_token=bool(self.token_schema.get("include_cluster_token", True)),
            include_brightness_token=bool(self.token_schema.get("include_brightness_token", True)),
            include_direction_token=bool(self.token_schema.get("include_direction_token", False)),
        )
        rare_score, rare_itemset, rare_support, rare_reason = self._rare_score(tokens)
        violation_score, violated_rules, violation_reasons = self._rule_violation_score(tokens)
        reasons = []
        if rare_reason:
            reasons.append(rare_reason)
        reasons.extend(violation_reasons)
        return RuleScore(
            tokens=tokens,
            rare_token_score=rare_score,
            rare_itemset=rare_itemset,
            rare_itemset_support=rare_support,
            rule_violation_score=violation_score,
            violated_rules=violated_rules,
            reasons=reasons,
        )

    def _rare_score(self, tokens: list[str]) -> tuple[float, list[str], float, str | None]:
        candidates = _rare_candidates(tokens, self.rare_itemset_size)
        if not candidates:
            return 0.0, [], 0.0, None

        best_score = -1.0
        best_itemset: list[str] = []
        best_support = 0.0
        denominator = max(self.min_support, 1.0e-12)
        for candidate in candidates:
            key = tuple(sorted(candidate))
            support = self.itemset_support.get(key, 0.0)
            score = 1.0 - min(support / denominator, 1.0)
            score = min(float(np.clip(score, 0.0, 1.0)), self.rare_score_cap)
            if score > best_score:
                best_score = score
                best_itemset = list(key)
                best_support = support

        reason = None
        if best_itemset and best_support < self.min_support:
            reason = (
                "token combination {{{items}}} has support={support:.4f} below min_support={minimum:.4f}".format(
                    items=", ".join(best_itemset),
                    support=best_support,
                    minimum=self.min_support,
                )
            )
        return float(np.clip(best_score, 0.0, 1.0)), best_itemset, float(best_support), reason

    def _rule_violation_score(self, tokens: list[str]) -> tuple[float, list[str], list[str]]:
        transaction = set(tokens)
        violations: list[dict[str, Any]] = []
        for rule in self.rules:
            antecedent = {str(item) for item in rule.get("antecedent", [])}
            consequent = {str(item) for item in rule.get("consequent", [])}
            if not antecedent or not consequent or not antecedent.issubset(transaction):
                continue
            if consequent.issubset(transaction):
                continue
            strength = float(rule.get("confidence", 0.0)) * min(float(rule.get("lift", 0.0)) / 2.0, 1.0)
            strength = float(np.clip(strength, 0.0, 1.0))
            missing = sorted(consequent - transaction)
            violations.append(
                {
                    "rule_id": str(rule.get("rule_id", "")),
                    "strength": strength,
                    "antecedent": sorted(antecedent),
                    "missing": missing,
                    "reason": _violation_reason(rule, transaction, missing),
                }
            )

        if not violations:
            return 0.0, [], []
        violations.sort(key=lambda item: (-float(item["strength"]), item["rule_id"]))
        top = violations[:3]
        return (
            float(top[0]["strength"]),
            [str(item["rule_id"]) for item in top if item["rule_id"]],
            [str(item["reason"]) for item in top],
        )


def load_rule_scorer(
    config: AnomalyConfig,
    rule_dir: str | Path | None,
    requested: bool,
) -> RuleLoadResult:
    if not requested:
        return RuleLoadResult(scorer=None, rule_dir=None, warnings=[])

    resolved_dir = Path(rule_dir) if rule_dir is not None else config.rules.model_dir or (config.rules.output_root / config.dataset)
    required = [
        resolved_dir / "rule_manifest.json",
        resolved_dir / "token_bins.json",
        resolved_dir / "itemsets.json",
        resolved_dir / "rules.json",
    ]
    missing = [str(path) for path in required if not path.exists()]
    if missing:
        return RuleLoadResult(
            scorer=None,
            rule_dir=resolved_dir,
            warnings=[f"rule scoring disabled because artifact file(s) are missing: {', '.join(missing)}"],
        )

    manifest = read_json(required[0])
    warnings: list[str] = []
    if manifest.get("dataset") != config.dataset:
        warnings.append(
            f"rule scoring disabled because rule dataset={manifest.get('dataset')!r} does not match config dataset={config.dataset!r}"
        )
        return RuleLoadResult(scorer=None, rule_dir=resolved_dir, warnings=warnings)
    if list(manifest.get("feature_columns", [])) != config.feature_columns:
        warnings.append("rule scoring disabled because rule feature_columns do not match config scoring.feature_columns")
        return RuleLoadResult(scorer=None, rule_dir=resolved_dir, warnings=warnings)

    scorer = RuleScorer(
        rule_dir=resolved_dir,
        manifest=manifest,
        token_bins=read_json(required[1]),
        itemsets=read_json(required[2]),
        rules=read_json(required[3]),
        config=config,
    )
    return RuleLoadResult(scorer=scorer, rule_dir=resolved_dir, warnings=warnings)


def _rare_candidates(tokens: list[str], max_size: int) -> list[list[str]]:
    by_prefix = {token.split("=", 1)[0]: token for token in tokens if "=" in token}
    specs = [
        ("cell", "motion", "density"),
        ("cell", "motion", "cluster"),
        ("cell", "density", "cluster"),
        ("cell", "brightness", "motion"),
    ]
    candidates: list[list[str]] = []
    seen: set[tuple[str, ...]] = set()
    for spec in specs:
        if len(spec) > max_size:
            continue
        if not all(prefix in by_prefix for prefix in spec):
            continue
        candidate = tuple(sorted(by_prefix[prefix] for prefix in spec))
        if candidate in seen:
            continue
        seen.add(candidate)
        candidates.append(list(candidate))
    return candidates


def _violation_reason(rule: dict[str, Any], transaction: set[str], missing: list[str]) -> str:
    antecedent = ", ".join(str(item) for item in rule.get("antecedent", []))
    expected = ", ".join(missing)
    observed = _observed_for_missing(transaction, missing)
    if observed:
        return f"rule {rule.get('rule_id')} expected {expected} for {{{antecedent}}}, but transaction has {observed}"
    return f"rule {rule.get('rule_id')} expected {expected} for {{{antecedent}}}"


def _observed_for_missing(transaction: set[str], missing: list[str]) -> str:
    prefixes = [item.split("=", 1)[0] for item in missing if "=" in item]
    observed: list[str] = []
    for prefix in prefixes:
        observed.extend(sorted(item for item in transaction if item.startswith(f"{prefix}=")))
    return ", ".join(observed)
</file>

<file path="src/kpdl_anomaly/tokenization.py">
from __future__ import annotations

import csv
from collections.abc import Mapping
from pathlib import Path
from typing import Any

import numpy as np

from kpdl_preprocess.config import ConfigError

from .io import validate_feature_header
from .schema import METADATA_COLUMNS, RULE_SCHEMA_VERSION

MOTION_COLUMN = "motion_magnitude_mean"
DENSITY_COLUMN = "motion_density"
BRIGHTNESS_COLUMN = "brightness_mean"
BRIGHTNESS_DELTA_COLUMN = "brightness_delta"
TOKEN_NUMERIC_COLUMNS = [
    MOTION_COLUMN,
    DENSITY_COLUMN,
    BRIGHTNESS_COLUMN,
    BRIGHTNESS_DELTA_COLUMN,
]


def fit_token_bins(
    feature_path: str | Path,
    expected_split: str = "train",
    limit_rows: int | None = None,
) -> dict[str, Any]:
    required_columns = METADATA_COLUMNS + TOKEN_NUMERIC_COLUMNS
    validate_feature_header(feature_path, required_columns)

    values: dict[str, list[float]] = {column: [] for column in TOKEN_NUMERIC_COLUMNS}
    rows_read = 0
    rows_used = 0
    invalid_rows = 0
    split_mismatches = 0

    with Path(feature_path).open("r", newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        for row in reader:
            if limit_rows is not None and rows_read >= limit_rows:
                break
            rows_read += 1
            if row.get("split") != expected_split:
                split_mismatches += 1
                continue

            parsed: dict[str, float] = {}
            try:
                for column in TOKEN_NUMERIC_COLUMNS:
                    value = float(row[column])
                    if not np.isfinite(value):
                        raise ValueError(column)
                    parsed[column] = value
            except (KeyError, TypeError, ValueError):
                invalid_rows += 1
                continue

            for column, value in parsed.items():
                values[column].append(value)
            rows_used += 1

    if split_mismatches:
        raise ConfigError(f"{feature_path} contains {split_mismatches} row(s) outside split={expected_split!r}")
    if rows_used == 0:
        raise ConfigError(f"No valid {expected_split!r} rows found for token bin fitting")

    motion_values = _array(values[MOTION_COLUMN])
    motion_positive = motion_values[motion_values > 1.0e-12]
    motion_quantiles = _quantiles(motion_values, [0.20, 0.40, 0.60, 0.80])
    positive_quantiles = _quantiles(motion_positive, [0.25, 0.50, 0.75]) if motion_positive.size else [0.0, 0.0, 0.0]

    return {
        "schema_version": RULE_SCHEMA_VERSION,
        "fit_split": expected_split,
        "rows_read": rows_read,
        "rows_used": rows_used,
        "invalid_rows": invalid_rows,
        "limit_rows": limit_rows,
        "features": {
            MOTION_COLUMN: {
                "q20": motion_quantiles[0],
                "q40": motion_quantiles[1],
                "q60": motion_quantiles[2],
                "q80": motion_quantiles[3],
                "epsilon": 1.0e-12,
                "zero_heavy": bool(motion_quantiles[1] <= 1.0e-12 and motion_positive.size > 0),
                "positive_q25": positive_quantiles[0],
                "positive_q50": positive_quantiles[1],
                "positive_q75": positive_quantiles[2],
            },
            DENSITY_COLUMN: _named_quantiles(values[DENSITY_COLUMN], {"q33": 0.33, "q66": 0.66}),
            BRIGHTNESS_COLUMN: _named_quantiles(values[BRIGHTNESS_COLUMN], {"q33": 0.33, "q66": 0.66}),
            "brightness_delta_abs": _named_quantiles(
                [abs(value) for value in values[BRIGHTNESS_DELTA_COLUMN]],
                {"q80": 0.80},
            ),
        },
    }


def row_to_tokens(
    row: Mapping[str, Any],
    bins: Mapping[str, Any],
    cluster_id: int | str | None,
    include_cell_token: bool = True,
    include_cluster_token: bool = True,
    include_brightness_token: bool = True,
    include_direction_token: bool = False,
) -> list[str]:
    tokens: list[str] = []
    if include_cell_token:
        cell_id = str(row.get("cell_id", "")).strip()
        if cell_id:
            tokens.append(f"cell={cell_id}")
        cell_row = _format_index(row.get("cell_row"))
        if cell_row is not None:
            tokens.append(f"cell_row={cell_row}")
        cell_col = _format_index(row.get("cell_col"))
        if cell_col is not None:
            tokens.append(f"cell_col={cell_col}")

    motion = _motion_bucket(_float_or_none(row.get(MOTION_COLUMN)), bins)
    if motion is not None:
        tokens.append(f"motion={motion}")

    density = _three_bucket(_float_or_none(row.get(DENSITY_COLUMN)), bins, DENSITY_COLUMN, ("low", "medium", "high"))
    if density is not None:
        tokens.append(f"density={density}")

    if include_brightness_token:
        brightness = _three_bucket(
            _float_or_none(row.get(BRIGHTNESS_COLUMN)),
            bins,
            BRIGHTNESS_COLUMN,
            ("dark", "normal", "bright"),
        )
        if brightness is not None:
            tokens.append(f"brightness={brightness}")

        brightness_delta = _brightness_delta_bucket(_float_or_none(row.get(BRIGHTNESS_DELTA_COLUMN)), bins)
        if brightness_delta is not None:
            tokens.append(f"brightness_delta={brightness_delta}")

    if include_cluster_token:
        tokens.append(f"cluster={_cluster_label(cluster_id)}")

    if include_direction_token:
        direction = _direction_bucket(row)
        if direction is not None:
            tokens.append(f"direction={direction}")

    return _dedupe(tokens)


def _motion_bucket(value: float | None, bins: Mapping[str, Any]) -> str | None:
    if value is None:
        return None
    payload = dict(bins.get("features", {}).get(MOTION_COLUMN, {}))
    epsilon = float(payload.get("epsilon", 1.0e-12))
    if bool(payload.get("zero_heavy", False)):
        if value <= epsilon:
            return "still"
        if value <= float(payload.get("positive_q25", 0.0)):
            return "slow"
        if value <= float(payload.get("positive_q50", 0.0)):
            return "medium"
        if value <= float(payload.get("positive_q75", 0.0)):
            return "fast"
        return "very_fast"
    if value <= float(payload.get("q20", 0.0)):
        return "still"
    if value <= float(payload.get("q40", 0.0)):
        return "slow"
    if value <= float(payload.get("q60", 0.0)):
        return "medium"
    if value <= float(payload.get("q80", 0.0)):
        return "fast"
    return "very_fast"


def _three_bucket(
    value: float | None,
    bins: Mapping[str, Any],
    column: str,
    labels: tuple[str, str, str],
) -> str | None:
    if value is None:
        return None
    payload = dict(bins.get("features", {}).get(column, {}))
    if value <= float(payload.get("q33", 0.0)):
        return labels[0]
    if value <= float(payload.get("q66", 0.0)):
        return labels[1]
    return labels[2]


def _brightness_delta_bucket(value: float | None, bins: Mapping[str, Any]) -> str | None:
    if value is None:
        return None
    payload = dict(bins.get("features", {}).get("brightness_delta_abs", {}))
    return "stable" if abs(value) <= float(payload.get("q80", 0.0)) else "changing"


def _direction_bucket(row: Mapping[str, Any]) -> str | None:
    values: list[float] = []
    for index in range(8):
        value = _float_or_none(row.get(f"direction_hist_{index}"))
        if value is None:
            return None
        values.append(value)
    total = sum(values)
    if total <= 1.0e-12:
        return None
    return f"bin_{int(np.argmax(np.asarray(values)))}"


def _cluster_label(cluster_id: int | str | None) -> str:
    if cluster_id is None:
        return "unknown"
    if isinstance(cluster_id, str):
        cleaned = cluster_id.strip()
        if not cleaned:
            return "unknown"
        return cleaned if cleaned.startswith("C") or cleaned == "unknown" else f"C{cleaned}"
    return "unknown" if int(cluster_id) < 0 else f"C{int(cluster_id)}"


def _format_index(raw: Any) -> str | None:
    try:
        return f"{int(raw):02d}"
    except (TypeError, ValueError):
        return None


def _float_or_none(raw: Any) -> float | None:
    try:
        value = float(raw)
    except (TypeError, ValueError):
        return None
    if not np.isfinite(value):
        return None
    return value


def _array(values: list[float] | np.ndarray) -> np.ndarray:
    return np.asarray(values, dtype=np.float64)


def _quantiles(values: list[float] | np.ndarray, quantiles: list[float]) -> list[float]:
    arr = _array(values)
    if arr.size == 0:
        return [0.0 for _ in quantiles]
    return [float(np.quantile(arr, quantile)) for quantile in quantiles]


def _named_quantiles(values: list[float], quantiles: dict[str, float]) -> dict[str, float]:
    computed = _quantiles(values, list(quantiles.values()))
    return {name: value for name, value in zip(quantiles.keys(), computed, strict=True)}


def _dedupe(tokens: list[str]) -> list[str]:
    seen: set[str] = set()
    result: list[str] = []
    for token in tokens:
        if not token or token in seen:
            continue
        seen.add(token)
        result.append(token)
    return result
</file>

<file path="src/rules.py">
from __future__ import annotations

import argparse
import json
import sys
from dataclasses import replace
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_anomaly.config import load_anomaly_config
from kpdl_anomaly.rule_model import train_rule_model
from kpdl_preprocess.config import resolve_path


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Train SPEC 5 token/rule artifacts.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative paths are resolved here.")
    parser.add_argument("--model", default=None, help="SPEC 3 model directory. Defaults to output.model_root/dataset.")
    parser.add_argument("--output-dir", default=None, help="Override rule artifact directory.")
    parser.add_argument("--limit-rows", type=int, default=None, help="Limit train feature rows for smoke tests.")
    parser.add_argument("--write-transactions", action="store_true", help="Write all train transactions as JSONL.")
    parser.add_argument("--min-support", type=float, default=None, help="Override rules.min_support.")
    parser.add_argument("--min-confidence", type=float, default=None, help="Override rules.min_confidence.")
    parser.add_argument("--min-lift", type=float, default=None, help="Override rules.min_lift.")
    parser.add_argument("--max-rules", type=int, default=None, help="Override rules.max_rules.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    summary = run_rules(
        config_path=args.config,
        project_root=args.project_root,
        model_dir=args.model,
        output_dir=args.output_dir,
        limit_rows=args.limit_rows,
        write_transactions=args.write_transactions,
        min_support=args.min_support,
        min_confidence=args.min_confidence,
        min_lift=args.min_lift,
        max_rules=args.max_rules,
    )
    print(json.dumps(_public_summary(summary), indent=2))
    return 0


def run_rules(
    config_path: str | Path,
    project_root: str | Path = ".",
    model_dir: str | Path | None = None,
    output_dir: str | Path | None = None,
    limit_rows: int | None = None,
    write_transactions: bool = False,
    min_support: float | None = None,
    min_confidence: float | None = None,
    min_lift: float | None = None,
    max_rules: int | None = None,
) -> dict:
    config = load_anomaly_config(config_path=config_path, project_root=project_root)
    config = _with_overrides(
        config,
        min_support=min_support,
        min_confidence=min_confidence,
        min_lift=min_lift,
        max_rules=max_rules,
    )
    resolved_model_dir = resolve_path(model_dir, config.project_root) if model_dir is not None else config.model_dir
    resolved_output_dir = resolve_path(output_dir, config.project_root) if output_dir is not None else None
    return train_rule_model(
        config,
        model_dir=resolved_model_dir,
        output_path=resolved_output_dir,
        limit_rows=limit_rows,
        write_transactions=write_transactions,
    )


def _with_overrides(
    config,
    min_support: float | None,
    min_confidence: float | None,
    min_lift: float | None,
    max_rules: int | None,
):
    rules = config.rules
    if min_support is not None:
        rules = replace(rules, min_support=min_support)
    if min_confidence is not None:
        rules = replace(rules, min_confidence=min_confidence)
    if min_lift is not None:
        rules = replace(rules, min_lift=min_lift)
    if max_rules is not None:
        rules = replace(rules, max_rules=max_rules)
    return replace(config, rules=rules)


def _public_summary(summary: dict) -> dict:
    manifest = summary["manifest"]
    return {
        "dataset": summary["dataset"],
        "rule_dir": summary["rule_dir"],
        "num_transactions": manifest["num_transactions"],
        "num_itemsets": manifest["num_itemsets"],
        "num_rules": manifest["num_rules"],
        "warnings": manifest["warnings"],
    }


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path=".agents/skills/Dev/SKILL.md">
---
name: Dev
description: Enforce a repository workflow where Codex must read the PRD first, then read repomix-output.xml for project context, implement user requests from spec files, maintain *_processed.md progress files for completed spec work, run tests, and only then refresh repomix-output.xml with repomix.
---

# PRD + Repomix Spec Workflow

Follow this workflow for any request in this repository, especially when the user asks to implement, update, fix, or complete a spec.

## Required Startup

1. Read the PRD first.
   - Prefer a PRD path named by the user.
   - Otherwise look for common PRD files such as `PRD.md`, `prd.md`, `docs/PRD.md`, `docs/prd.md`, or files matching `*prd*.md`.
   - Do not start implementation before reading the PRD. If no PRD can be found, report that and ask for the PRD path.

2. Read `repomix-output.xml` after the PRD.
   - Prefer the repository-root `repomix-output.xml`.
   - Use it to understand project structure, architecture, existing conventions, and relevant files.
   - Treat `repomix-output.xml` as context, not as a substitute for checking live files. Before editing, inspect the current source files directly.
   - If `repomix-output.xml` is missing, report that and continue only after collecting equivalent context from the repository files.

3. Read the requested spec after the PRD and repomix context.
   - If the user names a spec file, read that file.
   - If the request implies a spec but does not name one, search for likely spec files and choose the most relevant one, or ask when the choice is ambiguous.

## Processed Spec File

For every spec file being implemented, create or update a sibling processed file named like the original spec with `_processed` added before the extension.

Examples:

- `spec_1.md` -> `spec_1_processed.md`
- `docs/spec-login.md` -> `docs/spec-login_processed.md`

Use the processed file to record implementation progress. Keep it concise but auditable:

- Original spec path
- Date/time processed
- Checklist of spec items
- Status for each item: `Done`, `Skipped`, or `Blocked`
- Files changed for each completed item when useful
- Test or verification evidence
- Remaining risks or follow-up notes, if any

Update the processed file as work progresses. Do not modify the original spec just to mark progress unless the user explicitly asks.

## Implementation Rules

- Let the PRD define product intent and constraints.
- Let `repomix-output.xml` define broad project context.
- Let the live repository files define the source of truth for code edits.
- Follow existing architecture, naming, style, and test patterns.
- Keep changes scoped to the requested spec.
- Do not overwrite unrelated user changes.
- If a spec conflicts with the PRD, pause and explain the conflict before implementing that part.

## Verification

Before considering the work complete:

1. Run the relevant tests, lint, type checks, build, or validation commands for the changed area.
2. If no obvious command exists, inspect project scripts and choose the closest meaningful verification.
3. Fix failures caused by the implementation.
4. Record the final verification result in the processed spec file.

Do not run the final repomix update until the implementation is complete and the verification commands pass. If verification cannot be run, explain why and do not claim the work is fully complete.

## Final Repomix Update

After all requested work is complete and tests pass, update `repomix-output.xml`.

Preferred order:

1. Run the repository's configured repomix script if one exists.
2. Otherwise run `repomix` from the repository root.
3. If `repomix` is unavailable, report the exact failure and leave `repomix-output.xml` unchanged.

After updating, confirm that `repomix-output.xml` changed as expected.

## Final Response

In the final response, summarize:

- The spec implemented
- The processed spec file created or updated
- Key files changed
- Verification commands and results
- Whether `repomix-output.xml` was refreshed
</file>

<file path=".gitignore">
# Local datasets and downloaded archives
/New folder/
/dataset/
/src/dataset/

# Generated preprocessing outputs
/outputs/
/src/outputs/

# External reference papers kept outside project docs
/*.pdf

# Python runtime/cache files
__pycache__/
*.py[cod]
.venv/
venv/

# OS/editor noise
.DS_Store
Thumbs.db
</file>

<file path="latex/main.aux">
\relax 
\providecommand \babel@aux [2]{\global \let \babel@toc \@gobbletwo }
\@nameuse{bbl@beforestart}
\catcode `"\active 
\providecommand\hyper@newdestlabel[2]{}
\providecommand\HyField@AuxAddToFields[1]{}
\providecommand\HyField@AuxAddToCoFields[2]{}
\pgfsyspdfmark {pgfid1}{256679}{0}
\babel@aux{vietnamese}{}
\@writefile{toc}{\contentsline {chapter}{\numberline {I}Mô tả đề tài}{2}{chapter.1}\protected@file@percent }
\@writefile{lof}{\addvspace {10\p@ }}
\@writefile{lot}{\addvspace {10\p@ }}
\@writefile{toc}{\contentsline {section}{\numberline {1}Tên đề tài}{2}{section.1.1}\protected@file@percent }
\@writefile{toc}{\contentsline {section}{\numberline {2}Mục tiêu}{2}{section.1.2}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {2.1}Mục tiêu tổng quát}{2}{subsection.1.2.1}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {2.2}Mục tiêu cụ thể}{2}{subsection.1.2.2}\protected@file@percent }
\@writefile{toc}{\contentsline {section}{\numberline {3}Phương pháp}{2}{section.1.3}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {3.1}Trích xuất đặc trưng hành vi-không gian--thời gian}{2}{subsection.1.3.1}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {3.2}Phân cụm hành vi}{3}{subsection.1.3.2}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {3.3}Khai phá luật kết hợp ngữ cảnh}{3}{subsection.1.3.3}\protected@file@percent }
\@writefile{toc}{\contentsline {section}{\numberline {4}Cách triển khai}{3}{section.1.4}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.1}Giai đoạn 1: Chuẩn bị dữ liệu}{3}{subsection.1.4.1}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.2}Giai đoạn 2: Tiền xử lý video}{3}{subsection.1.4.2}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.3}Giai đoạn 3: Trích xuất đặc trưng}{3}{subsection.1.4.3}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.4}Giai đoạn 4: Phân cụm hành vi}{4}{subsection.1.4.4}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.5}Giai đoạn 5: Khai phá luật kết hợp}{4}{subsection.1.4.5}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.6}Giai đoạn 6: Phát hiện bất thường}{4}{subsection.1.4.6}\protected@file@percent }
\@writefile{toc}{\contentsline {subsection}{\numberline {4.7}Giai đoạn 7: Đánh giá}{4}{subsection.1.4.7}\protected@file@percent }
\@writefile{toc}{\contentsline {section}{\numberline {5}Kết quả kỳ vọng}{4}{section.1.5}\protected@file@percent }
\gdef \@abspage@last{5}
</file>

<file path="latex/main.fdb_latexmk">
# Fdb version 4
["pdflatex"] 1778877406.48408 "c:/Users/Dell/Desktop/KPDL/Project/latex/main.tex" "main.pdf" "main" 1778877407.68632 0
  "C:/Program Files/MiKTeX/fonts/enc/dvips/lm/lm-t5.enc" 1256929440 2902 39ef7ea122fd143131d26a653f4643ee ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/cm/cmr12.tfm" 1136765053 1288 655e228510b4c2a1abe905c368440826 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmex10.tfm" 1256929440 992 ce925c9346c7613270a79afbee98c070 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmmi10.tfm" 1256929440 1528 6d36b2385e0ca062a654de6ac59cb34f ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmmi12.tfm" 1256929440 1524 753b192b18f2991794f9d41a8228510b ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmmi7.tfm" 1256929440 1528 d5b028dd23da623848ef0645c96a1ed7 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmsy10.tfm" 1256929440 1308 02cc510f9dd6012e5815d0c0ffbf6869 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/lmsy7.tfm" 1256929440 1304 32f22a15acc296b2a4e15698403dcb88 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/rm-lmr10.tfm" 1256929440 11868 4f81e9b6033c032bdaf9884f4d7ef412 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/rm-lmr12.tfm" 1256929440 11888 6841b91e46b65cf41a49b160e6e74130 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/rm-lmr7.tfm" 1256929440 11852 5a9022f105fd1ee2797df861e79ae9a0 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/t5-lmbx12.tfm" 1256929440 21168 598ec3d50576b4094ca507af5cfe70b2 ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/t5-lmr12.tfm" 1256929440 21132 be3d7683d9e7cc687a0acd99d4e8f5ec ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/t5-lmr17.tfm" 1256929440 21164 3207f0418107c8aac3f141c388a23f7b ""
  "C:/Program Files/MiKTeX/fonts/tfm/public/lm/t5-lmri12.tfm" 1256929440 24556 92b4dcef86572b81617a8d2cb915afb2 ""
  "C:/Program Files/MiKTeX/fonts/tfm/vntex/vnr/vnr12.tfm" 1644004041 3984 be7b630197255361b12d153ce5e1bc5b ""
  "C:/Program Files/MiKTeX/fonts/type1/public/lm/lmbx12.pfb" 1256929440 116908 9a7a12d1e4df280ea0ab4aad83cdef88 ""
  "C:/Program Files/MiKTeX/fonts/type1/public/lm/lmr12.pfb" 1256929440 113634 9384aebdf26f9881e046a4b5574566db ""
  "C:/Program Files/MiKTeX/fonts/type1/public/lm/lmri12.pfb" 1256929440 109265 e6c7d377db6dfccd4a123fb26da8cffb ""
  "C:/Program Files/MiKTeX/tex/context/base/mkii/supp-pdf.mkii" 1580390158 71627 94eb9990bed73c364d7f53f960cc8c5b ""
  "C:/Program Files/MiKTeX/tex/generic/babel/babel.sty" 1765441417 146654 eaac99efd6386094bb8a3980e34ec87b ""
  "C:/Program Files/MiKTeX/tex/generic/babel/locale/vi/babel-vi.ini" 1765441426 4607 b69aefb73dbed871df4e7fd030166008 ""
  "C:/Program Files/MiKTeX/tex/generic/babel/locale/vi/babel-vietnamese.tex" 1765441426 377 64e7dd9685eaf7c56d45042ca54a4951 ""
  "C:/Program Files/MiKTeX/tex/generic/babel/txtbabel.def" 1765441417 5231 82bb24bf20af7e4d760717f2cadb5170 ""
  "C:/Program Files/MiKTeX/tex/generic/bigintcalc/bigintcalc.sty" 1576433602 40635 c40361e206be584d448876bba8a64a3b ""
  "C:/Program Files/MiKTeX/tex/generic/bitset/bitset.sty" 1575926576 33961 6b5c75130e435b2bfdb9f480a09a39f9 ""
  "C:/Program Files/MiKTeX/tex/generic/gettitlestring/gettitlestring.sty" 1576433666 8371 9d55b8bd010bc717624922fb3477d92e ""
  "C:/Program Files/MiKTeX/tex/generic/iftex/iftex.sty" 1734114575 7984 7dbb9280f03c0a315425f1b4f35d43ee ""
  "C:/Program Files/MiKTeX/tex/generic/iftex/ifvtex.sty" 1734114575 1057 525c2192b5febbd8c1f662c9468335bb ""
  "C:/Program Files/MiKTeX/tex/generic/infwarerr/infwarerr.sty" 1575399508 8356 7bbb2c2373aa810be568c29e333da8ed ""
  "C:/Program Files/MiKTeX/tex/generic/intcalc/intcalc.sty" 1576433764 31769 002a487f55041f8e805cfbf6385ffd97 ""
  "C:/Program Files/MiKTeX/tex/generic/kvdefinekeys/kvdefinekeys.sty" 1576763304 5412 d5a2436094cd7be85769db90f29250a6 ""
  "C:/Program Files/MiKTeX/tex/generic/ltxcmds/ltxcmds.sty" 1702206890 17865 1a9bd36b4f98178fa551aca822290953 ""
  "C:/Program Files/MiKTeX/tex/generic/pdfescape/pdfescape.sty" 1575926700 19007 15924f7228aca6c6d184b115f4baa231 ""
  "C:/Program Files/MiKTeX/tex/generic/pdftexcmds/pdftexcmds.sty" 1623005277 20089 80423eac55aa175305d35b49e04fe23b ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcore.code.tex" 1756451144 1016 1c2b89187d12a2768764b83b4945667c ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorearrows.code.tex" 1756451144 43906 06058dc09064474303f3b5dd62d982c0 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoreexternal.code.tex" 1756451144 19324 f4e4c6403dd0f1605fd20ed22fa79dea ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoregraphicstate.code.tex" 1756451143 6038 ccb406740cc3f03bbfb58ad504fe8c27 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoreimage.code.tex" 1756451143 6911 f6d4cf5a3fef5cc879d668b810e82868 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorelayers.code.tex" 1756451144 4883 42daaf41e27c3735286e23e48d2d7af9 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoreobjects.code.tex" 1756451144 2544 8c06d2a7f0f469616ac9e13db6d2f842 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorepathconstruct.code.tex" 1756451144 44195 5e390c414de027626ca5e2df888fa68d ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorepathprocessing.code.tex" 1756451144 17311 e001219836e75b16c4af9a112785f30a ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorepathusage.code.tex" 1756451144 21302 788a79944eb22192a4929e46963a3067 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorepatterns.code.tex" 1756451144 9691 3d42d89522f4650c2f3dc616ca2b925e ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorepoints.code.tex" 1756451144 33335 dd1fa4814d4e51f18be97d88bf0da60c ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorequick.code.tex" 1756451144 2965 4c2b1f4e0826925746439038172e5d6f ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorerdf.code.tex" 1756451144 5196 2cc249e0ee7e03da5f5f6589257b1e5b ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcorescopes.code.tex" 1756451144 20821 7579108c1e9363e61a0b1584778804aa ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoreshade.code.tex" 1756451143 35251 5ff5b5b310c5ac882610e0ccc99095e7 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoretransformations.code.tex" 1756451143 22012 81b34a0aa8fa1a6158cc6220b00e4f10 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/basiclayer/pgfcoretransparency.code.tex" 1756451144 8893 e851de2175338fdf7c17f3e091d94618 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibrarypositioning.code.tex" 1756451144 3937 3f208572dd82c71103831da976d74f1a ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibraryshapes.multipart.code.tex" 1756451144 923 c7a223b32ffdeb1c839d97935eee61ff ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibrarytopaths.code.tex" 1756451144 11518 738408f795261b70ce8dd47459171309 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/frontendlayer/tikz/tikz.code.tex" 1756451144 186859 0445d9a41a87648b4723e04765409541 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/libraries/pgflibraryarrows.meta.code.tex" 1756451144 58801 1e750fb0692eb99aaac45698bbec96b1 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/libraries/pgflibraryplothandlers.code.tex" 1756451144 32995 ac577023e12c0e4bd8aa420b2e852d1a ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/libraries/shapes/pgflibraryshapes.multipart.code.tex" 1756451144 65895 c22f5222d3f2fa976abe8f78d657cd2a ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfint.code.tex" 1756451144 3063 8c415c68a0f3394e45cfeca0b65f6ee6 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmath.code.tex" 1756451144 949 cea70942e7b7eddabfb3186befada2e6 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathcalc.code.tex" 1756451144 13272 7777a64fbd07131a37d276b131c17ee2 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfloat.code.tex" 1756451144 104717 9b2393fbf004a0ce7fa688dbce423848 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.base.code.tex" 1756451144 10165 cec5fa73d49da442e56efc2d605ef154 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.basic.code.tex" 1756451144 28178 41c17713108e0795aac6fef3d275fbca ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.code.tex" 1756451144 9649 85779d3d8d573bfd2cd4137ba8202e60 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.comparison.code.tex" 1756451144 3865 ac538ab80c5cf82b345016e474786549 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.integerarithmetics.code.tex" 1756451144 3177 27d85c44fbfe09ff3b2cf2879e3ea434 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.misc.code.tex" 1756451144 11024 0179538121bc2dba172013a3ef89519f ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.random.code.tex" 1756451144 7889 d0e193914ddc35444510f5b569e26b3d ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.round.code.tex" 1756451144 3379 781797a101f647bab82741a99944a229 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathfunctions.trigonometric.code.tex" 1756451144 92405 f515f31275db273f97b9d8f52e1b0736 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathparser.code.tex" 1756451144 37733 0fe471ac50324723cf6ab693e5c0916c ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/math/pgfmathutil.code.tex" 1756451144 8471 c2883569d03f69e8e1cabfef4999cfd7 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/modules/pgfmodulematrix.code.tex" 1756451143 21211 1e73ec76bd73964d84197cc3d2685b01 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/modules/pgfmoduleplot.code.tex" 1756451143 16218 98503859deba28f16813029fd927ed8e ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/modules/pgfmoduleshapes.code.tex" 1756451143 44792 c4a5a3feba777682c1d16420f2f01a5b ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/pgf.revision.tex" 1756451143 116 760d50e6a16543bf6edb475635793673 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgf.cfg" 1756451144 926 2963ea0dcf6cc6c0a770b69ec46a477b ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgfsys-common-pdf.def" 1756451144 5542 32f75a31ea6c3a7e1148cd6d5e93dbb7 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgfsys-pdftex.def" 1756451144 12612 7774ba67bfd72e593c4436c2de6201e3 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgfsys.code.tex" 1756451144 61355 39904e7552da3800a6838d41440943a5 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgfsysprotocol.code.tex" 1756451144 1896 b8e0ca0ac371d74c0ca05583f6313c91 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/systemlayer/pgfsyssoftpath.code.tex" 1756451144 7778 53c8b5623d80238f6a20aa1df1868e63 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgffor.code.tex" 1756451143 24033 d8893a1ec4d1bfa101b172754743d340 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgfkeys.code.tex" 1756451143 39784 414c54e866ebab4b801e2ad81d9b21d8 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgfkeyslibraryfiltered.code.tex" 1756451143 37436 50ba7794827e363eec9ea3467c15c6d7 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgfrcs.code.tex" 1756451143 4385 510565c2f07998c8a0e14f0ec07ff23c ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgfutil-common.tex" 1756451143 30029 c49ea8f95207c46731469c614daf4e33 ""
  "C:/Program Files/MiKTeX/tex/generic/pgf/utilities/pgfutil-latex.def" 1756451143 7067 11553488d1600cac6a0cfca012fca111 ""
  "C:/Program Files/MiKTeX/tex/generic/stringenc/stringenc.sty" 1575105840 21514 b7557edcee22835ef6b03ede1802dad4 ""
  "C:/Program Files/MiKTeX/tex/generic/uniquecounter/uniquecounter.sty" 1576434012 7008 f92eaa0a3872ed622bbf538217cd2ab7 ""
  "C:/Program Files/MiKTeX/tex/latex/00miktex/epstopdf-sys.cfg" 1616067285 584 2a1075dd71571459f59146da9f7502ad ""
  "C:/Program Files/MiKTeX/tex/latex/arabi/bblopts.cfg" 1705750854 902 c30e5c373bc58bde21f8f63a3091626f ""
  "C:/Program Files/MiKTeX/tex/latex/babel-vietnamese/vietnamese.ldf" 1451823267 5845 4c316f1620fa004601754fb032466a9b ""
  "C:/Program Files/MiKTeX/tex/latex/base/exscale.sty" 1761900684 2581 e3b93c96ce990e6e2ffc819a28b77ea9 ""
  "C:/Program Files/MiKTeX/tex/latex/base/fontenc.sty" 1761900684 5275 6f9d359641b36842524cdb97716ab75f ""
  "C:/Program Files/MiKTeX/tex/latex/base/inputenc.sty" 1761900684 5048 0270515b828149155424600fd2d58ac5 ""
  "C:/Program Files/MiKTeX/tex/latex/base/size12.clo" 1761900684 8449 8dc66c6c313c8eb2d774af83bca435dd ""
  "C:/Program Files/MiKTeX/tex/latex/caption/caption.sty" 1696169587 56128 c2ccf1a29d78c33bc553880402e4fb9a ""
  "C:/Program Files/MiKTeX/tex/latex/caption/caption3.sty" 1696169587 72619 ee90b6612147680fd73c3b1406a74245 ""
  "C:/Program Files/MiKTeX/tex/latex/caption/ltcaption.sty" 1696169587 7418 021d7c4eb11bde94592761855a3d046e ""
  "C:/Program Files/MiKTeX/tex/latex/caption/subcaption.sty" 1696169587 12494 0c0cdb824278a4d51cefeb2e79901315 ""
  "C:/Program Files/MiKTeX/tex/latex/epstopdf-pkg/epstopdf-base.sty" 1623003186 13886 d1306dcf79a944f6988e688c1785f9ce ""
  "C:/Program Files/MiKTeX/tex/latex/eso-pic/eso-pic.sty" 1766748757 11838 c56b1b2e06d66f65afcf19adbfba71b5 ""
  "C:/Program Files/MiKTeX/tex/latex/etoolbox/etoolbox.sty" 1759409308 46885 8953c67ffba03252c6090aa19568b8ba ""
  "C:/Program Files/MiKTeX/tex/latex/extsizes/extreport.cls" 969409740 23786 cbd79291aa0922c48a73753b97cabb5f ""
  "C:/Program Files/MiKTeX/tex/latex/float/float.sty" 1469023953 6916 16d2656a1984957e674b149555f1ea1d ""
  "C:/Program Files/MiKTeX/tex/latex/geometry/geometry.cfg" 1773583286 1083 f69f5eb2d8e249286c59dd1340122e59 ""
  "C:/Program Files/MiKTeX/tex/latex/geometry/geometry.sty" 1773583286 41904 7711079366884e0d15963c9d40019853 ""
  "C:/Program Files/MiKTeX/tex/latex/graphics-cfg/color.cfg" 1465894292 1213 620bba36b25224fa9b7e1ccb4ecb76fd ""
  "C:/Program Files/MiKTeX/tex/latex/graphics-cfg/graphics.cfg" 1465894292 1224 978390e9c2234eab29404bc21b268d1e ""
  "C:/Program Files/MiKTeX/tex/latex/graphics-def/pdftex.def" 1762694372 19626 23e2822b9b2b5005f4c549ca98b9334d ""
  "C:/Program Files/MiKTeX/tex/latex/graphics/graphics.sty" 1749322634 18363 69bb4f5538964bfea50d1e6d89cbe69f ""
  "C:/Program Files/MiKTeX/tex/latex/graphics/graphicx.sty" 1749322634 8118 43b99e52946c33a23f5f43b52d5cc5ec ""
  "C:/Program Files/MiKTeX/tex/latex/graphics/keyval.sty" 1749322634 2671 d9941f4bf4750e9b0603c9a2ec54693b ""
  "C:/Program Files/MiKTeX/tex/latex/graphics/mathcolor.ltx" 1749322634 2885 9c645d672ae17285bba324998918efd8 ""
  "C:/Program Files/MiKTeX/tex/latex/graphics/trig.sty" 1749322634 4023 e66acf578d6b564c4670fb57ff336a7a ""
  "C:/Program Files/MiKTeX/tex/latex/hycolor/hycolor.sty" 1580380792 18571 4c28a13fc3d975e6e81c9bea1d697276 ""
  "C:/Program Files/MiKTeX/tex/latex/hyperref/hpdftex.def" 1752319040 48140 5e8a3a4aa88ae09b90d524926a067201 ""
  "C:/Program Files/MiKTeX/tex/latex/hyperref/hyperref.sty" 1752319040 223112 93e90b2b1b3ef21af41adaf029922dd3 ""
  "C:/Program Files/MiKTeX/tex/latex/hyperref/nameref.sty" 1752319040 11027 0fe7ce2c6b5291fd809c2de7bbdca37e ""
  "C:/Program Files/MiKTeX/tex/latex/hyperref/pd1enc.def" 1752319041 14249 e14b403fb70abdf1f6742598a63b0e2a ""
  "C:/Program Files/MiKTeX/tex/latex/hyperref/puenc.def" 1752319041 117118 e2f5f7983a43f89e2ffcd709fc59d37c ""
  "C:/Program Files/MiKTeX/tex/latex/kvoptions/kvoptions.sty" 1656236481 22555 6d8e155cfef6d82c3d5c742fea7c992e ""
  "C:/Program Files/MiKTeX/tex/latex/kvsetkeys/kvsetkeys.sty" 1665066333 13815 760b0c02f691ea230f5359c4e1de23a7 ""
  "C:/Program Files/MiKTeX/tex/latex/l3backend/l3backend-pdftex.def" 1761202581 30662 bfd6e864f4ffc5018b0e2b6260c3181c ""
  "C:/Program Files/MiKTeX/tex/latex/lm/lmodern.sty" 1430474546 1608 b00724785a9e9c599e5181bb8729160b ""
  "C:/Program Files/MiKTeX/tex/latex/lm/omllmm.fd" 1430474546 890 57f5adccd504fb5c98bdf99ed7e7f195 ""
  "C:/Program Files/MiKTeX/tex/latex/lm/omslmsy.fd" 1430474546 807 3de192f3efa968913bd2f096a7b430d8 ""
  "C:/Program Files/MiKTeX/tex/latex/lm/omxlmex.fd" 1430474546 568 a5494d810f2680caf10205cd1226c76c ""
  "C:/Program Files/MiKTeX/tex/latex/lm/ot1lmr.fd" 1430474546 1882 28c08db1407ebff35a658fd141753d16 ""
  "C:/Program Files/MiKTeX/tex/latex/lm/t5lmr.fd" 1430474546 1867 13879a4ae9774e19049624dae5d988a8 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/basiclayer/pgf.sty" 1756451144 1090 bae35ef70b3168089ef166db3e66f5b2 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/basiclayer/pgfcore.sty" 1756451144 373 00b204b1d7d095b892ad31a7494b0373 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/compatibility/pgfcomp-version-0-65.sty" 1756451144 21013 f4ff83d25bb56552493b030f27c075ae ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/compatibility/pgfcomp-version-1-18.sty" 1756451144 989 c49c8ae06d96f8b15869da7428047b1e ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/frontendlayer/tikz.sty" 1756451144 339 c2e180022e3afdb99c7d0ea5ce469b7d ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/math/pgfmath.sty" 1756451144 306 c56a323ca5bf9242f54474ced10fca71 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/systemlayer/pgfsys.sty" 1756451144 443 8c872229db56122037e86bcda49e14f3 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/utilities/pgffor.sty" 1756451144 348 ee405e64380c11319f0e249fed57e6c5 ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/utilities/pgfkeys.sty" 1756451144 274 5ae372b7df79135d240456a1c6f2cf9a ""
  "C:/Program Files/MiKTeX/tex/latex/pgf/utilities/pgfrcs.sty" 1756451144 325 f9f16d12354225b7dd52a3321f085955 ""
  "C:/Program Files/MiKTeX/tex/latex/psnfss/times.sty" 1585926771 888 8e0e5c8cca7b18e0400f97f5a2b90a99 ""
  "C:/Program Files/MiKTeX/tex/latex/ragged2e/ragged2e.sty" 1687525147 10085 3dececaeed388e31285c366e124c0c4e ""
  "C:/Program Files/MiKTeX/tex/latex/refcount/refcount.sty" 1576433952 9878 9e94e8fa600d95f9c7731bb21dfb67a4 ""
  "C:/Program Files/MiKTeX/tex/latex/rerunfilecheck/rerunfilecheck.sty" 1751104012 9684 a33a14b82ce60d6e77cb9be689d79ee6 ""
  "C:/Program Files/MiKTeX/tex/latex/setspace/setspace.sty" 1670403145 22490 8cac309b79a4c53a4ffce4b1b07aead0 ""
  "C:/Program Files/MiKTeX/tex/latex/titlesec/titlesec.sty" 1736608268 48766 87a17a4ef312a39cd43896e34a679a56 ""
  "C:/Program Files/MiKTeX/tex/latex/tocloft/tocloft.sty" 1578908763 37263 3e78d14f0f4b1a30560fea5e04de805d ""
  "C:/Program Files/MiKTeX/tex/latex/tools/array.sty" 1761901070 15651 9d7c62df82cb29a555c00550babfe364 ""
  "C:/Program Files/MiKTeX/tex/latex/tools/longtable.sty" 1761901070 16014 dfa4b4d4e04a78a3f05a90c19711737a ""
  "C:/Program Files/MiKTeX/tex/latex/url/url.sty" 1388490452 12796 8edb7d69a20b857904dd0ea757c14ec9 ""
  "C:/Program Files/MiKTeX/tex/latex/vntex/t5cmr.fd" 1368738347 1333 ab1c3d512aa3d88df568aed3641db58f ""
  "C:/Program Files/MiKTeX/tex/latex/vntex/t5enc.def" 1368738347 21986 ccb3b8f7cc27fbd61b3daabbbb82ad3f ""
  "C:/Program Files/MiKTeX/tex/latex/vntex/t5enc.dfu" 1368738347 13949 387dd4fa58aea5f873839c93430fffed ""
  "C:/Program Files/MiKTeX/tex/latex/xcolor/xcolor.sty" 1729265222 55384 b454dec21c2d9f45ec0b793f0995b992 ""
  "C:/Users/Dell/AppData/Local/MiKTeX/fonts/map/pdftex/pdftex.map" 1778825213 80909 eab91d9745dd2edfd62a31d53cd5fe15 ""
  "C:/Users/Dell/AppData/Local/MiKTeX/miktex/data/le/pdftex/pdflatex.fmt" 1778826040 13323417 0fa8f4677c79b47fd68b55f6d0e5af45 ""
  "border.jpeg" 1778877385 107822 08594ea88c528b3c6a86336f740530db ""
  "c:/Users/Dell/Desktop/KPDL/Project/latex/main.tex" 1778877361 3032 cb36b9b2ae2633bea9eed7ecdbb6866f ""
  "logo.jpeg" 1778877368 37537 11d70df0906359df771f5d6685a0cfda ""
  "main.aux" 1778877407 338 1efec02c3b6ff5dac452be3568d88e1b "pdflatex"
  "main.out" 1778877407 0 d41d8cd98f00b204e9800998ecf8427e "pdflatex"
  "main.tex" 1778877361 3032 cb36b9b2ae2633bea9eed7ecdbb6866f ""
  "main.toc" 1778877407 33 781f3c76fc0ac7703b06ef6e1db9bb18 "pdflatex"
  "titlepage.tex" 1778877353 1364 7bdb36f318759457e0e99989b4f80a28 ""
  (generated)
  "main.aux"
  "main.log"
  "main.out"
  "main.pdf"
  "main.toc"
  (rewritten before read)
</file>

<file path="latex/main.fls">
PWD c:\Users\Dell\Desktop\KPDL\Project\latex
INPUT C:\Users\Dell\AppData\Local\MiKTeX\miktex\data\le\pdftex\pdflatex.fmt
INPUT c:\Users\Dell\Desktop\KPDL\Project\latex\main.tex
OUTPUT main.log
INPUT C:\Program Files\MiKTeX\tex\latex\extsizes\extreport.cls
INPUT C:\Program Files\MiKTeX\tex\latex\extsizes\extreport.cls
INPUT C:\Program Files\MiKTeX\tex\latex\base\size12.clo
INPUT C:\Program Files\MiKTeX\tex\latex\base\size12.clo
INPUT C:\Program Files\MiKTeX\tex\latex\base\size12.clo
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\cm\cmr12.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\base\exscale.sty
INPUT C:\Program Files\MiKTeX\tex\latex\base\exscale.sty
INPUT C:\Program Files\MiKTeX\tex\latex\base\inputenc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\base\inputenc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\base\fontenc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\base\fontenc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.dfu
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.dfu
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.dfu
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5cmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5cmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5cmr.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\vntex\vnr\vnr12.tfm
INPUT C:\Program Files\MiKTeX\tex\generic\babel\babel.sty
INPUT C:\Program Files\MiKTeX\tex\generic\babel\babel.sty
INPUT C:\Program Files\MiKTeX\tex\generic\babel\txtbabel.def
INPUT C:\Program Files\MiKTeX\tex\latex\arabi\bblopts.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\arabi\bblopts.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\arabi\bblopts.cfg
INPUT C:\Program Files\MiKTeX\tex\generic\babel\locale\vi\babel-vietnamese.tex
INPUT C:\Program Files\MiKTeX\tex\generic\babel\locale\vi\babel-vietnamese.tex
INPUT C:\Program Files\MiKTeX\tex\generic\babel\locale\vi\babel-vietnamese.tex
INPUT C:\Program Files\MiKTeX\tex\generic\babel\locale\vi\babel-vi.ini
INPUT C:\Program Files\MiKTeX\tex\latex\babel-vietnamese\vietnamese.ldf
INPUT C:\Program Files\MiKTeX\tex\latex\babel-vietnamese\vietnamese.ldf
INPUT C:\Program Files\MiKTeX\tex\latex\babel-vietnamese\vietnamese.ldf
INPUT C:\Program Files\MiKTeX\tex\latex\vntex\t5enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\geometry\geometry.sty
INPUT C:\Program Files\MiKTeX\tex\latex\geometry\geometry.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\keyval.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\keyval.sty
INPUT C:\Program Files\MiKTeX\tex\generic\iftex\ifvtex.sty
INPUT C:\Program Files\MiKTeX\tex\generic\iftex\ifvtex.sty
INPUT C:\Program Files\MiKTeX\tex\generic\iftex\iftex.sty
INPUT C:\Program Files\MiKTeX\tex\generic\iftex\iftex.sty
INPUT C:\Program Files\MiKTeX\tex\latex\geometry\geometry.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\geometry\geometry.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\geometry\geometry.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\psnfss\times.sty
INPUT C:\Program Files\MiKTeX\tex\latex\psnfss\times.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\graphicx.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\graphicx.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\graphics.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\graphics.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\trig.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\trig.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\graphics.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\graphics.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\graphics.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-def\pdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-def\pdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-def\pdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\titlesec\titlesec.sty
INPUT C:\Program Files\MiKTeX\tex\latex\titlesec\titlesec.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tocloft\tocloft.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tocloft\tocloft.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\frontendlayer\tikz.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\frontendlayer\tikz.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\basiclayer\pgf.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\basiclayer\pgf.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgfrcs.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgfrcs.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfutil-common.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfutil-latex.def
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfrcs.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfrcs.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfrcs.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\pgf.revision.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\pgf.revision.tex
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\basiclayer\pgfcore.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\basiclayer\pgfcore.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\systemlayer\pgfsys.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\systemlayer\pgfsys.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfkeys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfkeyslibraryfiltered.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgf.cfg
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys-pdftex.def
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys-pdftex.def
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsys-common-pdf.def
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsyssoftpath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsyssoftpath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsyssoftpath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsysprotocol.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsysprotocol.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\systemlayer\pgfsysprotocol.code.tex
INPUT C:\Program Files\MiKTeX\tex\latex\xcolor\xcolor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\xcolor\xcolor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\color.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\color.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics-cfg\color.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\mathcolor.ltx
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\mathcolor.ltx
INPUT C:\Program Files\MiKTeX\tex\latex\graphics\mathcolor.ltx
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcore.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcore.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcore.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathutil.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathparser.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.basic.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.trigonometric.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.random.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.comparison.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.base.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.round.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.misc.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfunctions.integerarithmetics.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathcalc.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmathfloat.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfint.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorepoints.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorepathconstruct.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorepathusage.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorescopes.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoregraphicstate.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoretransformations.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorequick.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoreobjects.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorepathprocessing.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorearrows.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoreshade.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoreimage.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoreexternal.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorelayers.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcoretransparency.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorepatterns.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\basiclayer\pgfcorerdf.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\modules\pgfmoduleshapes.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\modules\pgfmoduleplot.code.tex
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\compatibility\pgfcomp-version-0-65.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\compatibility\pgfcomp-version-0-65.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\compatibility\pgfcomp-version-1-18.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\compatibility\pgfcomp-version-1-18.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgffor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgffor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgfkeys.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\utilities\pgfkeys.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfkeys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfkeys.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgfkeys.code.tex
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\math\pgfmath.sty
INPUT C:\Program Files\MiKTeX\tex\latex\pgf\math\pgfmath.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\math\pgfmath.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgffor.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgffor.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\utilities\pgffor.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\tikz.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\tikz.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\tikz.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\pgflibraryplothandlers.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\pgflibraryplothandlers.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\modules\pgfmodulematrix.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibrarytopaths.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibrarytopaths.code.tex
INPUT C:\Program Files\MiKTeX\tex\latex\setspace\setspace.sty
INPUT C:\Program Files\MiKTeX\tex\latex\setspace\setspace.sty
INPUT C:\Program Files\MiKTeX\tex\latex\eso-pic\eso-pic.sty
INPUT C:\Program Files\MiKTeX\tex\latex\eso-pic\eso-pic.sty
INPUT C:\Program Files\MiKTeX\tex\latex\float\float.sty
INPUT C:\Program Files\MiKTeX\tex\latex\float\float.sty
INPUT C:\Program Files\MiKTeX\tex\latex\ragged2e\ragged2e.sty
INPUT C:\Program Files\MiKTeX\tex\latex\ragged2e\ragged2e.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tools\array.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tools\array.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tools\longtable.sty
INPUT C:\Program Files\MiKTeX\tex\latex\tools\longtable.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\subcaption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\subcaption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\caption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\caption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\caption3.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\caption3.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\ltcaption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\caption\ltcaption.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\hyperref.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\hyperref.sty
INPUT C:\Program Files\MiKTeX\tex\latex\kvsetkeys\kvsetkeys.sty
INPUT C:\Program Files\MiKTeX\tex\latex\kvsetkeys\kvsetkeys.sty
INPUT C:\Program Files\MiKTeX\tex\generic\kvdefinekeys\kvdefinekeys.sty
INPUT C:\Program Files\MiKTeX\tex\generic\kvdefinekeys\kvdefinekeys.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pdfescape\pdfescape.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pdfescape\pdfescape.sty
INPUT C:\Program Files\MiKTeX\tex\generic\ltxcmds\ltxcmds.sty
INPUT C:\Program Files\MiKTeX\tex\generic\ltxcmds\ltxcmds.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pdftexcmds\pdftexcmds.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pdftexcmds\pdftexcmds.sty
INPUT C:\Program Files\MiKTeX\tex\generic\infwarerr\infwarerr.sty
INPUT C:\Program Files\MiKTeX\tex\generic\infwarerr\infwarerr.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hycolor\hycolor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hycolor\hycolor.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\nameref.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\nameref.sty
INPUT C:\Program Files\MiKTeX\tex\latex\refcount\refcount.sty
INPUT C:\Program Files\MiKTeX\tex\latex\refcount\refcount.sty
INPUT C:\Program Files\MiKTeX\tex\generic\gettitlestring\gettitlestring.sty
INPUT C:\Program Files\MiKTeX\tex\generic\gettitlestring\gettitlestring.sty
INPUT C:\Program Files\MiKTeX\tex\latex\kvoptions\kvoptions.sty
INPUT C:\Program Files\MiKTeX\tex\latex\kvoptions\kvoptions.sty
INPUT C:\Program Files\MiKTeX\tex\latex\etoolbox\etoolbox.sty
INPUT C:\Program Files\MiKTeX\tex\latex\etoolbox\etoolbox.sty
INPUT C:\Program Files\MiKTeX\tex\generic\stringenc\stringenc.sty
INPUT C:\Program Files\MiKTeX\tex\generic\stringenc\stringenc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\pd1enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\pd1enc.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\pd1enc.def
INPUT C:\Program Files\MiKTeX\tex\generic\intcalc\intcalc.sty
INPUT C:\Program Files\MiKTeX\tex\generic\intcalc\intcalc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\puenc.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\puenc.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\puenc.def
INPUT C:\Program Files\MiKTeX\tex\latex\url\url.sty
INPUT C:\Program Files\MiKTeX\tex\latex\url\url.sty
INPUT C:\Program Files\MiKTeX\tex\generic\bitset\bitset.sty
INPUT C:\Program Files\MiKTeX\tex\generic\bitset\bitset.sty
INPUT C:\Program Files\MiKTeX\tex\generic\bigintcalc\bigintcalc.sty
INPUT C:\Program Files\MiKTeX\tex\generic\bigintcalc\bigintcalc.sty
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\hpdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\hpdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\hyperref\hpdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\rerunfilecheck\rerunfilecheck.sty
INPUT C:\Program Files\MiKTeX\tex\latex\rerunfilecheck\rerunfilecheck.sty
INPUT C:\Program Files\MiKTeX\tex\generic\uniquecounter\uniquecounter.sty
INPUT C:\Program Files\MiKTeX\tex\generic\uniquecounter\uniquecounter.sty
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibrarypositioning.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibrarypositioning.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\pgflibraryarrows.meta.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\pgflibraryarrows.meta.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\pgflibraryarrows.meta.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibraryshapes.multipart.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\frontendlayer\tikz\libraries\tikzlibraryshapes.multipart.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\shapes\pgflibraryshapes.multipart.code.tex
INPUT C:\Program Files\MiKTeX\tex\generic\pgf\libraries\shapes\pgflibraryshapes.multipart.code.tex
INPUT C:\Program Files\MiKTeX\tex\latex\lm\lmodern.sty
INPUT C:\Program Files\MiKTeX\tex\latex\lm\lmodern.sty
INPUT C:\Program Files\MiKTeX\tex\latex\lm\t5lmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\t5lmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\t5lmr.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmr12.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\l3backend\l3backend-pdftex.def
INPUT C:\Program Files\MiKTeX\tex\latex\l3backend\l3backend-pdftex.def
INPUT .\main.aux
INPUT .\main.aux
INPUT main.aux
OUTPUT main.aux
INPUT C:\Program Files\MiKTeX\tex\context\base\mkii\supp-pdf.mkii
INPUT C:\Program Files\MiKTeX\tex\context\base\mkii\supp-pdf.mkii
INPUT C:\Program Files\MiKTeX\tex\context\base\mkii\supp-pdf.mkii
INPUT C:\Program Files\MiKTeX\tex\latex\epstopdf-pkg\epstopdf-base.sty
INPUT C:\Program Files\MiKTeX\tex\latex\epstopdf-pkg\epstopdf-base.sty
INPUT C:\Program Files\MiKTeX\tex\latex\00miktex\epstopdf-sys.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\00miktex\epstopdf-sys.cfg
INPUT C:\Program Files\MiKTeX\tex\latex\00miktex\epstopdf-sys.cfg
INPUT .\main.out
INPUT .\main.out
INPUT main.out
INPUT main.out
INPUT .\main.out
INPUT .\main.out
OUTPUT main.out
OUTPUT main.pdf
INPUT .\titlepage.tex
INPUT .\titlepage.tex
INPUT titlepage.tex
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmbx12.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmbx12.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmbx12.tfm
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT .\logo.jpeg
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmr12.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\lm\ot1lmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\ot1lmr.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\ot1lmr.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\rm-lmr12.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\rm-lmr10.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\rm-lmr7.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omllmm.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omllmm.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omllmm.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmmi12.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmmi10.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmmi7.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omslmsy.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omslmsy.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omslmsy.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmsy10.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmsy10.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmsy7.tfm
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omxlmex.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omxlmex.fd
INPUT C:\Program Files\MiKTeX\tex\latex\lm\omxlmex.fd
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\lmex10.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmri12.tfm
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT .\border.jpeg
INPUT C:\Users\Dell\AppData\Local\MiKTeX\fonts\map\pdftex\pdftex.map
INPUT C:\Program Files\MiKTeX\fonts\enc\dvips\lm\lm-t5.enc
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmr17.tfm
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\t5-lmbx12.tfm
INPUT .\main.toc
INPUT .\main.toc
INPUT main.toc
INPUT C:\Program Files\MiKTeX\fonts\tfm\public\lm\rm-lmr12.tfm
OUTPUT main.toc
INPUT main.aux
INPUT .\main.out
INPUT .\main.out
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmbx12.pfb
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmbx12.pfb
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmr12.pfb
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmr12.pfb
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmri12.pfb
INPUT C:\Program Files\MiKTeX\fonts\type1\public\lm\lmri12.pfb
</file>

<file path="latex/main.out">
\BOOKMARK [0][-]{chapter.1}{\376\377\000M\000\364\000\040\000t\036\243\000\040\001\021\036\301\000\040\000t\000\340\000i}{}% 1
\BOOKMARK [1][-]{section.1.1}{\376\377\000T\000\352\000n\000\040\001\021\036\301\000\040\000t\000\340\000i}{chapter.1}% 2
\BOOKMARK [1][-]{section.1.2}{\376\377\000M\036\345\000c\000\040\000t\000i\000\352\000u}{chapter.1}% 3
\BOOKMARK [2][-]{subsection.1.2.1}{\376\377\000M\036\345\000c\000\040\000t\000i\000\352\000u\000\040\000t\036\325\000n\000g\000\040\000q\000u\000\341\000t}{section.1.2}% 4
\BOOKMARK [2][-]{subsection.1.2.2}{\376\377\000M\036\345\000c\000\040\000t\000i\000\352\000u\000\040\000c\036\345\000\040\000t\000h\036\303}{section.1.2}% 5
\BOOKMARK [1][-]{section.1.3}{\376\377\000P\000h\001\260\001\241\000n\000g\000\040\000p\000h\000\341\000p}{chapter.1}% 6
\BOOKMARK [2][-]{subsection.1.3.1}{\376\377\000T\000r\000\355\000c\000h\000\040\000x\000u\036\245\000t\000\040\001\021\036\267\000c\000\040\000t\000r\001\260\000n\000g\000\040\000h\000\340\000n\000h\000\040\000v\000i\000-\000k\000h\000\364\000n\000g\000\040\000g\000i\000a\000n\040\023\000t\000h\036\335\000i\000\040\000g\000i\000a\000n}{section.1.3}% 7
\BOOKMARK [2][-]{subsection.1.3.2}{\376\377\000P\000h\000\342\000n\000\040\000c\036\345\000m\000\040\000h\000\340\000n\000h\000\040\000v\000i}{section.1.3}% 8
\BOOKMARK [2][-]{subsection.1.3.3}{\376\377\000K\000h\000a\000i\000\040\000p\000h\000\341\000\040\000l\000u\036\255\000t\000\040\000k\036\277\000t\000\040\000h\036\343\000p\000\040\000n\000g\036\357\000\040\000c\036\243\000n\000h}{section.1.3}% 9
\BOOKMARK [1][-]{section.1.4}{\376\377\000C\000\341\000c\000h\000\040\000t\000r\000i\036\303\000n\000\040\000k\000h\000a\000i}{chapter.1}% 10
\BOOKMARK [2][-]{subsection.1.4.1}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0001\000:\000\040\000C\000h\000u\036\251\000n\000\040\000b\036\313\000\040\000d\036\357\000\040\000l\000i\036\307\000u}{section.1.4}% 11
\BOOKMARK [2][-]{subsection.1.4.2}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0002\000:\000\040\000T\000i\036\301\000n\000\040\000x\036\355\000\040\000l\000\375\000\040\000v\000i\000d\000e\000o}{section.1.4}% 12
\BOOKMARK [2][-]{subsection.1.4.3}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0003\000:\000\040\000T\000r\000\355\000c\000h\000\040\000x\000u\036\245\000t\000\040\001\021\036\267\000c\000\040\000t\000r\001\260\000n\000g}{section.1.4}% 13
\BOOKMARK [2][-]{subsection.1.4.4}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0004\000:\000\040\000P\000h\000\342\000n\000\040\000c\036\345\000m\000\040\000h\000\340\000n\000h\000\040\000v\000i}{section.1.4}% 14
\BOOKMARK [2][-]{subsection.1.4.5}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0005\000:\000\040\000K\000h\000a\000i\000\040\000p\000h\000\341\000\040\000l\000u\036\255\000t\000\040\000k\036\277\000t\000\040\000h\036\343\000p}{section.1.4}% 15
\BOOKMARK [2][-]{subsection.1.4.6}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0006\000:\000\040\000P\000h\000\341\000t\000\040\000h\000i\036\307\000n\000\040\000b\036\245\000t\000\040\000t\000h\001\260\036\335\000n\000g}{section.1.4}% 16
\BOOKMARK [2][-]{subsection.1.4.7}{\376\377\000G\000i\000a\000i\000\040\001\021\000o\036\241\000n\000\040\0007\000:\000\040\001\020\000\341\000n\000h\000\040\000g\000i\000\341}{section.1.4}% 17
\BOOKMARK [1][-]{section.1.5}{\376\377\000K\036\277\000t\000\040\000q\000u\036\243\000\040\000k\036\363\000\040\000v\036\315\000n\000g}{chapter.1}% 18
</file>

<file path="latex/main.tex">
\documentclass[12pt, a4paper]{extreport}

% --- CÁC GÓI CẦN THIẾT ---
\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc} 
\usepackage[vietnamese]{babel}
\usepackage{geometry}
\usepackage{times} 
\usepackage{graphicx} 
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{tikz}
\usepackage{setspace}
\usepackage{eso-pic} 
\usepackage{float} 
\usepackage{ragged2e}
\usepackage{array}
\usepackage{longtable}
\usepackage{subcaption}
\usepackage{tikz}
\usepackage{hyperref}
\usetikzlibrary{positioning, arrows.meta, shapes.multipart}

% Thiết lập lề cho tài liệu
\geometry{a4paper, top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm}

% --- ĐỊNH DẠNG ĐÁNH SỐ ĐỀ MỤC ---

\renewcommand{\thechapter}{\Roman{chapter}}
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
\renewcommand{\theparagraph}{\alph{paragraph})}

% --- ĐỊNH DẠNG ĐÁNH SỐ HÌNH ẢNH/BẢNG BIỂU SỐ Ả RẬP ---
\renewcommand{\thefigure}{\arabic{chapter}.\arabic{figure}}
\renewcommand{\thetable}{\arabic{chapter}.\arabic{table}}

% --- ĐỊNH DẠNG TIÊU ĐỀ (titlesec) ---

\titleformat{\chapter}[block]
  {\normalfont\Large\bfseries\centering}
  {CHƯƠNG \thechapter. }
  {0pt}
  {\MakeUppercase}
\titlespacing*{\chapter}{0pt}{-20pt}{20pt}

\titleformat{\section}
  {\normalfont\large\bfseries}
  {\thesection. }
  {0.3em}
  {}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsection. }
  {0.3em}
  {}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsubsection. }
  {0.3em}
  {}

\titleformat{\paragraph}[runin]
  {\normalfont\normalsize\bfseries}
  {\theparagraph \space}
  {0pt}
  {}
\titlespacing*{\paragraph}{0pt}{1ex}{1em}

% --- ĐỊNH DẠNG MỤC LỤC (tocloft) ---

\renewcommand{\cftchappresnum}{CHƯƠNG }
\renewcommand{\cftchapaftersnum}{.}
\setlength{\cftchapnumwidth}{7.5em} 

\setlength{\cftsecindent}{0pt}
\setlength{\cftsecnumwidth}{2em}
\setlength{\cftsubsecindent}{1.5em}
\setlength{\cftsubsecnumwidth}{2.5em}
\setlength{\cftsubsubsecindent}{3.5em}
\setlength{\cftsubsubsecnumwidth}{3em}

\setcounter{secnumdepth}{4} % Đánh số đến tận mục paragraph (a, b, c)
\setcounter{tocdepth}{3}    % Hiển thị đến mục 1.1.1 trong mục lục
\usepackage{lmodern}

\onehalfspacing 

% --- BẮT ĐẦU TÀI LIỆU ---
\begin{document}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}

% --- TRANG BÌA ---
\input{titlepage.tex}

% --- MỤC LỤC ---
\tableofcontents
\newpage

% --- DANH MỤC VIẾT TẮT ---
% \input{sections/abbreviations.tex}
\newpage

% --- NỘI DUNG BÁO CÁO ---

% Mô tả đề tài
\input{sections/project_overview.tex}

% Lời nói đầu
% \input{sections/preface.tex}
% % Chương 1
% \input{sections/chapter1.tex}

% % Chương 2
% \input{sections/chapter2.tex}

% % Chương 3
% \input{sections/chapter3.tex}

% % Chương 4
% \input{sections/chapter4.tex}

% % Kết luận
% \input{sections/conclusion.tex}

\end{document}
</file>

<file path="latex/main.toc">
\babel@toc {vietnamese}{}\relax 
\contentsline {chapter}{\numberline {I}Mô tả đề tài}{2}{chapter.1}%
\contentsline {section}{\numberline {1}Tên đề tài}{2}{section.1.1}%
\contentsline {section}{\numberline {2}Mục tiêu}{2}{section.1.2}%
\contentsline {subsection}{\numberline {2.1}Mục tiêu tổng quát}{2}{subsection.1.2.1}%
\contentsline {subsection}{\numberline {2.2}Mục tiêu cụ thể}{2}{subsection.1.2.2}%
\contentsline {section}{\numberline {3}Phương pháp}{2}{section.1.3}%
\contentsline {subsection}{\numberline {3.1}Trích xuất đặc trưng hành vi-không gian--thời gian}{2}{subsection.1.3.1}%
\contentsline {subsection}{\numberline {3.2}Phân cụm hành vi}{3}{subsection.1.3.2}%
\contentsline {subsection}{\numberline {3.3}Khai phá luật kết hợp ngữ cảnh}{3}{subsection.1.3.3}%
\contentsline {section}{\numberline {4}Cách triển khai}{3}{section.1.4}%
\contentsline {subsection}{\numberline {4.1}Giai đoạn 1: Chuẩn bị dữ liệu}{3}{subsection.1.4.1}%
\contentsline {subsection}{\numberline {4.2}Giai đoạn 2: Tiền xử lý video}{3}{subsection.1.4.2}%
\contentsline {subsection}{\numberline {4.3}Giai đoạn 3: Trích xuất đặc trưng}{3}{subsection.1.4.3}%
\contentsline {subsection}{\numberline {4.4}Giai đoạn 4: Phân cụm hành vi}{4}{subsection.1.4.4}%
\contentsline {subsection}{\numberline {4.5}Giai đoạn 5: Khai phá luật kết hợp}{4}{subsection.1.4.5}%
\contentsline {subsection}{\numberline {4.6}Giai đoạn 6: Phát hiện bất thường}{4}{subsection.1.4.6}%
\contentsline {subsection}{\numberline {4.7}Giai đoạn 7: Đánh giá}{4}{subsection.1.4.7}%
\contentsline {section}{\numberline {5}Kết quả kỳ vọng}{4}{section.1.5}%
</file>

<file path="latex/sections/project_overview.tex">
\chapter{Mô tả đề tài}

\section{Tên đề tài}

\textbf{Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian--thời gian và khai phá luật kết hợp}

\section{Mục tiêu}

\subsection{Mục tiêu tổng quát}

Đề tài hướng tới xây dựng một phương pháp hỗ trợ hệ thống camera giám sát có khả năng tự động phân tích video và cảnh báo các hành vi bất thường dựa trên cả đặc trưng chuyển động lẫn ngữ cảnh xuất hiện của hành vi.

\subsection{Mục tiêu cụ thể}

\begin{itemize}
    \item Trích xuất các đặc trưng hành vi-không gian--thời gian từ video giám sát.
    \item Phân cụm các hành vi tương tự để hình thành các mẫu hành vi bình thường thường gặp trong cảnh.
    \item Khai phá các luật kết hợp giữa hành vi và ngữ cảnh như vị trí, thời gian, loại đối tượng hoặc hướng di chuyển.
    \item Phát hiện các hành vi lệch khỏi mẫu bình thường hoặc xuất hiện trong bối cảnh không phù hợp.
    \item Hỗ trợ đưa ra cảnh báo có khả năng giải thích cho người vận hành.
\end{itemize}

\section{Phương pháp}

Phương pháp đề xuất kết hợp ba thành phần chính: trích xuất đặc trưng hành vi-không gian--thời gian, phân cụm hành vi và khai phá luật kết hợp ngữ cảnh.

\subsection{Trích xuất đặc trưng hành vi-không gian--thời gian}

Từ video camera giám sát, hệ thống thu thập các thông tin như vị trí, tốc độ, hướng di chuyển, thời gian xuất hiện, vùng đi qua, thời gian dừng và các đặc điểm quỹ đạo khác. Những thông tin này được kết hợp để tạo thành biểu diễn đặc trưng cho từng hành vi.

\subsection{Phân cụm hành vi}

Các đặc trưng sau khi trích xuất được đưa vào thuật toán phân cụm nhằm tìm ra các nhóm hành vi bình thường thường lặp lại trong cảnh, chẳng hạn như đi thẳng, rẽ, dừng, đứng lâu hoặc di chuyển theo các hướng quen thuộc.

\subsection{Khai phá luật kết hợp ngữ cảnh}

Từ kết quả phân cụm và thông tin ngữ cảnh, hệ thống xây dựng các giao dịch hành vi và khai phá các luật phổ biến, ví dụ:

\begin{itemize}
    \item người đi bộ thường xuất hiện trên vỉa hè;
    \item xe thường di chuyển trong làn xe;
    \item một số hành vi chỉ thường xảy ra tại các vùng hoặc thời điểm nhất định.
\end{itemize}

Một hành vi được xem là bất thường khi nó lệch xa các cụm hành vi bình thường hoặc vi phạm các luật ngữ cảnh đã khai phá.

\section{Cách triển khai}

\subsection{Giai đoạn 1: Chuẩn bị dữ liệu}

Lựa chọn các bộ dữ liệu video giám sát công khai phù hợp như UCSD Pedestrian, CUHK Avenue, ShanghaiTech Campus, Street Scene hoặc NWPU Campus.

\subsection{Giai đoạn 2: Tiền xử lý video}

\begin{itemize}
    \item Chuẩn hóa video.
    \item Phát hiện vùng chuyển động.
    \item Theo dõi đối tượng.
    \item Chia cảnh thành các vùng không gian.
\end{itemize}

\subsection{Giai đoạn 3: Trích xuất đặc trưng}

Tạo tập dữ liệu có cấu trúc gồm các đặc trưng về chuyển động, vị trí, thời gian, quỹ đạo và vùng xuất hiện.

\subsection{Giai đoạn 4: Phân cụm hành vi}

Áp dụng thuật toán phân cụm để xác định các mẫu hành vi bình thường trong từng cảnh.

\subsection{Giai đoạn 5: Khai phá luật kết hợp}

Chuyển các hành vi thành giao dịch ngữ cảnh và sinh ra các luật hành vi thường gặp.

\subsection{Giai đoạn 6: Phát hiện bất thường}

Kết hợp độ lệch khỏi cụm hành vi và mức độ vi phạm luật ngữ cảnh để tính điểm bất thường và sinh cảnh báo.

\subsection{Giai đoạn 7: Đánh giá}

Đánh giá hiệu quả của phương pháp bằng các chỉ số như Precision, Recall, F1-score, ROC-AUC, tỷ lệ cảnh báo giả và thời gian xử lý.

\section{Kết quả kỳ vọng}

\begin{itemize}
    \item Xây dựng được mô hình phát hiện hành vi bất thường theo ngữ cảnh trên video giám sát.
    \item Phát hiện được cả hành vi lạ và hành vi quen thuộc nhưng xảy ra sai bối cảnh.
    \item Giảm cảnh báo giả so với phương pháp chỉ xét chuyển động.
    \item Tạo ra được các cảnh báo có khả năng giải thích, hỗ trợ người vận hành giám sát hiệu quả hơn.
\end{itemize}
</file>

<file path="latex/titlepage.tex">
\begin{titlepage}

\thispagestyle{empty}
\centering

% ===== VIỀN HOA VĂN (ẢNH) =====
\AddToShipoutPictureBG{
  \begin{tikzpicture}[remember picture,overlay]
    \node [
      xshift=0cm, % đẩy sang phải (chừa gáy)
      yshift=0cm
    ]
    at (current page.center) {
      \includegraphics[
        width=0.85\paperwidth,
        height=0.9\paperheight
      ]{border.jpeg}
    };
  \end{tikzpicture}
}


\begin{center}
  \textbf{\Large TRƯỜNG ĐẠI HỌC THỦY LỢI}\\[0.3cm]
  \textbf{\large KHOA CÔNG NGHỆ THÔNG TIN}

  \vspace{0.8cm}

  \includegraphics[width=5cm]{logo.jpeg}

  \vspace{0.8cm}

  \textbf{\Large BÀI TẬP LỚN MÔN \\ KHAI PHÁ DỮ LIỆU}\\[0.3cm]

  \vspace{0.4cm}

  \textbf{\Large ĐỀ TÀI: Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian–thời gian và khai phá luật kết hợp}
  
  \vspace{0.8cm}\textbf{\large Giảng viên phụ trách môn học: Th.S Trần Mạnh Tuấn}

  \vspace{0.8cm}\textbf{\large Nhóm thực hiện: } \large Nhóm 14
  
  \vspace{0.4cm}\textbf{\large Thành viên nhóm: }

  \vspace{0.4cm}
  \begin{tabular}{p{5cm} p{3cm}}

  \end{tabular}
  
\end{center}

\vspace{1cm}

\vfill

\begin{center}
  \textit{Hà Nội, 15 tháng 5 năm 2026}
\end{center}
\end{titlepage}
\ClearShipoutPictureBG
</file>

<file path="src/csv_to_arff.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_preprocess.arff_cli import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/doc/Kmean.md">
### Baseline A numClusters = 3
## Number of iterations: 10
## Within cluster sum of squared errors: 10814.94276553674 
## Cluster centroids 
                                     Cluster#
Attribute                Full Data          0          1          2
                        (477312.0) (250213.0) (190678.0)  (36421.0)
===================================================================
foreground_ratio                -0    -0.2587    -0.2492     3.0824
motion_magnitude_mean           -0    -0.2398    -0.2652     3.0359
motion_magnitude_std             0    -0.2606    -0.2408      3.051
motion_density                   0     -0.257    -0.2478      3.063
brightness_mean                  0    -0.8822     1.1107     0.2462
brightness_delta            0.0055     0.0071     0.0197    -0.0799

## Clustered Instances 
0      250213 ( 52%)
1      190678 ( 40%)
2       36421 (  8%)


### Baseline A numClusters = 5
## Number of iterations: 37
## Within cluster sum of squared errors: 6491.182908722241
## Cluster centroids:
                                     Cluster#
Attribute                Full Data          0          1          2          3          4
                        (477312.0)  (27842.0) (182948.0)  (16491.0) (116779.0) (133252.0)
=========================================================================================
foreground_ratio                -0     1.6539    -0.2889     4.3947    -0.3128    -0.2187
motion_magnitude_mean           -0     1.6991    -0.3077     4.2516    -0.3537    -0.1487
motion_magnitude_std             0     2.2719    -0.3149     3.6867    -0.3873    -0.1592
motion_density                   0     1.5698    -0.2838     4.4482    -0.3026    -0.2237
brightness_mean                  0     0.4286     1.1285     0.1163    -1.1691    -0.6288
brightness_delta            0.0055    -0.0129     0.0194    -0.1427     0.0039       0.01

## Clustered Instances
0       27842 (  6%)
1      182948 ( 38%)
2       16491 (  3%)
3      116779 ( 24%)
4      133252 ( 28%)
## supplied test set
# Clustered Instances
0       29347 (  8%)
1      123477 ( 33%)
2       26080 (  7%)
3       93161 ( 25%)
4      104639 ( 28%)


### Baseline A numClusters = 8
## Number of iterations: 51
## Within cluster sum of squared errors: 3869.1108683416223
## Cluster centroids:
                                     Cluster#
Attribute                Full Data          0          1          2          3          4          5          6          7
                        (477312.0)  (27565.0) (159741.0)  (14811.0)  (16345.0) (133532.0)  (63213.0)  (21903.0)  (40202.0)
==========================================================================================================================
foreground_ratio                -0    -0.0177    -0.3111     4.5471     0.8197     -0.304    -0.3134     1.9319    -0.3103
motion_magnitude_mean           -0     0.0498    -0.3425     4.3868     1.2892    -0.2899    -0.3694     1.9261     -0.319
motion_magnitude_std             0     0.3218    -0.3777     3.7401      1.397    -0.3154    -0.4015     2.5294    -0.3651
motion_density                   0    -0.0556    -0.3014     4.6088     0.6942    -0.2959     -0.303     1.8638    -0.3005
brightness_mean                  0     0.7127      1.188     0.1029    -0.7458    -0.8548    -1.3204     0.5251    -0.3145
brightness_delta            0.0055     0.1183     0.0056    -0.1008    -0.0719     0.0146     0.0032    -0.0591      0.007

## Clustered Instances
0       27565 (  6%)
1      159741 ( 33%)
2       14811 (  3%)
3       16345 (  3%)
4      133532 ( 28%)
5       63213 ( 13%)
6       21903 (  5%)
7       40202 (  8%)

### Baseline B numClusters = 5
## Number of iterations: 19
## Within cluster sum of squared errors: 39528.79068974148
## Cluster centroids:
                                     Cluster#
Attribute                Full Data          0          1          2          3          4
                        (477312.0) (103507.0) (104017.0)  (34287.0) (120553.0) (114948.0)
=========================================================================================
cell_row                        -0     0.9375     0.9449     0.2294    -0.8318    -0.8953
cell_col                         0       0.86    -0.8791    -0.1423     0.8836    -0.8631
foreground_ratio                -0    -0.2431    -0.2401     3.1826    -0.2535    -0.2473
motion_magnitude_mean           -0    -0.2481    -0.2538     3.1319    -0.2225    -0.2477
motion_magnitude_std             0    -0.2294    -0.2278        3.1    -0.2454    -0.2547
motion_density                   0     -0.241    -0.2397     3.1667    -0.2523     -0.246
brightness_mean                  0     0.9392     1.0332     0.2326    -0.8241    -0.9858
brightness_delta                 0     0.0067     0.0062    -0.0444     0.0009     0.0007

## Clustered Instances
0      103507 ( 22%)
1      104017 ( 22%)
2       34287 (  7%)
3      120553 ( 25%)
4      114948 ( 24%)
## supplied test set
# Clustered Instances
0       71095 ( 19%)
1       74284 ( 20%)
2       44780 ( 12%)
3       94962 ( 25%)
4       91583 ( 24%)
</file>

<file path="src/doc/prd.md">
# PRD - Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian-thời gian và khai phá luật kết hợp

## 1. Tổng quan

### 1.1. Tên dự án

**Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian-thời gian và khai phá luật kết hợp**.

Tên tiếng Anh tham khảo: **Context-Aware Abnormal Behavior Detection using Spatio-Temporal Behavior Clustering and Association Rule Mining**.

### 1.2. Bối cảnh

Trong hệ thống camera giám sát, phần lớn thời gian video chỉ chứa các hoạt động bình thường lặp đi lặp lại. Với camera cố định, cùng một vùng trong khung hình thường có các mẫu chuyển động ổn định:

- vỉa hè thường có người đi bộ;
- làn đường thường có phương tiện di chuyển;
- cổng ra vào thường có người đứng hoặc di chuyển chậm;
- ban ngày và ban đêm có mật độ, ánh sáng và mức nhiễu khác nhau.

Ý tưởng của dự án là tận dụng tính lặp lại này để học các mẫu hành vi bình thường theo từng vùng của camera. Khi xuất hiện chuyển động lệch khỏi cụm hành vi đã học, hoặc một tổ hợp ngữ cảnh hiếm/không hợp lý theo các luật kết hợp, hệ thống sẽ gán điểm bất thường và sinh cảnh báo.

Dự án tham khảo tư duy giải quyết bài toán từ Lu et al. trong bài báo **"Abnormal Event Detection at 150 FPS in MATLAB"**, đặc biệt là cách nhìn camera cố định như một nguồn dữ liệu có cấu trúc không gian-thời gian lặp lại. Paper này chỉ được dùng như tài liệu tham khảo về tư duy tiền xử lý, chia vùng và học mẫu bình thường; dự án không đặt mục tiêu sao chép, tái hiện hoặc làm giàu thêm phương pháp của paper đó.

### 1.3. Mục tiêu học tập và nghiên cứu

Mục tiêu chính của đề tài là học hỏi và tìm hiểu sâu về:

- tiền xử lý dữ liệu video giám sát;
- biểu diễn hành vi bằng đặc trưng không gian-thời gian;
- phân cụm hành vi bình thường theo vùng camera;
- rời rạc hóa đặc trưng liên tục thành token/item;
- khai phá luật kết hợp bằng Apriori hoặc FP-Growth;
- kết hợp kết quả phân cụm và luật kết hợp để phát hiện, giải thích hành vi bất thường.

Đề tài ưu tiên tính rõ ràng, khả năng phân tích và khả năng giải thích hơn việc đạt hiệu năng cao nhất trên benchmark.

### 1.4. Mục tiêu sản phẩm thử nghiệm

Xây dựng một pipeline phát hiện bất thường cho camera cố định có khả năng:

- học các mẫu chuyển động bình thường theo từng vùng trong khung hình;
- tính điểm bất thường cho từng vùng và từng frame/cube;
- phát hiện các hành vi như di chuyển sai vùng, hướng lạ, tốc độ lạ, mật độ lạ hoặc chuyển động không phù hợp với thời điểm;
- tạo heatmap bất thường trên video;
- sinh lý do cảnh báo ở dạng dễ hiểu bằng token/rule;
- chạy đủ nhanh cho xử lý gần thời gian thực trên video giám sát.

### 1.5. Nguyên tắc thiết kế

Hệ thống không cố nhận diện ngữ nghĩa phức tạp ngay từ đầu. Thay vào đó, hệ thống ưu tiên các tín hiệu rẻ, ổn định và có thể học từ dữ liệu bình thường:

- chuyển động;
- hướng;
- mật độ;
- vị trí;
- độ sáng;
- thời điểm;
- cụm hành vi bình thường.

Hai trụ cột nghiên cứu của hệ thống là **phân cụm hành vi-không gian-thời gian** và **khai phá luật kết hợp**. Phân cụm cung cấp điểm lệch định lượng so với mẫu bình thường; luật kết hợp cung cấp ngữ cảnh, khả năng giải thích và tín hiệu phát hiện các tổ hợp hành vi hiếm. Trong triển khai, không nên để một luật đơn lẻ quyết định toàn bộ cảnh báo; kết quả cuối nên tổng hợp cả độ lệch cụm, độ hiếm của token và mức vi phạm luật.

---

## 2. Vấn đề cần giải quyết

### 2.1. Vấn đề nghiệp vụ

Người vận hành camera phải theo dõi lượng video lớn trong thời gian dài. Các hành vi bất thường thường hiếm, ngắn và dễ bị bỏ sót. Một hệ thống tự động cần giúp:

- lọc ra các thời điểm đáng chú ý;
- chỉ ra vùng bất thường trong khung hình;
- giảm số cảnh báo giả;
- giải thích lý do cảnh báo ở mức người vận hành có thể hiểu.

### 2.2. Vấn đề kỹ thuật

Phát hiện bất thường trong video không phải bài toán phân loại thông thường vì:

- khó liệt kê toàn bộ loại bất thường có thể xảy ra;
- dữ liệu bất thường ít và không cân bằng;
- cùng một hành vi có thể bình thường ở vùng này nhưng bất thường ở vùng khác;
- điều kiện ánh sáng, bóng đổ, nhiễu và mật độ đám đông làm đặc trưng chuyển động biến động;
- xử lý từng frame bằng mô hình nặng có thể không phù hợp yêu cầu real-time.

### 2.3. Giả định chính

MVP dựa trên các giả định sau:

- camera cố định hoặc gần như cố định;
- cảnh giám sát có cấu trúc không gian ổn định;
- dữ liệu huấn luyện chủ yếu chứa hành vi bình thường;
- hành vi bất thường biểu hiện qua chuyển động, vị trí, mật độ, hướng hoặc tổ hợp ngữ cảnh khác thường;
- không yêu cầu nhận diện danh tính cá nhân.

---

## 3. Người dùng và giá trị

### 3.1. Người dùng mục tiêu

- Nhân viên an ninh;
- người vận hành trung tâm camera;
- quản lý tòa nhà, bãi xe, hành lang, cổng ra vào, khu vực công cộng;
- nhóm nghiên cứu thị giác máy tính và khai phá dữ liệu;
- sinh viên triển khai đề tài phát hiện bất thường trong video.

### 3.2. Giá trị mang lại

- Giảm tải việc quan sát liên tục;
- phát hiện sớm các hành vi đáng chú ý;
- tạo heatmap giúp biết bất thường xảy ra ở đâu;
- có giải thích bằng token/rule thay vì chỉ trả về điểm số;
- có thể học riêng cho từng camera mà không cần bộ nhãn bất thường lớn;
- phù hợp triển khai baseline nhanh trước khi dùng mô hình deep learning nặng hơn.

---

## 4. Phạm vi sản phẩm

### 4.1. Phạm vi MVP

MVP tập trung vào:

- video từ camera cố định;
- xử lý video ghi sẵn trước, sau đó có thể mở rộng sang stream;
- chia frame thành grid hoặc zone cố định;
- tạo spatio-temporal cube ngắn từ 5-10 frame;
- trích xuất đặc trưng chuyển động nhẹ;
- học mô hình phân cụm hành vi bình thường theo từng cell/zone;
- tính anomaly score theo cell và frame;
- hiển thị heatmap bất thường;
- tạo token và rule để giải thích cảnh báo.

### 4.2. Loại bất thường MVP cần bắt

- Chuyển động quá nhanh ở vùng thường chậm;
- chuyển động sai hướng hoặc ngược hướng phổ biến;
- mật độ chuyển động bất thường;
- vùng thường yên tĩnh bỗng có chuyển động lớn;
- đối tượng/chuyển động có kích thước hoặc tốc độ không phù hợp với vùng;
- mẫu chuyển động không thuộc cụm bình thường của vùng đó;
- tổ hợp token hiếm, ví dụ `zone=sidewalk` + `motion=very_fast` + `object_size=large`.

### 4.3. Ngoài phạm vi MVP

- Nhận diện danh tính người;
- nhận diện biển số xe;
- phân loại chi tiết hành vi như đánh nhau, trộm cắp, bỏ quên đồ;
- tracking đa đối tượng chính xác trong đám đông;
- học liên camera;
- suy luận ý định;
- xử lý camera rung mạnh hoặc thay đổi góc nhìn liên tục;
- dashboard vận hành hoàn chỉnh.

---

## 5. Kiến trúc hệ thống

### 5.1. Kiến trúc tổng quát

```mermaid
flowchart LR
    A["Video camera cố định"] --> B["Tiền xử lý frame"]
    B --> C["Chia grid / zone"]
    C --> D["Tạo spatio-temporal cube"]
    D --> E["Trích xuất đặc trưng nhẹ"]
    E --> F["Phân cụm hành vi theo từng cell"]
    F --> G["Anomaly score theo cell"]
    G --> H["Làm mượt theo thời gian / không gian"]
    H --> I["Frame-level alert + heatmap"]
    E --> J["Token hóa đặc trưng"]
    J --> K["Association rules"]
    K --> L["Giải thích / cộng điểm phụ"]
    L --> I
```

### 5.2. Luồng huấn luyện offline

```mermaid
flowchart TD
    A["Training videos bình thường"] --> B["Resize + chuẩn hóa"]
    B --> C["Grid/zone cố định"]
    C --> D["Cube 5-10 frame"]
    D --> E["Feature extraction"]
    E --> F["Chuẩn hóa feature theo cell"]
    F --> G["Huấn luyện mô hình phân cụm từng cell"]
    E --> H["Discretize thành token"]
    H --> I["Mine association rules"]
    G --> J["Lưu model"]
    I --> J
```

### 5.3. Luồng suy luận online/offline

```mermaid
flowchart TD
    A["Video / stream mới"] --> B["Tạo cube"]
    B --> C["Trích xuất feature"]
    C --> D["Tính khoảng cách tới cụm bình thường"]
    C --> E["Sinh token"]
    E --> F["Kiểm tra support/rule violation"]
    D --> G["Tổng hợp anomaly score"]
    F --> G
    G --> H["Temporal smoothing"]
    H --> I["Heatmap + alert + reason"]
```

---

## 6. Thành phần chức năng

### 6.1. Module Input Video

#### Mục tiêu

Đọc video từ dataset hoặc file camera và cung cấp frame tuần tự cho pipeline.

#### Yêu cầu

- Đọc được `.avi`, `.mp4`, chuỗi ảnh `.jpg/.png/.tif`;
- hỗ trợ dataset CUHK Avenue, UCSD Ped1/Ped2, ShanghaiTech nếu có sẵn;
- giữ thông tin `video_id`, `frame_id`, `timestamp`;
- cho phép giới hạn số frame để debug nhanh.

#### Đầu ra

```text
FrameRecord {
  video_id,
  frame_id,
  timestamp,
  frame_rgb,
  frame_gray
}
```

### 6.2. Module Tiền Xử Lý

#### Mục tiêu

Chuẩn hóa frame để giảm chi phí tính toán và giảm nhiễu.

#### Yêu cầu

- Resize về kích thước cấu hình được, ví dụ `320x240` hoặc `160x120`;
- chuyển grayscale;
- tùy chọn cân bằng sáng nhẹ;
- tùy chọn blur nhẹ để giảm nhiễu;
- giữ mapping từ frame resize về frame gốc để vẽ heatmap.

### 6.3. Module Grid/Zone

#### Mục tiêu

Chia frame thành các vùng cố định để học normal pattern riêng theo vị trí.

#### Yêu cầu

- Hỗ trợ grid đều, ví dụ `16x12`, `20x15`;
- hỗ trợ zone thủ công bằng file cấu hình trong giai đoạn sau;
- mỗi cell có `cell_id`, tọa độ, kích thước;
- có thể bỏ qua cell ít thông tin, ví dụ vùng tường/trời.

#### Cấu hình mẫu

```yaml
grid:
  rows: 12
  cols: 16
  ignore_cells: []
```

### 6.4. Module Spatio-Temporal Cube

#### Mục tiêu

Gom thông tin nhiều frame liên tiếp để phát hiện chuyển động thay vì chỉ phân tích frame đơn.

#### Yêu cầu

- Cube gồm `T=5` hoặc `T=10` frame;
- stride cấu hình được, ví dụ `1`, `2`, `5`;
- mỗi cube gắn với một cell/zone cố định;
- hỗ trợ multi-scale trong giai đoạn mở rộng nếu cần nghiên cứu thêm.

### 6.5. Module Feature Extraction

#### Mục tiêu

Tạo vector đặc trưng nhẹ, đủ mô tả chuyển động và ngữ cảnh vùng.

#### Feature bắt buộc cho MVP

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
direction_hist_8bins
motion_density
brightness_mean
brightness_delta
cell_row
cell_col
cell_id
```

#### Feature tùy chọn

```text
object_size_estimate
edge_energy
gradient_3d_energy
flow_consistency
time_bucket
```

#### Phương pháp motion

MVP có thể bắt đầu bằng:

- frame differencing;
- background subtraction MOG2;
- optical flow Farneback nếu cần hướng chuyển động tốt hơn.

Khuyến nghị triển khai:

1. Baseline nhanh: frame differencing + motion density.
2. Baseline tốt hơn: Farneback optical flow + direction histogram.
3. Feature nâng cao: 3D gradient trên cube để khảo sát thêm đặc trưng không gian-thời gian.

### 6.6. Module Phân Cụm Hành Vi-Không Gian-Thời Gian

#### Mục tiêu

Học các cụm hành vi bình thường riêng cho từng cell/zone dựa trên đặc trưng không gian-thời gian.

#### Thuật toán MVP

Ưu tiên **MiniBatchKMeans theo từng cell** vì dễ triển khai, chạy nhanh và dễ giải thích.

Quy trình:

1. Với mỗi cell, gom feature từ training video bình thường.
2. Chuẩn hóa feature bằng mean/std riêng của cell.
3. Huấn luyện KMeans với `K=3..8`.
4. Tính phân phối khoảng cách từ feature training tới centroid gần nhất.
5. Lưu percentile distance làm ngưỡng anomaly.

#### Thuật toán mở rộng để so sánh

- Gaussian Mixture Model;
- Isolation Forest;
- One-Class SVM;
- DBSCAN/HDBSCAN nếu muốn khảo sát cụm tự nhiên và outlier;
- K-Medoids nếu muốn mẫu đại diện là điểm dữ liệu thật, dễ giải thích.

### 6.7. Module Tokenization

#### Mục tiêu

Biến feature liên tục thành token rời rạc để khai phá luật và giải thích.

#### Ví dụ token

```text
cell=08_05
zone=sidewalk
motion=slow
direction=left_to_right
density=low
brightness=day_like
cluster=C3
```

#### Yêu cầu

- Rời rạc hóa tốc độ thành `still`, `slow`, `medium`, `fast`, `very_fast`;
- rời rạc hóa hướng thành 8 hướng hoặc nhóm hướng chính;
- rời rạc hóa mật độ thành `low`, `medium`, `high`;
- rời rạc hóa độ sáng thành `dark`, `normal`, `bright`;
- thêm `cluster=Cx` từ mô hình phân cụm;
- không tạo quá nhiều token khiến luật bị vụn.

### 6.8. Module Association Rules

#### Mục tiêu

Tìm các tổ hợp token phổ biến trong dữ liệu bình thường để hỗ trợ giải thích và cộng điểm bất thường.

#### Thuật toán

- FP-Growth là lựa chọn ưu tiên nếu số transaction lớn;
- Apriori dùng được cho thí nghiệm nhỏ.

#### Chỉ số luật

- `support`;
- `confidence`;
- `lift`;
- số lần xuất hiện theo cell/zone.

#### Ví dụ luật

```text
{cell=08_05, brightness=day_like} -> {motion=slow}
{cell=10_03, density=low} -> {direction=left_to_right}
{zone=entrance, density=high} -> {motion=still_or_slow}
```

#### Vai trò trong MVP

Luật kết hợp là một thành phần nghiên cứu chính của đề tài, dùng để học quan hệ giữa vùng, thời điểm, hướng, mật độ, tốc độ và cụm hành vi. Trong phát hiện bất thường, luật nên được kết hợp với kết quả phân cụm thay vì dùng đơn độc. Chúng dùng để:

- giải thích vì sao một mẫu bị xem là lạ;
- phát hiện tổ hợp token hiếm;
- cộng thêm điểm nếu rule mạnh bị vi phạm;
- hỗ trợ phân tích định tính trong báo cáo.

### 6.9. Module Anomaly Scoring

#### Mục tiêu

Tính điểm bất thường liên tục cho từng cell/cube và tổng hợp thành điểm frame.

#### Thành phần điểm

```text
cluster_distance_score:
  độ xa so với cụm bình thường gần nhất của cell

temporal_change_score:
  độ thay đổi đột ngột so với các cube gần trước đó

rare_token_score:
  mức hiếm của tổ hợp token trong dữ liệu training

rule_violation_score:
  mức vi phạm các luật có confidence/lift cao
```

#### Công thức MVP

```text
score = 0.65 * cluster_distance_score
      + 0.20 * temporal_change_score
      + 0.10 * rare_token_score
      + 0.05 * rule_violation_score
```

Trọng số có thể điều chỉnh qua validation. Nếu chưa có đủ rule ổn định, MVP có thể đặt:

```text
score = 0.80 * cluster_distance_score
      + 0.20 * temporal_change_score
```

#### Tổng hợp frame-level

```text
frame_score = mean(top_k_cell_scores)
```

hoặc:

```text
frame_score = max(cell_scores)
```

Khuyến nghị dùng `top_k mean` để giảm nhiễu từ một cell đơn lẻ.

### 6.10. Module Smoothing và Alert

#### Mục tiêu

Giảm cảnh báo giả do nhiễu từng frame/cube.

#### Yêu cầu

- Làm mượt score theo thời gian bằng moving average hoặc EMA;
- làm mượt nhẹ theo không gian giữa các cell lân cận;
- chỉ alert nếu score vượt ngưỡng trong `N` cube liên tiếp;
- sinh mức độ cảnh báo: `low`, `medium`, `high`.

#### Logic đề xuất

```text
if smoothed_frame_score > threshold_high for >= N frames:
    alert = high
elif smoothed_frame_score > threshold_medium for >= N frames:
    alert = medium
else:
    alert = none
```

### 6.11. Module Output và Visualization

#### Đầu ra bắt buộc

- `frame_score`;
- `cell_scores`;
- heatmap bất thường;
- danh sách cell bất thường;
- lý do cảnh báo dạng text;
- video/frame overlay kết quả.

#### Ví dụ output JSON

```json
{
  "video_id": "avenue_test_01",
  "frame_id": 352,
  "frame_score": 0.87,
  "severity": "high",
  "abnormal_cells": ["08_05", "08_06", "09_05"],
  "reasons": [
    "cell=08_05 has motion=very_fast while normal pattern is slow",
    "token combination {cell=08_05, motion=very_fast, density=high} has low support",
    "nearest normal cluster is C3 but distance is above the 99th percentile"
  ]
}
```

---

## 7. Yêu cầu chức năng

### 7.1. Bắt buộc cho MVP

- Đọc video hoặc chuỗi frame từ dataset;
- resize và chuẩn hóa frame;
- chia frame thành grid cố định;
- tạo cube 5-10 frame;
- trích xuất feature chuyển động nhẹ;
- huấn luyện mô hình phân cụm theo từng cell;
- tính anomaly score theo cell/cube;
- tổng hợp frame-level score;
- làm mượt score để giảm nhiễu;
- xuất heatmap bất thường;
- xuất file kết quả dạng JSON/CSV;
- tạo token từ feature;
- khai phá luật cơ bản từ training data;
- sinh lý do cảnh báo dựa trên cluster distance và token/rule.

### 7.2. Nên có

- CLI train/test rõ ràng;
- cấu hình bằng YAML;
- lưu model bằng pickle/joblib;
- script visualize overlay heatmap lên video;
- báo cáo FPS;
- báo cáo AUC/EER nếu dataset có ground truth;
- ablation giữa score có rule và không rule.

### 7.3. Có thể mở rộng sau

- Zone ngữ nghĩa do người dùng vẽ;
- multi-scale grid/pyramid để so sánh ảnh hưởng của độ phân giải vùng;
- thuật toán phân cụm khác như GMM, DBSCAN, K-Medoids;
- thuật toán khai phá mẫu tuần tự nếu muốn xét thứ tự thời gian mạnh hơn luật kết hợp;
- cập nhật model online;
- dashboard web;
- stream RTSP;
- cảnh báo theo cấp hành vi/đoạn thời gian thay vì chỉ theo frame-level;
- tích hợp detector nhẹ để ước lượng loại đối tượng.

---

## 8. Yêu cầu phi chức năng

### 8.1. Hiệu năng

- MVP nên xử lý tối thiểu 15 FPS ở độ phân giải resize trên CPU phổ thông;
- mục tiêu tốt: 25-30 FPS với frame differencing hoặc MOG2;
- optical flow có thể chậm hơn nhưng vẫn cần đủ nhanh cho thí nghiệm offline;
- huấn luyện offline chấp nhận chậm hơn suy luận.

### 8.2. Khả năng giải thích

Mỗi alert cần có ít nhất một lý do:

- lệch cụm bình thường;
- vượt percentile distance của cell;
- motion/density/direction bất thường;
- tổ hợp token hiếm;
- vi phạm rule mạnh.

### 8.3. Tính ổn định

- Không alert do một frame đơn lẻ nếu chưa qua smoothing;
- xử lý được cell ít chuyển động bằng fallback threshold;
- không crash khi video thiếu frame hoặc frame lỗi;
- log rõ số feature, số cell, số model được huấn luyện.

### 8.4. Tính mở rộng

- Module hóa theo `data`, `features`, `models`, `rules`, `scoring`, `visualization`;
- cấu hình được grid size, cube length, stride, thuật toán feature và thuật toán phân cụm;
- dễ thay KMeans bằng model khác.

---

## 9. Dữ liệu và đánh giá

### 9.1. Dataset ưu tiên trong workspace

Workspace hiện có các dataset phù hợp:

- CUHK Avenue;
- UCSD Ped1/Ped2;
- ShanghaiTech.

Khuyến nghị thứ tự:

1. **UCSD Ped2**: nhỏ hơn, dễ chạy baseline.
2. **CUHK Avenue**: có nhiều hành vi bất thường trực quan.
3. **ShanghaiTech**: đa cảnh, dùng sau khi pipeline ổn định.

### 9.2. Chia dữ liệu

Mặc định:

- training videos: dùng làm dữ liệu normal;
- testing videos: dùng để đánh giá anomaly;
- validation split từ training nếu cần chọn threshold.

### 9.3. Metric định lượng

- Frame-level ROC-AUC;
- frame-level PR-AUC;
- EER nếu cần so sánh với các báo cáo học thuật;
- false positive rate;
- recall tại một mức FPR cố định;
- FPS hoặc ms/frame;
- số cảnh báo/hành vi đúng và sai.

### 9.4. Đánh giá định tính

Cần lưu các ví dụ:

- phát hiện đúng với heatmap rõ;
- cảnh báo sai do ánh sáng/bóng/nhiễu;
- bỏ sót vì chuyển động quá nhỏ;
- rule giải thích tốt;
- rule gây hiểu sai.

---

## 10. Thí nghiệm và Ablation

### 10.1. Baseline bắt buộc

1. Motion magnitude threshold đơn giản.
2. Per-cell KMeans chỉ dùng motion feature.
3. Per-cell KMeans + temporal smoothing.
4. Per-cell KMeans + token/rule explanation.
5. Full score: cluster distance + temporal change + rare token + rule violation.

### 10.2. Ablation

- Không dùng cell-specific model, chỉ dùng model toàn cục;
- không dùng direction histogram;
- không dùng brightness/time token;
- không dùng temporal smoothing;
- không dùng rule score;
- thay `max cell score` bằng `top-k mean`;
- thay grid size `8x6`, `16x12`, `20x15`;
- thay cube length `5`, `10`, `15`.

### 10.3. Kỳ vọng kết quả

- Model theo từng cell phải tốt hơn model toàn cục;
- smoothing phải giảm false positive;
- rule/token có thể không tăng AUC mạnh nhưng phải tăng khả năng giải thích;
- grid quá nhỏ dễ nhiễu, grid quá lớn dễ mất chi tiết;
- optical flow nên phát hiện hướng tốt hơn frame differencing nhưng tốn thời gian hơn.

---

## 11. Thiết kế cấu hình

### 11.1. File cấu hình mẫu

```yaml
data:
  dataset: "ucsd_ped2"
  train_path: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2/Train"
  test_path: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2/Test"

video:
  resize_width: 320
  resize_height: 240
  grayscale: true

grid:
  rows: 12
  cols: 16

cube:
  length: 5
  stride: 1

features:
  motion_method: "farneback"
  direction_bins: 8
  use_brightness: true

model:
  type: "minibatch_kmeans"
  clusters_per_cell: 5
  min_samples_per_cell: 50

rules:
  enabled: true
  algorithm: "fpgrowth"
  min_support: 0.01
  min_confidence: 0.6
  min_lift: 1.1

scoring:
  cluster_weight: 0.65
  temporal_weight: 0.20
  rare_token_weight: 0.10
  rule_weight: 0.05
  top_k_cells: 5
  smoothing_window: 5
```

---

## 12. Giao diện dòng lệnh đề xuất

### 12.1. Train

```bash
python train.py --config configs/ucsd_ped2.yaml
```

Đầu ra:

```text
outputs/models/ucsd_ped2/
  config.yaml
  scalers.joblib
  cell_models.joblib
  thresholds.json
  rules.json
  feature_stats.json
```

### 12.2. Test

```bash
python test.py --config configs/ucsd_ped2.yaml --model outputs/models/ucsd_ped2
```

Đầu ra:

```text
outputs/results/ucsd_ped2/
  frame_scores.csv
  cell_scores.npz
  alerts.json
  metrics.json
```

### 12.3. Visualize

```bash
python visualize.py --video test_001 --results outputs/results/ucsd_ped2
```

Đầu ra:

```text
outputs/visualizations/ucsd_ped2/test_001_overlay.mp4
```

---

## 13. Rủi ro và giảm thiểu

### 13.1. Background subtraction nhiễu

**Rủi ro:** Ánh sáng, bóng đổ, mưa hoặc nén video tạo chuyển động giả.

**Giảm thiểu:** Dùng smoothing, bỏ qua cell có motion nhỏ không ổn định, thử optical flow, chuẩn hóa brightness và thêm threshold tối thiểu.

### 13.2. Rule quá vụn

**Rủi ro:** Quá nhiều token làm support thấp, luật khó tổng quát.

**Giảm thiểu:** Giới hạn số token, rời rạc hóa thô, lọc luật bằng support/confidence/lift, chỉ dùng rule làm tín hiệu phụ.

### 13.3. Support thấp không đồng nghĩa bất thường

**Rủi ro:** Hành vi hợp lệ nhưng hiếm bị báo bất thường.

**Giảm thiểu:** Rare token score chỉ chiếm trọng số nhỏ; alert cần cluster distance hoặc temporal change đủ cao.

### 13.4. Grid không khớp ngữ nghĩa cảnh

**Rủi ro:** Một cell chứa cả đường và vỉa hè, làm mẫu normal bị trộn.

**Giảm thiểu:** Thử grid nhỏ hơn, dùng zone thủ công ở giai đoạn sau, hoặc multi-scale.

### 13.5. Dữ liệu training chứa bất thường

**Rủi ro:** Model học nhầm bất thường thành bình thường.

**Giảm thiểu:** Lọc outlier trong training, dùng percentile robust, kiểm tra top distance trong training.

### 13.6. False positive từ camera noise

**Rủi ro:** Một cell đơn lẻ bị nhiễu gây alert.

**Giảm thiểu:** Dùng top-k mean, spatial smoothing, yêu cầu alert kéo dài nhiều frame.

---

## 14. Kế hoạch triển khai

### Giai đoạn 1 - Baseline xử lý video và feature

- Đọc dataset UCSD Ped2 hoặc Avenue;
- resize frame;
- chia grid;
- tạo cube;
- trích xuất frame difference hoặc optical flow feature;
- lưu feature theo `video_id`, `frame_id`, `cell_id`.

### Giai đoạn 2 - Phân cụm hành vi theo cell

- Chuẩn hóa feature theo cell;
- train MiniBatchKMeans;
- tính distance distribution;
- chọn threshold theo percentile;
- xuất cell score và frame score.

### Giai đoạn 3 - Visualization và đánh giá

- Tạo heatmap cell score;
- overlay lên frame/video;
- tính FPS;
- tính ROC-AUC nếu có ground truth;
- lưu ví dụ đúng/sai.

### Giai đoạn 4 - Token và association rules

- Discretize motion/density/direction/brightness;
- tạo transaction;
- chạy FP-Growth hoặc Apriori;
- lưu rules;
- tạo reason text cho alert.

### Giai đoạn 5 - Tổng hợp score và ablation

- Thêm rare token score;
- thêm rule violation score;
- tối ưu trọng số;
- so sánh với baseline không rule;
- viết báo cáo phân tích.

### Giai đoạn 6 - Mở rộng nghiên cứu sau MVP

- Thêm multi-scale pyramid;
- thử 3D gradient feature;
- thử thêm thuật toán phân cụm khác như GMM, DBSCAN, K-Medoids;
- thử thêm biến thể luật kết hợp và ngưỡng support/confidence/lift;
- so sánh tốc độ, khả năng giải thích và độ chính xác với KMeans baseline.

---

## 15. Tiêu chí hoàn thành MVP

MVP được xem là hoàn thành khi:

- huấn luyện được mô hình phân cụm hành vi trên ít nhất một dataset;
- test được trên video mới và sinh `frame_scores.csv`;
- tạo được heatmap overlay;
- có alert JSON với lý do cảnh báo;
- có ít nhất 3 thí nghiệm baseline/ablation;
- báo cáo được FPS và ít nhất một metric định lượng;
- code chạy lại được bằng config;
- tài liệu mô tả rõ pipeline, feature, model và scoring.

---

## 16. Kết quả bàn giao

- Source code pipeline;
- file cấu hình dataset;
- model đã train thử;
- kết quả score dạng CSV/JSON;
- video/frame visualization;
- báo cáo thực nghiệm;
- phân tích ưu/nhược điểm;
- phần thảo luận về paper Lu et al. như tài liệu tham khảo tư duy, không phải phương pháp cần tái hiện.

---

## 17. Định hướng dài hạn

Sau MVP, hệ thống có thể mở rộng theo các hướng:

- học online để thích nghi thay đổi theo thời gian;
- zone ngữ nghĩa do người dùng định nghĩa;
- kết hợp object detector nhẹ để phân biệt người/xe;
- phát hiện bất thường theo cấp hành vi/đoạn thời gian thay vì chỉ theo frame-level;
- thêm quan hệ giữa nhiều vùng liền kề;
- dùng mô hình deep feature cho cảnh phức tạp;
- triển khai dashboard realtime;
- đánh giá trên nhiều camera và nhiều điều kiện môi trường hơn.
</file>

<file path="src/doc/spec_1.md">
# SPEC 1 - Kế hoạch tiền xử lý dữ liệu UCSD và CUHK Avenue

## 1. Mục tiêu

Tài liệu này mô tả kế hoạch tiền xử lý dữ liệu cho hai bộ dataset:

- UCSD Ped1/Ped2;
- CUHK Avenue.

Mục tiêu của giai đoạn này là biến dữ liệu video/frame thô thành tập dữ liệu đặc trưng có cấu trúc, sẵn sàng cho các bước phân cụm hành vi, token hóa và khai phá luật kết hợp trong pipeline phát hiện bất thường.

Kết quả đầu ra không phải là các file ảnh/video đã xử lý đơn lẻ, mà là:

- frame đã được chuẩn hóa để pipeline có thể đọc lại;
- metadata về video/frame/cell/cube;
- feature dạng bảng CSV/Parquet;
- mapping giữa feature và vị trí trên frame để vẽ heatmap sau này.

## 2. Nguyên tắc thiết kế

- Tiền xử lý video/frame bằng Python và OpenCV, không dùng WEKA cho bước xử lý ảnh thô.
- WEKA chỉ được dùng sau khi đã trích xuất feature thành bảng CSV/ARFF.
- Giữ nguyên cấu trúc train/test gốc của dataset.
- Không trộn frame train và test trong quá trình tính thống kê chuẩn hóa.
- Mỗi bản ghi feature phải truy vết được về `dataset`, `split`, `video_id`, `frame_id`, `cube_id` và `cell_id`.
- Ưu tiên pipeline chạy lặp lại được bằng config.
- Lưu các thông số tiền xử lý để bảo đảm train/test dùng cùng một cách xử lý.

## 3. Phạm vi

### 3.1. Nằm trong phạm vi

- Đọc frame `.tif` của UCSD Ped1/Ped2.
- Đọc video `.avi` của CUHK Avenue.
- Resize frame về kích thước cấu hình.
- Chuyển grayscale.
- Làm trơn nhẹ bằng Gaussian blur nếu cấu hình bật.
- Chuẩn hóa độ sáng nhẹ nếu cấu hình bật.
- Chia frame thành grid cố định.
- Tạo spatio-temporal cube.
- Trích xuất feature chuyển động và ngữ cảnh cơ bản.
- Lưu feature và metadata.
- Kiểm tra số lượng frame, cube, cell và feature.

### 3.2. Ngoài phạm vi

- Train model phân cụm.
- Tính anomaly score.
- Khai phá association rules.
- Vẽ heatmap kết quả bất thường.
- Đánh giá ROC-AUC/EER.
- Xử lý semantic object detection.

## 4. Cấu trúc dataset hiện có

### 4.1. UCSD

Thư mục gốc:

```text
dataset/UCSD_Anomaly_Dataset.v1p2/
  UCSDped1/
  UCSDped2/
```

Mỗi tập Ped1/Ped2 có cấu trúc chính:

```text
UCSDpedX/
  Train/
    Train001/
      001.tif
      002.tif
      ...
  Test/
    Test001/
      001.tif
      002.tif
      ...
    Test001_gt/
      frame001.bmp
      ...
```

Đặc điểm:

- dữ liệu đã tách thành chuỗi frame `.tif`;
- train mặc định là normal;
- test có thể có thư mục ground truth mask `_gt`;
- Ped2 nhỏ hơn, nên ưu tiên làm baseline đầu tiên.

### 4.2. CUHK Avenue

Thư mục gốc:

```text
dataset/CUHK-avenue/
  training_videos/
  testing_videos/
  training_vol/
  testing_vol/
```

Đặc điểm:

- video train/test ở dạng `.avi`;
- các file `.mat` trong `training_vol` và `testing_vol` có thể dùng đối chiếu hoặc nghiên cứu sau;
- nên đọc trực tiếp `.avi` bằng OpenCV cho pipeline MVP;
- train mặc định là normal.

## 5. Đầu ra chuẩn hóa

Tất cả dataset sau tiền xử lý cần quy về cùng một schema logic.

### 5.1. FrameRecord

```text
FrameRecord {
  dataset,
  split,
  video_id,
  frame_id,
  timestamp,
  source_path,
  original_width,
  original_height,
  resized_width,
  resized_height,
  frame_gray
}
```

### 5.2. CellRecord

```text
CellRecord {
  cell_id,
  row,
  col,
  x1,
  y1,
  x2,
  y2,
  width,
  height
}
```

### 5.3. CubeRecord

```text
CubeRecord {
  dataset,
  split,
  video_id,
  cube_id,
  start_frame_id,
  end_frame_id,
  center_frame_id,
  cell_id
}
```

### 5.4. FeatureRecord

```text
FeatureRecord {
  dataset,
  split,
  video_id,
  cube_id,
  start_frame_id,
  end_frame_id,
  center_frame_id,
  cell_id,
  cell_row,
  cell_col,
  foreground_ratio,
  motion_magnitude_mean,
  motion_magnitude_std,
  motion_density,
  direction_hist_0,
  direction_hist_1,
  direction_hist_2,
  direction_hist_3,
  direction_hist_4,
  direction_hist_5,
  direction_hist_6,
  direction_hist_7,
  brightness_mean,
  brightness_delta
}
```

## 6. Cấu hình tiền xử lý đề xuất

### 6.1. UCSD Ped2 baseline

```yaml
data:
  dataset: "ucsd_ped2"
  root: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true
```

### 6.2. CUHK Avenue baseline

```yaml
data:
  dataset: "avenue"
  root: "dataset/CUHK-avenue"
  train_path: "training_videos"
  test_path: "testing_videos"

video:
  input_type: "video"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true
```

## 7. Pipeline tiền xử lý chung

### Bước 1 - Quét dataset

Với mỗi dataset, tạo danh sách video/sequence:

```text
dataset, split, video_id, source_path, input_type
```

Quy tắc:

- UCSD: mỗi thư mục `TrainXXX` hoặc `TestXXX` là một sequence.
- Avenue: mỗi file `.avi` là một video.
- Bỏ qua các thư mục ground truth `_gt` trong quá trình trích xuất feature đầu vào.

### Bước 2 - Đọc frame

UCSD:

- đọc ảnh `.tif` theo thứ tự tăng dần;
- `frame_id` lấy từ tên file;
- `timestamp` có thể gán theo thứ tự frame nếu không có FPS.

Avenue:

- đọc `.avi` bằng `cv2.VideoCapture`;
- `frame_id` là chỉ số frame bắt đầu từ 1;
- `timestamp = frame_id / fps` nếu đọc được FPS, ngược lại gán `null`.

### Bước 3 - Chuẩn hóa frame

Mỗi frame cần được xử lý theo thứ tự:

1. đọc frame RGB/BGR;
2. resize về kích thước cấu hình;
3. chuyển grayscale;
4. blur nhẹ nếu bật;
5. chuẩn hóa độ sáng nếu bật;
6. lưu thông tin kích thước gốc và kích thước resize.

Khuyến nghị MVP:

- bật resize và grayscale;
- bật blur kernel `3x3`;
- tắt brightness normalization trong lần đầu để pipeline đơn giản và dễ giải thích.

### Bước 4 - Tạo grid

Grid mặc định:

```text
rows = 12
cols = 16
```

Với frame `320x240`, mỗi cell có kích thước xấp xỉ:

```text
cell_width = 20
cell_height = 20
```

Cần lưu `cell_id` theo dạng:

```text
row_col
```

Ví dụ:

```text
08_05
```

### Bước 5 - Tạo cube

Cube là cửa sổ trượt theo thời gian:

```text
length = 5
stride = 1
```

Với mỗi video/sequence có `N` frame, số cube:

```text
num_cubes = floor((N - length) / stride) + 1
```

Mỗi cube được cắt theo từng cell, nhưng không cần lưu ảnh cube ra đĩa nếu không cần debug. Chỉ cần trích xuất feature và lưu metadata.

### Bước 6 - Trích xuất feature

Baseline dùng frame differencing:

1. tính chênh lệch tuyệt đối giữa các frame liên tiếp trong cube;
2. threshold để tạo motion mask;
3. tính feature theo từng cell.

Feature bắt buộc:

- `foreground_ratio`: tỷ lệ pixel có chuyển động trong cell;
- `motion_magnitude_mean`: trung bình độ lớn chuyển động;
- `motion_magnitude_std`: độ lệch chuẩn độ lớn chuyển động;
- `motion_density`: tỷ lệ pixel vượt ngưỡng chuyển động;
- `direction_hist_8bins`: histogram hướng chuyển động, nếu chưa dùng optical flow thì có thể gán zero hoặc ước lượng sau;
- `brightness_mean`: độ sáng trung bình trong cell;
- `brightness_delta`: chênh lệch độ sáng giữa frame đầu và frame cuối cube.

Ghi chú:

- Nếu `motion_method = frame_diff`, direction histogram có thể chưa đáng tin cậy.
- Khi cần hướng chuyển động tốt hơn, thêm biến thể `motion_method = farneback`.

### Bước 7 - Lưu feature

Đầu ra đề xuất:

```text
outputs/preprocessed/
  ucsd_ped2/
    frames_manifest.csv
    videos_manifest.csv
    grid.json
    features_train.csv
    features_test.csv
    preprocess_stats.json
  avenue/
    frames_manifest.csv
    videos_manifest.csv
    grid.json
    features_train.csv
    features_test.csv
    preprocess_stats.json
```

Nếu dữ liệu lớn, có thể dùng Parquet:

```text
features_train.parquet
features_test.parquet
```

CSV vẫn nên được hỗ trợ để có thể import vào WEKA.

### Bước 8 - Xuất ARFF cho WEKA

Sau khi có CSV feature, tạo thêm file ARFF nếu cần chạy WEKA:

```text
outputs/weka/
  ucsd_ped2_features_train.arff
  ucsd_ped2_features_test.arff
  avenue_features_train.arff
  avenue_features_test.arff
```

Quy tắc:

- bỏ các cột text dài nếu WEKA xử lý chậm;
- giữ `cell_id` dạng nominal nếu cần phân tích theo cell;
- các cột numeric giữ nguyên;
- chưa gán label anomaly trong file train.

## 8. Kế hoạch riêng cho UCSD

### 8.1. Thứ tự ưu tiên

1. Làm UCSD Ped2 trước.
2. Kiểm tra pipeline với 1 sequence train và 1 sequence test.
3. Chạy toàn bộ Ped2.
4. Sau khi ổn định, mở rộng sang Ped1.

### 8.2. Xử lý frame

- Đọc các file `.tif` theo thứ tự tên file.
- Tên sequence là tên thư mục, ví dụ `Train001`, `Test001`.
- Không đọc thư mục `*_gt` trong tiền xử lý feature.
- Ground truth mask chỉ dùng trong giai đoạn đánh giá sau.

### 8.3. Lưu metadata UCSD

`videos_manifest.csv`:

```text
dataset,split,video_id,source_path,input_type,num_frames
```

`frames_manifest.csv`:

```text
dataset,split,video_id,frame_id,source_path,original_width,original_height,resized_width,resized_height
```

### 8.4. Rủi ro UCSD

- Frame `.tif` có thể đọc thành grayscale sẵn, cần xử lý thống nhất.
- Ped1 và Ped2 có kích thước frame khác nhau, không hard-code theo một bộ.
- Ground truth mask có tên file khác frame source, cần tách riêng cho giai đoạn evaluation.

## 9. Kế hoạch riêng cho CUHK Avenue

### 9.1. Thứ tự ưu tiên

1. Đọc `training_videos/*.avi`.
2. Đọc `testing_videos/*.avi`.
3. Bỏ qua `training_vol` và `testing_vol` trong MVP tiền xử lý.
4. Sau khi pipeline feature ổn định, nghiên cứu file `.mat` để lấy thêm annotation nếu cần.

### 9.2. Xử lý video

- Dùng OpenCV đọc từng frame.
- Lấy FPS, width, height từ `VideoCapture`.
- Nếu video lỗi frame, log cảnh báo và tiếp tục nếu có thể.
- `video_id` lấy từ tên file không có đuôi, ví dụ `01`, `02`, `21`.

### 9.3. Lưu metadata Avenue

`videos_manifest.csv`:

```text
dataset,split,video_id,source_path,input_type,fps,num_frames,original_width,original_height
```

`frames_manifest.csv`:

```text
dataset,split,video_id,frame_id,timestamp,original_width,original_height,resized_width,resized_height
```

### 9.4. Rủi ro Avenue

- `.avi` có thể có FPS/metadata không ổn định, cần fallback theo frame index.
- Video dài hơn UCSD, feature CSV có thể lớn.
- File `.mat` không cần dùng trong MVP đọc video, tránh làm chậm tiến độ.

## 10. Kiểm tra chất lượng đầu ra

Sau mỗi lần chạy tiền xử lý, cần có báo cáo:

```text
dataset
split
num_videos
num_frames
num_cubes
num_cells
num_feature_rows
missing_frames
failed_videos
avg_motion_density
avg_brightness
```

Điều kiện chấp nhận:

- đọc được tất cả video/sequence trong split;
- số frame trong manifest khớp với số frame thực tế đọc được;
- số dòng feature xấp xỉ `num_cubes * rows * cols`;
- không có cột feature numeric bị `NaN` hàng loạt;
- `foreground_ratio` và `motion_density` nằm trong khoảng `[0, 1]`;
- `brightness_mean` nằm trong khoảng `[0, 255]`;
- grid mapping đúng kích thước frame resize.

## 11. Checklist thực hiện

- Tạo config riêng cho `ucsd_ped2`.
- Tạo config riêng cho `avenue`.
- Viết dataset scanner cho frame sequence và video.
- Viết frame reader cho UCSD.
- Viết video reader cho Avenue.
- Viết preprocess transform dùng chung.
- Viết grid generator.
- Viết cube iterator.
- Viết feature extractor baseline bằng frame differencing.
- Lưu manifest, grid và feature CSV.
- Tạo script convert CSV sang ARFF cho WEKA.
- Chạy smoke test trên 1 video/sequence mỗi dataset.
- Chạy full preprocess cho UCSD Ped2.
- Chạy full preprocess cho Avenue.
- Ghi `preprocess_stats.json`.

## 12. Tiêu chí hoàn thành

Giai đoạn tiền xử lý được xem là hoàn thành khi:

- UCSD Ped2 có `features_train.csv` và `features_test.csv`;
- Avenue có `features_train.csv` và `features_test.csv`;
- mỗi feature row truy vết được về video/frame/cell/cube;
- có `grid.json` để dùng lại khi vẽ heatmap;
- có `preprocess_stats.json` cho mỗi dataset;
- có thể xuất ARFF để thử nghiệm WEKA;
- pipeline có thể chạy lại bằng config mà không cần sửa code.
</file>

<file path="src/doc/spec_3_processed.md">
# SPEC 3 Processed - Triển khai Python anomaly baseline

Spec gốc: `src/doc/spec_3.md`

Ngày xử lý: 2026-05-26 23:38:23 +07:00

## 1. Tóm tắt

Đã triển khai pipeline Python cho SPEC 3: train per-cell `StandardScaler` + `MiniBatchKMeans`, lưu model artifact, score test features, tổng hợp frame score, smoothing theo video và sinh alert JSON có reason tối thiểu.

Dataset xác minh chính: `ucsd_ped2`.

## 2. Checklist triển khai

- [Done] Bổ sung dependency `scikit-learn` và `joblib`.
- [Done] Tạo package `src/kpdl_anomaly/`.
- [Done] Tạo loader đọc feature CSV và validate schema.
- [Done] Tạo feature selector dùng baseline feature, bỏ `direction_hist_0..7`.
- [Done] Tạo train per-cell scaler + `MiniBatchKMeans`.
- [Done] Tạo threshold percentile theo train distance.
- [Done] Lưu model artifact bằng joblib/JSON.
- [Done] Tạo test scorer đọc model và feature test.
- [Done] Xuất `cell_scores.csv`.
- [Done] Tổng hợp `frame_scores.csv` bằng top-k mean.
- [Done] Thêm smoothing theo video.
- [Done] Sinh `alerts.json` với reason tối thiểu.
- [Done] Thêm CLI `src/train.py`.
- [Done] Thêm CLI `src/test.py`.
- [Done] Chạy smoke test với `--limit-rows`.
- [Done] Chạy full UCSD Ped2 train/test.
- [Done] Ghi `src/doc/spec_3_processed.md`.

## 3. File thay đổi

- `src/requirements.txt`
- `src/configs/ucsd_ped2.yaml`
- `src/train.py`
- `src/test.py`
- `src/kpdl_anomaly/__init__.py`
- `src/kpdl_anomaly/config.py`
- `src/kpdl_anomaly/io.py`
- `src/kpdl_anomaly/schema.py`
- `src/kpdl_anomaly/feature_selection.py`
- `src/kpdl_anomaly/modeling.py`
- `src/kpdl_anomaly/thresholds.py`
- `src/kpdl_anomaly/scoring.py`
- `src/kpdl_anomaly/smoothing.py`
- `src/kpdl_anomaly/alerts.py`
- `src/kpdl_anomaly/train.py`
- `src/kpdl_anomaly/test.py`
- `src/doc/spec_3_processed.md`

## 4. Artifact sinh ra

Model:

- `src/outputs/models/ucsd_ped2/config.yaml`
- `src/outputs/models/ucsd_ped2/model_manifest.json`
- `src/outputs/models/ucsd_ped2/cell_models.joblib`
- `src/outputs/models/ucsd_ped2/cell_scalers.joblib`
- `src/outputs/models/ucsd_ped2/thresholds.json`
- `src/outputs/models/ucsd_ped2/feature_stats.json`

Result:

- `src/outputs/results/ucsd_ped2/cell_scores.csv`
- `src/outputs/results/ucsd_ped2/frame_scores.csv`
- `src/outputs/results/ucsd_ped2/alerts.json`
- `src/outputs/results/ucsd_ped2/scoring_stats.json`

## 5. Kết quả full UCSD Ped2

- Train rows read/loaded: `477312` / `477312`.
- Cell models trained: `192`.
- Fallback cells: `0`.
- K: `5`.
- Threshold percentile: `99.0`.
- Test rows scored: `376704`.
- Frame scores: `1962`.
- Severity counts: `none=795`, `medium=1167`, `high=0`.
- Alerts: `16`.
- Cell score range: `0.00099569` to `0.99361403`.
- Smoothed frame score range: `0.35047918` to `0.85956522`.

## 6. Verification

- [Done] `python -m compileall kpdl_anomaly train.py test.py`
- [Done] `python train.py --config configs\ucsd_ped2.yaml --model-root outputs\models_smoke --limit-rows 50000`
- [Done] `python test.py --config configs\ucsd_ped2.yaml --model outputs\models_smoke\ucsd_ped2 --result-root outputs\results_smoke --limit-rows 50000`
- [Done] `python train.py --config configs\ucsd_ped2.yaml`
- [Done] `python test.py --config configs\ucsd_ped2.yaml --model outputs\models\ucsd_ped2`
- [Done] Kiểm tra `cell_scores.csv` có `376704` dòng score.
- [Done] Kiểm tra `frame_scores.csv` có `1962` frame và score nằm trong `[0, 1]`.
- [Done] Kiểm tra `alerts.json` có reason tối thiểu gồm cell, threshold, nearest cluster và consecutive frames.
- [Done] `git diff --check` không báo lỗi whitespace; chỉ có cảnh báo line-ending LF/CRLF từ Git trên Windows.
- [Done] `repomix.cmd` chạy thành công từ repo root và refresh `repomix-output.xml`.

## 7. Rủi ro và follow-up

- SPEC 3 vẫn là baseline cluster/distance, chưa dùng `direction_hist_0..7`, token/rule, heatmap hoặc ground truth evaluation.
- Một số frame có severity medium khá nhiều; cần SPEC sau hoặc bước phân tích định tính để chọn threshold/smoothing phù hợp hơn.
- Smoke artifact/result được ghi vào `src/outputs/models_smoke` và `src/outputs/results_smoke`; các thư mục output đang nằm trong `.gitignore`.
</file>

<file path="src/doc/spec_3.md">
# SPEC 3 - Pipeline Python phân cụm theo cell và anomaly scoring sau tiền xử lý

## 1. Mục tiêu

Tài liệu này mô tả bước triển khai tiếp theo sau khi giai đoạn tiền xử lý đã sinh feature CSV/ARFF. Mục tiêu của SPEC 3 là chuyển từ dữ liệu đặc trưng đã chuẩn hóa sang một pipeline Python có thể chạy lặp lại để:

- huấn luyện mô hình phân cụm hành vi bình thường theo từng cell;
- tính khoảng cách của mỗi feature row tới cụm bình thường gần nhất;
- chọn threshold theo phân phối train normal;
- sinh anomaly score ở mức cell/cube và frame;
- làm mượt score theo thời gian;
- xuất kết quả phục vụ bước heatmap, alert, token/rule và đánh giá sau này.

SPEC 3 ưu tiên UCSD Ped2 vì dữ liệu nhỏ hơn, đã có đầy đủ `features_train.csv`, `features_test.csv`, ARFF và `preprocess_stats.json`.

## 2. Liên hệ với PRD và các spec trước

Theo PRD, giai đoạn sau tiền xử lý cần học normal pattern riêng theo vùng camera, sau đó dùng độ lệch so với normal pattern để tính bất thường. SPEC 1 đã tạo feature đầu vào. SPEC 2 mô tả hướng dùng WEKA để thử nghiệm, phân tích và đối chiếu. SPEC 3 triển khai nhánh Python tự động hóa để có thể train, test, lưu model, tính score và chạy lại bằng config.

SPEC 3 không thay thế SPEC 2. Kết quả WEKA như `src/doc/Kmean.md` có thể dùng để chọn cấu hình khởi đầu, ví dụ `K=5` cho baseline behavior-only trên UCSD Ped2.

## 3. Trạng thái dữ liệu đầu vào hiện có

Feature đầu vào mặc định:

```text
src/outputs/preprocessed/
  ucsd_ped2/
    features_train.csv
    features_test.csv
    grid.json
    preprocess_stats.json
  ucsd_ped1/
    features_train.csv
    features_test.csv
    grid.json
    preprocess_stats.json
  avenue/
    features_train.csv
    features_test.csv
    grid.json
    preprocess_stats.json
```

Kích thước hiện tại theo `preprocess_stats.json`:

```text
UCSD Ped2 train:
  videos: 16
  frames: 2550
  cubes: 2486
  cells: 192
  feature rows: 477312

UCSD Ped2 test:
  videos: 12
  frames: 2010
  cubes: 1962
  cells: 192
  feature rows: 376704

UCSD Ped1 train:
  videos: 34
  frames: 6800
  cubes: 6664
  cells: 192
  feature rows: 1279488

UCSD Ped1 test:
  videos: 36
  frames: 7200
  cubes: 7056
  cells: 192
  feature rows: 1354752

Avenue train:
  videos: 16
  frames: 15328
  cubes: 15264
  cells: 192
  feature rows: 2930688

Avenue test:
  videos: 21
  frames: 15324
  cubes: 15240
  cells: 192
  feature rows: 2926080
```

Schema feature đang dùng:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cell_row
cell_col
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
direction_hist_0..7
brightness_mean
brightness_delta
```

Baseline đầu tiên chỉ dùng các feature hành vi sau:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Không dùng `direction_hist_0..7` trong SPEC 3 baseline vì với `motion_method = frame_diff` các cột này hiện chưa mang thông tin hướng đáng tin cậy.

## 4. Phạm vi

### 4.1. Nằm trong phạm vi

- Đọc feature CSV từ `src/outputs/preprocessed/{dataset}`.
- Huấn luyện MiniBatchKMeans riêng cho từng `cell_id`.
- Chuẩn hóa feature bằng scaler riêng cho từng cell.
- Lưu model, scaler, centroid, threshold và metadata train.
- Tính cluster distance cho train/test.
- Chọn threshold theo percentile train của từng cell.
- Tính `cluster_distance_score` theo từng cell/cube.
- Tính `temporal_change_score` baseline theo lịch sử score cùng cell.
- Tổng hợp thành `cell_score` và `frame_score`.
- Làm mượt frame score bằng moving average hoặc EMA.
- Sinh `frame_scores.csv`, `cell_scores.csv` và `alerts.json`.
- Ghi log train/test đủ để tái hiện.

### 4.2. Ngoài phạm vi

- Heatmap overlay lên video.
- ROC-AUC/EER hoàn chỉnh theo ground truth mask.
- Tokenization và association rules production.
- Optical flow hoặc direction histogram thật.
- Dashboard hoặc realtime stream.
- Tối ưu model nâng cao như GMM, Isolation Forest, DBSCAN.

Các phần ngoài phạm vi nên đưa sang SPEC sau, sau khi SPEC 3 đã có score ổn định và có thể kiểm tra định tính.

## 5. Kiến trúc module đề xuất

Tạo package mới để tách rõ tiền xử lý và mô hình:

```text
src/kpdl_anomaly/
  __init__.py
  config.py
  io.py
  schema.py
  feature_selection.py
  modeling.py
  thresholds.py
  scoring.py
  smoothing.py
  alerts.py
  train.py
  test.py
```

Entrypoint đề xuất:

```text
src/train.py
src/test.py
```

Hoặc nếu muốn giữ style hiện tại của `src/tool/`, có thể thêm:

```text
src/tool/train_anomaly.py
src/tool/test_anomaly.py
```

Ưu tiên giữ CLI ngắn và tái dùng config:

```bash
python src/train.py --config src/configs/ucsd_ped2.yaml
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2
```

## 6. Dependency cần bổ sung

SPEC 3 cần thêm thư viện machine learning nhẹ:

```text
scikit-learn>=1.3
joblib>=1.3
```

Nếu muốn đọc CSV lớn tiện hơn có thể thêm `pandas`, nhưng MVP nên cân nhắc đọc bằng `csv` chuẩn và gom theo cell bằng chunk/buffer để giảm dependency. Với UCSD Ped2, dùng `numpy` và `csv` là đủ.

## 7. Cấu hình đề xuất

Bổ sung các mục sau vào YAML dataset hiện có hoặc tạo file model riêng như `src/configs/ucsd_ped2_anomaly.yaml`:

```yaml
model:
  type: "minibatch_kmeans"
  clusters_per_cell: 5
  min_samples_per_cell: 50
  batch_size: 1024
  max_iter: 200
  random_state: 10
  threshold_percentile: 99.0

scoring:
  feature_columns:
    - foreground_ratio
    - motion_magnitude_mean
    - motion_magnitude_std
    - motion_density
    - brightness_mean
    - brightness_delta
  cluster_weight: 0.80
  temporal_weight: 0.20
  top_k_cells: 5
  smoothing_window: 5
  alert_threshold_medium: 0.70
  alert_threshold_high: 0.90
  min_consecutive_alerts: 3

output:
  model_root: "src/outputs/models"
  result_root: "src/outputs/results"
```

Trong MVP, công thức score dùng:

```text
score = 0.80 * cluster_distance_score
      + 0.20 * temporal_change_score
```

Khi token/rule đã sẵn sàng ở SPEC sau, có thể mở rộng:

```text
score = 0.65 * cluster_distance_score
      + 0.20 * temporal_change_score
      + 0.10 * rare_token_score
      + 0.05 * rule_violation_score
```

## 8. Quy trình train

### Bước 1 - Load config và input

Đọc:

```text
src/outputs/preprocessed/{dataset}/features_train.csv
src/outputs/preprocessed/{dataset}/grid.json
src/outputs/preprocessed/{dataset}/preprocess_stats.json
```

Kiểm tra:

- file tồn tại;
- schema feature có đủ cột bắt buộc;
- `split` trong train file là `train`;
- số dòng đọc được khớp với `preprocess_stats.json` hoặc ghi warning rõ.

### Bước 2 - Gom feature theo cell

Với mỗi dòng train:

1. lấy `cell_id`;
2. lấy feature columns đã cấu hình;
3. bỏ qua dòng có giá trị numeric không hợp lệ;
4. thêm vector vào buffer của cell.

MVP có thể load toàn bộ UCSD Ped2 train vào RAM. Với Avenue, cần hỗ trợ đọc streaming/chunk để không phụ thuộc vào RAM lớn.

### Bước 3 - Train scaler và KMeans theo cell

Với mỗi cell:

1. nếu số mẫu `< min_samples_per_cell`, tạo fallback model;
2. fit `StandardScaler` trên feature train của cell;
3. transform feature;
4. fit `MiniBatchKMeans`;
5. tính khoảng cách từng mẫu train tới centroid gần nhất;
6. lưu percentile distance làm threshold.

Fallback cho cell ít dữ liệu:

- dùng thống kê global train nếu có;
- hoặc đánh dấu `model_status = insufficient_samples`;
- score của cell đó dùng z-score đơn giản hoặc luôn thấp cho đến khi có đủ dữ liệu.

### Bước 4 - Lưu model artifact

Output đề xuất:

```text
src/outputs/models/{dataset}/
  config.yaml
  model_manifest.json
  cell_models.joblib
  cell_scalers.joblib
  thresholds.json
  feature_stats.json
```

`model_manifest.json` cần có:

```text
dataset
trained_at
schema_version
feature_columns
num_cells
num_models_trained
num_fallback_cells
clusters_per_cell
threshold_percentile
train_feature_path
grid_path
```

`thresholds.json` cần có:

```text
cell_id
distance_mean
distance_std
distance_p95
distance_p99
threshold
num_train_samples
```

## 9. Quy trình test/scoring

### Bước 1 - Load model và feature test

Đọc:

```text
src/outputs/models/{dataset}/
src/outputs/preprocessed/{dataset}/features_test.csv
```

Kiểm tra model manifest khớp:

- dataset;
- schema version;
- feature columns;
- grid rows/cols nếu có lưu trong manifest.

### Bước 2 - Tính cluster distance

Với mỗi feature row:

1. lấy model/scaler theo `cell_id`;
2. chuẩn hóa vector;
3. tìm centroid gần nhất;
4. tính distance;
5. chuẩn hóa distance thành `cluster_distance_score`.

Công thức MVP:

```text
cluster_distance_score = min(distance / threshold, 2.0) / 2.0
```

Nếu `threshold <= 0`, dùng fallback:

```text
cluster_distance_score = 0.0 nếu distance gần 0
cluster_distance_score = 1.0 nếu distance > 0
```

### Bước 3 - Tính temporal change

Theo từng `(video_id, cell_id)`, giữ score trước đó:

```text
temporal_change_score = abs(current_cluster_score - previous_cluster_score)
```

Chuẩn hóa về `[0, 1]` bằng clipping. Với dòng đầu tiên của mỗi video/cell, đặt `temporal_change_score = 0.0`.

### Bước 4 - Tính cell score

```text
cell_score = cluster_weight * cluster_distance_score
           + temporal_weight * temporal_change_score
```

Clip `cell_score` về `[0, 1]`.

### Bước 5 - Tổng hợp frame score

Nhóm theo:

```text
dataset, split, video_id, center_frame_id
```

Lấy `top_k_cells` cell score cao nhất:

```text
frame_score = mean(top_k cell_score)
```

Nếu số cell ít hơn `top_k_cells`, lấy mean các cell có sẵn.

### Bước 6 - Smoothing và alert

Làm mượt `frame_score` theo từng video:

```text
smoothed_frame_score = moving_average(frame_score, window=smoothing_window)
```

Sinh severity:

```text
if smoothed_frame_score >= alert_threshold_high for >= min_consecutive_alerts:
    severity = "high"
elif smoothed_frame_score >= alert_threshold_medium for >= min_consecutive_alerts:
    severity = "medium"
else:
    severity = "none"
```

## 10. Output kết quả

Output mặc định:

```text
src/outputs/results/{dataset}/
  cell_scores.csv
  frame_scores.csv
  alerts.json
  scoring_stats.json
```

`cell_scores.csv`:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cell_row
cell_col
nearest_cluster
cluster_distance
cluster_threshold
cluster_distance_score
temporal_change_score
cell_score
```

`frame_scores.csv`:

```text
dataset
split
video_id
frame_id
frame_score
smoothed_frame_score
severity
top_cells
```

`alerts.json`:

```json
[
  {
    "dataset": "ucsd_ped2",
    "video_id": "Test001",
    "start_frame_id": 61,
    "end_frame_id": 78,
    "max_score": 0.93,
    "severity": "high",
    "top_cells": ["08_05", "08_06", "09_05"],
    "reasons": [
      "cell=08_05 cluster distance is above its train p99 threshold",
      "frame score stayed high for 3 consecutive frames"
    ]
  }
]
```

## 11. Reason text MVP

SPEC 3 chưa làm token/rule đầy đủ, nhưng mỗi alert cần có reason tối thiểu:

- cell nào có score cao nhất;
- distance có vượt threshold train hay không;
- nearest cluster là cluster nào;
- score có kéo dài qua nhiều frame hay không.

Ví dụ:

```text
cell=08_05 has cluster_distance=1.42 above train threshold=0.87
nearest normal cluster is C3
smoothed frame score exceeded high threshold for 4 consecutive frames
```

## 12. CLI đề xuất

Train:

```bash
python src/train.py --config src/configs/ucsd_ped2.yaml
```

Train với override nhanh:

```bash
python src/train.py --config src/configs/ucsd_ped2.yaml --clusters-per-cell 5 --threshold-percentile 99
```

Test:

```bash
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2
```

Debug với giới hạn dòng:

```bash
python src/test.py --config src/configs/ucsd_ped2.yaml --model src/outputs/models/ucsd_ped2 --limit-rows 50000
```

## 13. Thí nghiệm bắt buộc

### Experiment 1 - UCSD Ped2 per-cell KMeans K=5

Input:

```text
src/outputs/preprocessed/ucsd_ped2/features_train.csv
src/outputs/preprocessed/ucsd_ped2/features_test.csv
```

Config:

```text
clusters_per_cell = 5
threshold_percentile = 99
feature_columns = foreground_ratio, motion_magnitude_mean, motion_magnitude_std, motion_density, brightness_mean, brightness_delta
```

Kết quả cần có:

- model cho đủ 192 cell hoặc fallback rõ cho cell thiếu mẫu;
- `frame_scores.csv`;
- `alerts.json`;
- thống kê số frame vượt ngưỡng medium/high.

### Experiment 2 - So sánh threshold percentile

Chạy:

```text
threshold_percentile = 95, 97.5, 99
```

Ghi:

- số alert;
- số frame severity medium/high;
- phân phối score train/test;
- nhận xét threshold nào ít nhiễu hơn.

### Experiment 3 - So sánh K

Chạy:

```text
clusters_per_cell = 3, 5, 8
```

Ghi:

- train distance mean/std/p99;
- số cell có cụm quá nhỏ;
- thời gian train/test;
- nhận xét khả năng giải thích.

### Experiment 4 - Chạy Avenue hoặc UCSD Ped1 sau khi Ped2 ổn

Chỉ chạy sau khi UCSD Ped2 hoàn thành, vì dữ liệu lớn hơn. Cần log runtime và RAM nếu có thể.

## 14. Tiêu chí đánh giá

Một lần chạy SPEC 3 được xem là đạt khi:

- train hoàn tất trên UCSD Ped2 không lỗi;
- có model artifact đầy đủ trong `src/outputs/models/ucsd_ped2`;
- test hoàn tất trên UCSD Ped2 không lỗi;
- `cell_scores.csv` và `frame_scores.csv` truy vết được về `video_id`, `cube_id`, `frame_id`, `cell_id`;
- score nằm trong khoảng `[0, 1]` sau clipping;
- `alerts.json` có reason tối thiểu;
- cấu hình và model manifest đủ để chạy lại;
- runtime không phụ thuộc thao tác thủ công trong WEKA.

## 15. Rủi ro và giảm thiểu

### 15.1. Dữ liệu lớn gây tốn RAM

Rủi ro: Avenue có gần 3 triệu dòng mỗi split, load toàn bộ có thể tốn RAM.

Giảm thiểu:

- ưu tiên UCSD Ped2;
- đọc CSV theo chunk;
- train từng cell hoặc gom từng cell ra file tạm nếu cần;
- chỉ export score cần thiết, tránh giữ toàn bộ object lớn trong RAM.

### 15.2. Cell ít chuyển động tạo threshold quá nhỏ

Rủi ro: cell gần như đứng yên có train distance rất nhỏ, một nhiễu nhẹ cũng tạo score cao.

Giảm thiểu:

- dùng threshold floor;
- dùng `min_samples_per_cell`;
- thêm `motion_density` floor trước khi alert;
- yêu cầu smoothing và consecutive frames.

### 15.3. Feature frame_diff thiếu hướng chuyển động

Rủi ro: model chưa phát hiện tốt bất thường do sai hướng.

Giảm thiểu:

- ghi rõ SPEC 3 là baseline cluster/distance;
- không dùng direction hist zero trong model;
- tạo SPEC sau cho Farneback optical flow hoặc hướng chuyển động thật.

### 15.4. Không có ground truth trong scoring MVP

Rủi ro: chỉ nhìn score/alert chưa biết đúng sai định lượng.

Giảm thiểu:

- lưu output truy vết đầy đủ;
- chuẩn bị SPEC sau cho evaluation với mask UCSD;
- kiểm tra định tính bằng các frame top score trước.

## 16. Checklist triển khai

- [ ] Bổ sung dependency `scikit-learn` và `joblib`.
- [ ] Tạo package `src/kpdl_anomaly/`.
- [ ] Tạo loader đọc feature CSV và validate schema.
- [ ] Tạo feature selector bỏ metadata và direction histogram baseline.
- [ ] Tạo train per-cell scaler + MiniBatchKMeans.
- [ ] Tạo threshold percentile theo train distance.
- [ ] Lưu model artifact bằng joblib/JSON.
- [ ] Tạo test scorer đọc model và feature test.
- [ ] Xuất `cell_scores.csv`.
- [ ] Tổng hợp `frame_scores.csv` bằng top-k mean.
- [ ] Thêm smoothing theo video.
- [ ] Sinh `alerts.json` với reason tối thiểu.
- [ ] Thêm CLI `src/train.py`.
- [ ] Thêm CLI `src/test.py`.
- [ ] Chạy smoke test với `--limit-rows`.
- [ ] Chạy full UCSD Ped2 train/test.
- [ ] Ghi `src/doc/spec_3_processed.md`.

## 17. Tiêu chí hoàn thành SPEC 3

SPEC 3 hoàn thành khi:

- UCSD Ped2 train/test chạy được end-to-end bằng CLI Python;
- output model và result nằm đúng cấu trúc;
- có log hoặc processed file ghi rõ cấu hình K, threshold percentile, số cell train thành công, số fallback cell;
- có ít nhất một file `frame_scores.csv` và `alerts.json` dùng được cho bước visualization;
- có verification bằng smoke test và full Ped2;
- không cần mở WEKA để tính score.

## 18. Hướng sau SPEC 3

Sau SPEC 3, bước tiếp theo nên là:

- SPEC 4: heatmap overlay và kiểm tra định tính top anomaly frames;
- SPEC 5: tokenization, rare token score và association rules trong Python;
- SPEC 6: evaluation ROC-AUC/EER với ground truth UCSD;
- SPEC 7: optical flow hoặc direction feature thật để bắt bất thường sai hướng.
</file>

<file path="src/doc/spec_4_processed.md">
# SPEC 4 Processed - Heatmap overlay and qualitative anomaly review

Original spec path: `src/doc/spec_4.md`

Date/time processed: 2026-05-26 23:58:44 +07:00

## Summary

Implementation is complete for SPEC 4. The scope is a visualization layer over existing SPEC 3 outputs: heatmap PNGs, alert peak images, overlay videos, JSON index/stats, and a qualitative Markdown report. The scoring formula is unchanged.

2026-05-27 re-check: SPEC 4 is complete in the live repository. No source changes were required during this check; only verification artifacts were regenerated under separate `src/outputs/visualizations/ucsd_ped2_codex_*` folders and `repomix-output.xml` was refreshed.

## Checklist

- [Done] Read PRD, `repomix-output.xml`, and `src/doc/spec_4.md`.
- [Done] Inspect current SPEC 3 result schemas and preprocessing frame readers.
- [Done] Add `visualization` config defaults to dataset YAML files.
- [Done] Add `src/kpdl_anomaly/frames.py` for preprocessed test frame lookup.
- [Done] Add `src/kpdl_anomaly/visualization.py` for score loading, heatmap rendering, PNG/MP4 export, index, and stats.
- [Done] Add `src/kpdl_anomaly/qualitative.py` for Markdown report generation.
- [Done] Add CLI entrypoint `src/visualize.py`.
- [Done] Run compile and smoke visualization commands.
- [Done] Validate generated images/videos are readable and nonblank.
- [Done] Refresh `repomix-output.xml` after verification passes.

## Files Changed

- `src/configs/ucsd_ped2.yaml`
- `src/configs/ucsd_ped1.yaml`
- `src/configs/avenue.yaml`
- `src/kpdl_anomaly/frames.py`
- `src/kpdl_anomaly/visualization.py`
- `src/kpdl_anomaly/qualitative.py`
- `src/visualize.py`
- `src/doc/spec_4_processed.md`

## Verification Evidence

- [Done] `python -m compileall src\kpdl_anomaly src\visualize.py`
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --top-frames 5`
  - Result: 5 frames selected, 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --alerts --limit-frames 5`
  - Result: 5 alert peak frames selected, 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --video-id Test001 --start-frame 150 --end-frame 180 --write-video --limit-frames 10`
  - Result: 1 overlay video written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --top-frames 5 --alerts --video-id Test001 --start-frame 150 --end-frame 180 --write-video --limit-frames 10`
  - Final artifact set: 15 images and 1 video in `src/outputs/visualizations/ucsd_ped2`.
- [Done] Automated artifact validation with OpenCV.
  - Result: 15 index image paths exist, no blank images, 1 video opens, 0 missing frames, 0 missing cell score frames.
- [Done] Scoped whitespace check: `git diff --check -- src\configs\ucsd_ped2.yaml src\configs\ucsd_ped1.yaml src\configs\avenue.yaml src\kpdl_anomaly\frames.py src\kpdl_anomaly\visualization.py src\kpdl_anomaly\qualitative.py src\visualize.py src\doc\spec_4_processed.md`
  - Result: no whitespace errors in files changed for SPEC 4; Git reported only LF-to-CRLF warnings for YAML files.
- [Blocked] Full `git diff --check`
  - Reason: generated `repomix-output.xml` has trailing whitespace from packed LaTeX/KMeans text sections after the final SPEC 4 repomix refresh; scoped SPEC 4 source files are clean.
- [Done] `repomix.cmd`
  - Result: Repomix v1.14.0 packed 62 files successfully and wrote `repomix-output.xml`.

## 2026-05-27 Re-check Evidence

- [Done] `python -m compileall src\kpdl_anomaly src\visualize.py`
  - Result: compile completed successfully.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --top-frames 5 --output-dir src\outputs\visualizations\ucsd_ped2_codex_verify_top`
  - Result: 5 frames selected, 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --alerts --limit-frames 5 --output-dir src\outputs\visualizations\ucsd_ped2_codex_verify_alerts`
  - Result: 5 alert peak frames selected, 5 images written, 0 missing frames, 0 missing cell score frames.
- [Done] `python src\visualize.py --config src\configs\ucsd_ped2.yaml --video-id Test001 --start-frame 150 --end-frame 180 --write-video --limit-frames 10 --output-dir src\outputs\visualizations\ucsd_ped2_codex_verify_video`
  - Result: 1 overlay video written, 0 missing frames, 0 missing cell score frames.
- [Done] Acceptance run: top 30 frames, all 16 alert peaks, and one `Test001` overlay video were exported to `src/outputs/visualizations/ucsd_ped2_codex_accept_*`.
- [Done] Automated OpenCV validation.
  - Result: 46 acceptance images exist and are nonblank; `Test001_overlay.mp4` opens and contains 29 frames, matching `visualization_stats.json`.
- [Done] `git diff --check -- src\configs\ucsd_ped2.yaml src\configs\ucsd_ped1.yaml src\configs\avenue.yaml src\kpdl_anomaly\frames.py src\kpdl_anomaly\visualization.py src\kpdl_anomaly\qualitative.py src\visualize.py src\doc\spec_4_processed.md`
  - Result: no whitespace errors.
- [Done] `repomix.cmd`
  - Result: Repomix v1.14.0 packed 62 files successfully and wrote `repomix-output.xml`.

## Remaining Risks

- Full Avenue visualization is not verified yet; SPEC 4 smoke verification focuses on UCSD Ped2 artifacts from SPEC 3.
- MVP supports `visualization.frame_source: preprocessed`; original-frame coordinate scaling is intentionally left for a later extension.
</file>

<file path="src/doc/spec_4.md">
# SPEC 4 - Heatmap overlay và kiểm tra định tính top anomaly frames

## 1. Mục tiêu

Tài liệu này mô tả bước triển khai tiếp theo sau SPEC 3. Mục tiêu của SPEC 4 là biến các score đã sinh bởi pipeline anomaly baseline thành artifact trực quan để kiểm tra định tính:

- dựng heatmap bất thường theo cell lên frame gốc hoặc frame đã resize;
- xuất ảnh top anomaly frames kèm overlay, top cells và reason;
- xuất video overlay cho từng test video hoặc một đoạn frame được chọn;
- tạo báo cáo định tính giúp xem nhanh cảnh báo đúng/sai trước khi làm ROC-AUC/EER ở SPEC sau;
- bảo đảm kết quả visualization truy vết được về `video_id`, `frame_id`, `cell_id`, `cell_score` và `alerts.json`.

SPEC 4 không thay đổi công thức anomaly score của SPEC 3. Đây là lớp hiển thị và phân tích định tính dựa trên các artifact hiện có.

## 2. Liên hệ với PRD và SPEC 3

Theo PRD, MVP cần có heatmap bất thường trên video, danh sách vùng bất thường và lý do cảnh báo dễ hiểu. SPEC 3 đã sinh:

```text
src/outputs/results/{dataset}/
  cell_scores.csv
  frame_scores.csv
  alerts.json
  scoring_stats.json
```

SPEC 4 dùng trực tiếp các output này cùng với:

```text
src/outputs/preprocessed/{dataset}/
  grid.json
  frames_manifest.csv
  videos_manifest.csv
```

và frame gốc trong dataset để tạo overlay. Với UCSD Ped2, frame được đọc lại từ chuỗi `.tif`; với Avenue, frame được đọc lại từ `.avi`.

SPEC 4 là bước chuẩn bị bắt buộc trước SPEC 6 evaluation định lượng, vì cần nhìn được các frame score cao nhất để biết score hiện tại đang bám vào chuyển động bất thường thật hay bị nhiễu bởi ánh sáng, bóng, vùng đứng yên hoặc threshold quá nhạy.

## 3. Trạng thái đầu vào hiện có

Dataset ưu tiên:

```text
ucsd_ped2
```

Artifact đã có sau SPEC 3:

```text
src/outputs/models/ucsd_ped2/
  model_manifest.json
  cell_models.joblib
  cell_scalers.joblib
  thresholds.json
  feature_stats.json

src/outputs/results/ucsd_ped2/
  cell_scores.csv
  frame_scores.csv
  alerts.json
  scoring_stats.json
```

Kích thước output UCSD Ped2 hiện tại:

```text
cell_scores.csv: 376704 dòng score
frame_scores.csv: 1962 frame
alerts.json: 16 alert segment
grid: 12 x 16 = 192 cell
resize: 320 x 240
```

Schema quan trọng:

```text
cell_scores.csv:
  dataset, split, video_id, cube_id, start_frame_id, end_frame_id,
  center_frame_id, cell_id, cell_row, cell_col, nearest_cluster,
  cluster_distance, cluster_threshold, cluster_distance_score,
  temporal_change_score, cell_score

frame_scores.csv:
  dataset, split, video_id, frame_id, frame_score,
  smoothed_frame_score, severity, top_cells

alerts.json:
  dataset, video_id, start_frame_id, end_frame_id,
  max_score, severity, top_cells, reasons
```

Grid mapping:

```text
grid.json:
  resized_width, resized_height, rows, cols, cells[]

cell:
  cell_id, row, col, x1, y1, x2, y2, width, height
```

## 4. Phạm vi

### 4.1. Nằm trong phạm vi

- Đọc `frame_scores.csv`, `cell_scores.csv`, `alerts.json` và `grid.json`.
- Chọn top anomaly frames theo `smoothed_frame_score` hoặc theo alert segment.
- Đọc lại frame test từ dataset bằng config hiện có.
- Tạo heatmap 2D từ `cell_score` theo từng `frame_id`.
- Resize hoặc map heatmap đúng kích thước frame hiển thị.
- Blend heatmap lên frame grayscale/RGB bằng OpenCV colormap.
- Vẽ viền cell top bất thường và nhãn ngắn nếu cần.
- Ghi ảnh overlay `.png` cho top frames.
- Ghi video overlay `.mp4` cho một video hoặc một đoạn alert.
- Tạo file index JSON/CSV mô tả các ảnh đã xuất.
- Tạo report Markdown định tính với top frame, score, top cells, reason và đường dẫn artifact.
- Thêm CLI `src/visualize.py`.
- Thêm package/module visualization trong `src/kpdl_anomaly/`.

### 4.2. Ngoài phạm vi

- Không tính lại model, threshold hoặc anomaly score.
- Không đánh giá ROC-AUC/EER.
- Không làm dashboard web.
- Không tạo annotation editor.
- Không huấn luyện optical flow hoặc direction feature thật.
- Không thay thế `alerts.json` của SPEC 3, trừ khi phát hiện lỗi schema rõ ràng.

## 5. Kiến trúc module đề xuất

Bổ sung các file:

```text
src/kpdl_anomaly/
  visualization.py
  frames.py
  qualitative.py

src/visualize.py
```

Vai trò:

```text
frames.py:
  đọc lại frame theo dataset/config/video_id/frame_id
  hỗ trợ frame_sequence và video
  tái dùng logic scan/read/preprocess từ kpdl_preprocess khi phù hợp

visualization.py:
  đọc score/grid
  tạo heatmap cell-level
  blend heatmap lên frame
  vẽ top cell boxes
  ghi PNG/MP4

qualitative.py:
  chọn top frames/top alert segments
  ghép reason từ alerts.json và top_cells từ frame_scores.csv
  tạo report Markdown/JSON

src/visualize.py:
  CLI entrypoint cho SPEC 4
```

Không nên đưa logic visualization vào `scoring.py`, vì SPEC 3 scoring cần giữ là pipeline số liệu thuần.

## 6. Cấu hình đề xuất

Bổ sung vào YAML dataset hiện có:

```yaml
visualization:
  output_root: "src/outputs/visualizations"
  colormap: "JET"
  alpha: 0.45
  heatmap_normalization: "per_frame"
  draw_grid: false
  draw_top_cells: true
  top_cells_per_frame: 5
  top_frames: 30
  min_score: 0.70
  frame_source: "preprocessed"
  image_format: "png"
  video_fps: 10
```

Ý nghĩa:

- `heatmap_normalization = per_frame`: chuẩn hóa heatmap theo max cell score của frame, dễ nhìn top vùng nóng.
- `heatmap_normalization = global`: dùng cùng scale toàn video, phù hợp so sánh giữa frame.
- `frame_source = preprocessed`: overlay lên frame đã resize/grayscale giống pipeline score.
- `frame_source = original`: overlay lên kích thước gốc, cần scale cell box từ resize về original.

MVP nên dùng `preprocessed` để đơn giản, đúng với `grid.json` và ít rủi ro lệch tọa độ.

## 7. Quy trình tạo heatmap cho một frame

### Bước 1 - Load grid

Đọc:

```text
src/outputs/preprocessed/{dataset}/grid.json
```

Tạo mapping:

```text
cell_id -> {x1, y1, x2, y2, row, col}
```

Kiểm tra:

- số cell trong `grid.json` khớp config `rows * cols` trừ `ignore_cells`;
- `resized_width` và `resized_height` khớp frame đọc lại;
- mọi `cell_id` trong `cell_scores.csv` tồn tại trong grid.

### Bước 2 - Load score theo frame

Đọc `cell_scores.csv`, nhóm theo:

```text
video_id, center_frame_id
```

Mỗi nhóm giữ:

```text
cell_id
cell_score
cluster_distance_score
temporal_change_score
cluster_distance
cluster_threshold
nearest_cluster
```

Để tránh load file lớn khi chỉ visualize vài frame, MVP có thể stream CSV và chỉ giữ những frame đã chọn từ `frame_scores.csv`/`alerts.json`.

### Bước 3 - Chọn frame cần visualize

Nguồn chọn:

```text
top smoothed_frame_score từ frame_scores.csv
top frame trong mỗi alert segment từ alerts.json
frame_id do người dùng chỉ định
range frame do người dùng chỉ định
```

Ưu tiên MVP:

1. `--top-frames 30`: chọn top 30 frame toàn test set.
2. `--video-id Test001 --start-frame 150 --end-frame 180`: xuất đoạn cụ thể.
3. `--alerts`: xuất peak frame của từng alert.

### Bước 4 - Tạo heatmap cell-level

Tạo mảng heatmap kích thước:

```text
resized_height x resized_width
```

Với mỗi cell:

```text
heatmap[y1:y2, x1:x2] = cell_score
```

Nếu một cell thiếu score, đặt `0.0` và ghi warning nếu thiếu hàng loạt.

### Bước 5 - Normalize và colorize

MVP:

```text
normalized = clip(heatmap, 0, 1)
colored = cv2.applyColorMap((normalized * 255).astype(uint8), cv2.COLORMAP_JET)
overlay = cv2.addWeighted(frame_rgb, 1 - alpha, colored, alpha, 0)
```

Nếu `per_frame`, trước khi colorize:

```text
normalized = heatmap / max(heatmap)
```

với `max(heatmap) > 0`. Nếu max bằng 0, giữ heatmap zero.

### Bước 6 - Vẽ top cells

Vẽ viền cho các cell trong `top_cells`:

```text
cv2.rectangle(overlay, (x1, y1), (x2, y2), color, thickness=1 hoặc 2)
```

Nhãn text nên ngắn để không che frame:

```text
08_10 0.91
```

Nếu nhiều cell gần nhau, MVP có thể chỉ vẽ viền và ghi top cell list vào report thay vì text trên ảnh.

## 8. Output mong muốn

Thư mục:

```text
src/outputs/visualizations/{dataset}/
  top_frames/
    Test001_000164_score_0.796.png
    Test002_000143_score_0.820.png
  alerts/
    Test001_000164_000178_peak.png
    Test001_000164_000178_overlay.mp4
  videos/
    Test001_overlay.mp4
  qualitative_report.md
  visualization_index.json
  visualization_stats.json
```

`visualization_index.json`:

```json
[
  {
    "dataset": "ucsd_ped2",
    "video_id": "Test001",
    "frame_id": 164,
    "frame_score": 0.79,
    "smoothed_frame_score": 0.79,
    "severity": "medium",
    "top_cells": ["08_10", "08_11", "09_11"],
    "image_path": "src/outputs/visualizations/ucsd_ped2/top_frames/Test001_000164_score_0.796.png",
    "alert_id": "Test001:164-178"
  }
]
```

`visualization_stats.json`:

```text
dataset
config_path
result_dir
grid_path
num_frames_selected
num_images_written
num_videos_written
missing_frames
missing_cell_score_frames
generated_at
```

`qualitative_report.md` cần có:

- bảng top frames theo score;
- bảng alert segments;
- ảnh preview cho peak frames nếu Markdown renderer hỗ trợ path tương đối;
- reason từ `alerts.json`;
- nhận xét thủ công placeholder để sinh viên điền sau khi xem ảnh.

## 9. CLI đề xuất

Xuất top frames:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --top-frames 30
```

Xuất peak frame của alert segments:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --alerts
```

Xuất đoạn frame cụ thể:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --video-id Test001 --start-frame 150 --end-frame 180
```

Xuất video overlay cho một video:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --video-id Test001 --write-video
```

Override output:

```bash
python src/visualize.py --config src/configs/ucsd_ped2.yaml --result-dir src/outputs/results/ucsd_ped2 --output-dir src/outputs/visualizations/ucsd_ped2_debug
```

Các tham số CLI cần có:

```text
--config
--project-root
--result-dir
--output-dir
--top-frames
--alerts
--video-id
--start-frame
--end-frame
--write-video
--alpha
--colormap
--min-score
--limit-frames
```

## 10. Đọc frame theo dataset

### 10.1. UCSD frame sequence

Với `input_type = frame_sequence`, có thể đọc lại frame từ:

```text
dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2/Test/Test001/001.tif
```

hoặc path đã lưu trong `frames_manifest.csv`.

Quy tắc:

- `video_id` khớp tên thư mục `Test001`;
- `frame_id` khớp tên file `.tif`;
- frame cần được preprocess lại cùng resize/grayscale/blur để khớp score;
- nếu overlay lên original frame, dùng raw frame trước resize và scale heatmap.

MVP nên tái dùng:

```text
kpdl_preprocess.datasets.scan_dataset
kpdl_preprocess.readers.iter_preprocessed_frames
```

để tránh lệch logic preprocess.

### 10.2. Avenue video

Với `input_type = video`, có thể đọc `.avi` bằng `cv2.VideoCapture` và seek tới frame cần xuất.

MVP cho Avenue chỉ cần hỗ trợ:

- xuất top frames bằng cách đọc tuần tự từng video;
- xuất video overlay tuần tự;
- không cần random seek tối ưu ngay.

## 11. Báo cáo định tính

SPEC 4 cần tạo report để sinh viên đánh giá thủ công. Mỗi record nên có:

```text
dataset
video_id
frame_id hoặc alert range
frame_score
smoothed_frame_score
severity
top_cells
reasons
image_path
manual_label placeholder: true_positive | false_positive | uncertain
notes placeholder
```

Mẫu Markdown:

```markdown
## Top Anomaly Frames

| Rank | Video | Frame | Score | Severity | Top cells | Image | Manual label | Notes |
| --- | --- | ---: | ---: | --- | --- | --- | --- | --- |
| 1 | Test002 | 143 | 0.820 | medium | 09_10, 10_10 | top_frames/...png | TBD | TBD |
```

Phần report này không phải metric chính thức. Nó dùng để phát hiện các vấn đề như:

- score cao vì vùng nền nhiễu;
- heatmap nằm lệch vị trí chuyển động;
- top cell hợp lý nhưng threshold quá nhạy;
- alert kéo quá dài sau smoothing;
- frame score cao trong video nhưng không tương ứng bất thường rõ ràng.

## 12. Thí nghiệm bắt buộc

### Experiment 1 - UCSD Ped2 top 30 frames

Input:

```text
src/outputs/results/ucsd_ped2/frame_scores.csv
src/outputs/results/ucsd_ped2/cell_scores.csv
src/outputs/preprocessed/ucsd_ped2/grid.json
```

Output:

```text
src/outputs/visualizations/ucsd_ped2/top_frames/*.png
src/outputs/visualizations/ucsd_ped2/qualitative_report.md
```

Yêu cầu:

- xuất đủ 30 ảnh;
- ảnh không blank;
- heatmap đúng kích thước frame;
- top cells trong ảnh khớp `frame_scores.csv`.

### Experiment 2 - UCSD Ped2 alert peaks

Input:

```text
src/outputs/results/ucsd_ped2/alerts.json
```

Mỗi alert chọn frame có `smoothed_frame_score` cao nhất trong `[start_frame_id, end_frame_id]`.

Yêu cầu:

- xuất ít nhất 1 ảnh cho mỗi alert;
- report ghi reason từ alert;
- top cells và reason không bị mất dấu schema.

### Experiment 3 - UCSD Ped2 one overlay video

Chọn video có alert rõ, ví dụ `Test001` hoặc video có `max_score` cao nhất.

Output:

```text
src/outputs/visualizations/ucsd_ped2/videos/Test001_overlay.mp4
```

Yêu cầu:

- video mở được bằng OpenCV;
- số frame xuất khớp range đã chọn hoặc toàn video;
- overlay chạy đúng theo thời gian, không đứng một heatmap cố định.

### Experiment 4 - So sánh normalization

Chạy top frames với:

```text
heatmap_normalization = per_frame
heatmap_normalization = global
```

Ghi nhận:

- `per_frame` dễ thấy vùng nóng hơn;
- `global` tốt hơn khi so sánh độ mạnh giữa frame;
- chọn mặc định phù hợp cho report.

## 13. Kiểm tra chất lượng

Điều kiện chấp nhận:

- Không crash khi frame score có top cell không tìm thấy trong cell score, nhưng phải ghi warning.
- Không crash khi frame source thiếu frame, nhưng phải ghi `missing_frames`.
- Cell box không vượt biên frame.
- Heatmap có cùng width/height với frame overlay.
- Giá trị heatmap nằm trong `[0, 1]` trước khi colorize.
- File ảnh/video được ghi bằng path có dataset/video/frame rõ ràng.
- Report đủ thông tin để truy ngược về `frame_scores.csv`, `cell_scores.csv`, `alerts.json`.
- CLI chạy lại được chỉ bằng config và output của SPEC 3.

Kiểm tra tự động đề xuất:

```bash
python -m compileall src/kpdl_anomaly src/visualize.py
python src/visualize.py --config src/configs/ucsd_ped2.yaml --top-frames 5
python src/visualize.py --config src/configs/ucsd_ped2.yaml --alerts --limit-frames 5
```

Sau khi chạy, kiểm tra:

```text
num_images_written > 0
mọi image path trong visualization_index.json tồn tại
ảnh đọc lại bằng cv2.imread không None
ảnh có variance pixel > 0
visualization_stats.json không có missing frame nghiêm trọng
```

## 14. Checklist triển khai

- [ ] Bổ sung config `visualization` vào YAML dataset.
- [ ] Tạo `src/kpdl_anomaly/frames.py`.
- [ ] Tạo hàm đọc frame test theo `video_id` và `frame_id`.
- [ ] Tạo `src/kpdl_anomaly/visualization.py`.
- [ ] Load `grid.json` và validate cell bounds.
- [ ] Load `frame_scores.csv` và chọn top frames.
- [ ] Stream `cell_scores.csv` để lấy score cho frame được chọn.
- [ ] Tạo heatmap cell-level.
- [ ] Blend heatmap lên frame bằng OpenCV colormap.
- [ ] Vẽ top cell boxes.
- [ ] Xuất ảnh `.png` top frames.
- [ ] Xuất peak frame cho từng alert segment.
- [ ] Tạo `visualization_index.json`.
- [ ] Tạo `visualization_stats.json`.
- [ ] Tạo `qualitative_report.md`.
- [ ] Thêm CLI `src/visualize.py`.
- [ ] Thêm tùy chọn xuất video overlay.
- [ ] Chạy smoke visualization trên UCSD Ped2 top 5 frames.
- [ ] Chạy alert peak export trên UCSD Ped2.
- [ ] Kiểm tra ảnh/video không blank.
- [ ] Cập nhật `src/doc/spec_4_processed.md` sau khi triển khai.

## 15. Tiêu chí hoàn thành SPEC 4

SPEC 4 được xem là hoàn thành khi:

- có thể chạy CLI visualization từ config UCSD Ped2;
- xuất được ảnh heatmap overlay cho top anomaly frames;
- xuất được ảnh peak frame cho từng alert segment;
- xuất được ít nhất một video overlay cho một test video hoặc alert range;
- `visualization_index.json` và `visualization_stats.json` được ghi đầy đủ;
- `qualitative_report.md` có bảng top frames và alert segments;
- output có thể truy vết về score/reason của SPEC 3;
- có smoke test hoặc validation tự động chứng minh ảnh không blank và path tồn tại;
- `src/doc/spec_4_processed.md` ghi rõ file thay đổi, lệnh verification và kết quả.

## 16. Rủi ro và giảm thiểu

### 16.1. Heatmap lệch frame

Rủi ro: overlay dùng frame original nhưng grid là resize `320 x 240`, làm cell box lệch.

Giảm thiểu:

- MVP overlay lên preprocessed frame đã resize;
- nếu dùng original frame, scale `x1/y1/x2/y2` theo tỷ lệ original/resized;
- ghi rõ `frame_source` trong stats.

### 16.2. Load `cell_scores.csv` lớn

Rủi ro: Avenue có score lớn, load toàn bộ vào RAM không cần thiết.

Giảm thiểu:

- chọn frame trước từ `frame_scores.csv`;
- stream `cell_scores.csv` và chỉ giữ frame cần visualize;
- hỗ trợ `--video-id` và `--limit-frames`.

### 16.3. Overlay khó đọc

Rủi ro: colormap/alpha quá mạnh che mất nội dung frame.

Giảm thiểu:

- mặc định `alpha=0.45`;
- cho phép override `--alpha`;
- chỉ vẽ text cho số top cell nhỏ;
- report chứa top cell list thay vì nhồi nhiều chữ vào ảnh.

### 16.4. Alert kéo dài quá mức

Rủi ro: smoothing làm một alert segment dài, xuất video/ảnh quá nhiều.

Giảm thiểu:

- với `--alerts`, mặc định chỉ xuất peak frame;
- thêm `--write-video` khi cần video segment;
- giới hạn `--max-alerts` nếu cần debug nhanh.

### 16.5. Nhận xét định tính không đồng nhất

Rủi ro: manual label/notes tùy người xem, khó so sánh.

Giảm thiểu:

- report có cột label cố định: `true_positive`, `false_positive`, `uncertain`;
- thêm tiêu chí ngắn trong báo cáo dự án;
- SPEC 6 sẽ dùng ground truth để đánh giá định lượng.

## 17. Hướng sau SPEC 4

Sau SPEC 4, các bước tiếp theo nên là:

- SPEC 5: tokenization, rare token score và association rules trong Python;
- SPEC 6: evaluation ROC-AUC/EER với ground truth UCSD;
- SPEC 7: optical flow hoặc direction feature thật để bắt bất thường sai hướng;
- tinh chỉnh threshold/smoothing dựa trên report định tính từ SPEC 4 và metric từ SPEC 6.
</file>

<file path="src/kpdl_anomaly/__init__.py">
"""SPEC 3 anomaly scoring pipeline."""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
</file>

<file path="src/kpdl_anomaly/alerts.py">
from __future__ import annotations

from typing import Any


def assign_severities(
    records: list[dict[str, Any]],
    medium_threshold: float,
    high_threshold: float,
    min_consecutive: int,
) -> None:
    for record in records:
        record["severity"] = "none"

    _mark_runs(records, high_threshold, min_consecutive, "high")
    _mark_runs(records, medium_threshold, min_consecutive, "medium", preserve_higher=True)


def _mark_runs(
    records: list[dict[str, Any]],
    threshold: float,
    min_consecutive: int,
    severity: str,
    preserve_higher: bool = False,
) -> None:
    run_start: int | None = None
    for index, record in enumerate(records):
        if float(record["smoothed_frame_score"]) >= threshold:
            if run_start is None:
                run_start = index
            continue
        _apply_run(records, run_start, index, min_consecutive, severity, preserve_higher)
        run_start = None
    _apply_run(records, run_start, len(records), min_consecutive, severity, preserve_higher)


def _apply_run(
    records: list[dict[str, Any]],
    start: int | None,
    end: int,
    min_consecutive: int,
    severity: str,
    preserve_higher: bool,
) -> None:
    if start is None or end - start < min_consecutive:
        return
    for record in records[start:end]:
        if preserve_higher and record["severity"] == "high":
            continue
        record["severity"] = severity


def build_alerts(
    dataset: str,
    records_by_video: dict[str, list[dict[str, Any]]],
    min_consecutive: int,
) -> list[dict[str, Any]]:
    alerts: list[dict[str, Any]] = []
    for video_id, records in records_by_video.items():
        segment: list[dict[str, Any]] = []
        active_severity = "none"
        for record in records:
            severity = str(record["severity"])
            if severity == "none":
                _flush_segment(alerts, dataset, video_id, segment, active_severity, min_consecutive)
                segment = []
                active_severity = "none"
                continue
            if segment and severity != active_severity:
                _flush_segment(alerts, dataset, video_id, segment, active_severity, min_consecutive)
                segment = []
            active_severity = severity
            segment.append(record)
        _flush_segment(alerts, dataset, video_id, segment, active_severity, min_consecutive)
    return alerts


def _flush_segment(
    alerts: list[dict[str, Any]],
    dataset: str,
    video_id: str,
    segment: list[dict[str, Any]],
    severity: str,
    min_consecutive: int,
) -> None:
    if not segment or severity == "none":
        return

    peak = max(segment, key=lambda item: float(item["smoothed_frame_score"]))
    top_entries = list(peak.get("top_entries", []))
    top_cells = [entry["cell_id"] for entry in top_entries]
    reasons = _reasons(peak, severity, min_consecutive)
    alerts.append(
        {
            "dataset": dataset,
            "video_id": video_id,
            "start_frame_id": int(segment[0]["frame_id"]),
            "end_frame_id": int(segment[-1]["frame_id"]),
            "max_score": float(peak["smoothed_frame_score"]),
            "severity": severity,
            "top_cells": top_cells,
            "reasons": reasons,
        }
    )


def _reasons(record: dict[str, Any], severity: str, min_consecutive: int) -> list[str]:
    top_entries = list(record.get("top_entries", []))
    reasons: list[str] = []
    if top_entries:
        top = top_entries[0]
        threshold = float(top["cluster_threshold"])
        distance = float(top["cluster_distance"])
        relation = "above" if distance > threshold else "within"
        reasons.append(
            f"cell={top['cell_id']} has cluster_distance={distance:.6f} "
            f"{relation} train threshold={threshold:.6f}"
        )
        nearest = int(top["nearest_cluster"])
        if nearest >= 0:
            reasons.append(f"nearest normal cluster is C{nearest}")
        for reason in top.get("token_rule_reasons", []):
            if reason:
                reasons.append(str(reason))
    reasons.append(
        f"smoothed frame score met {severity} threshold for at least "
        f"{min_consecutive} consecutive frames"
    )
    return reasons
</file>

<file path="src/kpdl_anomaly/config.py">
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Any

from kpdl_preprocess.config import ConfigError, copy_config, get_nested, load_config, resolve_path

from .schema import DEFAULT_FEATURE_COLUMNS


@dataclass(frozen=True)
class RulesConfig:
    enabled: bool
    output_root: Path
    model_dir: Path | None
    algorithm: str
    min_support: float
    min_confidence: float
    min_lift: float
    max_itemset_size: int
    max_rules: int
    include_cell_token: bool
    include_cluster_token: bool
    include_brightness_token: bool
    include_direction_token: bool
    rare_itemset_size: int
    rare_support_floor: float
    rare_score_cap: float


@dataclass(frozen=True)
class AnomalyConfig:
    raw: dict[str, Any]
    config_path: Path
    project_root: Path
    dataset: str
    preprocessed_dir: Path
    train_feature_path: Path
    test_feature_path: Path
    grid_path: Path
    preprocess_stats_path: Path
    model_root: Path
    result_root: Path
    model_dir: Path
    result_dir: Path
    model_type: str
    clusters_per_cell: int
    min_samples_per_cell: int
    batch_size: int
    max_iter: int
    random_state: int
    threshold_percentile: float
    threshold_floor: float
    feature_columns: list[str]
    cluster_weight: float
    temporal_weight: float
    rare_token_weight: float
    rule_weight: float
    top_k_cells: int
    smoothing_window: int
    alert_threshold_medium: float
    alert_threshold_high: float
    min_consecutive_alerts: int
    rules: RulesConfig


def load_anomaly_config(
    config_path: str | Path,
    project_root: str | Path = ".",
    model_root: str | Path | None = None,
    result_root: str | Path | None = None,
    clusters_per_cell: int | None = None,
    threshold_percentile: float | None = None,
) -> AnomalyConfig:
    config_path = Path(config_path)
    project_root = Path(project_root).resolve()
    raw = copy_config(load_config(config_path))

    model = raw.setdefault("model", {})
    model.setdefault("type", "minibatch_kmeans")
    model.setdefault("clusters_per_cell", 5)
    model.setdefault("min_samples_per_cell", 50)
    model.setdefault("batch_size", 1024)
    model.setdefault("max_iter", 200)
    model.setdefault("random_state", 10)
    model.setdefault("threshold_percentile", 99.0)
    model.setdefault("threshold_floor", 1.0e-9)
    if clusters_per_cell is not None:
        model["clusters_per_cell"] = clusters_per_cell
    if threshold_percentile is not None:
        model["threshold_percentile"] = threshold_percentile

    rules = raw.setdefault("rules", {})
    rules.setdefault("enabled", False)
    rules.setdefault("output_root", "src/outputs/rules")
    rules.setdefault("model_dir", None)
    rules.setdefault("algorithm", "bounded_apriori")
    rules.setdefault("min_support", 0.01)
    rules.setdefault("min_confidence", 0.60)
    rules.setdefault("min_lift", 1.05)
    rules.setdefault("max_itemset_size", 3)
    rules.setdefault("max_rules", 200)
    rules.setdefault("include_cell_token", True)
    rules.setdefault("include_cluster_token", True)
    rules.setdefault("include_brightness_token", True)
    rules.setdefault("include_direction_token", False)
    rules.setdefault("rare_itemset_size", 3)
    rules.setdefault("rare_support_floor", 0.001)
    rules.setdefault("rare_score_cap", 1.0)

    scoring = raw.setdefault("scoring", {})
    rules_enabled = bool(get_nested(raw, "rules", "enabled", default=False))
    scoring.setdefault("feature_columns", list(DEFAULT_FEATURE_COLUMNS))
    scoring.setdefault("cluster_weight", 0.65 if rules_enabled else 0.80)
    scoring.setdefault("temporal_weight", 0.20)
    scoring.setdefault("rare_token_weight", 0.10 if rules_enabled else 0.0)
    scoring.setdefault("rule_weight", 0.05 if rules_enabled else 0.0)
    scoring.setdefault("top_k_cells", 5)
    scoring.setdefault("smoothing_window", 5)
    scoring.setdefault("alert_threshold_medium", 0.70)
    scoring.setdefault("alert_threshold_high", 0.90)
    scoring.setdefault("min_consecutive_alerts", 3)

    output = raw.setdefault("output", {})
    output.setdefault("root", "src/outputs/preprocessed")
    output.setdefault("model_root", "src/outputs/models")
    output.setdefault("result_root", "src/outputs/results")
    if model_root is not None:
        output["model_root"] = str(model_root)
    if result_root is not None:
        output["result_root"] = str(result_root)

    dataset = str(get_nested(raw, "data", "dataset"))
    preprocessed_root = resolve_path(str(get_nested(raw, "output", "root")), project_root)
    model_root_path = resolve_path(str(get_nested(raw, "output", "model_root")), project_root)
    result_root_path = resolve_path(str(get_nested(raw, "output", "result_root")), project_root)
    rule_output_root = resolve_path(str(get_nested(raw, "rules", "output_root")), project_root)
    rule_model_dir_value = get_nested(raw, "rules", "model_dir", default=None)
    rule_model_dir = (
        resolve_path(str(rule_model_dir_value), project_root)
        if rule_model_dir_value not in {None, ""}
        else None
    )
    preprocessed_dir = preprocessed_root / dataset

    feature_columns = list(get_nested(raw, "scoring", "feature_columns", default=DEFAULT_FEATURE_COLUMNS))
    if not feature_columns or not all(isinstance(column, str) for column in feature_columns):
        raise ConfigError("scoring.feature_columns must be a non-empty list of strings")

    model_type = str(get_nested(raw, "model", "type"))
    if model_type != "minibatch_kmeans":
        raise ConfigError("SPEC 3 currently supports only model.type='minibatch_kmeans'")

    clusters = _positive_int(raw, "model", "clusters_per_cell")
    min_samples = _positive_int(raw, "model", "min_samples_per_cell")
    batch_size = _positive_int(raw, "model", "batch_size")
    max_iter = _positive_int(raw, "model", "max_iter")
    random_state = int(get_nested(raw, "model", "random_state", default=10))
    percentile = float(get_nested(raw, "model", "threshold_percentile", default=99.0))
    if percentile <= 0.0 or percentile > 100.0:
        raise ConfigError("model.threshold_percentile must be in the interval (0, 100]")
    threshold_floor = float(get_nested(raw, "model", "threshold_floor", default=1.0e-9))
    if threshold_floor < 0.0:
        raise ConfigError("model.threshold_floor must be non-negative")

    cluster_weight = float(get_nested(raw, "scoring", "cluster_weight", default=0.80))
    temporal_weight = float(get_nested(raw, "scoring", "temporal_weight", default=0.20))
    rare_token_weight = float(get_nested(raw, "scoring", "rare_token_weight", default=0.0))
    rule_weight = float(get_nested(raw, "scoring", "rule_weight", default=0.0))
    if cluster_weight < 0.0 or temporal_weight < 0.0 or rare_token_weight < 0.0 or rule_weight < 0.0:
        raise ConfigError("scoring weights must be non-negative")

    medium = float(get_nested(raw, "scoring", "alert_threshold_medium", default=0.70))
    high = float(get_nested(raw, "scoring", "alert_threshold_high", default=0.90))
    if not 0.0 <= medium <= high <= 1.0:
        raise ConfigError("alert thresholds must satisfy 0 <= medium <= high <= 1")

    algorithm = str(get_nested(raw, "rules", "algorithm", default="bounded_apriori"))
    if algorithm != "bounded_apriori":
        raise ConfigError("rules.algorithm currently supports only 'bounded_apriori'")
    min_support = _probability(raw, "rules", "min_support", allow_zero=False)
    min_confidence = _probability(raw, "rules", "min_confidence", allow_zero=False)
    min_lift = float(get_nested(raw, "rules", "min_lift", default=1.05))
    if min_lift <= 0.0:
        raise ConfigError("rules.min_lift must be positive")
    rare_support_floor = _probability(raw, "rules", "rare_support_floor", allow_zero=True)
    rare_score_cap = _probability(raw, "rules", "rare_score_cap", allow_zero=True)

    return AnomalyConfig(
        raw=raw,
        config_path=config_path,
        project_root=project_root,
        dataset=dataset,
        preprocessed_dir=preprocessed_dir,
        train_feature_path=preprocessed_dir / "features_train.csv",
        test_feature_path=preprocessed_dir / "features_test.csv",
        grid_path=preprocessed_dir / "grid.json",
        preprocess_stats_path=preprocessed_dir / "preprocess_stats.json",
        model_root=model_root_path,
        result_root=result_root_path,
        model_dir=model_root_path / dataset,
        result_dir=result_root_path / dataset,
        model_type=model_type,
        clusters_per_cell=clusters,
        min_samples_per_cell=min_samples,
        batch_size=batch_size,
        max_iter=max_iter,
        random_state=random_state,
        threshold_percentile=percentile,
        threshold_floor=threshold_floor,
        feature_columns=feature_columns,
        cluster_weight=cluster_weight,
        temporal_weight=temporal_weight,
        rare_token_weight=rare_token_weight,
        rule_weight=rule_weight,
        top_k_cells=_positive_int(raw, "scoring", "top_k_cells"),
        smoothing_window=_positive_int(raw, "scoring", "smoothing_window"),
        alert_threshold_medium=medium,
        alert_threshold_high=high,
        min_consecutive_alerts=_positive_int(raw, "scoring", "min_consecutive_alerts"),
        rules=RulesConfig(
            enabled=rules_enabled,
            output_root=rule_output_root,
            model_dir=rule_model_dir,
            algorithm=algorithm,
            min_support=min_support,
            min_confidence=min_confidence,
            min_lift=min_lift,
            max_itemset_size=_positive_int(raw, "rules", "max_itemset_size"),
            max_rules=_positive_int(raw, "rules", "max_rules"),
            include_cell_token=bool(get_nested(raw, "rules", "include_cell_token", default=True)),
            include_cluster_token=bool(get_nested(raw, "rules", "include_cluster_token", default=True)),
            include_brightness_token=bool(get_nested(raw, "rules", "include_brightness_token", default=True)),
            include_direction_token=bool(get_nested(raw, "rules", "include_direction_token", default=False)),
            rare_itemset_size=_positive_int(raw, "rules", "rare_itemset_size"),
            rare_support_floor=rare_support_floor,
            rare_score_cap=rare_score_cap,
        ),
    )


def _positive_int(config: dict[str, Any], section: str, key: str) -> int:
    value = int(get_nested(config, section, key))
    if value <= 0:
        raise ConfigError(f"{section}.{key} must be a positive integer")
    return value


def _probability(config: dict[str, Any], section: str, key: str, allow_zero: bool) -> float:
    value = float(get_nested(config, section, key))
    lower_ok = value >= 0.0 if allow_zero else value > 0.0
    if not lower_ok or value > 1.0:
        bound = "[0, 1]" if allow_zero else "(0, 1]"
        raise ConfigError(f"{section}.{key} must be in the interval {bound}")
    return value
</file>

<file path="src/kpdl_anomaly/feature_selection.py">
from __future__ import annotations

from collections.abc import Mapping

import numpy as np


def row_to_vector(row: Mapping[str, str], feature_columns: list[str]) -> np.ndarray | None:
    values: list[float] = []
    try:
        for column in feature_columns:
            values.append(float(row[column]))
    except (KeyError, TypeError, ValueError):
        return None
    vector = np.asarray(values, dtype=np.float64)
    if not np.all(np.isfinite(vector)):
        return None
    return vector
</file>

<file path="src/kpdl_anomaly/frames.py">
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path

import numpy as np

from kpdl_preprocess.config import ConfigError
from kpdl_preprocess.datasets import scan_dataset
from kpdl_preprocess.readers import iter_preprocessed_frames

from .config import AnomalyConfig


@dataclass(frozen=True)
class LoadedFrame:
    video_id: str
    frame_id: int
    gray: np.ndarray
    source_path: Path
    resized_width: int
    resized_height: int


@dataclass(frozen=True)
class FrameBatch:
    frames: dict[tuple[str, int], LoadedFrame]
    missing: list[dict[str, object]]


def load_preprocessed_frames(
    config: AnomalyConfig,
    frame_requests: dict[str, set[int]],
) -> FrameBatch:
    """Read resized grayscale test frames requested by video/frame id."""
    if not frame_requests:
        return FrameBatch(frames={}, missing=[])

    sources = {
        source.video_id: source
        for source in scan_dataset(config.raw, config.project_root, split_filter="test")
    }
    loaded: dict[tuple[str, int], LoadedFrame] = {}
    missing: list[dict[str, object]] = []

    for video_id, requested_ids in sorted(frame_requests.items()):
        requested = {int(frame_id) for frame_id in requested_ids}
        if not requested:
            continue

        source = sources.get(video_id)
        if source is None:
            for frame_id in sorted(requested):
                missing.append(
                    {
                        "video_id": video_id,
                        "frame_id": frame_id,
                        "reason": "video_source_not_found",
                    }
                )
            continue

        max_requested = max(requested)
        remaining = set(requested)
        for record in iter_preprocessed_frames(source, config.raw):
            frame_id = int(record.frame_id)
            if frame_id in remaining:
                loaded[(video_id, frame_id)] = LoadedFrame(
                    video_id=video_id,
                    frame_id=frame_id,
                    gray=record.gray,
                    source_path=record.source_path,
                    resized_width=int(record.resized_width),
                    resized_height=int(record.resized_height),
                )
                remaining.remove(frame_id)
                if not remaining:
                    break
            if frame_id > max_requested and source.input_type in {"frame_sequence", "video"}:
                break

        for frame_id in sorted(remaining):
            missing.append(
                {
                    "video_id": video_id,
                    "frame_id": frame_id,
                    "reason": "frame_not_found",
                }
            )

    return FrameBatch(frames=loaded, missing=missing)


def ensure_preprocessed_frame_source(frame_source: str) -> None:
    if frame_source != "preprocessed":
        raise ConfigError(
            "SPEC 4 MVP currently supports visualization.frame_source='preprocessed' only"
        )
</file>

<file path="src/kpdl_anomaly/io.py">
from __future__ import annotations

import csv
import json
from dataclasses import dataclass
from pathlib import Path
from typing import Any

from kpdl_preprocess.config import ConfigError
from kpdl_preprocess.utils import ensure_dir

from .feature_selection import row_to_vector
from .schema import METADATA_COLUMNS


@dataclass
class FeatureLoadResult:
    by_cell: dict[str, list[list[float]]]
    header: list[str]
    rows_read: int
    rows_loaded: int
    invalid_rows: int
    split_mismatches: int
    limit_rows: int | None


def require_files(paths: list[Path]) -> None:
    missing = [str(path) for path in paths if not path.exists()]
    if missing:
        raise ConfigError(f"Required file(s) not found: {', '.join(missing)}")


def read_json(path: str | Path) -> Any:
    with Path(path).open("r", encoding="utf-8") as handle:
        return json.load(handle)


def write_json(path: str | Path, payload: Any) -> None:
    with Path(path).open("w", encoding="utf-8") as handle:
        json.dump(payload, handle, indent=2, ensure_ascii=False, default=_json_default)
        handle.write("\n")


def write_config_yaml(path: str | Path, payload: dict[str, Any]) -> None:
    try:
        import yaml
    except ImportError as exc:
        raise ConfigError("PyYAML is required to write config.yaml artifacts") from exc
    with Path(path).open("w", encoding="utf-8") as handle:
        yaml.safe_dump(payload, handle, sort_keys=False, allow_unicode=True)


def validate_feature_header(path: str | Path, required_columns: list[str]) -> list[str]:
    with Path(path).open("r", newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        header = list(reader.fieldnames or [])
    missing = [column for column in required_columns if column not in header]
    if missing:
        raise ConfigError(f"{path} is missing required column(s): {', '.join(missing)}")
    return header


def load_features_by_cell(
    path: str | Path,
    feature_columns: list[str],
    expected_split: str,
    limit_rows: int | None = None,
) -> FeatureLoadResult:
    required_columns = METADATA_COLUMNS + feature_columns
    header = validate_feature_header(path, required_columns)
    by_cell: dict[str, list[list[float]]] = {}
    rows_read = 0
    rows_loaded = 0
    invalid_rows = 0
    split_mismatches = 0

    with Path(path).open("r", newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        for row in reader:
            if limit_rows is not None and rows_read >= limit_rows:
                break
            rows_read += 1
            if row.get("split") != expected_split:
                split_mismatches += 1
                continue
            vector = row_to_vector(row, feature_columns)
            if vector is None:
                invalid_rows += 1
                continue
            by_cell.setdefault(str(row["cell_id"]), []).append(vector.tolist())
            rows_loaded += 1

    if split_mismatches:
        raise ConfigError(f"{path} contains {split_mismatches} row(s) outside split={expected_split!r}")

    return FeatureLoadResult(
        by_cell=by_cell,
        header=header,
        rows_read=rows_read,
        rows_loaded=rows_loaded,
        invalid_rows=invalid_rows,
        split_mismatches=split_mismatches,
        limit_rows=limit_rows,
    )


def output_dir(path: str | Path) -> Path:
    return ensure_dir(path)


def _json_default(value: Any) -> Any:
    if isinstance(value, Path):
        return str(value)
    if hasattr(value, "item"):
        return value.item()
    raise TypeError(f"Object of type {type(value).__name__} is not JSON serializable")
</file>

<file path="src/kpdl_anomaly/modeling.py">
from __future__ import annotations

from dataclasses import dataclass
from typing import Any

import numpy as np
from sklearn.cluster import MiniBatchKMeans
from sklearn.preprocessing import StandardScaler

from .config import AnomalyConfig
from .thresholds import summarize_distances


@dataclass
class TrainingResult:
    models: dict[str, MiniBatchKMeans]
    scalers: dict[str, StandardScaler]
    thresholds: dict[str, dict[str, Any]]
    feature_stats: dict[str, Any]
    num_models_trained: int
    num_fallback_cells: int


def train_per_cell(
    features_by_cell: dict[str, list[list[float]]],
    cell_ids: list[str],
    config: AnomalyConfig,
) -> TrainingResult:
    models: dict[str, MiniBatchKMeans] = {}
    scalers: dict[str, StandardScaler] = {}
    thresholds: dict[str, dict[str, Any]] = {}
    stats_by_cell: dict[str, dict[str, Any]] = {}
    num_models_trained = 0
    num_fallback_cells = 0

    for cell_id in sorted(cell_ids):
        samples = features_by_cell.get(cell_id, [])
        sample_count = len(samples)
        if sample_count:
            raw_values = np.asarray(samples, dtype=np.float64)
            stats_by_cell[cell_id] = {
                "num_train_samples": sample_count,
                "feature_mean": np.mean(raw_values, axis=0).tolist(),
                "feature_std": np.std(raw_values, axis=0).tolist(),
            }
        else:
            raw_values = np.empty((0, len(config.feature_columns)), dtype=np.float64)
            stats_by_cell[cell_id] = {
                "num_train_samples": 0,
                "feature_mean": [],
                "feature_std": [],
            }

        if sample_count < config.min_samples_per_cell:
            thresholds[cell_id] = _fallback_threshold(cell_id, sample_count, "insufficient_samples")
            num_fallback_cells += 1
            continue

        n_clusters = min(config.clusters_per_cell, sample_count)
        if n_clusters <= 0:
            thresholds[cell_id] = _fallback_threshold(cell_id, sample_count, "insufficient_samples")
            num_fallback_cells += 1
            continue

        scaler = StandardScaler()
        scaled_values = scaler.fit_transform(raw_values)
        model = MiniBatchKMeans(
            n_clusters=n_clusters,
            batch_size=min(config.batch_size, sample_count),
            max_iter=config.max_iter,
            random_state=config.random_state,
            n_init=10,
        )
        model.fit(scaled_values)
        distances_by_cluster = model.transform(scaled_values)
        nearest_clusters = np.argmin(distances_by_cluster, axis=1)
        nearest_distances = distances_by_cluster[np.arange(sample_count), nearest_clusters]
        cluster_sizes = np.bincount(nearest_clusters, minlength=n_clusters).astype(int).tolist()

        threshold_stats = summarize_distances(
            nearest_distances,
            percentile=config.threshold_percentile,
            threshold_floor=config.threshold_floor,
        )
        threshold_stats.update(
            {
                "cell_id": cell_id,
                "model_status": "trained",
                "num_train_samples": sample_count,
                "num_clusters": n_clusters,
                "cluster_sizes": cluster_sizes,
            }
        )
        thresholds[cell_id] = threshold_stats
        scalers[cell_id] = scaler
        models[cell_id] = model
        num_models_trained += 1

    return TrainingResult(
        models=models,
        scalers=scalers,
        thresholds=thresholds,
        feature_stats={
            "feature_columns": config.feature_columns,
            "cells": stats_by_cell,
        },
        num_models_trained=num_models_trained,
        num_fallback_cells=num_fallback_cells,
    )


def _fallback_threshold(cell_id: str, sample_count: int, status: str) -> dict[str, Any]:
    return {
        "cell_id": cell_id,
        "model_status": status,
        "num_train_samples": sample_count,
        "num_clusters": 0,
        "cluster_sizes": [],
        "distance_mean": 0.0,
        "distance_std": 0.0,
        "distance_p95": 0.0,
        "distance_p99": 0.0,
        "threshold": 0.0,
    }
</file>

<file path="src/kpdl_anomaly/qualitative.py">
from __future__ import annotations

from pathlib import Path
from typing import Any

from kpdl_preprocess.utils import ensure_dir


def write_qualitative_report(
    path: str | Path,
    dataset: str,
    index_records: list[dict[str, Any]],
    alert_records: list[dict[str, Any]],
    video_records: list[dict[str, Any]],
    stats: dict[str, Any],
) -> None:
    report_path = Path(path)
    ensure_dir(report_path.parent)
    lines: list[str] = []

    lines.append(f"# Qualitative Visualization Report - {dataset}")
    lines.append("")
    lines.append("Generated by SPEC 4 visualization.")
    lines.append("")
    lines.append("## Summary")
    lines.append("")
    lines.append(f"- Dataset: `{dataset}`")
    lines.append(f"- Output dir: `{stats.get('output_dir', '')}`")
    lines.append(f"- Images written: `{stats.get('num_images_written', 0)}`")
    lines.append(f"- Videos written: `{stats.get('num_videos_written', 0)}`")
    lines.append(f"- Missing frames: `{len(stats.get('missing_frames', []))}`")
    lines.append(f"- Missing cell score frames: `{len(stats.get('missing_cell_score_frames', []))}`")
    lines.append("")

    _append_top_frames(lines, index_records)
    _append_alerts(lines, index_records, alert_records)
    _append_videos(lines, video_records)

    lines.append("## Manual Review Notes")
    lines.append("")
    lines.append("Use `true_positive`, `false_positive`, or `uncertain` in the manual label column.")
    lines.append("")
    report_path.write_text("\n".join(lines), encoding="utf-8")


def _append_top_frames(lines: list[str], index_records: list[dict[str, Any]]) -> None:
    rows = [record for record in index_records if record.get("selection_type") == "top_frame"]
    rows.sort(key=lambda item: int(item.get("rank", 0)))
    lines.append("## Top Anomaly Frames")
    lines.append("")
    lines.append("| Rank | Video | Frame | Score | Severity | Top cells | Image | Manual label | Notes |")
    lines.append("| ---: | --- | ---: | ---: | --- | --- | --- | --- | --- |")
    if not rows:
        lines.append("| - | - | - | - | - | - | - | TBD | TBD |")
    for record in rows:
        lines.append(
            "| {rank} | {video_id} | {frame_id} | {score:.3f} | {severity} | {cells} | {image} | TBD | TBD |".format(
                rank=record.get("rank", ""),
                video_id=record.get("video_id", ""),
                frame_id=record.get("frame_id", ""),
                score=float(record.get("smoothed_frame_score", 0.0)),
                severity=record.get("severity", ""),
                cells=", ".join(record.get("top_cells", [])),
                image=_markdown_link(record),
            )
        )
    lines.append("")


def _append_alerts(
    lines: list[str],
    index_records: list[dict[str, Any]],
    alert_records: list[dict[str, Any]],
) -> None:
    image_by_alert = {
        str(record.get("alert_id")): record
        for record in index_records
        if record.get("selection_type") == "alert_peak"
    }
    lines.append("## Alert Segments")
    lines.append("")
    lines.append("| Alert | Video | Range | Peak frame | Score | Severity | Top cells | Reasons | Image | Manual label | Notes |")
    lines.append("| --- | --- | --- | ---: | ---: | --- | --- | --- | --- | --- | --- |")
    if not alert_records:
        lines.append("| - | - | - | - | - | - | - | - | - | TBD | TBD |")
    for alert in alert_records:
        alert_id = str(alert.get("alert_id", ""))
        image_record = image_by_alert.get(alert_id, {})
        lines.append(
            "| {alert_id} | {video_id} | {start}-{end} | {peak} | {score:.3f} | {severity} | {cells} | {reasons} | {image} | TBD | TBD |".format(
                alert_id=alert_id,
                video_id=alert.get("video_id", ""),
                start=alert.get("start_frame_id", ""),
                end=alert.get("end_frame_id", ""),
                peak=image_record.get("frame_id", ""),
                score=float(alert.get("max_score", 0.0)),
                severity=alert.get("severity", ""),
                cells=", ".join(alert.get("top_cells", [])),
                reasons="<br>".join(alert.get("reasons", [])),
                image=_markdown_link(image_record) if image_record else "-",
            )
        )
    lines.append("")


def _append_videos(lines: list[str], video_records: list[dict[str, Any]]) -> None:
    lines.append("## Overlay Videos")
    lines.append("")
    lines.append("| Video | Frames written | Path |")
    lines.append("| --- | ---: | --- |")
    if not video_records:
        lines.append("| - | - | - |")
    for record in video_records:
        path = str(record.get("relative_path") or record.get("video_path") or "")
        lines.append(f"| {record.get('video_id', '')} | {record.get('frames_written', 0)} | `{path}` |")
    lines.append("")


def _markdown_link(record: dict[str, Any]) -> str:
    path = str(record.get("relative_path") or record.get("image_path") or "")
    if not path:
        return "-"
    label = Path(path).name
    return f"[{label}]({path.replace(' ', '%20')})"
</file>

<file path="src/kpdl_anomaly/schema.py">
from __future__ import annotations

SCHEMA_VERSION = "spec_3.anomaly.v1"
RULE_SCHEMA_VERSION = "spec_5.rules.v1"

METADATA_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "cube_id",
    "start_frame_id",
    "end_frame_id",
    "center_frame_id",
    "cell_id",
    "cell_row",
    "cell_col",
]

DEFAULT_FEATURE_COLUMNS = [
    "foreground_ratio",
    "motion_magnitude_mean",
    "motion_magnitude_std",
    "motion_density",
    "brightness_mean",
    "brightness_delta",
]

CELL_SCORE_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "cube_id",
    "start_frame_id",
    "end_frame_id",
    "center_frame_id",
    "cell_id",
    "cell_row",
    "cell_col",
    "nearest_cluster",
    "cluster_distance",
    "cluster_threshold",
    "cluster_distance_score",
    "temporal_change_score",
    "cell_score",
]

RULE_SCORE_COLUMNS = [
    "tokens",
    "rare_token_score",
    "rare_itemset",
    "rare_itemset_support",
    "rule_violation_score",
    "violated_rules",
    "token_rule_reasons",
]

FRAME_SCORE_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "frame_id",
    "frame_score",
    "smoothed_frame_score",
    "severity",
    "top_cells",
]
</file>

<file path="src/kpdl_anomaly/scoring.py">
from __future__ import annotations

import csv
import json
from collections import defaultdict
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import joblib
import numpy as np

from kpdl_preprocess.config import ConfigError

from .alerts import assign_severities, build_alerts
from .config import AnomalyConfig
from .feature_selection import row_to_vector
from .io import output_dir, read_json, require_files, validate_feature_header, write_json
from .rule_scoring import RuleScore, load_rule_scorer
from .schema import CELL_SCORE_COLUMNS, FRAME_SCORE_COLUMNS, METADATA_COLUMNS, RULE_SCORE_COLUMNS, SCHEMA_VERSION
from .smoothing import moving_average
from .thresholds import distance_to_score


def score_features(
    config: AnomalyConfig,
    model_dir: str | Path,
    result_dir: str | Path | None = None,
    limit_rows: int | None = None,
    rules_dir: str | Path | None = None,
    use_rules: bool | None = None,
) -> dict[str, Any]:
    model_dir = Path(model_dir)
    result_path = output_dir(result_dir or config.result_dir)
    require_files(
        [
            config.test_feature_path,
            model_dir / "model_manifest.json",
            model_dir / "cell_models.joblib",
            model_dir / "cell_scalers.joblib",
            model_dir / "thresholds.json",
        ]
    )
    required_columns = METADATA_COLUMNS + config.feature_columns
    validate_feature_header(config.test_feature_path, required_columns)

    manifest = read_json(model_dir / "model_manifest.json")
    _validate_manifest(config, manifest)
    models = joblib.load(model_dir / "cell_models.joblib")
    scalers = joblib.load(model_dir / "cell_scalers.joblib")
    thresholds_payload = read_json(model_dir / "thresholds.json")
    thresholds = dict(thresholds_payload.get("cells", {}))
    requested_rules = config.rules.enabled if use_rules is None else bool(use_rules)
    rule_load = load_rule_scorer(config, rules_dir, requested=requested_rules)
    rule_scorer = rule_load.scorer
    rules_active = rule_scorer is not None
    score_weights = _score_weights(config, rules_active)

    cell_scores_path = result_path / "cell_scores.csv"
    cell_score_columns = CELL_SCORE_COLUMNS + (RULE_SCORE_COLUMNS if rules_active else [])
    frame_groups: dict[tuple[str, str, str, int], list[dict[str, Any]]] = {}
    previous_cluster_scores: dict[tuple[str, str], float] = {}
    rare_scores: list[float] = []
    rule_violation_scores: list[float] = []
    token_reason_rows = 0
    rows_read = 0
    rows_scored = 0
    invalid_rows = 0
    split_mismatches = 0
    fallback_rows = 0

    with config.test_feature_path.open("r", newline="", encoding="utf-8") as input_handle, cell_scores_path.open(
        "w", newline="", encoding="utf-8"
    ) as output_handle:
        reader = csv.DictReader(input_handle)
        writer = csv.DictWriter(output_handle, fieldnames=cell_score_columns)
        writer.writeheader()

        for row in reader:
            if limit_rows is not None and rows_read >= limit_rows:
                break
            rows_read += 1
            if row.get("split") != "test":
                split_mismatches += 1
                continue
            vector = row_to_vector(row, config.feature_columns)
            if vector is None:
                invalid_rows += 1
                continue

            cell_id = str(row["cell_id"])
            score = _score_row(cell_id, vector, models, scalers, thresholds)
            if not score["trained"]:
                fallback_rows += 1

            temporal_key = (str(row["video_id"]), cell_id)
            previous = previous_cluster_scores.get(temporal_key)
            temporal_change_score = 0.0 if previous is None else abs(score["cluster_distance_score"] - previous)
            temporal_change_score = float(np.clip(temporal_change_score, 0.0, 1.0))
            previous_cluster_scores[temporal_key] = score["cluster_distance_score"]

            rule_score = _empty_rule_score()
            if rule_scorer is not None:
                rule_score = rule_scorer.score(row, score["nearest_cluster"])
                rare_scores.append(rule_score.rare_token_score)
                rule_violation_scores.append(rule_score.rule_violation_score)
                if rule_score.reasons:
                    token_reason_rows += 1

            cell_score = (
                score_weights["cluster"] * score["cluster_distance_score"]
                + score_weights["temporal"] * temporal_change_score
                + score_weights["rare"] * rule_score.rare_token_score
                + score_weights["rule"] * rule_score.rule_violation_score
            )
            cell_score = float(np.clip(cell_score, 0.0, 1.0))

            output_row = {
                "dataset": row["dataset"],
                "split": row["split"],
                "video_id": row["video_id"],
                "cube_id": row["cube_id"],
                "start_frame_id": row["start_frame_id"],
                "end_frame_id": row["end_frame_id"],
                "center_frame_id": row["center_frame_id"],
                "cell_id": cell_id,
                "cell_row": row["cell_row"],
                "cell_col": row["cell_col"],
                "nearest_cluster": score["nearest_cluster"],
                "cluster_distance": _fmt(score["cluster_distance"]),
                "cluster_threshold": _fmt(score["cluster_threshold"]),
                "cluster_distance_score": _fmt(score["cluster_distance_score"]),
                "temporal_change_score": _fmt(temporal_change_score),
                "cell_score": _fmt(cell_score),
            }
            if rules_active:
                output_row.update(_rule_output_columns(rule_score))
            writer.writerow(output_row)

            frame_id = int(row["center_frame_id"])
            frame_key = (str(row["dataset"]), str(row["split"]), str(row["video_id"]), frame_id)
            _add_top_cell(
                frame_groups,
                frame_key,
                {
                    "cell_id": cell_id,
                    "cell_score": cell_score,
                    "cluster_distance": score["cluster_distance"],
                    "cluster_threshold": score["cluster_threshold"],
                    "nearest_cluster": score["nearest_cluster"],
                    "rare_token_score": rule_score.rare_token_score,
                    "rule_violation_score": rule_score.rule_violation_score,
                    "token_rule_reasons": rule_score.reasons,
                },
                config.top_k_cells,
            )
            rows_scored += 1

    if split_mismatches:
        raise ConfigError(f"{config.test_feature_path} contains {split_mismatches} row(s) outside split='test'")

    frame_records = _frame_records(frame_groups)
    records_by_video = _smooth_and_label(config, frame_records)
    alerts = build_alerts(config.dataset, records_by_video, config.min_consecutive_alerts)

    frame_scores_path = result_path / "frame_scores.csv"
    with frame_scores_path.open("w", newline="", encoding="utf-8") as handle:
        writer = csv.DictWriter(handle, fieldnames=FRAME_SCORE_COLUMNS)
        writer.writeheader()
        for record in frame_records:
            writer.writerow(
                {
                    "dataset": record["dataset"],
                    "split": record["split"],
                    "video_id": record["video_id"],
                    "frame_id": record["frame_id"],
                    "frame_score": _fmt(record["frame_score"]),
                    "smoothed_frame_score": _fmt(record["smoothed_frame_score"]),
                    "severity": record["severity"],
                    "top_cells": json.dumps(record["top_cells"], ensure_ascii=False),
                }
            )

    stats = _stats(
        config=config,
        model_dir=model_dir,
        result_path=result_path,
        rows_read=rows_read,
        rows_scored=rows_scored,
        invalid_rows=invalid_rows,
        fallback_rows=fallback_rows,
        frame_records=frame_records,
        alerts=alerts,
        limit_rows=limit_rows,
        rule_dir=rule_load.rule_dir,
        rule_warnings=rule_load.warnings,
        rules_requested=requested_rules,
        rules_active=rules_active,
        score_weights=score_weights,
        rare_scores=rare_scores,
        rule_violation_scores=rule_violation_scores,
        token_reason_rows=token_reason_rows,
    )
    write_json(result_path / "alerts.json", alerts)
    write_json(result_path / "scoring_stats.json", stats)
    return stats


def _validate_manifest(config: AnomalyConfig, manifest: dict[str, Any]) -> None:
    if manifest.get("dataset") != config.dataset:
        raise ConfigError(f"model dataset={manifest.get('dataset')!r} does not match config dataset={config.dataset!r}")
    if list(manifest.get("feature_columns", [])) != config.feature_columns:
        raise ConfigError("model feature_columns do not match config scoring.feature_columns")


def _score_weights(config: AnomalyConfig, rules_active: bool) -> dict[str, float]:
    if rules_active:
        return {
            "cluster": config.cluster_weight,
            "temporal": config.temporal_weight,
            "rare": config.rare_token_weight,
            "rule": config.rule_weight,
        }
    if config.rare_token_weight > 0.0 or config.rule_weight > 0.0:
        return {"cluster": 0.80, "temporal": 0.20, "rare": 0.0, "rule": 0.0}
    return {"cluster": config.cluster_weight, "temporal": config.temporal_weight, "rare": 0.0, "rule": 0.0}


def _empty_rule_score() -> RuleScore:
    return RuleScore(
        tokens=[],
        rare_token_score=0.0,
        rare_itemset=[],
        rare_itemset_support=0.0,
        rule_violation_score=0.0,
        violated_rules=[],
        reasons=[],
    )


def _rule_output_columns(rule_score: RuleScore) -> dict[str, str]:
    return {
        "tokens": json.dumps(rule_score.tokens, ensure_ascii=False),
        "rare_token_score": _fmt(rule_score.rare_token_score),
        "rare_itemset": json.dumps(rule_score.rare_itemset, ensure_ascii=False),
        "rare_itemset_support": _fmt(rule_score.rare_itemset_support),
        "rule_violation_score": _fmt(rule_score.rule_violation_score),
        "violated_rules": json.dumps(rule_score.violated_rules, ensure_ascii=False),
        "token_rule_reasons": json.dumps(rule_score.reasons, ensure_ascii=False),
    }


def _score_row(
    cell_id: str,
    vector: np.ndarray,
    models: dict[str, Any],
    scalers: dict[str, Any],
    thresholds: dict[str, dict[str, Any]],
) -> dict[str, Any]:
    model = models.get(cell_id)
    scaler = scalers.get(cell_id)
    threshold_info = thresholds.get(cell_id, {})
    threshold = float(threshold_info.get("threshold", 0.0))
    if model is None or scaler is None:
        return {
            "trained": False,
            "nearest_cluster": -1,
            "cluster_distance": 0.0,
            "cluster_threshold": threshold,
            "cluster_distance_score": 0.0,
        }

    scaled = scaler.transform(vector.reshape(1, -1))
    distances = model.transform(scaled)[0]
    nearest_cluster = int(np.argmin(distances))
    distance = float(distances[nearest_cluster])
    return {
        "trained": True,
        "nearest_cluster": nearest_cluster,
        "cluster_distance": distance,
        "cluster_threshold": threshold,
        "cluster_distance_score": distance_to_score(distance, threshold),
    }


def _add_top_cell(
    frame_groups: dict[tuple[str, str, str, int], list[dict[str, Any]]],
    frame_key: tuple[str, str, str, int],
    entry: dict[str, Any],
    top_k: int,
) -> None:
    top_entries = frame_groups.setdefault(frame_key, [])
    top_entries.append(entry)
    top_entries.sort(key=lambda item: float(item["cell_score"]), reverse=True)
    if len(top_entries) > top_k:
        top_entries.pop()


def _frame_records(frame_groups: dict[tuple[str, str, str, int], list[dict[str, Any]]]) -> list[dict[str, Any]]:
    records: list[dict[str, Any]] = []
    for (dataset, split, video_id, frame_id), top_entries in sorted(frame_groups.items()):
        frame_score = float(np.mean([entry["cell_score"] for entry in top_entries])) if top_entries else 0.0
        records.append(
            {
                "dataset": dataset,
                "split": split,
                "video_id": video_id,
                "frame_id": frame_id,
                "frame_score": frame_score,
                "smoothed_frame_score": frame_score,
                "severity": "none",
                "top_entries": top_entries,
                "top_cells": [entry["cell_id"] for entry in top_entries],
            }
        )
    return records


def _smooth_and_label(
    config: AnomalyConfig,
    frame_records: list[dict[str, Any]],
) -> dict[str, list[dict[str, Any]]]:
    records_by_video: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for record in frame_records:
        records_by_video[str(record["video_id"])].append(record)

    for records in records_by_video.values():
        records.sort(key=lambda item: int(item["frame_id"]))
        smoothed = moving_average([float(record["frame_score"]) for record in records], config.smoothing_window)
        for record, value in zip(records, smoothed, strict=True):
            record["smoothed_frame_score"] = float(np.clip(value, 0.0, 1.0))
        assign_severities(
            records,
            medium_threshold=config.alert_threshold_medium,
            high_threshold=config.alert_threshold_high,
            min_consecutive=config.min_consecutive_alerts,
        )
    frame_records.sort(key=lambda item: (str(item["video_id"]), int(item["frame_id"])))
    return dict(records_by_video)


def _stats(
    config: AnomalyConfig,
    model_dir: Path,
    result_path: Path,
    rows_read: int,
    rows_scored: int,
    invalid_rows: int,
    fallback_rows: int,
    frame_records: list[dict[str, Any]],
    alerts: list[dict[str, Any]],
    limit_rows: int | None,
    rule_dir: Path | None,
    rule_warnings: list[str],
    rules_requested: bool,
    rules_active: bool,
    score_weights: dict[str, float],
    rare_scores: list[float],
    rule_violation_scores: list[float],
    token_reason_rows: int,
) -> dict[str, Any]:
    frame_scores = [float(record["frame_score"]) for record in frame_records]
    smoothed_scores = [float(record["smoothed_frame_score"]) for record in frame_records]
    severity_counts = {
        "none": sum(1 for record in frame_records if record["severity"] == "none"),
        "medium": sum(1 for record in frame_records if record["severity"] == "medium"),
        "high": sum(1 for record in frame_records if record["severity"] == "high"),
    }
    return {
        "schema_version": SCHEMA_VERSION,
        "dataset": config.dataset,
        "split": "test",
        "scored_at": datetime.now(timezone.utc).isoformat(),
        "test_feature_path": str(config.test_feature_path),
        "model_dir": str(model_dir),
        "result_dir": str(result_path),
        "rows_read": rows_read,
        "rows_scored": rows_scored,
        "invalid_rows": invalid_rows,
        "fallback_rows": fallback_rows,
        "limit_rows": limit_rows,
        "num_frames": len(frame_records),
        "severity_counts": severity_counts,
        "num_alerts": len(alerts),
        "frame_score": _distribution(frame_scores),
        "smoothed_frame_score": _distribution(smoothed_scores),
        "rules": {
            "requested": rules_requested,
            "active": rules_active,
            "rule_dir": str(rule_dir) if rule_dir is not None else None,
            "warnings": rule_warnings,
            "score_weights": score_weights,
            "token_reason_rows": token_reason_rows,
            "rare_token_score": _distribution(rare_scores),
            "rule_violation_score": _distribution(rule_violation_scores),
        },
    }


def _distribution(values: list[float]) -> dict[str, float]:
    if not values:
        return {"min": 0.0, "mean": 0.0, "max": 0.0}
    arr = np.asarray(values, dtype=np.float64)
    return {"min": float(np.min(arr)), "mean": float(np.mean(arr)), "max": float(np.max(arr))}


def _fmt(value: float) -> str:
    return f"{float(value):.8f}"
</file>

<file path="src/kpdl_anomaly/smoothing.py">
from __future__ import annotations

from collections import deque


def moving_average(values: list[float], window: int) -> list[float]:
    if window <= 1:
        return list(values)

    smoothed: list[float] = []
    active: deque[float] = deque(maxlen=window)
    total = 0.0
    for value in values:
        if len(active) == active.maxlen:
            total -= active[0]
        active.append(value)
        total += value
        smoothed.append(total / len(active))
    return smoothed
</file>

<file path="src/kpdl_anomaly/test.py">
from __future__ import annotations

import argparse
import json
from pathlib import Path

from kpdl_preprocess.config import resolve_path

from .config import load_anomaly_config
from .scoring import score_features


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Score SPEC 3/5 anomaly models on test features.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative paths are resolved here.")
    parser.add_argument("--model", default=None, help="Model directory. Defaults to output.model_root/dataset.")
    parser.add_argument("--result-root", default=None, help="Override result root.")
    parser.add_argument("--limit-rows", type=int, default=None, help="Limit test feature rows for smoke tests.")
    parser.add_argument("--rules", default=None, help="Rule artifact directory. Enables token/rule scoring.")
    parser.add_argument("--no-rules", action="store_true", help="Disable token/rule scoring even if config enables it.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    summary = run_test(
        config_path=args.config,
        project_root=args.project_root,
        model_dir=args.model,
        result_root=args.result_root,
        limit_rows=args.limit_rows,
        rules_dir=args.rules,
        use_rules=False if args.no_rules else (True if args.rules is not None else None),
    )
    print(json.dumps(_public_summary(summary), indent=2))
    return 0


def run_test(
    config_path: str | Path,
    project_root: str | Path = ".",
    model_dir: str | Path | None = None,
    result_root: str | Path | None = None,
    limit_rows: int | None = None,
    rules_dir: str | Path | None = None,
    use_rules: bool | None = None,
) -> dict:
    config = load_anomaly_config(
        config_path=config_path,
        project_root=project_root,
        result_root=result_root,
    )
    resolved_model_dir = resolve_path(model_dir, config.project_root) if model_dir is not None else config.model_dir
    resolved_rules_dir = resolve_path(rules_dir, config.project_root) if rules_dir is not None else None
    return score_features(
        config,
        model_dir=resolved_model_dir,
        result_dir=config.result_dir,
        limit_rows=limit_rows,
        rules_dir=resolved_rules_dir,
        use_rules=use_rules,
    )


def _public_summary(summary: dict) -> dict:
    return {
        "dataset": summary["dataset"],
        "result_dir": summary["result_dir"],
        "rows_scored": summary["rows_scored"],
        "num_frames": summary["num_frames"],
        "severity_counts": summary["severity_counts"],
        "num_alerts": summary["num_alerts"],
        "rules": {
            "requested": summary.get("rules", {}).get("requested"),
            "active": summary.get("rules", {}).get("active"),
            "rule_dir": summary.get("rules", {}).get("rule_dir"),
        },
    }
</file>

<file path="src/kpdl_anomaly/thresholds.py">
from __future__ import annotations

import numpy as np


def summarize_distances(distances: np.ndarray, percentile: float, threshold_floor: float) -> dict[str, float]:
    if distances.size == 0:
        return {
            "distance_mean": 0.0,
            "distance_std": 0.0,
            "distance_p95": 0.0,
            "distance_p99": 0.0,
            "threshold": 0.0,
        }

    threshold = float(np.percentile(distances, percentile))
    if threshold_floor > 0.0:
        threshold = max(threshold, threshold_floor)

    return {
        "distance_mean": float(np.mean(distances)),
        "distance_std": float(np.std(distances)),
        "distance_p95": float(np.percentile(distances, 95.0)),
        "distance_p99": float(np.percentile(distances, 99.0)),
        "threshold": threshold,
    }


def distance_to_score(distance: float, threshold: float) -> float:
    if threshold <= 0.0:
        return 0.0 if abs(distance) <= 1.0e-12 else 1.0
    return float(min(distance / threshold, 2.0) / 2.0)
</file>

<file path="src/kpdl_anomaly/train.py">
from __future__ import annotations

import argparse
import json
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import joblib

from .config import load_anomaly_config
from .io import load_features_by_cell, output_dir, read_json, require_files, write_config_yaml, write_json
from .modeling import train_per_cell
from .schema import SCHEMA_VERSION


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Train SPEC 3 per-cell anomaly models.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative paths are resolved here.")
    parser.add_argument("--model-root", default=None, help="Override output model root.")
    parser.add_argument("--clusters-per-cell", type=int, default=None, help="Override model.clusters_per_cell.")
    parser.add_argument("--threshold-percentile", type=float, default=None, help="Override model threshold percentile.")
    parser.add_argument("--limit-rows", type=int, default=None, help="Limit train feature rows for smoke tests.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    summary = run_train(
        config_path=args.config,
        project_root=args.project_root,
        model_root=args.model_root,
        clusters_per_cell=args.clusters_per_cell,
        threshold_percentile=args.threshold_percentile,
        limit_rows=args.limit_rows,
    )
    print(json.dumps(_public_summary(summary), indent=2))
    return 0


def run_train(
    config_path: str | Path,
    project_root: str | Path = ".",
    model_root: str | Path | None = None,
    clusters_per_cell: int | None = None,
    threshold_percentile: float | None = None,
    limit_rows: int | None = None,
) -> dict[str, Any]:
    config = load_anomaly_config(
        config_path=config_path,
        project_root=project_root,
        model_root=model_root,
        clusters_per_cell=clusters_per_cell,
        threshold_percentile=threshold_percentile,
    )
    require_files([config.train_feature_path, config.grid_path, config.preprocess_stats_path])

    grid = read_json(config.grid_path)
    preprocess_stats = read_json(config.preprocess_stats_path)
    cell_ids = [str(cell["cell_id"]) for cell in grid.get("cells", [])]
    loaded = load_features_by_cell(
        config.train_feature_path,
        feature_columns=config.feature_columns,
        expected_split="train",
        limit_rows=limit_rows,
    )
    training = train_per_cell(loaded.by_cell, cell_ids, config)

    model_dir = output_dir(config.model_dir)
    write_config_yaml(model_dir / "config.yaml", config.raw)
    joblib.dump(training.models, model_dir / "cell_models.joblib")
    joblib.dump(training.scalers, model_dir / "cell_scalers.joblib")

    expected_rows = _expected_rows(preprocess_stats, "train")
    warnings: list[str] = []
    if limit_rows is not None:
        warnings.append(f"limit_rows={limit_rows} was applied; artifact is for smoke/debug use")
    elif expected_rows is not None and expected_rows != loaded.rows_read:
        warnings.append(f"train row count mismatch: expected {expected_rows}, read {loaded.rows_read}")
    if loaded.invalid_rows:
        warnings.append(f"skipped {loaded.invalid_rows} row(s) with invalid numeric feature values")

    manifest = {
        "schema_version": SCHEMA_VERSION,
        "dataset": config.dataset,
        "trained_at": datetime.now(timezone.utc).isoformat(),
        "config_path": str(config.config_path),
        "feature_columns": config.feature_columns,
        "num_cells": int(grid.get("num_cells", len(cell_ids))),
        "num_models_trained": training.num_models_trained,
        "num_fallback_cells": training.num_fallback_cells,
        "clusters_per_cell": config.clusters_per_cell,
        "threshold_percentile": config.threshold_percentile,
        "threshold_floor": config.threshold_floor,
        "train_feature_path": str(config.train_feature_path),
        "grid_path": str(config.grid_path),
        "preprocess_schema_version": preprocess_stats.get("schema", {}).get("version"),
        "grid": {
            "rows": grid.get("rows"),
            "cols": grid.get("cols"),
            "num_cells": grid.get("num_cells"),
            "resized_width": grid.get("resized_width"),
            "resized_height": grid.get("resized_height"),
        },
        "train_rows_read": loaded.rows_read,
        "train_rows_loaded": loaded.rows_loaded,
        "invalid_rows": loaded.invalid_rows,
        "limit_rows": limit_rows,
        "warnings": warnings,
    }
    thresholds_payload = {
        "schema_version": SCHEMA_VERSION,
        "threshold_percentile": config.threshold_percentile,
        "cells": training.thresholds,
    }
    feature_stats_payload = dict(training.feature_stats)
    feature_stats_payload.update(
        {
            "schema_version": SCHEMA_VERSION,
            "generated_at": manifest["trained_at"],
            "train_rows_loaded": loaded.rows_loaded,
        }
    )

    write_json(model_dir / "model_manifest.json", manifest)
    write_json(model_dir / "thresholds.json", thresholds_payload)
    write_json(model_dir / "feature_stats.json", feature_stats_payload)

    return {
        "dataset": config.dataset,
        "model_dir": str(model_dir),
        "manifest": manifest,
        "thresholds_path": str(model_dir / "thresholds.json"),
        "feature_stats_path": str(model_dir / "feature_stats.json"),
    }


def _expected_rows(preprocess_stats: dict[str, Any], split: str) -> int | None:
    try:
        return int(preprocess_stats["splits"][split]["num_feature_rows"])
    except (KeyError, TypeError, ValueError):
        return None


def _public_summary(summary: dict[str, Any]) -> dict[str, Any]:
    manifest = summary["manifest"]
    return {
        "dataset": summary["dataset"],
        "model_dir": summary["model_dir"],
        "num_cells": manifest["num_cells"],
        "num_models_trained": manifest["num_models_trained"],
        "num_fallback_cells": manifest["num_fallback_cells"],
        "train_rows_read": manifest["train_rows_read"],
        "train_rows_loaded": manifest["train_rows_loaded"],
        "warnings": manifest["warnings"],
    }
</file>

<file path="src/kpdl_anomaly/visualization.py">
from __future__ import annotations

import csv
import json
from dataclasses import dataclass, field
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import numpy as np

from kpdl_preprocess.config import ConfigError, resolve_path
from kpdl_preprocess.utils import ensure_dir

from .config import AnomalyConfig, load_anomaly_config
from .frames import ensure_preprocessed_frame_source, load_preprocessed_frames
from .io import read_json, require_files, write_json
from .qualitative import write_qualitative_report


@dataclass(frozen=True)
class GridCell:
    cell_id: str
    row: int
    col: int
    x1: int
    y1: int
    x2: int
    y2: int


@dataclass(frozen=True)
class GridSpec:
    resized_width: int
    resized_height: int
    rows: int
    cols: int
    cells: dict[str, GridCell]


@dataclass(frozen=True)
class FrameScore:
    dataset: str
    video_id: str
    frame_id: int
    frame_score: float
    smoothed_frame_score: float
    severity: str
    top_cells: list[str]


@dataclass
class FrameSelection:
    dataset: str
    video_id: str
    frame_id: int
    frame_score: float
    smoothed_frame_score: float
    severity: str
    top_cells: list[str]
    selection_type: str
    rank: int = 0
    alert_id: str | None = None
    alert_start_frame_id: int | None = None
    alert_end_frame_id: int | None = None
    reasons: list[str] = field(default_factory=list)


@dataclass(frozen=True)
class VisualizationSettings:
    output_dir: Path
    colormap: str
    alpha: float
    heatmap_normalization: str
    draw_grid: bool
    draw_top_cells: bool
    top_cells_per_frame: int
    top_frames: int
    min_score: float
    frame_source: str
    image_format: str
    video_fps: float


def run_visualization(
    config_path: str | Path,
    project_root: str | Path = ".",
    result_dir: str | Path | None = None,
    output_dir: str | Path | None = None,
    top_frames: int | None = None,
    include_alerts: bool = False,
    video_id: str | None = None,
    start_frame: int | None = None,
    end_frame: int | None = None,
    write_video: bool = False,
    alpha: float | None = None,
    colormap: str | None = None,
    min_score: float | None = None,
    limit_frames: int | None = None,
) -> dict[str, Any]:
    config = load_anomaly_config(config_path=config_path, project_root=project_root)
    settings = _settings(config, output_dir, alpha, colormap, min_score)
    ensure_preprocessed_frame_source(settings.frame_source)
    output_path = ensure_dir(settings.output_dir)

    result_path = resolve_path(result_dir, config.project_root) if result_dir is not None else config.result_dir
    frame_scores_path = result_path / "frame_scores.csv"
    cell_scores_path = result_path / "cell_scores.csv"
    alerts_path = result_path / "alerts.json"
    require_files([config.grid_path, frame_scores_path, cell_scores_path])

    grid = load_grid(config.grid_path)
    frame_scores = load_frame_scores(frame_scores_path)
    alerts = load_alerts(alerts_path) if alerts_path.exists() else []

    top_count = _top_count(settings, top_frames, include_alerts, video_id)
    selections: list[FrameSelection] = []
    selected_alerts: list[dict[str, Any]] = []

    if top_count > 0:
        selections.extend(
            select_top_frames(
                frame_scores=frame_scores,
                count=top_count,
                min_score=settings.min_score,
                video_id=video_id if start_frame is None and end_frame is None else None,
            )
        )

    if include_alerts:
        alert_selections, selected_alerts = select_alert_peaks(
            frame_scores=frame_scores,
            alerts=alerts,
            limit=limit_frames,
        )
        selections.extend(alert_selections)

    if video_id is not None and not write_video and top_frames is None and not include_alerts:
        selections.extend(
            select_video_range(
                frame_scores=frame_scores,
                video_id=video_id,
                start_frame=start_frame,
                end_frame=end_frame,
                limit=limit_frames,
            )
        )

    image_keys = {(selection.video_id, selection.frame_id) for selection in selections}
    video_selections: list[FrameSelection] = []
    if write_video:
        video_selections = select_video_range(
            frame_scores=frame_scores,
            video_id=video_id or _default_video_id(frame_scores, alerts),
            start_frame=start_frame,
            end_frame=end_frame,
            limit=limit_frames,
            selection_type="video_frame",
        )
    video_keys = {(selection.video_id, selection.frame_id) for selection in video_selections}
    requested_keys = image_keys | video_keys

    cell_scores, global_max, missing_cell_score_frames = load_selected_cell_scores(
        cell_scores_path,
        requested_keys,
    )
    frame_batch = load_preprocessed_frames(config, _requests_by_video(requested_keys))

    index_records: list[dict[str, Any]] = []
    images_written = 0
    for selection in selections:
        loaded_frame = frame_batch.frames.get((selection.video_id, selection.frame_id))
        if loaded_frame is None:
            continue
        score_rows = cell_scores.get((selection.video_id, selection.frame_id), [])
        category = "alerts" if selection.selection_type == "alert_peak" else "top_frames"
        image_path = _image_path(output_path, category, selection, settings.image_format)
        overlay = render_overlay(
            frame_gray=loaded_frame.gray,
            score_rows=score_rows,
            grid=grid,
            top_cells=selection.top_cells[: settings.top_cells_per_frame],
            settings=settings,
            global_max=global_max,
        )
        _write_image(image_path, overlay)
        images_written += 1
        index_records.append(_index_record(config, output_path, image_path, selection))

    video_records: list[dict[str, Any]] = []
    if write_video and video_selections:
        video_record = _write_video_overlay(
            config=config,
            output_dir=output_path,
            frame_batch=frame_batch,
            cell_scores=cell_scores,
            grid=grid,
            selections=video_selections,
            settings=settings,
            global_max=global_max,
        )
        if video_record is not None:
            video_records.append(video_record)

    stats = {
        "dataset": config.dataset,
        "config_path": str(config.config_path),
        "result_dir": _display_path(result_path, config.project_root),
        "grid_path": _display_path(config.grid_path, config.project_root),
        "output_dir": _display_path(output_path, config.project_root),
        "num_frames_selected": len(selections),
        "num_images_written": images_written,
        "num_videos_written": len(video_records),
        "missing_frames": frame_batch.missing,
        "missing_cell_score_frames": missing_cell_score_frames,
        "generated_at": datetime.now(timezone.utc).isoformat(),
        "frame_source": settings.frame_source,
        "heatmap_normalization": settings.heatmap_normalization,
        "alpha": settings.alpha,
        "colormap": settings.colormap,
        "video_records": video_records,
    }

    write_json(output_path / "visualization_index.json", _public_index(index_records))
    write_json(output_path / "visualization_stats.json", stats)
    write_qualitative_report(
        output_path / "qualitative_report.md",
        dataset=config.dataset,
        index_records=index_records,
        alert_records=selected_alerts,
        video_records=video_records,
        stats=stats,
    )
    return stats


def load_grid(path: str | Path) -> GridSpec:
    payload = read_json(path)
    width = int(payload["resized_width"])
    height = int(payload["resized_height"])
    cells: dict[str, GridCell] = {}
    for item in payload.get("cells", []):
        cell = GridCell(
            cell_id=str(item["cell_id"]),
            row=int(item["row"]),
            col=int(item["col"]),
            x1=int(item["x1"]),
            y1=int(item["y1"]),
            x2=int(item["x2"]),
            y2=int(item["y2"]),
        )
        if cell.x1 < 0 or cell.y1 < 0 or cell.x2 > width or cell.y2 > height:
            raise ConfigError(f"Grid cell {cell.cell_id} is outside resized frame bounds")
        if cell.x2 <= cell.x1 or cell.y2 <= cell.y1:
            raise ConfigError(f"Grid cell {cell.cell_id} has invalid bounds")
        cells[cell.cell_id] = cell
    expected = int(payload["rows"]) * int(payload["cols"])
    if len(cells) > expected:
        raise ConfigError(f"Grid has {len(cells)} cells, more than rows*cols={expected}")
    return GridSpec(
        resized_width=width,
        resized_height=height,
        rows=int(payload["rows"]),
        cols=int(payload["cols"]),
        cells=cells,
    )


def load_frame_scores(path: str | Path) -> list[FrameScore]:
    scores: list[FrameScore] = []
    with Path(path).open("r", newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        for row in reader:
            scores.append(
                FrameScore(
                    dataset=str(row["dataset"]),
                    video_id=str(row["video_id"]),
                    frame_id=int(row["frame_id"]),
                    frame_score=float(row["frame_score"]),
                    smoothed_frame_score=float(row["smoothed_frame_score"]),
                    severity=str(row["severity"]),
                    top_cells=_parse_top_cells(row.get("top_cells", "")),
                )
            )
    return scores


def load_alerts(path: str | Path) -> list[dict[str, Any]]:
    alerts = read_json(path)
    normalized: list[dict[str, Any]] = []
    for alert in alerts:
        item = dict(alert)
        item["start_frame_id"] = int(item["start_frame_id"])
        item["end_frame_id"] = int(item["end_frame_id"])
        item["max_score"] = float(item.get("max_score", 0.0))
        item["alert_id"] = f"{item.get('video_id')}:{item['start_frame_id']}-{item['end_frame_id']}"
        normalized.append(item)
    return normalized


def select_top_frames(
    frame_scores: list[FrameScore],
    count: int,
    min_score: float,
    video_id: str | None = None,
) -> list[FrameSelection]:
    candidates = [
        score
        for score in frame_scores
        if score.smoothed_frame_score >= min_score and (video_id is None or score.video_id == video_id)
    ]
    candidates.sort(key=lambda item: item.smoothed_frame_score, reverse=True)
    return [
        _selection_from_score(score, "top_frame", rank=index)
        for index, score in enumerate(candidates[:count], start=1)
    ]


def select_alert_peaks(
    frame_scores: list[FrameScore],
    alerts: list[dict[str, Any]],
    limit: int | None = None,
) -> tuple[list[FrameSelection], list[dict[str, Any]]]:
    by_video: dict[str, list[FrameScore]] = {}
    for score in frame_scores:
        by_video.setdefault(score.video_id, []).append(score)
    for scores in by_video.values():
        scores.sort(key=lambda item: item.frame_id)

    selections: list[FrameSelection] = []
    selected_alerts: list[dict[str, Any]] = []
    for index, alert in enumerate(alerts, start=1):
        if limit is not None and len(selections) >= limit:
            break
        video_scores = by_video.get(str(alert.get("video_id")), [])
        in_range = [
            score
            for score in video_scores
            if int(alert["start_frame_id"]) <= score.frame_id <= int(alert["end_frame_id"])
        ]
        if not in_range:
            continue
        peak = max(in_range, key=lambda item: item.smoothed_frame_score)
        selection = _selection_from_score(peak, "alert_peak", rank=index)
        selection.alert_id = str(alert["alert_id"])
        selection.alert_start_frame_id = int(alert["start_frame_id"])
        selection.alert_end_frame_id = int(alert["end_frame_id"])
        selection.reasons = list(alert.get("reasons", []))
        selections.append(selection)
        selected_alerts.append(alert)
    return selections, selected_alerts


def select_video_range(
    frame_scores: list[FrameScore],
    video_id: str,
    start_frame: int | None = None,
    end_frame: int | None = None,
    limit: int | None = None,
    selection_type: str = "range_frame",
) -> list[FrameSelection]:
    selected = [
        score
        for score in frame_scores
        if score.video_id == video_id
        and (start_frame is None or score.frame_id >= start_frame)
        and (end_frame is None or score.frame_id <= end_frame)
    ]
    selected.sort(key=lambda item: item.frame_id)
    if limit is not None:
        selected = selected[:limit]
    return [
        _selection_from_score(score, selection_type, rank=index)
        for index, score in enumerate(selected, start=1)
    ]


def load_selected_cell_scores(
    path: str | Path,
    selected_keys: set[tuple[str, int]],
) -> tuple[dict[tuple[str, int], list[dict[str, Any]]], float, list[dict[str, object]]]:
    scores: dict[tuple[str, int], list[dict[str, Any]]] = {key: [] for key in selected_keys}
    global_max = 0.0
    with Path(path).open("r", newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        for row in reader:
            cell_score = float(row["cell_score"])
            global_max = max(global_max, cell_score)
            key = (str(row["video_id"]), int(row["center_frame_id"]))
            if key not in scores:
                continue
            scores[key].append(
                {
                    "cell_id": str(row["cell_id"]),
                    "cell_score": cell_score,
                    "cluster_distance_score": float(row["cluster_distance_score"]),
                    "temporal_change_score": float(row["temporal_change_score"]),
                    "cluster_distance": float(row["cluster_distance"]),
                    "cluster_threshold": float(row["cluster_threshold"]),
                    "nearest_cluster": int(row["nearest_cluster"]),
                }
            )
    missing = [
        {"video_id": video_id, "frame_id": frame_id}
        for video_id, frame_id in sorted(selected_keys)
        if not scores.get((video_id, frame_id))
    ]
    return scores, global_max, missing


def render_overlay(
    frame_gray: np.ndarray,
    score_rows: list[dict[str, Any]],
    grid: GridSpec,
    top_cells: list[str],
    settings: VisualizationSettings,
    global_max: float,
) -> np.ndarray:
    cv2 = _cv2()
    heatmap = np.zeros((grid.resized_height, grid.resized_width), dtype=np.float32)
    for row in score_rows:
        cell = grid.cells.get(str(row["cell_id"]))
        if cell is None:
            continue
        score = float(np.clip(float(row["cell_score"]), 0.0, 1.0))
        heatmap[cell.y1 : cell.y2, cell.x1 : cell.x2] = score

    if frame_gray.shape[:2] != heatmap.shape[:2]:
        heatmap = cv2.resize(heatmap, (frame_gray.shape[1], frame_gray.shape[0]), interpolation=cv2.INTER_NEAREST)

    normalized = _normalize_heatmap(heatmap, settings.heatmap_normalization, global_max)
    colored = cv2.applyColorMap((normalized * 255.0).astype(np.uint8), _colormap_id(settings.colormap))
    base = cv2.cvtColor(frame_gray, cv2.COLOR_GRAY2BGR) if frame_gray.ndim == 2 else frame_gray.copy()
    overlay = cv2.addWeighted(base, 1.0 - settings.alpha, colored, settings.alpha, 0.0)

    if settings.draw_grid:
        _draw_grid(overlay, grid)
    if settings.draw_top_cells:
        score_by_cell = {str(row["cell_id"]): float(row["cell_score"]) for row in score_rows}
        _draw_top_cells(overlay, grid, top_cells, score_by_cell)
    return overlay


def _settings(
    config: AnomalyConfig,
    output_dir: str | Path | None,
    alpha: float | None,
    colormap: str | None,
    min_score: float | None,
) -> VisualizationSettings:
    raw = config.raw.setdefault("visualization", {})
    output_root = raw.setdefault("output_root", "src/outputs/visualizations")
    resolved_output = (
        resolve_path(output_dir, config.project_root)
        if output_dir is not None
        else resolve_path(str(output_root), config.project_root) / config.dataset
    )
    resolved_alpha = float(alpha if alpha is not None else raw.setdefault("alpha", 0.45))
    if not 0.0 <= resolved_alpha <= 1.0:
        raise ConfigError("visualization alpha must be in [0, 1]")
    normalization = str(raw.setdefault("heatmap_normalization", "per_frame"))
    if normalization not in {"per_frame", "global"}:
        raise ConfigError("visualization.heatmap_normalization must be 'per_frame' or 'global'")
    image_format = str(raw.setdefault("image_format", "png")).lower().lstrip(".")
    if image_format not in {"png", "jpg", "jpeg"}:
        raise ConfigError("visualization.image_format must be png, jpg, or jpeg")
    return VisualizationSettings(
        output_dir=resolved_output,
        colormap=str(colormap if colormap is not None else raw.setdefault("colormap", "JET")),
        alpha=resolved_alpha,
        heatmap_normalization=normalization,
        draw_grid=bool(raw.setdefault("draw_grid", False)),
        draw_top_cells=bool(raw.setdefault("draw_top_cells", True)),
        top_cells_per_frame=int(raw.setdefault("top_cells_per_frame", 5)),
        top_frames=int(raw.setdefault("top_frames", 30)),
        min_score=float(min_score if min_score is not None else raw.setdefault("min_score", 0.70)),
        frame_source=str(raw.setdefault("frame_source", "preprocessed")),
        image_format=image_format,
        video_fps=float(raw.setdefault("video_fps", 10)),
    )


def _top_count(
    settings: VisualizationSettings,
    requested_top_frames: int | None,
    include_alerts: bool,
    video_id: str | None,
) -> int:
    if requested_top_frames is not None:
        return max(0, int(requested_top_frames))
    if include_alerts or video_id is not None:
        return 0
    return settings.top_frames


def _selection_from_score(score: FrameScore, selection_type: str, rank: int = 0) -> FrameSelection:
    return FrameSelection(
        dataset=score.dataset,
        video_id=score.video_id,
        frame_id=score.frame_id,
        frame_score=score.frame_score,
        smoothed_frame_score=score.smoothed_frame_score,
        severity=score.severity,
        top_cells=list(score.top_cells),
        selection_type=selection_type,
        rank=rank,
    )


def _default_video_id(frame_scores: list[FrameScore], alerts: list[dict[str, Any]]) -> str:
    if alerts:
        return str(max(alerts, key=lambda item: float(item.get("max_score", 0.0)))["video_id"])
    if not frame_scores:
        raise ConfigError("Cannot choose a default video because frame_scores.csv is empty")
    return max(frame_scores, key=lambda item: item.smoothed_frame_score).video_id


def _requests_by_video(keys: set[tuple[str, int]]) -> dict[str, set[int]]:
    requests: dict[str, set[int]] = {}
    for video_id, frame_id in keys:
        requests.setdefault(video_id, set()).add(frame_id)
    return requests


def _image_path(
    output_dir: Path,
    category: str,
    selection: FrameSelection,
    image_format: str,
) -> Path:
    ensure_dir(output_dir / category)
    score = f"{selection.smoothed_frame_score:.3f}"
    if selection.selection_type == "alert_peak":
        start = selection.alert_start_frame_id or selection.frame_id
        end = selection.alert_end_frame_id or selection.frame_id
        name = f"{selection.video_id}_{start:06d}_{end:06d}_peak.{image_format}"
    else:
        name = f"{selection.video_id}_{selection.frame_id:06d}_score_{score}.{image_format}"
    return output_dir / category / name


def _write_image(path: Path, image: np.ndarray) -> None:
    cv2 = _cv2()
    ensure_dir(path.parent)
    if not cv2.imwrite(str(path), image):
        raise ConfigError(f"Failed to write image: {path}")


def _write_video_overlay(
    config: AnomalyConfig,
    output_dir: Path,
    frame_batch: Any,
    cell_scores: dict[tuple[str, int], list[dict[str, Any]]],
    grid: GridSpec,
    selections: list[FrameSelection],
    settings: VisualizationSettings,
    global_max: float,
) -> dict[str, Any] | None:
    cv2 = _cv2()
    ensure_dir(output_dir / "videos")
    video_id = selections[0].video_id
    video_path = output_dir / "videos" / f"{video_id}_overlay.mp4"
    writer = None
    frames_written = 0
    try:
        for selection in selections:
            loaded_frame = frame_batch.frames.get((selection.video_id, selection.frame_id))
            if loaded_frame is None:
                continue
            overlay = render_overlay(
                frame_gray=loaded_frame.gray,
                score_rows=cell_scores.get((selection.video_id, selection.frame_id), []),
                grid=grid,
                top_cells=selection.top_cells[: settings.top_cells_per_frame],
                settings=settings,
                global_max=global_max,
            )
            if writer is None:
                height, width = overlay.shape[:2]
                fourcc = cv2.VideoWriter_fourcc(*"mp4v")
                writer = cv2.VideoWriter(str(video_path), fourcc, settings.video_fps, (width, height))
                if not writer.isOpened():
                    raise ConfigError(f"Cannot open video writer: {video_path}")
            writer.write(overlay)
            frames_written += 1
    finally:
        if writer is not None:
            writer.release()
    if frames_written == 0:
        return None
    return {
        "video_id": video_id,
        "video_path": _display_path(video_path, config.project_root),
        "relative_path": _relative_to(video_path, output_dir),
        "frames_written": frames_written,
        "fps": settings.video_fps,
    }


def _index_record(
    config: AnomalyConfig,
    output_dir: Path,
    image_path: Path,
    selection: FrameSelection,
) -> dict[str, Any]:
    return {
        "dataset": selection.dataset,
        "video_id": selection.video_id,
        "frame_id": selection.frame_id,
        "frame_score": selection.frame_score,
        "smoothed_frame_score": selection.smoothed_frame_score,
        "severity": selection.severity,
        "top_cells": selection.top_cells,
        "image_path": _display_path(image_path, config.project_root),
        "relative_path": _relative_to(image_path, output_dir),
        "selection_type": selection.selection_type,
        "rank": selection.rank,
        "alert_id": selection.alert_id,
        "reasons": selection.reasons,
    }


def _public_index(index_records: list[dict[str, Any]]) -> list[dict[str, Any]]:
    return [dict(record) for record in index_records]


def _display_path(path: str | Path, project_root: str | Path) -> str:
    path_obj = Path(path).resolve()
    root = Path(project_root).resolve()
    try:
        return str(path_obj.relative_to(root)).replace("\\", "/")
    except ValueError:
        return str(path_obj)


def _relative_to(path: str | Path, root: str | Path) -> str:
    try:
        return str(Path(path).resolve().relative_to(Path(root).resolve())).replace("\\", "/")
    except ValueError:
        return str(path)


def _parse_top_cells(raw_value: str) -> list[str]:
    if not raw_value:
        return []
    try:
        payload = json.loads(raw_value)
    except json.JSONDecodeError:
        return [item.strip() for item in raw_value.split(",") if item.strip()]
    if isinstance(payload, list):
        return [str(item) for item in payload]
    return []


def _normalize_heatmap(heatmap: np.ndarray, mode: str, global_max: float) -> np.ndarray:
    heatmap = np.clip(heatmap.astype(np.float32), 0.0, 1.0)
    if mode == "per_frame":
        max_value = float(np.max(heatmap)) if heatmap.size else 0.0
        return heatmap / max_value if max_value > 0.0 else heatmap
    if mode == "global":
        scale = max(float(global_max), 1.0e-12)
        return np.clip(heatmap / scale, 0.0, 1.0)
    raise ConfigError(f"Unsupported heatmap normalization: {mode}")


def _draw_grid(image: np.ndarray, grid: GridSpec) -> None:
    cv2 = _cv2()
    for cell in grid.cells.values():
        cv2.rectangle(image, (cell.x1, cell.y1), (cell.x2 - 1, cell.y2 - 1), (80, 80, 80), 1)


def _draw_top_cells(
    image: np.ndarray,
    grid: GridSpec,
    top_cells: list[str],
    score_by_cell: dict[str, float],
) -> None:
    cv2 = _cv2()
    for index, cell_id in enumerate(top_cells):
        cell = grid.cells.get(cell_id)
        if cell is None:
            continue
        color = (0, 255, 255) if index == 0 else (0, 255, 0)
        cv2.rectangle(image, (cell.x1, cell.y1), (cell.x2 - 1, cell.y2 - 1), color, 2)
        label = f"{cell_id} {score_by_cell.get(cell_id, 0.0):.2f}"
        y = max(cell.y1 + 12, 12)
        cv2.putText(image, label, (cell.x1 + 2, y), cv2.FONT_HERSHEY_SIMPLEX, 0.32, (0, 0, 0), 2, cv2.LINE_AA)
        cv2.putText(image, label, (cell.x1 + 2, y), cv2.FONT_HERSHEY_SIMPLEX, 0.32, (255, 255, 255), 1, cv2.LINE_AA)


def _colormap_id(name: str) -> int:
    cv2 = _cv2()
    attr = f"COLORMAP_{name.upper()}"
    if not hasattr(cv2, attr):
        raise ConfigError(f"Unknown OpenCV colormap: {name}")
    return int(getattr(cv2, attr))


def _cv2():
    try:
        import cv2
    except ImportError as exc:
        raise ConfigError(
            "OpenCV is required for visualization. "
            "Install dependencies with: python -m pip install -r src/requirements.txt"
        ) from exc
    return cv2
</file>

<file path="src/kpdl_preprocess/__init__.py">
"""Preprocessing pipeline for KPDL anomaly detection datasets."""

__version__ = "0.1.0"
</file>

<file path="src/kpdl_preprocess/arff_cli.py">
from __future__ import annotations

import argparse

from .arff import convert_csv_to_arff


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Convert feature CSV to WEKA ARFF.")
    parser.add_argument("--input", required=True, help="Input feature CSV.")
    parser.add_argument("--output", required=True, help="Output ARFF path.")
    parser.add_argument("--relation", default=None, help="Optional ARFF relation name.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    output = convert_csv_to_arff(args.input, args.output, args.relation)
    print(output)
    return 0
</file>

<file path="src/kpdl_preprocess/arff.py">
from __future__ import annotations

import csv
from pathlib import Path


STRING_COLUMNS = {"dataset", "split", "video_id", "cube_id"}
NOMINAL_COLUMNS = {"cell_id"}


def convert_csv_to_arff(input_csv: str | Path, output_arff: str | Path, relation: str | None = None) -> Path:
    input_path = Path(input_csv)
    output_path = Path(output_arff)
    if relation is None:
        relation = _safe_relation_name(input_path.stem)

    nominal_values = _collect_nominal_values(input_path)

    with input_path.open("r", newline="", encoding="utf-8") as src:
        reader = csv.DictReader(src)
        if not reader.fieldnames:
            raise ValueError(f"CSV has no header: {input_path}")
        fieldnames = reader.fieldnames

        output_path.parent.mkdir(parents=True, exist_ok=True)
        with output_path.open("w", newline="", encoding="utf-8") as dst:
            dst.write(f"@RELATION {_quote_arff(relation)}\n\n")
            for column in fieldnames:
                if column in STRING_COLUMNS:
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} STRING\n")
                elif column in NOMINAL_COLUMNS:
                    values = nominal_values.get(column) or ["unknown"]
                    joined = ",".join(_quote_arff(value) for value in values)
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} {{{joined}}}\n")
                else:
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} NUMERIC\n")

            dst.write("\n@DATA\n")
            for row in reader:
                values = [_format_value(column, row.get(column, "")) for column in fieldnames]
                dst.write(",".join(values) + "\n")

    return output_path


def _collect_nominal_values(input_path: Path) -> dict[str, list[str]]:
    values: dict[str, set[str]] = {column: set() for column in NOMINAL_COLUMNS}
    with input_path.open("r", newline="", encoding="utf-8") as src:
        reader = csv.DictReader(src)
        for row in reader:
            for column in NOMINAL_COLUMNS:
                value = row.get(column)
                if value:
                    values[column].add(value)
    return {column: sorted(items) for column, items in values.items()}


def _format_value(column: str, value: str) -> str:
    if value == "" or value is None:
        return "?"
    if column in STRING_COLUMNS or column in NOMINAL_COLUMNS:
        return _quote_arff(value)
    try:
        float(value)
    except ValueError:
        return "?"
    return value


def _quote_arff(value: object) -> str:
    text = str(value).replace("\\", "\\\\").replace("'", "\\'")
    return f"'{text}'"


def _safe_relation_name(name: str) -> str:
    return "".join(ch if ch.isalnum() or ch == "_" else "_" for ch in name)
</file>

<file path="src/kpdl_preprocess/config.py">
from __future__ import annotations

from copy import deepcopy
from pathlib import Path
from typing import Any


class ConfigError(ValueError):
    pass


def load_config(path: str | Path) -> dict[str, Any]:
    config_path = Path(path)
    if not config_path.exists():
        raise ConfigError(f"Config file not found: {config_path}")

    try:
        import yaml
    except ImportError as exc:
        raise ConfigError(
            "PyYAML is required to read YAML config files. "
            "Install dependencies with: python -m pip install -r src/requirements.txt"
        ) from exc

    with config_path.open("r", encoding="utf-8") as handle:
        data = yaml.safe_load(handle) or {}

    if not isinstance(data, dict):
        raise ConfigError(f"Config root must be a mapping: {config_path}")

    _validate_config(data)
    return data


def _validate_config(config: dict[str, Any]) -> None:
    required_sections = ["data", "video", "grid", "cube", "features"]
    missing = [section for section in required_sections if section not in config]
    if missing:
        raise ConfigError(f"Missing config section(s): {', '.join(missing)}")

    data = config["data"]
    for key in ["dataset", "root", "train_path", "test_path"]:
        if key not in data:
            raise ConfigError(f"Missing data.{key}")

    video = config["video"]
    if video.get("input_type") not in {"frame_sequence", "video"}:
        raise ConfigError("video.input_type must be 'frame_sequence' or 'video'")

    for key in ["resize_width", "resize_height"]:
        if int(video.get(key, 0)) <= 0:
            raise ConfigError(f"video.{key} must be a positive integer")

    grid = config["grid"]
    if int(grid.get("rows", 0)) <= 0 or int(grid.get("cols", 0)) <= 0:
        raise ConfigError("grid.rows and grid.cols must be positive integers")

    cube = config["cube"]
    if int(cube.get("length", 0)) <= 1:
        raise ConfigError("cube.length must be greater than 1")
    if int(cube.get("stride", 0)) <= 0:
        raise ConfigError("cube.stride must be a positive integer")


def get_nested(config: dict[str, Any], *keys: str, default: Any = None) -> Any:
    current: Any = config
    for key in keys:
        if not isinstance(current, dict) or key not in current:
            return default
        current = current[key]
    return current


def copy_config(config: dict[str, Any]) -> dict[str, Any]:
    return deepcopy(config)


def resolve_path(path_value: str | Path, project_root: str | Path) -> Path:
    path = Path(path_value)
    if path.is_absolute():
        return path
    return Path(project_root).resolve() / path
</file>

<file path="src/kpdl_preprocess/datasets.py">
from __future__ import annotations

from pathlib import Path

from .config import ConfigError, resolve_path
from .records import VideoSource
from .utils import sorted_natural


VIDEO_EXTENSIONS = {".avi", ".mp4", ".mov", ".mkv"}


def scan_dataset(
    config: dict,
    project_root: str | Path,
    split_filter: str | None = None,
) -> list[VideoSource]:
    data = config["data"]
    video = config["video"]
    dataset = str(data["dataset"])
    root = resolve_path(data["root"], project_root)
    input_type = str(video["input_type"])

    if not root.exists():
        raise ConfigError(f"Dataset root does not exist: {root}")

    splits = [("train", data["train_path"]), ("test", data["test_path"])]
    if split_filter:
        splits = [item for item in splits if item[0] == split_filter]
        if not splits:
            raise ConfigError(f"Unknown split: {split_filter}")

    sources: list[VideoSource] = []
    for split, split_path in splits:
        split_root = root / str(split_path)
        if not split_root.exists():
            raise ConfigError(f"Split path does not exist: {split_root}")

        if input_type == "frame_sequence":
            sources.extend(_scan_frame_sequences(dataset, split, split_root, input_type))
        elif input_type == "video":
            sources.extend(_scan_videos(dataset, split, split_root, input_type))
        else:
            raise ConfigError(f"Unsupported input_type: {input_type}")

    return sources


def _scan_frame_sequences(
    dataset: str,
    split: str,
    split_root: Path,
    input_type: str,
) -> list[VideoSource]:
    sources: list[VideoSource] = []
    for child in sorted_natural(p for p in split_root.iterdir() if p.is_dir()):
        if child.name.lower().endswith("_gt"):
            continue
        if not any(child.glob("*.tif")):
            continue
        sources.append(
            VideoSource(
                dataset=dataset,
                split=split,
                video_id=child.name,
                source_path=child,
                input_type=input_type,
            )
        )
    return sources


def _scan_videos(
    dataset: str,
    split: str,
    split_root: Path,
    input_type: str,
) -> list[VideoSource]:
    files = [
        path
        for path in split_root.iterdir()
        if path.is_file() and path.suffix.lower() in VIDEO_EXTENSIONS
    ]
    return [
        VideoSource(
            dataset=dataset,
            split=split,
            video_id=path.stem,
            source_path=path,
            input_type=input_type,
        )
        for path in sorted_natural(files)
    ]
</file>

<file path="src/kpdl_preprocess/features.py">
from __future__ import annotations

import numpy as np

from .config import get_nested
from .records import CellRecord, FrameRecord
from .schema import feature_columns


def extract_cube_features(
    cube_id: str,
    cube: list[FrameRecord],
    cells: list[CellRecord],
    config: dict,
) -> list[dict[str, object]]:
    method = str(get_nested(config, "features", "motion_method", default="frame_diff"))
    if method != "frame_diff":
        raise ValueError("Only features.motion_method='frame_diff' is implemented in SPEC 1 baseline")

    threshold = float(get_nested(config, "features", "frame_diff_threshold", default=15))
    direction_bins = int(get_nested(config, "features", "direction_bins", default=8))

    frames = np.stack([frame.gray for frame in cube]).astype(np.float32)
    diffs = np.abs(np.diff(frames, axis=0))
    masks = diffs > threshold

    first = cube[0]
    last = cube[-1]
    center = cube[len(cube) // 2]

    rows: list[dict[str, object]] = []
    for cell in cells:
        y1, y2, x1, x2 = cell.y1, cell.y2, cell.x1, cell.x2
        cell_diffs = diffs[:, y1:y2, x1:x2]
        cell_masks = masks[:, y1:y2, x1:x2]
        center_crop = center.gray[y1:y2, x1:x2].astype(np.float32)
        first_crop = first.gray[y1:y2, x1:x2].astype(np.float32)
        last_crop = last.gray[y1:y2, x1:x2].astype(np.float32)

        foreground_ratio = _safe_mean(np.any(cell_masks, axis=0))
        motion_density = _safe_mean(cell_masks)
        motion_magnitude_mean = _safe_mean(cell_diffs)
        motion_magnitude_std = _safe_std(cell_diffs)
        brightness_mean = _safe_mean(center_crop)
        brightness_delta = _safe_mean(last_crop) - _safe_mean(first_crop)

        row: dict[str, object] = {
            "dataset": first.dataset,
            "split": first.split,
            "video_id": first.video_id,
            "cube_id": cube_id,
            "start_frame_id": first.frame_id,
            "end_frame_id": last.frame_id,
            "center_frame_id": center.frame_id,
            "cell_id": cell.cell_id,
            "cell_row": cell.row,
            "cell_col": cell.col,
            "foreground_ratio": foreground_ratio,
            "motion_magnitude_mean": motion_magnitude_mean,
            "motion_magnitude_std": motion_magnitude_std,
            "motion_density": motion_density,
            "brightness_mean": brightness_mean,
            "brightness_delta": brightness_delta,
        }
        for bin_index in range(direction_bins):
            row[f"direction_hist_{bin_index}"] = 0.0
        rows.append(row)

    return rows


def _safe_mean(values: np.ndarray) -> float:
    if values.size == 0:
        return 0.0
    result = float(np.mean(values))
    return result if np.isfinite(result) else 0.0


def _safe_std(values: np.ndarray) -> float:
    if values.size == 0:
        return 0.0
    result = float(np.std(values))
    return result if np.isfinite(result) else 0.0
</file>

<file path="src/kpdl_preprocess/grid.py">
from __future__ import annotations

from .records import CellRecord


def generate_grid(width: int, height: int, rows: int, cols: int, ignore_cells: list[str] | None = None) -> list[CellRecord]:
    ignored = set(ignore_cells or [])
    cells: list[CellRecord] = []
    for row in range(rows):
        y1 = round(row * height / rows)
        y2 = round((row + 1) * height / rows)
        for col in range(cols):
            x1 = round(col * width / cols)
            x2 = round((col + 1) * width / cols)
            cell_id = f"{row:02d}_{col:02d}"
            if cell_id in ignored:
                continue
            cells.append(CellRecord(cell_id=cell_id, row=row, col=col, x1=x1, y1=y1, x2=x2, y2=y2))
    return cells


def grid_to_json(cells: list[CellRecord], width: int, height: int, rows: int, cols: int) -> dict:
    return {
        "resized_width": width,
        "resized_height": height,
        "rows": rows,
        "cols": cols,
        "num_cells": len(cells),
        "cells": [
            {
                "cell_id": cell.cell_id,
                "row": cell.row,
                "col": cell.col,
                "x1": cell.x1,
                "y1": cell.y1,
                "x2": cell.x2,
                "y2": cell.y2,
                "width": cell.width,
                "height": cell.height,
            }
            for cell in cells
        ],
    }
</file>

<file path="src/kpdl_preprocess/records.py">
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Any


@dataclass(frozen=True)
class VideoSource:
    dataset: str
    split: str
    video_id: str
    source_path: Path
    input_type: str


@dataclass(frozen=True)
class FrameRecord:
    dataset: str
    split: str
    video_id: str
    frame_id: int
    timestamp: float | None
    source_path: Path
    original_width: int
    original_height: int
    resized_width: int
    resized_height: int
    gray: Any


@dataclass(frozen=True)
class CellRecord:
    cell_id: str
    row: int
    col: int
    x1: int
    y1: int
    x2: int
    y2: int

    @property
    def width(self) -> int:
        return self.x2 - self.x1

    @property
    def height(self) -> int:
        return self.y2 - self.y1
</file>

<file path="src/kpdl_preprocess/schema.py">
from __future__ import annotations

SCHEMA_VERSION = "spec_1.common.v1"

FRAME_MANIFEST_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "frame_id",
    "timestamp",
    "source_path",
    "original_width",
    "original_height",
    "resized_width",
    "resized_height",
]

VIDEO_MANIFEST_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "source_path",
    "input_type",
    "fps",
    "num_frames",
    "num_cubes",
    "original_width",
    "original_height",
    "failed",
    "error",
]

BASE_FEATURE_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "cube_id",
    "start_frame_id",
    "end_frame_id",
    "center_frame_id",
    "cell_id",
    "cell_row",
    "cell_col",
    "foreground_ratio",
    "motion_magnitude_mean",
    "motion_magnitude_std",
    "motion_density",
]

TAIL_FEATURE_COLUMNS = [
    "brightness_mean",
    "brightness_delta",
]


def feature_columns(direction_bins: int = 8) -> list[str]:
    return (
        BASE_FEATURE_COLUMNS
        + [f"direction_hist_{index}" for index in range(direction_bins)]
        + TAIL_FEATURE_COLUMNS
    )
</file>

<file path="src/kpdl_preprocess/utils.py">
from __future__ import annotations

import re
from pathlib import Path
from typing import Iterable


def ensure_dir(path: str | Path) -> Path:
    resolved = Path(path)
    resolved.mkdir(parents=True, exist_ok=True)
    return resolved


def natural_key(value: str | Path) -> list[object]:
    text = Path(value).name if isinstance(value, Path) else str(value)
    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", text)]


def sorted_natural(paths: Iterable[Path]) -> list[Path]:
    return sorted(paths, key=natural_key)


def stem_to_frame_id(path: Path, fallback: int) -> int:
    match = re.search(r"\d+", path.stem)
    return int(match.group(0)) if match else fallback


def bool_config(value: object, default: bool = False) -> bool:
    if value is None:
        return default
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        return value.strip().lower() in {"1", "true", "yes", "y", "on"}
    return bool(value)
</file>

<file path="src/preprocess.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_preprocess.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/test.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_anomaly.test import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/tool/__init__.py">
"""Dataset-specific preprocessing entrypoints."""
</file>

<file path="src/tool/preprocess_avenue.py">
from __future__ import annotations

import argparse
from pathlib import Path

from _common import PROJECT_ROOT, add_common_args, run_dataset


DEFAULT_CONFIG = PROJECT_ROOT / "src" / "configs" / "avenue.yaml"


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Preprocess CUHK Avenue into the common SPEC 1 schema.")
    parser.add_argument("--config", default=None, help="Optional config override. Defaults to src/configs/avenue.yaml.")
    add_common_args(parser)
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    config_path = Path(args.config) if args.config else DEFAULT_CONFIG
    return run_dataset(args, config_path)


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/tool/preprocess_ucsd.py">
from __future__ import annotations

import argparse
from pathlib import Path

from _common import PROJECT_ROOT, add_common_args, run_dataset


CONFIGS = {
    "ped2": PROJECT_ROOT / "src" / "configs" / "ucsd_ped2.yaml",
    "ped1": PROJECT_ROOT / "src" / "configs" / "ucsd_ped1.yaml",
}


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Preprocess UCSD Ped1/Ped2 into the common SPEC 1 schema.")
    parser.add_argument("--ped", choices=sorted(CONFIGS), default="ped2", help="UCSD subset to process.")
    parser.add_argument("--config", default=None, help="Optional config override. If omitted, --ped selects a config.")
    add_common_args(parser)
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    config_path = Path(args.config) if args.config else CONFIGS[args.ped]
    return run_dataset(args, config_path)


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/train.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_anomaly.train import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/visualize.py">
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_anomaly.visualization import run_visualization


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Render SPEC 4 anomaly heatmap overlays.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative paths are resolved here.")
    parser.add_argument("--result-dir", default=None, help="Override SPEC 3 result directory.")
    parser.add_argument("--output-dir", default=None, help="Override visualization output directory.")
    parser.add_argument("--top-frames", type=int, default=None, help="Export the top N anomaly frames.")
    parser.add_argument("--alerts", action="store_true", help="Export peak frames from alert segments.")
    parser.add_argument("--video-id", default=None, help="Video id to visualize.")
    parser.add_argument("--start-frame", type=int, default=None, help="First frame id for range/video export.")
    parser.add_argument("--end-frame", type=int, default=None, help="Last frame id for range/video export.")
    parser.add_argument("--write-video", action="store_true", help="Write an MP4 overlay for the selected video/range.")
    parser.add_argument("--alpha", type=float, default=None, help="Heatmap blend alpha override.")
    parser.add_argument("--colormap", default=None, help="OpenCV colormap name, for example JET or TURBO.")
    parser.add_argument("--min-score", type=float, default=None, help="Minimum smoothed score for top-frame export.")
    parser.add_argument("--limit-frames", type=int, default=None, help="Limit selected frames for quick smoke runs.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    stats = run_visualization(
        config_path=args.config,
        project_root=args.project_root,
        result_dir=args.result_dir,
        output_dir=args.output_dir,
        top_frames=args.top_frames,
        include_alerts=args.alerts,
        video_id=args.video_id,
        start_frame=args.start_frame,
        end_frame=args.end_frame,
        write_video=args.write_video,
        alpha=args.alpha,
        colormap=args.colormap,
        min_score=args.min_score,
        limit_frames=args.limit_frames,
    )
    print(json.dumps(_public_summary(stats), indent=2))
    return 0


def _public_summary(stats: dict) -> dict:
    return {
        "dataset": stats["dataset"],
        "output_dir": stats["output_dir"],
        "num_frames_selected": stats["num_frames_selected"],
        "num_images_written": stats["num_images_written"],
        "num_videos_written": stats["num_videos_written"],
        "missing_frames": len(stats["missing_frames"]),
        "missing_cell_score_frames": len(stats["missing_cell_score_frames"]),
    }


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="src/configs/avenue.yaml">
data:
  dataset: "avenue"
  root: "src/dataset/CUHK-avenue"
  train_path: "training_videos"
  test_path: "testing_videos"

video:
  input_type: "video"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"

rules:
  enabled: true
  output_root: "src/outputs/rules"
  model_dir: null
  algorithm: "bounded_apriori"
  min_support: 0.01
  min_confidence: 0.60
  min_lift: 1.05
  max_itemset_size: 3
  max_rules: 200
  include_cell_token: true
  include_cluster_token: true
  include_brightness_token: true
  include_direction_token: false
  rare_itemset_size: 3
  rare_support_floor: 0.001
  rare_score_cap: 1.0

visualization:
  output_root: "src/outputs/visualizations"
  colormap: "JET"
  alpha: 0.45
  heatmap_normalization: "per_frame"
  draw_grid: false
  draw_top_cells: true
  top_cells_per_frame: 5
  top_frames: 30
  min_score: 0.70
  frame_source: "preprocessed"
  image_format: "png"
  video_fps: 10
</file>

<file path="src/configs/ucsd_ped1.yaml">
data:
  dataset: "ucsd_ped1"
  root: "src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped1"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"

rules:
  enabled: true
  output_root: "src/outputs/rules"
  model_dir: null
  algorithm: "bounded_apriori"
  min_support: 0.01
  min_confidence: 0.60
  min_lift: 1.05
  max_itemset_size: 3
  max_rules: 200
  include_cell_token: true
  include_cluster_token: true
  include_brightness_token: true
  include_direction_token: false
  rare_itemset_size: 3
  rare_support_floor: 0.001
  rare_score_cap: 1.0

visualization:
  output_root: "src/outputs/visualizations"
  colormap: "JET"
  alpha: 0.45
  heatmap_normalization: "per_frame"
  draw_grid: false
  draw_top_cells: true
  top_cells_per_frame: 5
  top_frames: 30
  min_score: 0.70
  frame_source: "preprocessed"
  image_format: "png"
  video_fps: 10
</file>

<file path="src/doc/spec_2_processed.md">
# SPEC 2 Processed - Tracker thực hành WEKA 3.8.7 K=5

Original spec path: `src/doc/spec_2.md`

Date/time prepared: 2026-05-27 Asia/Bangkok

## 1. Mục đích file này

File này là bảng theo dõi thực hành cho `src/doc/spec_2.md`.

Các mục thực hành WEKA được cố ý để trống bằng checkbox `[ ]` và các trường chưa điền để người thực hành tự đánh dấu sau khi chạy trên WEKA 3.8.7. Không có mục thực hành nào bên dưới được đánh dấu hoàn thành sẵn.

## 2. Việc đã chuẩn bị bởi Codex

- [x] Đọc PRD trước khi cập nhật SPEC 2.
- [x] Đọc `repomix-output.xml` để nắm cấu trúc repo.
- [x] Đọc `src/doc/spec_2.md`.
- [x] Tạo tracker thực hành trống cho các phần chưa chạy của SPEC 2.

## 3. Trạng thái tổng quan SPEC 2

- [ ] Xác nhận WEKA version là `3.8.7`.
- [ ] Mở WEKA với heap đủ lớn.
- [ ] Tạo thư mục output thực hành `src/outputs/weka_experiments/spec_2_weka_3_8_7/`.
- [ ] Tạo hoặc mở `spec_2_log.md`.
- [ ] Hoàn thành Experiment 1 - Global behavior KMeans K=5.
- [ ] Hoàn thành Experiment 2 - Supplied test set cho global behavior K=5.
- [ ] Hoàn thành Experiment 3 - Global spatio-behavior KMeans K=5.
- [ ] Hoàn thành Experiment 4 - Selected per-cell KMeans K=5.
- [ ] Hoàn thành Experiment 5 - Apriori trên dữ liệu đã rời rạc hóa.
- [ ] Chọn 5 đến 10 luật dễ giải thích.
- [ ] Ghi nhận xét cấu hình nào nên chuyển sang Python ở SPEC 3/SPEC 5.

## 4. Output artifact cần tự đánh dấu

### 4.1. Thư mục

- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/`
- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/`
- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/models/`
- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/reports/`
- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/assignments/`
- [ ] `src/outputs/weka_experiments/spec_2_weka_3_8_7/rules/`

### 4.2. Prepared ARFF

- [ ] `prepared/ucsd_ped2_train_behavior_k5_standardized.arff`
- [ ] `prepared/ucsd_ped2_test_behavior_k5_standardized.arff`
- [ ] `prepared/ucsd_ped2_train_spatiobehavior_k5_standardized.arff`
- [ ] `prepared/ucsd_ped2_train_rules_k5_discretized.arff`

### 4.3. Models

- [ ] `models/ucsd_ped2_global_behavior_simplekmeans_k5.model`
- [ ] `models/ucsd_ped2_global_spatiobehavior_simplekmeans_k5.model`
- [ ] `models/ucsd_ped2_cell_04_08_behavior_simplekmeans_k5.model`
- [ ] `models/ucsd_ped2_cell_06_08_behavior_simplekmeans_k5.model`
- [ ] `models/ucsd_ped2_cell_08_05_behavior_simplekmeans_k5.model`
- [ ] `models/ucsd_ped2_cell_09_06_behavior_simplekmeans_k5.model`

### 4.4. Reports

- [ ] `reports/ucsd_ped2_global_behavior_simplekmeans_k5_train.txt`
- [ ] `reports/ucsd_ped2_global_behavior_simplekmeans_k5_test.txt`
- [ ] `reports/ucsd_ped2_global_spatiobehavior_simplekmeans_k5_train.txt`
- [ ] `reports/ucsd_ped2_cell_04_08_behavior_simplekmeans_k5.txt`
- [ ] `reports/ucsd_ped2_cell_06_08_behavior_simplekmeans_k5.txt`
- [ ] `reports/ucsd_ped2_cell_08_05_behavior_simplekmeans_k5.txt`
- [ ] `reports/ucsd_ped2_cell_09_06_behavior_simplekmeans_k5.txt`
- [ ] `reports/ucsd_ped2_rules_apriori_k5.txt`

### 4.5. Assignments và rules

- [ ] `assignments/ucsd_ped2_train_behavior_k5_clusters.csv`
- [ ] `assignments/ucsd_ped2_test_behavior_k5_clusters.csv`
- [ ] `rules/ucsd_ped2_rules_k5_selected.md`

## 5. Thực hành 1 - Mở và kiểm tra ARFF

### 5.1. Train ARFF

- [ ] Mở `src/outputs/weka/ucsd_ped2_features_train.arff`.
- [ ] Kiểm tra số instances.
- [ ] Kiểm tra số attributes.
- [ ] Kiểm tra `cell_id` là nominal.
- [ ] Kiểm tra các feature chính là numeric.
- [ ] Kiểm tra missing value bất thường.

Ghi nhận:

```text
WEKA version:
Input:
instances:
attributes:
cell_id type:
missing summary:
notes:
```

### 5.2. Test ARFF

- [ ] Mở `src/outputs/weka/ucsd_ped2_features_test.arff`.
- [ ] Kiểm tra số instances.
- [ ] Kiểm tra số attributes.
- [ ] Kiểm tra schema khớp train.

Ghi nhận:

```text
Input:
instances:
attributes:
schema matches train: yes/no
notes:
```

## 6. Thực hành 2 - Tạo Baseline A behavior-only

- [ ] Dùng `Remove` giữ `11-14,23-24` cho train.
- [ ] Dùng `Standardize` cho train.
- [ ] Lưu `ucsd_ped2_train_behavior_k5_standardized.arff`.
- [ ] Dùng cùng feature set cho test.
- [ ] Dùng `Standardize` cho test theo quy trình thực hành Explorer.
- [ ] Lưu `ucsd_ped2_test_behavior_k5_standardized.arff`.

Ghi nhận train:

```text
Remove attributeIndices:
invertSelection:
attributes after filter:
Standardize applied: yes/no
output file:
notes:
```

Ghi nhận test:

```text
Remove attributeIndices:
invertSelection:
attributes after filter:
Standardize applied: yes/no
output file:
notes:
```

## 7. Experiment 1 - Global behavior KMeans K=5

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Chọn `weka.clusterers.SimpleKMeans`.
- [ ] Đặt `numClusters=5`.
- [ ] Đặt `seed=10`.
- [ ] Đặt `maxIterations=500`.
- [ ] Bật hoặc ghi nhận `preserveInstancesOrder=True`.
- [ ] Chạy `Use training set`.
- [ ] Lưu model.
- [ ] Lưu report train.
- [ ] Ghi centroid.
- [ ] Diễn giải 5 cụm.

Ghi nhận:

```text
experiment_id: ucsd_ped2_global_behavior_simplekmeans_k5
date:
WEKA version:
input file:
feature set:
filters:
algorithm:
K:
seed:
maxIterations:
preserveInstancesOrder:
runtime:
iterations:
within cluster SSE:
model file:
report file:
```

Cluster sizes:

```text
C0:
C1:
C2:
C3:
C4:
```

Centroid summary:

```text
C0:
C1:
C2:
C3:
C4:
```

Diễn giải:

```text
C0:
C1:
C2:
C3:
C4:
```

Nhận xét:

```text
cluster motion cao:
cluster motion thấp:
cluster brightness khác biệt:
cụm nhỏ cần chú ý:
accepted for next step: yes/no
reason:
```

## 8. Experiment 2 - Supplied test set cho global behavior K=5

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Mở hoặc chọn test set đã chuẩn bị.
- [ ] Dùng supplied test set cho model K=5.
- [ ] Ghi phân bố cụm test.
- [ ] So sánh phân bố train/test.
- [ ] Lưu report test.
- [ ] Xuất assignment nếu môi trường WEKA hỗ trợ.
- [ ] Join assignment với metadata CSV nếu xuất được.

Ghi nhận:

```text
experiment_id: ucsd_ped2_global_behavior_simplekmeans_k5_test
date:
train model:
test input:
report file:
assignment file:
runtime:
```

Test cluster sizes:

```text
C0:
C1:
C2:
C3:
C4:
```

So sánh train/test:

```text
cluster tăng rõ trên test:
cluster giảm rõ trên test:
nhận xét distribution shift:
assignment exported: yes/no
notes:
```

## 9. Experiment 3 - Global spatio-behavior KMeans K=5

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Tạo Baseline B bằng `Remove` giữ `9-14,23-24`.
- [ ] Dùng `Standardize`.
- [ ] Chạy `SimpleKMeans K=5`.
- [ ] Lưu model.
- [ ] Lưu report.
- [ ] Đọc centroid của `cell_row`, `cell_col`.
- [ ] So sánh với Baseline A.

Ghi nhận:

```text
experiment_id: ucsd_ped2_global_spatiobehavior_simplekmeans_k5
date:
input file:
feature set:
filters:
algorithm:
K:
seed:
maxIterations:
runtime:
iterations:
within cluster SSE:
model file:
report file:
```

Cluster sizes:

```text
C0:
C1:
C2:
C3:
C4:
```

Nhận xét:

```text
cell_row/cell_col có chi phối cụm không:
motion feature còn tạo cụm rõ không:
so sánh với Baseline A:
accepted for next step: yes/no
reason:
```

## 10. Experiment 4 - Selected per-cell KMeans K=5

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist chung:

- [ ] Chạy cell `04_08`.
- [ ] Chạy cell `06_08`.
- [ ] Chạy cell `08_05`.
- [ ] Chạy cell `09_06`.
- [ ] Với mỗi cell, lọc đúng `cell_id`.
- [ ] Với mỗi cell, giữ feature `11-14,23-24`.
- [ ] Với mỗi cell, dùng `Standardize`.
- [ ] Với mỗi cell, chạy `SimpleKMeans K=5`.
- [ ] Với mỗi cell, lưu report.
- [ ] Ghi cell nào không phù hợp với K=5 nếu có.

### 10.1. Cell 04_08

- [ ] Done
- [ ] Skipped
- [ ] Blocked

```text
cell_id: 04_08
num_train_instances:
feature_set:
algorithm:
K:
seed:
iterations:
within_cluster_sse:
model file:
report file:
cluster_sizes:
centroid_summary:
interpretation:
usable_for_python_pipeline: yes/no
notes:
```

### 10.2. Cell 06_08

- [ ] Done
- [ ] Skipped
- [ ] Blocked

```text
cell_id: 06_08
num_train_instances:
feature_set:
algorithm:
K:
seed:
iterations:
within_cluster_sse:
model file:
report file:
cluster_sizes:
centroid_summary:
interpretation:
usable_for_python_pipeline: yes/no
notes:
```

### 10.3. Cell 08_05

- [ ] Done
- [ ] Skipped
- [ ] Blocked

```text
cell_id: 08_05
num_train_instances:
feature_set:
algorithm:
K:
seed:
iterations:
within_cluster_sse:
model file:
report file:
cluster_sizes:
centroid_summary:
interpretation:
usable_for_python_pipeline: yes/no
notes:
```

### 10.4. Cell 09_06

- [ ] Done
- [ ] Skipped
- [ ] Blocked

```text
cell_id: 09_06
num_train_instances:
feature_set:
algorithm:
K:
seed:
iterations:
within_cluster_sse:
model file:
report file:
cluster_sizes:
centroid_summary:
interpretation:
usable_for_python_pipeline: yes/no
notes:
```

## 11. Thực hành 7 - Tạo dữ liệu cho khai phá luật

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Xác định có `cluster_id` từ KMeans K=5 hay chưa.
- [ ] Nếu có, thêm `cluster_id=C0..C4` vào dữ liệu rule.
- [ ] Nếu chưa có, ghi rõ Apriori chạy không có `cluster_id`.
- [ ] Giữ `cell_id`.
- [ ] Giữ `motion_magnitude_mean`.
- [ ] Giữ `motion_density`.
- [ ] Giữ `brightness_mean`.
- [ ] Giữ `brightness_delta`.
- [ ] Bỏ metadata không dùng cho luật.

Ghi nhận:

```text
input source:
cluster_id available: yes/no
attributes kept:
attributes removed:
output before discretize:
notes:
```

## 12. Thực hành 8 - Rời rạc hóa feature numeric

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Dùng `weka.filters.unsupervised.attribute.Discretize`.
- [ ] Đặt `bins=5`.
- [ ] Đặt `useEqualFrequency=True`.
- [ ] Kiểm tra các cột numeric đã thành nominal.
- [ ] Lưu `prepared/ucsd_ped2_train_rules_k5_discretized.arff`.
- [ ] Ghi cách diễn giải bin thành token dễ hiểu.

Ghi nhận:

```text
input:
Discretize bins:
useEqualFrequency:
numeric attributes discretized:
output file:
bin interpretation:
notes:
```

## 13. Experiment 5 - Apriori trên dữ liệu đã rời rạc hóa

Status:

- [ ] Not started
- [ ] Done
- [ ] Skipped
- [ ] Blocked

Checklist:

- [ ] Mở tab `Associate`.
- [ ] Chọn `weka.associations.Apriori`.
- [ ] Dùng dữ liệu đã discretize.
- [ ] Chạy cấu hình support/confidence đầu tiên.
- [ ] Lưu output thô.
- [ ] Thử ngưỡng khác nếu luật quá ít hoặc quá nhiều.
- [ ] Chọn 5 đến 10 luật tốt nhất.
- [ ] Ghi support/confidence/lift nếu có.
- [ ] Diễn giải luật bằng câu dễ hiểu.
- [ ] Ghi luật nào dùng được cho anomaly reason.

Ghi nhận cấu hình chính:

```text
experiment_id: ucsd_ped2_rules_discretize5_apriori_k5_s001_c06
date:
input file:
attributes:
numRules:
metricType:
minMetric:
lowerBoundMinSupport:
upperBoundMinSupport:
delta:
runtime:
raw rules count:
report file:
selected rules file:
```

Selected rules:

```text
Rule 1:
  antecedent:
  consequent:
  support:
  confidence:
  lift:
  interpretation:
  use_for_anomaly_reason:

Rule 2:
  antecedent:
  consequent:
  support:
  confidence:
  lift:
  interpretation:
  use_for_anomaly_reason:

Rule 3:
  antecedent:
  consequent:
  support:
  confidence:
  lift:
  interpretation:
  use_for_anomaly_reason:

Rule 4:
  antecedent:
  consequent:
  support:
  confidence:
  lift:
  interpretation:
  use_for_anomaly_reason:

Rule 5:
  antecedent:
  consequent:
  support:
  confidence:
  lift:
  interpretation:
  use_for_anomaly_reason:
```

Nếu chọn thêm luật:

```text
Rule 6:
Rule 7:
Rule 8:
Rule 9:
Rule 10:
```

Rejected rule patterns:

```text

```

Notes:

```text

```

## 14. Tiêu chí hoàn thành để tự tick

- [ ] Có bản thực hành WEKA 3.8.7 rõ ràng cho UCSD Ped2.
- [ ] Phân cụm chính dùng `SimpleKMeans` với `K=5`.
- [ ] Có report centroid và cluster size của global behavior K=5.
- [ ] Có phân bố cụm trên supplied test set.
- [ ] Có ít nhất một phân tích spatio-behavior K=5.
- [ ] Có ít nhất bốn thử nghiệm per-cell K=5 hoặc ghi rõ lý do không thể thao tác.
- [ ] Có ít nhất một lần chạy Apriori trên dữ liệu đã rời rạc hóa.
- [ ] Có 5 đến 10 luật được chọn và diễn giải.
- [ ] Có log thực nghiệm đủ để tái hiện.
- [ ] Có nhận xét cấu hình nào nên chuyển sang Python ở SPEC 3 và SPEC 5.

## 15. Verification do người thực hành điền

```text
WEKA version verified:
Date completed:
Commands or GUI actions verified:
Files manually checked:
Known issues:
Final notes:
```

## 16. Lượt cập nhật tracker

- [x] 2026-05-27: Codex tạo tracker trống cho các phần thực hành chưa hoàn thành của SPEC 2.
- [x] 2026-05-27: Codex kiểm tra `git diff --check -- src/doc/spec_2_processed.md`.
- [x] 2026-05-27: Codex chạy `repomix.cmd` từ repo root để refresh `repomix-output.xml`.
- [ ] Người thực hành cập nhật các checkbox và trường ghi nhận sau khi chạy WEKA.
</file>

<file path="src/doc/spec_2.md">
# SPEC 2 - Thực hành phân cụm và khai phá luật trên WEKA 3.8.7 với K=5

## 1. Mục tiêu

Tài liệu này mô tả chi tiết quy trình thực hành phân cụm và khai phá luật kết hợp bằng **WEKA 3.8.7** trên dữ liệu đặc trưng đã được sinh ở SPEC 1. SPEC 2 không xử lý ảnh hoặc video thô. Điểm bắt đầu của SPEC 2 là các file ARFF/CSV đã có trong `src/outputs`.

Trọng tâm của SPEC 2 là:

- mở và kiểm tra dữ liệu đặc trưng trong WEKA Explorer;
- chuẩn bị đúng bộ thuộc tính để phân cụm;
- chạy `SimpleKMeans` với **K=5** làm cấu hình thực hành chính;
- đọc centroid, kích thước cụm và phân bố train/test để hiểu các mẫu hành vi bình thường;
- tạo token `cluster=C0..C4` để phục vụ giải thích;
- rời rạc hóa đặc trưng liên tục;
- chạy `Apriori` để khai phá luật kết hợp trên dữ liệu normal;
- ghi lại báo cáo thực hành đủ chi tiết để chuyển kết quả sang pipeline Python ở SPEC sau.

Trong tài liệu này, **K=5 là cấu hình mặc định và bắt buộc**. Các giá trị K khác chỉ được nhắc như bối cảnh tham khảo từ lần thử trước, không phải yêu cầu thực hành chính của SPEC 2.

## 2. Liên hệ với PRD và các spec trước

Theo PRD, dự án có hai trụ cột nghiên cứu:

- phân cụm hành vi-không gian-thời gian để học mẫu bình thường theo vùng camera;
- khai phá luật kết hợp để giải thích quan hệ giữa vùng, chuyển động, mật độ, độ sáng và cụm hành vi.

SPEC 1 đã chuẩn bị dữ liệu đầu vào cho hai trụ cột này:

- đọc UCSD Ped1/Ped2 dạng chuỗi frame `.tif`;
- đọc CUHK Avenue dạng video `.avi`;
- resize, grayscale và blur theo cấu hình;
- chia frame thành grid `12 x 16`;
- tạo cube độ dài `5`, stride `1`;
- trích xuất feature bằng frame differencing;
- xuất `features_train.csv`, `features_test.csv`;
- xuất ARFF cho WEKA.

SPEC 2 dùng WEKA như môi trường thực hành, phân tích và đối chiếu. Runtime chính của hệ thống vẫn nên được tự động hóa bằng Python ở SPEC sau để train hàng loạt theo cell, tính anomaly score, xuất heatmap và đánh giá ROC-AUC/EER.

## 3. Nguyên tắc thực hành SPEC 2

### 3.1. Cố định phiên bản và cấu hình chính

Phiên bản công cụ:

```text
WEKA: 3.8.7
JVM heap khuyến nghị: 4g đến 8g cho UCSD Ped2, cao hơn nếu thử Avenue
Giao diện chính: WEKA Explorer
Tab dùng chính: Preprocess, Cluster, Associate
```

Cấu hình phân cụm chính:

```text
Algorithm: weka.clusterers.SimpleKMeans
numClusters: 5
seed: 10
maxIterations: 500
preserveInstancesOrder: True
distanceFunction: EuclideanDistance
feature scaling: Standardize trước khi train
```

Quy ước cụm:

```text
cluster_id = C0, C1, C2, C3, C4
```

Không được diễn giải `C0..C4` như nhãn ngữ nghĩa cố định trước khi đọc centroid. Ý nghĩa cụm phải được suy ra từ centroid, kích thước cụm và phân bố feature sau khi chuẩn hóa.

### 3.2. Ưu tiên dữ liệu

Thực hành theo thứ tự:

1. `ucsd_ped2_features_train.arff`: dữ liệu chính để train và phân tích.
2. `ucsd_ped2_features_test.arff`: dữ liệu để kiểm tra phân bố cụm trên test.
3. `ucsd_ped1_features_train.arff`: mở rộng nếu Ped2 ổn.
4. `avenue_features_train.arff`: chỉ thử sau cùng vì dữ liệu lớn.

SPEC 2 phải hoàn thành trên UCSD Ped2 trước khi mở rộng sang dataset khác.

### 3.3. Không đưa metadata vào KMeans baseline

Các cột như `dataset`, `split`, `video_id`, `cube_id`, `start_frame_id`, `end_frame_id`, `center_frame_id` dùng để truy vết, không dùng để phân cụm. Nếu đưa vào mô hình, KMeans có thể học định danh hoặc thứ tự thời gian thay vì hành vi.

Với baseline hành vi chính, chỉ dùng:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Trong ARFF gốc, đây là các cột:

```text
11-14,23-24
```

## 4. Dữ liệu đầu vào

Các file ARFF mặc định:

```text
src/outputs/weka/
  ucsd_ped2_features_train.arff
  ucsd_ped2_features_test.arff
  ucsd_ped1_features_train.arff
  ucsd_ped1_features_test.arff
  avenue_features_train.arff
  avenue_features_test.arff
```

Các file CSV metadata tương ứng:

```text
src/outputs/preprocessed/{dataset}/
  features_train.csv
  features_test.csv
  frames_manifest.csv
  videos_manifest.csv
  grid.json
  preprocess_stats.json
```

Kích thước dữ liệu hiện tại:

```text
UCSD Ped2 train:
  videos: 16
  frames: 2550
  cubes: 2486
  cells: 192
  feature rows: 477312

UCSD Ped2 test:
  videos: 12
  frames: 2010
  cubes: 1962
  cells: 192
  feature rows: 376704

UCSD Ped1 train:
  videos: 34
  frames: 6800
  cubes: 6664
  cells: 192
  feature rows: 1279488

UCSD Ped1 test:
  videos: 36
  frames: 7200
  cubes: 7056
  cells: 192
  feature rows: 1354752

Avenue train:
  videos: 16
  frames: 15328
  cubes: 15264
  cells: 192
  feature rows: 2930688

Avenue test:
  videos: 21
  frames: 15324
  cubes: 15240
  cells: 192
  feature rows: 2926080
```

Với Avenue, WEKA Explorer có thể chậm hoặc hết bộ nhớ. Không dùng Avenue làm bài thực hành đầu tiên.

## 5. Schema feature

ARFF từ SPEC 1 có các thuộc tính:

```text
1  dataset
2  split
3  video_id
4  cube_id
5  start_frame_id
6  end_frame_id
7  center_frame_id
8  cell_id
9  cell_row
10 cell_col
11 foreground_ratio
12 motion_magnitude_mean
13 motion_magnitude_std
14 motion_density
15 direction_hist_0
16 direction_hist_1
17 direction_hist_2
18 direction_hist_3
19 direction_hist_4
20 direction_hist_5
21 direction_hist_6
22 direction_hist_7
23 brightness_mean
24 brightness_delta
```

Ghi chú:

- `cell_id` là nominal, cần giữ khi khai phá luật hoặc truy vết, nhưng không dùng trực tiếp trong KMeans behavior-only.
- `cell_row`, `cell_col` chỉ dùng ở thí nghiệm spatio-behavior để kiểm tra ảnh hưởng vị trí.
- `direction_hist_0..7` hiện chưa mang thông tin hướng đáng tin cậy với `motion_method = frame_diff`, nên loại khỏi baseline KMeans.
- `brightness_mean` có thang đo khác motion feature, vì vậy phải dùng `Standardize` trước KMeans.

## 6. Bộ dữ liệu thực hành trong WEKA

### 6.1. Baseline A - Global behavior KMeans K=5

Mục tiêu: kiểm tra các mẫu hành vi tổng quát dựa trên motion và brightness, không cho model biết vị trí.

Giữ:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Trong ARFF gốc:

```text
Giữ: 11-14,23-24
Loại: 1-10,15-22
```

Đây là dataset chính của SPEC 2.

### 6.2. Baseline B - Global spatio-behavior KMeans K=5

Mục tiêu: kiểm tra nếu thêm vị trí cell thì cụm có bị chi phối bởi không gian hay không.

Giữ:

```text
cell_row
cell_col
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Trong ARFF gốc:

```text
Giữ: 9-14,23-24
Loại: 1-8,15-22
```

Baseline B chỉ dùng để phân tích. Nếu centroid chủ yếu tách theo `cell_row`, `cell_col`, không nên dùng nó làm baseline scoring chính.

### 6.3. Dataset C - Per-cell behavior KMeans K=5

Mục tiêu: bám sát PRD hơn vì mỗi cell/zone có normal pattern riêng.

Quy trình:

1. Lọc dữ liệu theo một `cell_id`.
2. Sau khi chỉ còn một cell, loại toàn bộ metadata.
3. Giữ 6 feature hành vi như Baseline A.
4. `Standardize`.
5. Chạy `SimpleKMeans` với `K=5`.

Cell khởi đầu đề xuất:

```text
04_08
06_08
08_05
09_06
```

Nếu muốn phân tích rộng hơn:

```text
04_08
05_08
06_08
07_08
08_05
08_06
09_05
09_06
```

Không chạy thủ công đủ `192` cell bằng Explorer. Nếu cần chạy tất cả cell, chuyển sang WEKA CLI hoặc Python.

### 6.4. Dataset D - Association rules sau KMeans K=5

Mục tiêu: tạo dữ liệu nominal để chạy Apriori.

Giữ:

```text
cell_id
motion_magnitude_mean
motion_density
brightness_mean
brightness_delta
cluster_id
```

Trong đó `cluster_id` là cụm `C0..C4` từ KMeans K=5. Nếu chưa có `cluster_id`, vẫn có thể chạy luật trên feature rời rạc hóa, nhưng phần rule sẽ thiếu token cụm hành vi.

Loại:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_row
cell_col
foreground_ratio nếu tạo quá nhiều luật nhiễu
motion_magnitude_std nếu khó diễn giải
direction_hist_0..7
```

## 7. Cấu trúc output thực hành

Kết quả thực hành nên lưu dưới thư mục output bị `.gitignore`:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/
  spec_2_log.md
  prepared/
    ucsd_ped2_train_behavior_k5_standardized.arff
    ucsd_ped2_test_behavior_k5_standardized.arff
    ucsd_ped2_train_spatiobehavior_k5_standardized.arff
    ucsd_ped2_train_rules_k5_discretized.arff
  models/
    ucsd_ped2_global_behavior_simplekmeans_k5.model
    ucsd_ped2_global_spatiobehavior_simplekmeans_k5.model
  reports/
    ucsd_ped2_global_behavior_simplekmeans_k5_train.txt
    ucsd_ped2_global_behavior_simplekmeans_k5_test.txt
    ucsd_ped2_global_spatiobehavior_simplekmeans_k5_train.txt
    ucsd_ped2_rules_apriori_k5.txt
  assignments/
    ucsd_ped2_train_behavior_k5_clusters.csv
    ucsd_ped2_test_behavior_k5_clusters.csv
  rules/
    ucsd_ped2_rules_k5_selected.md
```

Quy ước đặt tên:

```text
{dataset}_{scope}_{feature_set}_{algorithm}_k5
```

Ví dụ:

```text
ucsd_ped2_global_behavior_simplekmeans_k5
ucsd_ped2_global_spatiobehavior_simplekmeans_k5
ucsd_ped2_cell_08_05_behavior_simplekmeans_k5
ucsd_ped2_rules_discretize5_apriori_k5_s001_c06
```

## 8. Chuẩn bị WEKA 3.8.7

### 8.1. Mở WEKA với heap đủ lớn

Trên Windows, có thể mở bằng shortcut WEKA nếu dataset nhỏ. Với UCSD Ped2 full, nên mở bằng JVM heap rõ ràng:

```powershell
java -Xmx8g -cp "C:\Program Files\Weka-3-8-7\weka.jar" weka.gui.GUIChooser
```

Nếu đường dẫn khác, thay lại vị trí `weka.jar`.

Kiểm tra phiên bản:

1. Mở `GUIChooser`.
2. Chọn menu `Help`.
3. Chọn `About`.
4. Ghi vào log: `WEKA 3.8.7`.

### 8.2. Tạo log trước khi thao tác

Tạo hoặc mở:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/spec_2_log.md
```

Mỗi lần chạy cần ghi:

```text
experiment_id:
date:
weka_version:
input_file:
num_instances:
num_attributes_before_filter:
filter_steps:
algorithm:
parameters:
runtime:
output_files:
observations:
accepted_or_rejected:
reason:
```

Không chỉ chụp màn hình. Cần copy text output từ WEKA để kết quả có thể đọc lại.

## 9. Thực hành 1 - Mở và kiểm tra ARFF

### 9.1. Mở dữ liệu train

Trong WEKA Explorer:

1. Chọn `Explorer`.
2. Vào tab `Preprocess`.
3. Chọn `Open file`.
4. Mở:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Kiểm tra:

- số instances khoảng `477312`;
- số attributes là `24`;
- `cell_id` là nominal;
- các cột feature chính là numeric;
- không có missing value bất thường;
- relation name đọc được bình thường.

Ghi vào log:

```text
dataset = ucsd_ped2
split = train
instances = 477312
attributes = 24
```

### 9.2. Mở dữ liệu test

Lặp lại với:

```text
src/outputs/weka/ucsd_ped2_features_test.arff
```

Kiểm tra:

```text
instances = 376704
attributes = 24
```

Không apply filter lên test trước khi đã ghi rõ chuỗi filter dùng cho train.

## 10. Thực hành 2 - Tạo Baseline A behavior-only

### 10.1. Remove metadata và cột không dùng

Trong tab `Preprocess`, chọn filter:

```text
weka.filters.unsupervised.attribute.Remove
```

Cấu hình theo cách giữ cột:

```text
attributeIndices: 11-14,23-24
invertSelection: True
```

Sau khi nhấn `Apply`, dataset chỉ còn:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Điều kiện chấp nhận:

```text
instances = 477312
attributes = 6
all attributes = numeric
```

### 10.2. Xử lý missing value nếu có

Nếu WEKA báo missing value, dùng:

```text
weka.filters.unsupervised.attribute.ReplaceMissingValues
```

Kỳ vọng của SPEC 1 là không có missing value trong feature numeric. Nếu có missing value, phải ghi rõ cột nào và số lượng bao nhiêu.

### 10.3. Chuẩn hóa feature

Chọn filter:

```text
weka.filters.unsupervised.attribute.Standardize
```

Nhấn `Apply`.

Lý do bắt buộc:

- `brightness_mean` có thang `[0, 255]`;
- motion feature thường nhỏ hơn nhiều;
- KMeans dùng khoảng cách Euclidean nên sẽ bị lệch nếu không chuẩn hóa.

Sau khi chuẩn hóa, lưu dataset:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/
  ucsd_ped2_train_behavior_k5_standardized.arff
```

### 10.4. Lặp lại filter cho test

Mở `ucsd_ped2_features_test.arff` và áp dụng cùng chuỗi:

1. `Remove` giữ `11-14,23-24`.
2. `ReplaceMissingValues` nếu train đã dùng.
3. `Standardize`.

Lưu:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/
  ucsd_ped2_test_behavior_k5_standardized.arff
```

Lưu ý quan trọng: với thực hành Explorer, `Standardize` trên test có thể fit lại thống kê của test. Điều này chấp nhận được cho bài phân tích thủ công, nhưng khi triển khai production phải dùng scaler học từ train. Vì vậy, kết quả WEKA chỉ là thực nghiệm đối chiếu, không phải pipeline cuối.

## 11. Thực hành 3 - Chạy SimpleKMeans K=5 cho Baseline A

### 11.1. Cấu hình clusterer

Vào tab `Cluster`, chọn:

```text
weka.clusterers.SimpleKMeans
```

Cấu hình:

```text
numClusters: 5
seed: 10
maxIterations: 500
preserveInstancesOrder: True
displayStdDevs: True nếu có
dontReplaceMissingValues: False
distanceFunction: weka.core.EuclideanDistance
```

Nếu WEKA 3.8.7 hiển thị `initializationMethod`, dùng:

```text
initializationMethod: k-means++
```

Nếu giữ mặc định, ghi rõ trong log.

### 11.2. Chọn test mode

Đối với lần train đầu tiên:

```text
Test mode: Use training set
```

Nhấn `Start`.

Sau khi chạy xong, copy toàn bộ output vào:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/reports/
  ucsd_ped2_global_behavior_simplekmeans_k5_train.txt
```

Lưu model:

1. Nhấp phải vào result trong `Result list`.
2. Chọn `Save model`.
3. Lưu thành:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/models/
  ucsd_ped2_global_behavior_simplekmeans_k5.model
```

### 11.3. Kết quả tham chiếu đã có

Kết quả KMeans K=5 behavior-only đã từng ghi trong `src/doc/Kmean.md`:

```text
Algorithm: SimpleKMeans
Dataset: UCSD Ped2 train
Feature set: 11-14,23-24
K: 5
Iterations: 37
Within cluster sum of squared errors: 6491.182908722241

Train cluster sizes:
  C0: 27842  ( 6%)
  C1: 182948 (38%)
  C2: 16491  ( 3%)
  C3: 116779 (24%)
  C4: 133252 (28%)

Supplied test set cluster sizes:
  C0: 29347  ( 8%)
  C1: 123477 (33%)
  C2: 26080  ( 7%)
  C3: 93161  (25%)
  C4: 104639 (28%)
```

Centroid chuẩn hóa tham chiếu:

```text
C0:
  foreground_ratio          1.6539
  motion_magnitude_mean     1.6991
  motion_magnitude_std      2.2719
  motion_density            1.5698
  brightness_mean           0.4286
  brightness_delta         -0.0129

C1:
  foreground_ratio         -0.2889
  motion_magnitude_mean    -0.3077
  motion_magnitude_std     -0.3149
  motion_density           -0.2838
  brightness_mean           1.1285
  brightness_delta          0.0194

C2:
  foreground_ratio          4.3947
  motion_magnitude_mean     4.2516
  motion_magnitude_std      3.6867
  motion_density            4.4482
  brightness_mean           0.1163
  brightness_delta         -0.1427

C3:
  foreground_ratio         -0.3128
  motion_magnitude_mean    -0.3537
  motion_magnitude_std     -0.3873
  motion_density           -0.3026
  brightness_mean          -1.1691
  brightness_delta          0.0039

C4:
  foreground_ratio         -0.2187
  motion_magnitude_mean    -0.1487
  motion_magnitude_std     -0.1592
  motion_density           -0.2237
  brightness_mean          -0.6288
  brightness_delta          0.0100
```

Diễn giải khởi đầu:

- `C2` là cụm nhỏ, motion rất cao, có thể đại diện cho chuyển động mạnh hoặc nhiễu hiếm.
- `C0` là cụm motion cao vừa, không hiếm bằng `C2`.
- `C1`, `C3`, `C4` chủ yếu là motion thấp nhưng khác nhau theo độ sáng.
- Vì dữ liệu train là normal, cụm nhỏ không tự động đồng nghĩa bất thường. Cụm nhỏ chỉ là ứng viên để kiểm tra kỹ hơn ở bước scoring.

### 11.4. Tiêu chí chấp nhận kết quả K=5

Kết quả K=5 được xem là dùng được nếu:

- WEKA chạy xong không lỗi bộ nhớ;
- không có một cụm duy nhất chiếm gần toàn bộ dữ liệu;
- centroid có thể diễn giải bằng motion và brightness;
- có ít nhất một cụm motion cao hoặc motion hiếm để phân tích;
- train/test có phân bố cụm không lệch quá bất thường;
- model được lưu lại;
- report được copy ra file text.

## 12. Thực hành 4 - Supplied test set cho KMeans K=5

### 12.1. Chạy test trong tab Cluster

Sau khi train model trên train:

1. Giữ cấu hình `SimpleKMeans K=5`.
2. Trong `Test mode`, chọn `Supplied test set`.
3. Chọn file đã chuẩn bị:

```text
ucsd_ped2_test_behavior_k5_standardized.arff
```

4. Nhấn `Start`.
5. Copy output vào:

```text
src/outputs/weka_experiments/spec_2_weka_3_8_7/reports/
  ucsd_ped2_global_behavior_simplekmeans_k5_test.txt
```

Ghi lại:

```text
cluster sizes on test
runtime
nhận xét train/test distribution shift
```

### 12.2. Gán cụm về metadata

Mục tiêu cuối là có mapping:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cluster_id
```

Trong Explorer, việc export cluster assignment từng dòng có thể bất tiện với dữ liệu lớn. Có ba lựa chọn:

1. Dùng `preserveInstancesOrder=True`, xuất prediction/assignment nếu môi trường WEKA đang dùng hỗ trợ.
2. Dùng WEKA CLI để xuất cluster assignment theo thứ tự instance.
3. Chuyển sang Python ở SPEC 3 để gán cụm hàng loạt bằng model tương đương.

Trong SPEC 2, mức tối thiểu bắt buộc là ghi được phân bố cụm train/test và lưu model/report. File assignment từng dòng là khuyến nghị, không phải điều kiện duy nhất để hoàn thành phần thực hành WEKA thủ công.

Nếu xuất được assignment, join theo thứ tự dòng với:

```text
src/outputs/preprocessed/ucsd_ped2/features_train.csv
src/outputs/preprocessed/ucsd_ped2/features_test.csv
```

Không được làm mất các cột truy vết `video_id`, `cube_id`, `center_frame_id`, `cell_id`.

## 13. Thực hành 5 - Baseline B spatio-behavior KMeans K=5

### 13.1. Tạo dataset

Mở lại ARFF train gốc:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Filter `Remove`:

```text
attributeIndices: 9-14,23-24
invertSelection: True
```

Sau đó dùng:

```text
Standardize
```

Lưu:

```text
prepared/ucsd_ped2_train_spatiobehavior_k5_standardized.arff
```

### 13.2. Chạy SimpleKMeans K=5

Cấu hình giống Baseline A:

```text
SimpleKMeans
numClusters: 5
seed: 10
maxIterations: 500
preserveInstancesOrder: True
```

Lưu:

```text
models/ucsd_ped2_global_spatiobehavior_simplekmeans_k5.model
reports/ucsd_ped2_global_spatiobehavior_simplekmeans_k5_train.txt
```

### 13.3. Kết quả tham chiếu đã có

Kết quả đã ghi trong `src/doc/Kmean.md`:

```text
Algorithm: SimpleKMeans
Dataset: UCSD Ped2 train
Feature set: 9-14,23-24
K: 5
Iterations: 19
Within cluster sum of squared errors: 39528.79068974148

Train cluster sizes:
  C0: 103507 (22%)
  C1: 104017 (22%)
  C2: 34287  ( 7%)
  C3: 120553 (25%)
  C4: 114948 (24%)

Supplied test set cluster sizes:
  C0: 71095 (19%)
  C1: 74284 (20%)
  C2: 44780 (12%)
  C3: 94962 (25%)
  C4: 91583 (24%)
```

Nhận xét cần kiểm tra:

- nếu centroid của `cell_row`, `cell_col` tách cụm rất mạnh, model đang học vị trí nhiều hơn hành vi;
- nếu cụm motion cao vẫn xuất hiện rõ, Baseline B có thể hữu ích cho phân tích không gian;
- Baseline B không thay thế per-cell clustering trong pipeline chính.

## 14. Thực hành 6 - Per-cell KMeans K=5

### 14.1. Lý do

PRD nhấn mạnh rằng hành vi bình thường phụ thuộc vị trí. Một cell ở vỉa hè và một cell ở lòng đường có thể có motion normal khác nhau. Vì vậy, per-cell KMeans là hướng phù hợp hơn cho anomaly scoring, dù WEKA Explorer chỉ nên dùng để thử trên một số cell đại diện.

### 14.2. Lọc một cell trong WEKA Explorer

Mở:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Trong tab `Preprocess`, dùng filter instance:

```text
weka.filters.unsupervised.instance.RemoveWithValues
```

Cấu hình:

```text
attributeIndex: 8
nominalIndices: giá trị tương ứng với cell cần giữ
invertSelection: True
```

Vì `nominalIndices` phụ thuộc thứ tự giá trị nominal trong ARFF, cần kiểm tra danh sách giá trị của `cell_id` trong giao diện WEKA trước khi apply.

Nếu thao tác này khó hoặc dễ nhầm, tạo ARFF per-cell từ CSV bằng script riêng ở SPEC sau. Với SPEC 2, chỉ cần làm thủ công cho vài cell đại diện.

### 14.3. Chuẩn bị feature sau khi lọc cell

Sau khi chỉ còn một cell, dùng `Remove` để giữ:

```text
11-14,23-24
```

Sau đó:

```text
ReplaceMissingValues nếu cần
Standardize
SimpleKMeans K=5
```

Lưu report theo mẫu:

```text
reports/ucsd_ped2_cell_08_05_behavior_simplekmeans_k5.txt
```

### 14.4. Kết quả cần ghi cho mỗi cell

```text
cell_id:
num_train_instances:
feature_set:
algorithm: SimpleKMeans
K: 5
seed:
iterations:
within_cluster_sse:
cluster_sizes:
centroid_summary:
interpretation:
usable_for_python_pipeline: yes/no
notes:
```

Ví dụ diễn giải mong muốn:

```text
cell=08_05, K=5:
  C0: motion gần 0, brightness thấp
  C1: motion thấp, brightness cao
  C2: motion trung bình, density tăng
  C3: motion rất cao, cụm nhỏ
  C4: brightness_delta thay đổi mạnh, có thể là nhiễu ánh sáng
```

Không cần các cụm của mọi cell có cùng ý nghĩa. `C2` ở cell này không nhất thiết tương đương `C2` ở cell khác.

## 15. Thực hành 7 - Tạo dữ liệu cho khai phá luật

### 15.1. Mục tiêu rule mining

Apriori trong WEKA dùng để tìm các quan hệ phổ biến trong train normal, ví dụ:

```text
cell_id=08_05 brightness=normal -> motion_density=low
cluster_id=C1 motion_density=low -> brightness=bright
cell_id=06_08 motion=slow -> cluster_id=C3
```

Các luật này không trực tiếp là nhãn bất thường. Chúng mô tả pattern normal. Khi test vi phạm một luật có support/confidence tốt, pipeline sau có thể dùng nó như tín hiệu giải thích phụ.

### 15.2. Tạo cluster_id K=5

Nếu đã có cluster assignment từ KMeans, thêm cột:

```text
cluster_id
```

Giá trị:

```text
C0
C1
C2
C3
C4
```

Nếu chưa có assignment từng dòng, vẫn chạy Apriori không có `cluster_id`, nhưng phải ghi rõ:

```text
cluster_id not available in this WEKA manual run
```

### 15.3. Chọn thuộc tính cho Apriori

Dataset rules tối thiểu:

```text
cell_id
motion_magnitude_mean
motion_density
brightness_mean
brightness_delta
cluster_id
```

Có thể thêm:

```text
foreground_ratio
motion_magnitude_std
```

nhưng chỉ thêm nếu số luật không quá vụn và vẫn diễn giải được.

Không đưa:

```text
video_id
cube_id
frame_id
start_frame_id
end_frame_id
center_frame_id
direction_hist_0..7
```

## 16. Thực hành 8 - Rời rạc hóa feature numeric

### 16.1. Vì sao cần rời rạc hóa

`Apriori` trong WEKA làm việc tốt với thuộc tính nominal. Các feature như `motion_density`, `motion_magnitude_mean`, `brightness_mean` là numeric nên cần rời rạc hóa thành các khoảng.

Mục tiêu token:

```text
motion_magnitude_mean = still | slow | medium | fast | very_fast
motion_density = low | medium | high
brightness_mean = dark | normal | bright
brightness_delta = stable | changing
cluster_id = C0 | C1 | C2 | C3 | C4
cell_id = 08_05 | ...
```

WEKA có thể đặt tên bin dạng khoảng số. Khi viết báo cáo, cần dịch các khoảng số đó thành mô tả dễ hiểu.

### 16.2. Cách làm đơn giản trong WEKA

Trong tab `Preprocess`, sau khi chỉ giữ thuộc tính rule:

```text
weka.filters.unsupervised.attribute.Discretize
```

Cấu hình khởi đầu:

```text
bins: 5
useEqualFrequency: True
```

Áp dụng cho các cột numeric:

```text
motion_magnitude_mean
motion_density
brightness_mean
brightness_delta
foreground_ratio nếu có giữ
motion_magnitude_std nếu có giữ
```

Nếu `cluster_id` đang là numeric, chuyển nó sang nominal hoặc tạo lại dưới dạng chuỗi `C0..C4`.

Lưu:

```text
prepared/ucsd_ped2_train_rules_k5_discretized.arff
```

### 16.3. Cách diễn giải bin

Sau khi `Discretize`, WEKA có thể tạo khoảng như:

```text
motion_density='(-inf-0.002]'
motion_density='(0.002-0.015]'
motion_density='(0.015-inf)'
```

Trong báo cáo, diễn giải lại:

```text
motion_density bin thấp nhất  -> density=low
motion_density bin giữa       -> density=medium
motion_density bin cao nhất   -> density=high
motion_magnitude bin cao nhất -> motion=very_fast hoặc motion=high
```

Không cần sửa trực tiếp tên bin trong ARFF nếu chỉ làm thực hành WEKA, nhưng phần `selected.md` nên có bản diễn giải dễ đọc.

## 17. Thực hành 9 - Chạy Apriori trong WEKA 3.8.7

### 17.1. Mở tab Associate

Trong WEKA Explorer:

1. Vào tab `Associate`.
2. Chọn:

```text
weka.associations.Apriori
```

3. Dùng dataset đã rời rạc hóa:

```text
ucsd_ped2_train_rules_k5_discretized.arff
```

### 17.2. Cấu hình Apriori khởi đầu

Cấu hình:

```text
numRules: 50
metricType: Confidence
minMetric: 0.6
lowerBoundMinSupport: 0.01
upperBoundMinSupport: 1.0
delta: 0.05
```

Sau lần đầu, thử các ngưỡng:

```text
support = 0.005, 0.01, 0.02
confidence = 0.6, 0.7, 0.8
numRules = 50 hoặc 100
```

Mỗi tổ hợp phải ghi rõ trong log. Không chọn luật chỉ vì confidence cao nếu support quá thấp hoặc luật vô nghĩa.

### 17.3. Lưu kết quả

Copy output vào:

```text
reports/ucsd_ped2_rules_apriori_k5.txt
```

Chọn lại 5 đến 10 luật tốt nhất và viết vào:

```text
rules/ucsd_ped2_rules_k5_selected.md
```

Mỗi luật được chọn cần có:

```text
rule_id:
antecedent:
consequent:
support:
confidence:
lift nếu WEKA output có:
interpretation:
why_useful_for_anomaly_explanation:
warning:
```

### 17.4. Tiêu chí chọn luật tốt

Ưu tiên luật:

- có `cell_id` hoặc `cluster_id`;
- liên quan đến motion, density hoặc brightness;
- có support đủ lớn để không quá hiếm;
- có confidence đủ cao để diễn giải normal pattern;
- không chỉ lặp lại quan hệ hiển nhiên;
- có thể chuyển thành câu giải thích khi test bất thường.

Loại luật:

- chỉ nói về định danh hoặc thứ tự thời gian;
- confidence cao nhưng support cực thấp;
- luật do bin quá nhỏ tạo ra;
- luật không giúp giải thích hành vi;
- luật trùng lặp nhiều lần với nội dung gần giống nhau.

## 18. Ví dụ diễn giải rule

Ví dụ nếu WEKA sinh luật:

```text
cell_id=08_05 brightness_mean='normal' ==> motion_density='low'
```

Diễn giải:

```text
Trong cell 08_05, khi độ sáng ở mức bình thường, dữ liệu train normal thường có mật độ chuyển động thấp.
Nếu test xuất hiện cell=08_05, brightness=normal nhưng motion_density=high, đây là tổ hợp đáng chú ý.
```

Ví dụ nếu có `cluster_id`:

```text
cell_id=06_08 motion_magnitude_mean='slow' ==> cluster_id=C1
```

Diễn giải:

```text
Ở cell 06_08, chuyển động chậm thường rơi vào cụm C1.
Nếu mẫu mới có motion=slow nhưng rơi xa centroid C1 hoặc bị gán sang cụm motion cao, có thể dùng rule này để giải thích lệch mẫu.
```

Ví dụ luật cần loại:

```text
cluster_id=C1 ==> brightness_mean='bright'
```

Luật này chỉ hữu ích nếu `C1` đã được diễn giải rõ. Nếu không có `cell_id` hoặc motion token đi kèm, rule có thể quá chung và khó dùng cho cảnh báo.

## 19. CLI tham khảo để tái lập ngoài Explorer

WEKA Explorer là giao diện chính của SPEC 2. CLI dưới đây dùng để tái lập hoặc chạy batch khi GUI chậm. Kiểm tra lại path `weka.jar` trước khi chạy.

```powershell
$WEKA_JAR = "C:\Program Files\Weka-3-8-7\weka.jar"
```

Tạo Baseline A:

```powershell
java -Xmx8g -cp $WEKA_JAR weka.filters.unsupervised.attribute.Remove `
  -R 11-14,23-24 -V `
  -i src/outputs/weka/ucsd_ped2_features_train.arff `
  -o src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/ucsd_ped2_train_behavior_k5.arff
```

Standardize:

```powershell
java -Xmx8g -cp $WEKA_JAR weka.filters.unsupervised.attribute.Standardize `
  -i src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/ucsd_ped2_train_behavior_k5.arff `
  -o src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/ucsd_ped2_train_behavior_k5_standardized.arff
```

Train SimpleKMeans K=5:

```powershell
java -Xmx8g -cp $WEKA_JAR weka.clusterers.SimpleKMeans `
  -N 5 -S 10 -I 500 -P `
  -t src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/ucsd_ped2_train_behavior_k5_standardized.arff `
  > src/outputs/weka_experiments/spec_2_weka_3_8_7/reports/ucsd_ped2_global_behavior_simplekmeans_k5_train.txt
```

Chạy Apriori trên dữ liệu đã rời rạc hóa:

```powershell
java -Xmx8g -cp $WEKA_JAR weka.associations.Apriori `
  -N 50 -T 0 -C 0.6 -M 0.01 `
  -t src/outputs/weka_experiments/spec_2_weka_3_8_7/prepared/ucsd_ped2_train_rules_k5_discretized.arff `
  > src/outputs/weka_experiments/spec_2_weka_3_8_7/reports/ucsd_ped2_rules_apriori_k5.txt
```

Nếu option CLI khác với GUI trong máy đang dùng, ưu tiên cấu hình hiển thị trong WEKA 3.8.7 và ghi lại option thực tế vào log.

## 20. Thí nghiệm bắt buộc của SPEC 2

### Experiment 1 - Global behavior KMeans K=5

Input:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Feature:

```text
11-14,23-24
```

Filter:

```text
Remove
Standardize
```

Algorithm:

```text
SimpleKMeans
K=5
seed=10
maxIterations=500
```

Kết quả cần có:

- report train;
- model `.model`;
- cluster sizes;
- centroid;
- diễn giải 5 cụm;
- nhận xét cụm motion cao/hiếm.

### Experiment 2 - Supplied test set cho global behavior K=5

Input:

```text
src/outputs/weka/ucsd_ped2_features_test.arff
```

Yêu cầu:

- áp dụng cùng feature set;
- chạy supplied test set;
- ghi phân bố cụm test;
- so sánh với train;
- nếu xuất được assignment thì join với metadata CSV.

### Experiment 3 - Global spatio-behavior KMeans K=5

Feature:

```text
9-14,23-24
```

Mục tiêu:

- kiểm tra ảnh hưởng của `cell_row`, `cell_col`;
- xác định model có bị tách theo vị trí quá mạnh không;
- so sánh với Baseline A.

### Experiment 4 - Selected per-cell KMeans K=5

Cell tối thiểu:

```text
04_08
06_08
08_05
09_06
```

Mục tiêu:

- xem mỗi cell có đủ mẫu để chia 5 cụm không;
- đọc centroid theo cell;
- xác định per-cell KMeans có phù hợp để chuyển sang Python không.

### Experiment 5 - Apriori trên dữ liệu đã rời rạc hóa

Input:

```text
ucsd_ped2 train normal
feature rời rạc hóa
cluster_id C0..C4 nếu đã có
```

Yêu cầu:

- chạy ít nhất một cấu hình Apriori;
- sinh tối thiểu 20 luật thô nếu dữ liệu cho phép;
- chọn 5 đến 10 luật dễ giải thích;
- ghi rõ support/confidence;
- nhận xét luật nào có thể dùng làm reason trong anomaly alert.

## 21. Mẫu log thực nghiệm

```markdown
## ucsd_ped2_global_behavior_simplekmeans_k5

- Date:
- WEKA version: 3.8.7
- Input: src/outputs/weka/ucsd_ped2_features_train.arff
- Instances: 477312
- Original attributes: 24
- Kept attributes: 11-14,23-24
- Filters:
  - Remove(attributeIndices=11-14,23-24, invertSelection=True)
  - Standardize
- Algorithm: SimpleKMeans
- Parameters:
  - numClusters=5
  - seed=10
  - maxIterations=500
  - preserveInstancesOrder=True
- Runtime:
- Within cluster SSE:
- Cluster sizes:
  - C0:
  - C1:
  - C2:
  - C3:
  - C4:
- Centroid interpretation:
  - C0:
  - C1:
  - C2:
  - C3:
  - C4:
- Accepted for next step: yes/no
- Reason:
- Output files:
```

Mẫu log luật:

```markdown
## ucsd_ped2_rules_discretize5_apriori_k5_s001_c06

- Date:
- WEKA version: 3.8.7
- Input:
- Attributes:
- Discretize:
  - bins=5
  - useEqualFrequency=True
- Apriori:
  - lowerBoundMinSupport=0.01
  - minMetric=0.6
  - metricType=Confidence
  - numRules=50
- Raw rules count:
- Selected rules:
  1. rule:
     support:
     confidence:
     interpretation:
     use for anomaly explanation:
- Rejected rule patterns:
- Notes:
```

## 22. Tiêu chí đánh giá kết quả

### 22.1. KMeans K=5 đạt yêu cầu khi

- train UCSD Ped2 chạy xong trên WEKA 3.8.7;
- K được đặt đúng bằng `5`;
- có 5 centroid được ghi lại;
- cluster size không sụp vào một cụm duy nhất;
- centroid có thể diễn giải bằng motion/brightness;
- phân bố test không quá lệch so với train mà không có giải thích;
- model và report được lưu;
- log có đủ filter, feature, seed, runtime và nhận xét.

### 22.2. Per-cell KMeans đạt yêu cầu khi

- chạy được tối thiểu 4 cell đại diện;
- mỗi cell có report K=5 riêng;
- có nhận xét cụm nào là motion thấp, motion cao, brightness khác biệt hoặc nhiễu;
- ghi rõ cell nào không phù hợp với K=5 nếu cụm quá nhỏ hoặc centroid khó diễn giải.

### 22.3. Apriori đạt yêu cầu khi

- dữ liệu đầu vào là nominal hoặc đã discretize;
- có ít nhất một lần chạy Apriori thành công;
- chọn được 5 đến 10 luật có thể diễn giải;
- luật được ghi với support/confidence;
- có nhận xét luật dùng được như reason hay chỉ dùng tham khảo;
- không xem luật là quyết định bất thường độc lập.

## 23. Rủi ro và giảm thiểu

### 23.1. WEKA thiếu bộ nhớ

Rủi ro: ARFF lớn làm Explorer chậm hoặc crash.

Giảm thiểu:

- chạy UCSD Ped2 trước;
- mở WEKA bằng `-Xmx8g`;
- không mở Avenue full ngay;
- tạo sample ARFF nếu cần;
- dùng CLI hoặc Python khi chạy batch.

### 23.2. Standardize trên train/test trong Explorer không dùng chung thống kê

Rủi ro: thao tác thủ công có thể chuẩn hóa test bằng thống kê test.

Giảm thiểu:

- ghi rõ đây là thực nghiệm WEKA;
- dùng kết quả để phân tích, không dùng làm production;
- ở SPEC 3, dùng scaler fit trên train rồi transform test bằng Python.

### 23.3. K=5 không phù hợp với mọi cell

Rủi ro: cell ít chuyển động hoặc rất yên tĩnh có thể không chia được 5 cụm có ý nghĩa.

Giảm thiểu:

- vẫn thực hành K=5 để thống nhất bài làm;
- ghi cell nào có cụm quá nhỏ;
- ở pipeline sau, thêm `min_samples_per_cell`, threshold floor hoặc fallback.

### 23.4. Cụm nhỏ không đồng nghĩa bất thường

Rủi ro: train normal vẫn có cụm motion cao hoặc hiếm, nhưng đó có thể là normal pattern ít gặp.

Giảm thiểu:

- không gán nhãn bất thường chỉ dựa vào cluster size;
- dùng distance tới centroid và percentile threshold ở SPEC sau;
- kết hợp smoothing, rare token và rule violation.

### 23.5. Apriori sinh luật quá vụn

Rủi ro: nhiều `cell_id` và nhiều bin làm support thấp.

Giảm thiểu:

- bắt đầu với `minSupport=0.01`;
- giảm số thuộc tính nếu luật quá nhiều;
- dùng 3 hoặc 5 bin, không rời rạc quá mịn;
- chọn luật có ý nghĩa giải thích, không chọn máy móc theo confidence.

### 23.6. Luật chỉ phản ánh độ sáng hoặc vị trí

Rủi ro: rule mining có thể sinh luật đúng nhưng không hữu ích cho anomaly.

Giảm thiểu:

- ưu tiên luật có motion/density/cluster;
- loại luật chỉ mô tả brightness nếu không liên quan hành vi;
- ghi rõ luật dùng để giải thích phụ, không thay score chính.

## 24. Checklist thực hành

- [ ] Xác nhận WEKA version là `3.8.7`.
- [ ] Mở WEKA với heap đủ lớn.
- [ ] Mở `ucsd_ped2_features_train.arff`.
- [ ] Kiểm tra số instances và attributes.
- [ ] Tạo Baseline A bằng `Remove` giữ `11-14,23-24`.
- [ ] Dùng `Standardize` cho Baseline A.
- [ ] Lưu `ucsd_ped2_train_behavior_k5_standardized.arff`.
- [ ] Chạy `SimpleKMeans K=5` trên Baseline A.
- [ ] Lưu model K=5.
- [ ] Lưu report train K=5.
- [ ] Diễn giải 5 centroid.
- [ ] Mở `ucsd_ped2_features_test.arff`.
- [ ] Áp dụng cùng feature set cho test.
- [ ] Chạy supplied test set và ghi phân bố cụm.
- [ ] Tạo Baseline B giữ `9-14,23-24`.
- [ ] Chạy `SimpleKMeans K=5` cho Baseline B.
- [ ] So sánh Baseline A và Baseline B.
- [ ] Chạy per-cell KMeans K=5 cho ít nhất 4 cell.
- [ ] Tạo dataset rule có `cell_id`, motion/density/brightness và `cluster_id` nếu có.
- [ ] Rời rạc hóa bằng `Discretize`.
- [ ] Chạy Apriori.
- [ ] Chọn 5 đến 10 luật dễ giải thích.
- [ ] Ghi toàn bộ kết quả vào `spec_2_log.md`.
- [ ] Cập nhật `src/doc/spec_2_processed.md`.

## 25. Tiêu chí hoàn thành SPEC 2

SPEC 2 được xem là hoàn thành khi:

- có bản thực hành WEKA 3.8.7 rõ ràng cho UCSD Ped2;
- phân cụm chính dùng `SimpleKMeans` với `K=5`;
- có report centroid và cluster size của global behavior K=5;
- có phân bố cụm trên supplied test set;
- có ít nhất một phân tích spatio-behavior K=5;
- có ít nhất bốn thử nghiệm per-cell K=5 hoặc ghi rõ lý do không thể thao tác trong WEKA;
- có ít nhất một lần chạy Apriori trên dữ liệu đã rời rạc hóa;
- có 5 đến 10 luật được chọn và diễn giải;
- có log thực nghiệm đủ để tái hiện;
- có nhận xét cấu hình nào nên chuyển sang Python ở SPEC 3 và SPEC 5.

## 26. Hướng chuyển sang các spec sau

Sau SPEC 2:

- SPEC 3 dùng Python để train per-cell MiniBatchKMeans với `clusters_per_cell=5`;
- SPEC 3 tính distance tới centroid gần nhất và threshold theo percentile train;
- SPEC 5 tạo token `cluster=C0..C4`, rời rạc hóa feature và tính rare token/rule violation score;
- SPEC 4 và SPEC 6 dùng score để vẽ heatmap và đánh giá định lượng;
- WEKA tiếp tục đóng vai trò công cụ phân tích, không phải runtime production.
</file>

<file path="src/kpdl_preprocess/cli.py">
from __future__ import annotations

import argparse
import json
from pathlib import Path

from .pipeline import run_preprocess


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Run SPEC 1 preprocessing for UCSD/CUHK Avenue.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative dataset paths are resolved here.")
    parser.add_argument("--output-root", default=None, help="Override output root, for example outputs/preprocessed_smoke.")
    parser.add_argument("--split", choices=["train", "test"], default=None, help="Process only one split.")
    parser.add_argument("--limit-videos", type=int, default=None, help="Limit videos/sequences per split for smoke tests.")
    parser.add_argument("--limit-frames", type=int, default=None, help="Limit frames per video/sequence for smoke tests.")
    parser.add_argument(
        "--progress-every",
        type=int,
        default=250,
        help="Print progress every N frames while processing videos/sequences. Use 0 to disable.",
    )
    parser.add_argument("--export-arff", action="store_true", help="Also export WEKA ARFF files from feature CSV outputs.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    stats = run_preprocess(
        config_path=args.config,
        project_root=Path(args.project_root),
        output_root=args.output_root,
        split=args.split,
        limit_videos=args.limit_videos,
        limit_frames=args.limit_frames,
        progress_every=args.progress_every,
        export_arff=args.export_arff,
    )
    print(json.dumps(_public_stats(stats), indent=2))
    return 0


def _public_stats(stats: dict) -> dict:
    return {
        "dataset": stats.get("dataset"),
        "output_dir": stats.get("output_dir"),
        "splits": stats.get("splits"),
        "arff_outputs": stats.get("arff_outputs", []),
    }
</file>

<file path="src/kpdl_preprocess/pipeline.py">
from __future__ import annotations

import csv
import json
import sys
from collections import defaultdict, deque
from datetime import datetime, timezone
from pathlib import Path

from .arff import convert_csv_to_arff
from .config import get_nested, load_config, resolve_path
from .datasets import scan_dataset
from .features import extract_cube_features
from .grid import generate_grid, grid_to_json
from .readers import iter_preprocessed_frames, probe_video
from .records import FrameRecord, VideoSource
from .schema import FRAME_MANIFEST_COLUMNS, SCHEMA_VERSION, VIDEO_MANIFEST_COLUMNS, feature_columns
from .utils import ensure_dir


def run_preprocess(
    config_path: str | Path,
    project_root: str | Path,
    output_root: str | Path | None = None,
    split: str | None = None,
    limit_videos: int | None = None,
    limit_frames: int | None = None,
    progress_every: int | None = None,
    export_arff: bool = False,
) -> dict:
    config = load_config(config_path)
    project_root = Path(project_root).resolve()
    dataset = str(get_nested(config, "data", "dataset"))
    direction_bins = int(get_nested(config, "features", "direction_bins", default=8))
    resize_width = int(get_nested(config, "video", "resize_width"))
    resize_height = int(get_nested(config, "video", "resize_height"))
    grid_rows = int(get_nested(config, "grid", "rows"))
    grid_cols = int(get_nested(config, "grid", "cols"))
    ignore_cells = list(get_nested(config, "grid", "ignore_cells", default=[]) or [])
    cube_length = int(get_nested(config, "cube", "length"))
    cube_stride = int(get_nested(config, "cube", "stride"))

    output_root_value = output_root or get_nested(config, "output", "root", default="outputs/preprocessed")
    output_base = ensure_dir(resolve_path(output_root_value, project_root) / dataset)

    cells = generate_grid(resize_width, resize_height, grid_rows, grid_cols, ignore_cells)
    grid_payload = grid_to_json(cells, resize_width, resize_height, grid_rows, grid_cols)
    with (output_base / "grid.json").open("w", encoding="utf-8") as handle:
        json.dump(grid_payload, handle, indent=2)

    sources = scan_dataset(config, project_root, split)
    sources = _limit_sources_per_split(sources, limit_videos)
    active_splits = sorted({source.split for source in sources} or ({split} if split else {"train", "test"}))

    stats = _new_stats(dataset, grid_payload, config_path, active_splits, direction_bins)
    feature_paths: dict[str, Path] = {}

    with (output_base / "frames_manifest.csv").open("w", newline="", encoding="utf-8") as frames_file, (
        output_base / "videos_manifest.csv"
    ).open("w", newline="", encoding="utf-8") as videos_file:
        frame_writer = csv.DictWriter(frames_file, fieldnames=FRAME_MANIFEST_COLUMNS)
        video_writer = csv.DictWriter(videos_file, fieldnames=VIDEO_MANIFEST_COLUMNS)
        frame_writer.writeheader()
        video_writer.writeheader()

        feature_files = {}
        feature_writers = {}
        try:
            columns = feature_columns(direction_bins)
            for split_name in active_splits:
                feature_path = output_base / f"features_{split_name}.csv"
                feature_paths[split_name] = feature_path
                handle = feature_path.open("w", newline="", encoding="utf-8")
                feature_files[split_name] = handle
                writer = csv.DictWriter(handle, fieldnames=columns, extrasaction="ignore")
                writer.writeheader()
                feature_writers[split_name] = writer

            source_total = len(sources)
            for source_index, source in enumerate(sources, start=1):
                _process_source(
                    source=source,
                    config=config,
                    cells=cells,
                    frame_writer=frame_writer,
                    video_writer=video_writer,
                    feature_writer=feature_writers[source.split],
                    stats=stats,
                    source_index=source_index,
                    source_total=source_total,
                    cube_length=cube_length,
                    cube_stride=cube_stride,
                    limit_frames=limit_frames,
                    progress_every=progress_every,
                )
        finally:
            for handle in feature_files.values():
                handle.close()

    _finalize_stats(stats)

    stats["output_dir"] = str(output_base)

    stats_path = output_base / "preprocess_stats.json"
    with stats_path.open("w", encoding="utf-8") as handle:
        json.dump(stats, handle, indent=2)

    arff_paths: list[str] = []
    if export_arff:
        weka_root_value = get_nested(config, "output", "weka_root", default="src/outputs/weka")
        weka_root = ensure_dir(resolve_path(weka_root_value, project_root))
        for split_name, feature_path in feature_paths.items():
            arff_path = weka_root / f"{dataset}_features_{split_name}.arff"
            convert_csv_to_arff(feature_path, arff_path, relation=f"{dataset}_features_{split_name}")
            arff_paths.append(str(arff_path))
        stats["arff_outputs"] = arff_paths
        with stats_path.open("w", encoding="utf-8") as handle:
            json.dump(stats, handle, indent=2)

    return stats


def _process_source(
    source: VideoSource,
    config: dict,
    cells: list,
    frame_writer: csv.DictWriter,
    video_writer: csv.DictWriter,
    feature_writer: csv.DictWriter,
    stats: dict,
    source_index: int,
    source_total: int,
    cube_length: int,
    cube_stride: int,
    limit_frames: int | None,
    progress_every: int | None,
) -> None:
    split_stats = stats["splits"][source.split]
    split_stats["num_videos"] += 1

    video_probe = probe_video(source)
    num_frames = 0
    num_cubes = 0
    original_width = video_probe.get("original_width")
    original_height = video_probe.get("original_height")
    expected_frames = _expected_frame_count(video_probe.get("num_frames_hint"), limit_frames)
    buffer: deque[FrameRecord] = deque(maxlen=cube_length)
    should_log_progress = progress_every is not None and progress_every > 0

    if should_log_progress:
        _progress(
            f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
            f"start, expected_frames={_blank_none(expected_frames)}"
        )

    try:
        for frame in iter_preprocessed_frames(source, config, limit_frames=limit_frames):
            num_frames += 1
            if original_width is None:
                original_width = frame.original_width
            if original_height is None:
                original_height = frame.original_height
            frame_writer.writerow(_frame_manifest_row(frame))
            buffer.append(frame)

            if len(buffer) == cube_length:
                start_index = num_frames - cube_length
                if start_index % cube_stride == 0:
                    num_cubes += 1
                    cube = list(buffer)
                    cube_id = f"{source.video_id}_{cube[0].frame_id:06d}_{cube[-1].frame_id:06d}"
                    rows = extract_cube_features(cube_id, cube, cells, config)
                    for row in rows:
                        feature_writer.writerow(row)
                        split_stats["num_feature_rows"] += 1
                        split_stats["_motion_density_sum"] += float(row["motion_density"])
                        split_stats["_brightness_sum"] += float(row["brightness_mean"])
                        split_stats["_feature_value_count"] += 1
            if should_log_progress and num_frames % progress_every == 0:
                _progress(
                    f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                    f"frames={num_frames}{_progress_total(expected_frames)}, cubes={num_cubes}"
                )

        split_stats["num_frames"] += num_frames
        split_stats["num_cubes"] += num_cubes
        if expected_frames is not None and num_frames < expected_frames:
            split_stats["missing_frames"] += expected_frames - num_frames
        video_writer.writerow(
            {
                "dataset": source.dataset,
                "split": source.split,
                "video_id": source.video_id,
                "source_path": str(source.source_path),
                "input_type": source.input_type,
                "fps": _blank_none(video_probe.get("fps")),
                "num_frames": num_frames,
                "num_cubes": num_cubes,
                "original_width": _blank_none(original_width),
                "original_height": _blank_none(original_height),
                "failed": False,
                "error": "",
            }
        )
        if should_log_progress:
            _progress(
                f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                f"done, frames={num_frames}{_progress_total(expected_frames)}, cubes={num_cubes}"
            )
    except Exception as exc:
        split_stats["failed_videos"] += 1
        video_writer.writerow(
            {
                "dataset": source.dataset,
                "split": source.split,
                "video_id": source.video_id,
                "source_path": str(source.source_path),
                "input_type": source.input_type,
                "fps": _blank_none(video_probe.get("fps")),
                "num_frames": num_frames,
                "num_cubes": num_cubes,
                "original_width": _blank_none(original_width),
                "original_height": _blank_none(original_height),
                "failed": True,
                "error": str(exc),
            }
        )
        if should_log_progress:
            _progress(
                f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                f"failed after frames={num_frames}, error={exc}"
            )


def _frame_manifest_row(frame: FrameRecord) -> dict[str, object]:
    return {
        "dataset": frame.dataset,
        "split": frame.split,
        "video_id": frame.video_id,
        "frame_id": frame.frame_id,
        "timestamp": _blank_none(frame.timestamp),
        "source_path": str(frame.source_path),
        "original_width": frame.original_width,
        "original_height": frame.original_height,
        "resized_width": frame.resized_width,
        "resized_height": frame.resized_height,
    }


def _limit_sources_per_split(sources: list[VideoSource], limit_videos: int | None) -> list[VideoSource]:
    if limit_videos is None:
        return sources
    counts: dict[str, int] = defaultdict(int)
    limited: list[VideoSource] = []
    for source in sources:
        if counts[source.split] >= limit_videos:
            continue
        limited.append(source)
        counts[source.split] += 1
    return limited


def _new_stats(
    dataset: str,
    grid_payload: dict,
    config_path: str | Path,
    splits: list[str],
    direction_bins: int,
) -> dict:
    return {
        "dataset": dataset,
        "generated_at": datetime.now(timezone.utc).isoformat(),
        "config_path": str(config_path),
        "schema": {
            "version": SCHEMA_VERSION,
            "frames_manifest_columns": FRAME_MANIFEST_COLUMNS,
            "videos_manifest_columns": VIDEO_MANIFEST_COLUMNS,
            "feature_columns": feature_columns(direction_bins),
        },
        "grid": {
            "rows": grid_payload["rows"],
            "cols": grid_payload["cols"],
            "num_cells": grid_payload["num_cells"],
            "resized_width": grid_payload["resized_width"],
            "resized_height": grid_payload["resized_height"],
        },
        "splits": {split: _empty_split_stats(grid_payload["num_cells"]) for split in splits},
    }


def _empty_split_stats(num_cells: int) -> dict:
    return {
        "num_videos": 0,
        "num_frames": 0,
        "num_cubes": 0,
        "num_cells": num_cells,
        "num_feature_rows": 0,
        "missing_frames": 0,
        "failed_videos": 0,
        "avg_motion_density": 0.0,
        "avg_brightness": 0.0,
        "_motion_density_sum": 0.0,
        "_brightness_sum": 0.0,
        "_feature_value_count": 0,
    }


def _finalize_stats(stats: dict) -> None:
    for split_stats in stats["splits"].values():
        count = split_stats.pop("_feature_value_count", 0)
        motion_sum = split_stats.pop("_motion_density_sum", 0.0)
        brightness_sum = split_stats.pop("_brightness_sum", 0.0)
        if count:
            split_stats["avg_motion_density"] = motion_sum / count
            split_stats["avg_brightness"] = brightness_sum / count


def _blank_none(value: object) -> object:
    return "" if value is None else value


def _expected_frame_count(num_frames_hint: object, limit_frames: int | None) -> int | None:
    try:
        hint = int(num_frames_hint) if num_frames_hint is not None else None
    except (TypeError, ValueError):
        hint = None
    if hint is not None and hint <= 0:
        hint = None
    if limit_frames is not None:
        return min(limit_frames, hint) if hint is not None else limit_frames
    return hint


def _progress_total(expected_frames: int | None) -> str:
    return f"/{expected_frames}" if expected_frames is not None else ""


def _progress(message: str) -> None:
    print(message, file=sys.stderr, flush=True)
</file>

<file path="src/kpdl_preprocess/readers.py">
from __future__ import annotations

from pathlib import Path
from typing import Iterator

import numpy as np

from .config import ConfigError, get_nested
from .records import FrameRecord, VideoSource
from .utils import bool_config, sorted_natural, stem_to_frame_id


def _cv2():
    try:
        import cv2
    except ImportError as exc:
        raise ConfigError(
            "OpenCV is required for preprocessing. "
            "Install dependencies with: python -m pip install -r src/requirements.txt"
        ) from exc
    return cv2


def iter_preprocessed_frames(
    source: VideoSource,
    config: dict,
    limit_frames: int | None = None,
) -> Iterator[FrameRecord]:
    if source.input_type == "frame_sequence":
        yield from _iter_frame_sequence(source, config, limit_frames)
        return
    if source.input_type == "video":
        yield from _iter_video(source, config, limit_frames)
        return
    raise ConfigError(f"Unsupported source input_type: {source.input_type}")


def probe_video(source: VideoSource) -> dict:
    if source.input_type != "video":
        return {"fps": None, "num_frames_hint": None, "original_width": None, "original_height": None}

    cv2 = _cv2()
    cap = cv2.VideoCapture(str(source.source_path))
    if not cap.isOpened():
        return {"fps": None, "num_frames_hint": None, "original_width": None, "original_height": None}
    try:
        fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0) or None
        count = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) or None
        width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH) or 0) or None
        height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT) or 0) or None
        return {"fps": fps, "num_frames_hint": count, "original_width": width, "original_height": height}
    finally:
        cap.release()


def _iter_frame_sequence(
    source: VideoSource,
    config: dict,
    limit_frames: int | None,
) -> Iterator[FrameRecord]:
    cv2 = _cv2()
    files = sorted_natural(source.source_path.glob("*.tif"))
    if limit_frames is not None:
        files = files[:limit_frames]

    for index, image_path in enumerate(files, start=1):
        raw = cv2.imread(str(image_path), cv2.IMREAD_UNCHANGED)
        if raw is None:
            continue
        gray, original_width, original_height = preprocess_frame(raw, config)
        yield FrameRecord(
            dataset=source.dataset,
            split=source.split,
            video_id=source.video_id,
            frame_id=stem_to_frame_id(image_path, index),
            timestamp=float(index - 1),
            source_path=image_path,
            original_width=original_width,
            original_height=original_height,
            resized_width=gray.shape[1],
            resized_height=gray.shape[0],
            gray=gray,
        )


def _iter_video(
    source: VideoSource,
    config: dict,
    limit_frames: int | None,
) -> Iterator[FrameRecord]:
    cv2 = _cv2()
    cap = cv2.VideoCapture(str(source.source_path))
    if not cap.isOpened():
        raise ConfigError(f"Cannot open video: {source.source_path}")

    fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0)
    frame_count_hint = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) or None
    safety_limit = _video_safety_limit(frame_count_hint, limit_frames)
    frame_index = 0
    try:
        while True:
            if limit_frames is not None and frame_index >= limit_frames:
                break
            if safety_limit is not None and frame_index >= safety_limit:
                break
            ok, raw = cap.read()
            if not ok:
                break
            frame_index += 1
            gray, original_width, original_height = preprocess_frame(raw, config)
            timestamp = (frame_index / fps) if fps > 0 else None
            yield FrameRecord(
                dataset=source.dataset,
                split=source.split,
                video_id=source.video_id,
                frame_id=frame_index,
                timestamp=timestamp,
                source_path=source.source_path,
                original_width=original_width,
                original_height=original_height,
                resized_width=gray.shape[1],
                resized_height=gray.shape[0],
                gray=gray,
            )
    finally:
        cap.release()


def preprocess_frame(raw: np.ndarray, config: dict) -> tuple[np.ndarray, int, int]:
    cv2 = _cv2()
    resize_width = int(get_nested(config, "video", "resize_width"))
    resize_height = int(get_nested(config, "video", "resize_height"))

    original_height, original_width = raw.shape[:2]
    resized = cv2.resize(raw, (resize_width, resize_height), interpolation=cv2.INTER_AREA)

    if resized.ndim == 2:
        gray = resized
    elif resized.shape[2] == 4:
        gray = cv2.cvtColor(resized, cv2.COLOR_BGRA2GRAY)
    else:
        gray = cv2.cvtColor(resized, cv2.COLOR_BGR2GRAY)

    if gray.dtype != np.uint8:
        gray = np.clip(gray, 0, 255).astype(np.uint8)

    if bool_config(get_nested(config, "video", "blur", "enabled"), default=False):
        kernel_size = int(get_nested(config, "video", "blur", "kernel_size", default=3))
        if kernel_size % 2 == 0:
            kernel_size += 1
        kernel_size = max(kernel_size, 1)
        if kernel_size > 1:
            gray = cv2.GaussianBlur(gray, (kernel_size, kernel_size), 0)

    if bool_config(get_nested(config, "video", "brightness_normalization", "enabled"), default=False):
        gray = cv2.equalizeHist(gray)

    return gray, original_width, original_height


def _video_safety_limit(frame_count_hint: int | None, limit_frames: int | None) -> int | None:
    if frame_count_hint is None or frame_count_hint <= 0:
        return limit_frames
    tolerance = max(100, int(frame_count_hint * 0.05))
    guarded_limit = frame_count_hint + tolerance
    return min(guarded_limit, limit_frames) if limit_frames is not None else guarded_limit
</file>

<file path="src/tool/_common.py">
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

SRC_ROOT = Path(__file__).resolve().parents[1]
PROJECT_ROOT = SRC_ROOT.parent

if str(SRC_ROOT) not in sys.path:
    sys.path.insert(0, str(SRC_ROOT))

from kpdl_preprocess.pipeline import run_preprocess


def add_common_args(parser: argparse.ArgumentParser) -> None:
    parser.add_argument("--project-root", default=str(PROJECT_ROOT), help="Project root for resolving dataset paths.")
    parser.add_argument("--output-root", default=None, help="Override output root, for example outputs/preprocessed.")
    parser.add_argument("--split", choices=["train", "test"], default=None, help="Process only one split.")
    parser.add_argument("--limit-videos", type=int, default=None, help="Limit videos/sequences per split for smoke tests.")
    parser.add_argument("--limit-frames", type=int, default=None, help="Limit frames per video/sequence for smoke tests.")
    parser.add_argument(
        "--progress-every",
        type=int,
        default=250,
        help="Print progress every N frames while processing videos/sequences. Use 0 to disable.",
    )
    parser.add_argument("--export-arff", action="store_true", help="Also export WEKA ARFF files from feature CSV outputs.")


def run_dataset(args: argparse.Namespace, config_path: Path) -> int:
    stats = run_preprocess(
        config_path=config_path,
        project_root=Path(args.project_root),
        output_root=args.output_root,
        split=args.split,
        limit_videos=args.limit_videos,
        limit_frames=args.limit_frames,
        progress_every=args.progress_every,
        export_arff=args.export_arff,
    )
    print(json.dumps(_summary(stats), indent=2, ensure_ascii=False))
    return 0


def _summary(stats: dict) -> dict:
    schema = stats.get("schema", {})
    return {
        "dataset": stats.get("dataset"),
        "output_dir": stats.get("output_dir"),
        "schema_version": schema.get("version"),
        "feature_columns": schema.get("feature_columns"),
        "splits": stats.get("splits"),
        "arff_outputs": stats.get("arff_outputs", []),
    }
</file>

<file path="src/configs/ucsd_ped2.yaml">
data:
  dataset: "ucsd_ped2"
  root: "src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"
  model_root: "src/outputs/models"
  result_root: "src/outputs/results"

model:
  type: "minibatch_kmeans"
  clusters_per_cell: 5
  min_samples_per_cell: 50
  batch_size: 1024
  max_iter: 200
  random_state: 10
  threshold_percentile: 99.0
  threshold_floor: 1.0e-9

scoring:
  feature_columns:
    - foreground_ratio
    - motion_magnitude_mean
    - motion_magnitude_std
    - motion_density
    - brightness_mean
    - brightness_delta
  cluster_weight: 0.65
  temporal_weight: 0.20
  rare_token_weight: 0.10
  rule_weight: 0.05
  top_k_cells: 5
  smoothing_window: 5
  alert_threshold_medium: 0.70
  alert_threshold_high: 0.90
  min_consecutive_alerts: 3

rules:
  enabled: true
  output_root: "src/outputs/rules"
  model_dir: null
  algorithm: "bounded_apriori"
  min_support: 0.01
  min_confidence: 0.60
  min_lift: 1.05
  max_itemset_size: 3
  max_rules: 200
  include_cell_token: true
  include_cluster_token: true
  include_brightness_token: true
  include_direction_token: false
  rare_itemset_size: 3
  rare_support_floor: 0.001
  rare_score_cap: 1.0

visualization:
  output_root: "src/outputs/visualizations"
  colormap: "JET"
  alpha: 0.45
  heatmap_normalization: "per_frame"
  draw_grid: false
  draw_top_cells: true
  top_cells_per_frame: 5
  top_frames: 30
  min_score: 0.70
  frame_source: "preprocessed"
  image_format: "png"
  video_fps: 10
</file>

<file path="src/doc/spec_1_processed.md">
# SPEC 1 Processed - Trạng thái triển khai tiền xử lý UCSD và CUHK Avenue

Tài liệu này đánh dấu các mục đã thực hiện từ `doc/spec_1.md`.

Ngày cập nhật: 2026-05-26

## 1. Tóm tắt trạng thái

Đã tạo thư mục `src/` và triển khai pipeline tiền xử lý theo SPEC 1 ở mức MVP:

- đọc UCSD Ped1/Ped2 dạng chuỗi frame `.tif`;
- đọc CUHK Avenue dạng video `.avi`;
- resize, grayscale, Gaussian blur tùy config;
- tạo grid cố định;
- tạo spatio-temporal cube;
- trích xuất feature baseline bằng frame differencing;
- xuất manifest, grid, feature CSV, stats JSON;
- xuất ARFF cho WEKA;
- tách entrypoint riêng cho UCSD và CUHK Avenue trong `src/tool/`;
- đưa dataset SPEC 1 vào `src/dataset/`;
- đưa output tiền xử lý mặc định vào `src/outputs/`;
- chạy smoke test trên UCSD Ped1, UCSD Ped2 và CUHK Avenue;
- thêm `.gitignore` để không commit dataset, output, archive và paper PDF ở root.

Chưa chạy full preprocess toàn bộ UCSD Ped2 và toàn bộ Avenue trong bước này. Đã kiểm thử bằng giới hạn `1` video/sequence và `10-20` frame để xác nhận pipeline, schema và output.

## 2. Output đã có

Code chính:

- [X] `src/preprocess.py`
- [X] `src/csv_to_arff.py`
- [X] `src/tool/preprocess_ucsd.py`
- [X] `src/tool/preprocess_avenue.py`
- [X] `src/kpdl_preprocess/`
- [X] `src/configs/ucsd_ped2.yaml`
- [X] `src/configs/ucsd_ped1.yaml`
- [X] `src/configs/avenue.yaml`
- [X] `src/requirements.txt`
- [X] `src/README.md`
- [X] `.gitignore`

Dataset nội bộ `src`:

- [X] `src/dataset/UCSD_Anomaly_Dataset.v1p2/`
- [X] `src/dataset/CUHK-avenue/`

Output smoke test:

- [X] `src/outputs/preprocessed/ucsd_ped2/`
- [X] `src/outputs/preprocessed/avenue/`
- [X] `src/outputs/weka/ucsd_ped2_features_train.arff`
- [X] `src/outputs/weka/ucsd_ped2_features_test.arff`
- [X] `src/outputs/weka/avenue_features_train.arff`
- [X] `src/outputs/weka/avenue_features_test.arff`

Output cũ từng sinh ở project root đã được chuyển vào:

- [X] `src/outputs/legacy_root_outputs/`

## 3. Đánh dấu theo phạm vi SPEC 1

### 3.1. Nằm trong phạm vi

- [X] Đọc frame `.tif` của UCSD Ped1/Ped2.
- [X] Đọc video `.avi` của CUHK Avenue.
- [X] Resize frame về kích thước cấu hình.
- [X] Chuyển grayscale.
- [X] Làm trơn nhẹ bằng Gaussian blur nếu cấu hình bật.
- [X] Chuẩn hóa độ sáng nhẹ nếu cấu hình bật.
- [X] Chia frame thành grid cố định.
- [X] Tạo spatio-temporal cube.
- [X] Trích xuất feature chuyển động và ngữ cảnh cơ bản.
- [X] Lưu feature và metadata.
- [X] Kiểm tra số lượng frame, cube, cell và feature qua `preprocess_stats.json`.

### 3.2. Ngoài phạm vi

Các mục này chưa triển khai vì SPEC 1 xác định là ngoài phạm vi:

- [ ] Train model phân cụm.
- [ ] Tính anomaly score.
- [ ] Khai phá association rules.
- [ ] Vẽ heatmap kết quả bất thường.
- [ ] Đánh giá ROC-AUC/EER.
- [ ] Xử lý semantic object detection.

## 4. Đánh dấu theo cấu hình đề xuất

### 4.1. UCSD Ped2 baseline

- [X] `data.dataset = ucsd_ped2`
- [X] `data.root = src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2`
- [X] `video.input_type = frame_sequence`
- [X] `resize_width = 320`
- [X] `resize_height = 240`
- [X] `grayscale = true`
- [X] `blur.enabled = true`
- [X] `blur.kernel_size = 3`
- [X] `brightness_normalization.enabled = false`
- [X] `grid.rows = 12`
- [X] `grid.cols = 16`
- [X] `cube.length = 5`
- [X] `cube.stride = 1`
- [X] `motion_method = frame_diff`
- [X] `frame_diff_threshold = 15`
- [X] `direction_bins = 8`
- [X] `use_brightness = true`
- [X] `output.root = src/outputs/preprocessed`
- [X] `output.weka_root = src/outputs/weka`

File: `src/configs/ucsd_ped2.yaml`

### 4.2. CUHK Avenue baseline

- [X] `data.dataset = avenue`
- [X] `data.root = src/dataset/CUHK-avenue`
- [X] `video.input_type = video`
- [X] `resize_width = 320`
- [X] `resize_height = 240`
- [X] `grayscale = true`
- [X] `blur.enabled = true`
- [X] `blur.kernel_size = 3`
- [X] `brightness_normalization.enabled = false`
- [X] `grid.rows = 12`
- [X] `grid.cols = 16`
- [X] `cube.length = 5`
- [X] `cube.stride = 1`
- [X] `motion_method = frame_diff`
- [X] `frame_diff_threshold = 15`
- [X] `direction_bins = 8`
- [X] `use_brightness = true`
- [X] `output.root = src/outputs/preprocessed`
- [X] `output.weka_root = src/outputs/weka`

File: `src/configs/avenue.yaml`

## 5. Đánh dấu theo pipeline chung

### Bước 1 - Quét dataset

- [X] Tạo danh sách `dataset, split, video_id, source_path, input_type`.
- [X] UCSD: mỗi thư mục `TrainXXX` hoặc `TestXXX` là một sequence.
- [X] Avenue: mỗi file `.avi` là một video.
- [X] Bỏ qua thư mục ground truth `_gt` khi trích xuất feature.

Module: `src/kpdl_preprocess/datasets.py`

### Bước 2 - Đọc frame

UCSD:

- [X] Đọc ảnh `.tif` theo thứ tự tăng dần.
- [X] `frame_id` lấy từ tên file.
- [X] `timestamp` gán theo thứ tự frame khi không có FPS.

Avenue:

- [X] Đọc `.avi` bằng `cv2.VideoCapture`.
- [X] `frame_id` bắt đầu từ 1.
- [X] `timestamp = frame_id / fps` nếu đọc được FPS, ngược lại để trống.

Module: `src/kpdl_preprocess/readers.py`

### Bước 3 - Chuẩn hóa frame

- [X] Đọc frame RGB/BGR hoặc grayscale.
- [X] Resize theo config.
- [X] Chuyển grayscale.
- [X] Blur nhẹ nếu bật.
- [X] Chuẩn hóa độ sáng nếu bật.
- [X] Lưu kích thước gốc và kích thước resize trong manifest.

Module: `src/kpdl_preprocess/readers.py`

### Bước 4 - Tạo grid

- [X] Grid mặc định `rows = 12`, `cols = 16`.
- [X] Với `320x240`, tạo `192` cell.
- [X] Lưu `cell_id` dạng `row_col`, ví dụ `08_05`.
- [X] Xuất `grid.json`.

Module: `src/kpdl_preprocess/grid.py`

### Bước 5 - Tạo cube

- [X] Cube sliding window với `length = 5`, `stride = 1`.
- [X] Công thức số cube đúng: `floor((N - length) / stride) + 1`.
- [X] Không lưu ảnh cube ra đĩa.
- [X] Lưu metadata cube trong từng dòng feature.

Module: `src/kpdl_preprocess/pipeline.py`

### Bước 6 - Trích xuất feature

- [X] Tính chênh lệch tuyệt đối giữa các frame liên tiếp trong cube.
- [X] Threshold để tạo motion mask.
- [X] Tính feature theo từng cell.
- [X] `foreground_ratio`
- [X] `motion_magnitude_mean`
- [X] `motion_magnitude_std`
- [X] `motion_density`
- [X] `direction_hist_0..7`
- [X] `brightness_mean`
- [X] `brightness_delta`

Ghi chú: với `motion_method = frame_diff`, các cột `direction_hist_0..7` hiện được tạo và gán `0.0`, đúng với ghi chú trong SPEC 1 rằng direction histogram chưa đáng tin cậy nếu chưa dùng optical flow.

Module: `src/kpdl_preprocess/features.py`

### Bước 7 - Lưu feature

- [X] `frames_manifest.csv`
- [X] `videos_manifest.csv`
- [X] `grid.json`
- [X] `features_train.csv`
- [X] `features_test.csv`
- [X] `preprocess_stats.json`

Đã xác nhận bằng smoke test tại:

- `src/outputs/preprocessed/ucsd_ped2/`
- `src/outputs/preprocessed/avenue/`

ARFF mặc định lưu tại:

- `src/outputs/weka/`

### Bước 8 - Xuất ARFF cho WEKA

- [X] Convert feature CSV sang ARFF.
- [X] Giữ `cell_id` dạng nominal.
- [X] Cột numeric giữ dạng numeric.
- [X] Cột text dài như `dataset`, `split`, `video_id`, `cube_id` để dạng string.

Module:

- `src/kpdl_preprocess/arff.py`
- `src/csv_to_arff.py`

## 6. Đánh dấu theo kế hoạch riêng cho UCSD

- [X] Ưu tiên UCSD Ped2 trước.
- [X] Có script riêng `src/tool/preprocess_ucsd.py`.
- [X] Hỗ trợ `--ped ped2` và `--ped ped1`.
- [X] Dataset UCSD đã nằm trong `src/dataset/UCSD_Anomaly_Dataset.v1p2/`.
- [X] Đọc các file `.tif` theo thứ tự tên file.
- [X] Tên sequence là tên thư mục, ví dụ `Train001`, `Test001`.
- [X] Không đọc thư mục `*_gt` trong tiền xử lý feature.
- [X] Ground truth mask chưa dùng, đúng phạm vi SPEC 1.
- [X] Lưu `videos_manifest.csv`.
- [X] Lưu `frames_manifest.csv`.
- [X] Không hard-code kích thước frame gốc.
- [X] Smoke test UCSD Ped1 train với `1` sequence và `10` frame.
- [X] Smoke test UCSD Ped2 train/test với `1` sequence mỗi split và `10` frame.
- [X] Chạy full preprocess toàn bộ UCSD Ped2.
- [ ] Mở rộng/chạy full UCSD Ped1.

## 7. Đánh dấu theo kế hoạch riêng cho CUHK Avenue

- [X] Có script riêng `src/tool/preprocess_avenue.py`.
- [X] Dataset Avenue đã nằm trong `src/dataset/CUHK-avenue/`.
- [X] Đọc `training_videos/*.avi`.
- [X] Đọc `testing_videos/*.avi`.
- [X] Bỏ qua `training_vol` và `testing_vol` trong MVP.
- [X] Dùng OpenCV đọc từng frame.
- [X] Lấy FPS, width, height từ `VideoCapture`.
- [X] `video_id` lấy từ tên file không có đuôi, ví dụ `01`.
- [X] Lưu `videos_manifest.csv`.
- [X] Lưu `frames_manifest.csv`.
- [X] Smoke test Avenue train/test với `1` video mỗi split và `10` frame.
- [ ] Chạy full preprocess toàn bộ Avenue.
- [ ] Nghiên cứu file `.mat` để lấy annotation sau.

## 8. Kiểm tra chất lượng đầu ra

Đã có các trường thống kê trong `preprocess_stats.json`:

- [X] `dataset`
- [X] `split`
- [X] `num_videos`
- [X] `num_frames`
- [X] `num_cubes`
- [X] `num_cells`
- [X] `num_feature_rows`
- [X] `missing_frames`
- [X] `failed_videos`
- [X] `avg_motion_density`
- [X] `avg_brightness`

Kết quả smoke test:

- [X] UCSD Ped2 train/test đọc được `1` sequence mỗi split.
- [X] UCSD Ped1 train đọc được `1` sequence.
- [X] Avenue train/test đọc được `1` video mỗi split.
- [X] Với `10` frame, `cube.length = 5`, `stride = 1`: mỗi split có `6` cube.
- [X] Với `12 x 16 = 192` cell: mỗi split có `6 * 192 = 1152` dòng feature.
- [X] `failed_videos = 0`.
- [X] ARFF sinh được cho train/test của UCSD Ped2 và Avenue.
- [X] Header `features_train.csv`, `frames_manifest.csv`, `videos_manifest.csv` của UCSD và Avenue khớp nhau sau khi chuyển output vào `src/outputs`.

Chưa kiểm tra ở quy mô full:

- [ ] Đọc toàn bộ video/sequence trong split.
- [ ] So khớp frame manifest với toàn bộ frame thực tế.
- [ ] Kiểm tra NaN trên toàn bộ feature CSV lớn.
- [ ] Kiểm tra phân phối feature trên toàn bộ dataset.

## 9. Checklist thực hiện từ SPEC 1

- [X] Tạo config riêng cho `ucsd_ped2`.
- [X] Tạo config riêng cho `ucsd_ped1`.
- [X] Tạo config riêng cho `avenue`.
- [X] Tách file chạy riêng cho UCSD.
- [X] Tách file chạy riêng cho Avenue.
- [X] Viết dataset scanner cho frame sequence và video.
- [X] Viết frame reader cho UCSD.
- [X] Viết video reader cho Avenue.
- [X] Viết preprocess transform dùng chung.
- [X] Viết grid generator.
- [X] Viết cube iterator.
- [X] Viết feature extractor baseline bằng frame differencing.
- [X] Lưu manifest, grid và feature CSV.
- [X] Tạo script convert CSV sang ARFF cho WEKA.
- [X] Chạy smoke test trên 1 video/sequence mỗi dataset.
- [X] Chuyển dataset UCSD và Avenue vào `src/dataset`.
- [X] Chuyển output mặc định vào `src/outputs`.
- [X] Thêm `.gitignore` cho dataset/output/PDF/archive/cache.
- [X] Chạy full preprocess cho UCSD Ped2.
- [ ] Chạy full preprocess cho Avenue.
- [X] Ghi `preprocess_stats.json`.

## 10. Tiêu chí hoàn thành SPEC 1

- [ ] UCSD Ped2 có `features_train.csv` và `features_test.csv` full dataset.
- [ ] Avenue có `features_train.csv` và `features_test.csv` full dataset.
- [X] Mỗi feature row truy vết được về video/frame/cell/cube.
- [X] UCSD và Avenue xuất cùng một schema chung qua `src/kpdl_preprocess/schema.py`.
- [X] Có `grid.json` để dùng lại khi vẽ heatmap.
- [X] Có `preprocess_stats.json` cho mỗi dataset trong smoke test.
- [X] Có thể xuất ARFF để thử nghiệm WEKA.
- [X] Pipeline có thể chạy lại bằng config mà không cần sửa code.
- [X] Kết quả tiền xử lý mặc định nằm trong `src/outputs`.
- [X] Dataset và output không bị commit nhờ `.gitignore`.

## *Cài môi trường

Tạo virtual environment:

```powershell
python -m venv .venv
```

Kích hoạt virtual environment trên PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```

Cài toàn bộ thư viện cần thiết:

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

## 11. Lệnh đã dùng để kiểm thử (chỉ là bước test xem đầy đủ thư viện hoặc dataset ... chưa)

```bash
python tool/preprocess_ucsd.py --ped ped2 --limit-videos 1 --limit-frames 10 --export-arff
```

```bash
python tool/preprocess_avenue.py --limit-videos 1 --limit-frames 10 --export-arff
```

```bash
python tool/preprocess_ucsd.py --ped ped1 --split train --limit-videos 1 --limit-frames 10
```

## 12. Lệnh đề xuất để hoàn tất full preprocess (chạy cái này để tiền xử lý ra folder src/outputs)

```bash
python tool/preprocess_ucsd.py --ped ped2 --export-arff
```

```bash
python tool/preprocess_avenue.py --export-arff
```

## 13. Quản lý dữ liệu và Git

Đã thêm `.gitignore` để tránh commit các dữ liệu và artifact nặng:

- [X] `/New folder/`
- [X] `/dataset/`
- [X] `/src/dataset/`
- [X] `/outputs/`
- [X] `/src/outputs/`
- [X] `/*.pdf`
- [X] `__pycache__/`
- [X] `*.py[cod]`

Đã kiểm tra bằng `git check-ignore`:

- [X] `New folder`
- [X] `dataset`
- [X] `src/dataset`
- [X] `src/outputs`
- [X] các PDF paper ở project root.

## 14. Cập nhật 2026-05-26 - Avenue full preprocess chạy lâu

Spec gốc: `src/doc/spec_1.md`

- [X] Kiểm tra lại luồng `src/tool/preprocess_avenue.py`: entrypoint chỉ gọi pipeline dùng chung, nguyên nhân nằm ở đọc video/ghi feature và việc thiếu log tiến độ.
- [X] Probe toàn bộ CUHK Avenue: 16 video train, 21 video test, tổng `30652` frame; OpenCV đọc thô toàn bộ video kết thúc bình thường trong khoảng `9.37s`, không thấy file `.avi` bị đọc vô hạn.
- [X] Thêm `--progress-every` cho `src/tool/_common.py` và `src/kpdl_preprocess/cli.py` để in tiến độ theo video/frame khi chạy full preprocess.
- [X] Thêm guard trong `src/kpdl_preprocess/readers.py` dựa trên `CAP_PROP_FRAME_COUNT` cộng tolerance để tránh trường hợp `VideoCapture.read()` không tự kết thúc ở video lỗi.
- [X] Cập nhật `src/kpdl_preprocess/pipeline.py` để log start/progress/done/fail từng video và ghi `missing_frames` khi số frame đọc được thấp hơn metadata.
- [X] Verification: `python -m compileall src\kpdl_preprocess src\tool`
- [X] Verification: `python src\tool\preprocess_avenue.py --limit-videos 1 --limit-frames 10 --output-root src\outputs\preprocessed_fix_smoke --export-arff --progress-every 5`
- [ ] Chưa chạy full Avenue lại trong bước này vì full dataset sẽ tạo khoảng `5.85M` dòng feature trước khi export ARFF.
</file>

<file path="src/repomix-output.xml">
This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
configs/avenue.yaml
configs/ucsd_ped1.yaml
configs/ucsd_ped2.yaml
csv_to_arff.py
doc/prd.md
doc/spec_1_processed.md
doc/spec_1.md
doc/spec_2.md
kpdl_preprocess/__init__.py
kpdl_preprocess/arff_cli.py
kpdl_preprocess/arff.py
kpdl_preprocess/cli.py
kpdl_preprocess/config.py
kpdl_preprocess/datasets.py
kpdl_preprocess/features.py
kpdl_preprocess/grid.py
kpdl_preprocess/pipeline.py
kpdl_preprocess/readers.py
kpdl_preprocess/records.py
kpdl_preprocess/schema.py
kpdl_preprocess/utils.py
preprocess.py
requirements.txt
tool/__init__.py
tool/_common.py
tool/preprocess_avenue.py
tool/preprocess_ucsd.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="configs/avenue.yaml">
data:
  dataset: "avenue"
  root: "src/dataset/CUHK-avenue"
  train_path: "training_videos"
  test_path: "testing_videos"

video:
  input_type: "video"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"
</file>

<file path="configs/ucsd_ped1.yaml">
data:
  dataset: "ucsd_ped1"
  root: "src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped1"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"
</file>

<file path="configs/ucsd_ped2.yaml">
data:
  dataset: "ucsd_ped2"
  root: "src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true

output:
  root: "src/outputs/preprocessed"
  weka_root: "src/outputs/weka"
</file>

<file path="csv_to_arff.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_preprocess.arff_cli import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="doc/prd.md">
# PRD - Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian-thời gian và khai phá luật kết hợp

## 1. Tổng quan

### 1.1. Tên dự án

**Phát hiện hành vi bất thường theo ngữ cảnh trên camera giám sát dựa trên phân cụm hành vi-không gian-thời gian và khai phá luật kết hợp**.

Tên tiếng Anh tham khảo: **Context-Aware Abnormal Behavior Detection using Spatio-Temporal Behavior Clustering and Association Rule Mining**.

### 1.2. Bối cảnh

Trong hệ thống camera giám sát, phần lớn thời gian video chỉ chứa các hoạt động bình thường lặp đi lặp lại. Với camera cố định, cùng một vùng trong khung hình thường có các mẫu chuyển động ổn định:

- vỉa hè thường có người đi bộ;
- làn đường thường có phương tiện di chuyển;
- cổng ra vào thường có người đứng hoặc di chuyển chậm;
- ban ngày và ban đêm có mật độ, ánh sáng và mức nhiễu khác nhau.

Ý tưởng của dự án là tận dụng tính lặp lại này để học các mẫu hành vi bình thường theo từng vùng của camera. Khi xuất hiện chuyển động lệch khỏi cụm hành vi đã học, hoặc một tổ hợp ngữ cảnh hiếm/không hợp lý theo các luật kết hợp, hệ thống sẽ gán điểm bất thường và sinh cảnh báo.

Dự án tham khảo tư duy giải quyết bài toán từ Lu et al. trong bài báo **"Abnormal Event Detection at 150 FPS in MATLAB"**, đặc biệt là cách nhìn camera cố định như một nguồn dữ liệu có cấu trúc không gian-thời gian lặp lại. Paper này chỉ được dùng như tài liệu tham khảo về tư duy tiền xử lý, chia vùng và học mẫu bình thường; dự án không đặt mục tiêu sao chép, tái hiện hoặc làm giàu thêm phương pháp của paper đó.

### 1.3. Mục tiêu học tập và nghiên cứu

Mục tiêu chính của đề tài là học hỏi và tìm hiểu sâu về:

- tiền xử lý dữ liệu video giám sát;
- biểu diễn hành vi bằng đặc trưng không gian-thời gian;
- phân cụm hành vi bình thường theo vùng camera;
- rời rạc hóa đặc trưng liên tục thành token/item;
- khai phá luật kết hợp bằng Apriori hoặc FP-Growth;
- kết hợp kết quả phân cụm và luật kết hợp để phát hiện, giải thích hành vi bất thường.

Đề tài ưu tiên tính rõ ràng, khả năng phân tích và khả năng giải thích hơn việc đạt hiệu năng cao nhất trên benchmark.

### 1.4. Mục tiêu sản phẩm thử nghiệm

Xây dựng một pipeline phát hiện bất thường cho camera cố định có khả năng:

- học các mẫu chuyển động bình thường theo từng vùng trong khung hình;
- tính điểm bất thường cho từng vùng và từng frame/cube;
- phát hiện các hành vi như di chuyển sai vùng, hướng lạ, tốc độ lạ, mật độ lạ hoặc chuyển động không phù hợp với thời điểm;
- tạo heatmap bất thường trên video;
- sinh lý do cảnh báo ở dạng dễ hiểu bằng token/rule;
- chạy đủ nhanh cho xử lý gần thời gian thực trên video giám sát.

### 1.5. Nguyên tắc thiết kế

Hệ thống không cố nhận diện ngữ nghĩa phức tạp ngay từ đầu. Thay vào đó, hệ thống ưu tiên các tín hiệu rẻ, ổn định và có thể học từ dữ liệu bình thường:

- chuyển động;
- hướng;
- mật độ;
- vị trí;
- độ sáng;
- thời điểm;
- cụm hành vi bình thường.

Hai trụ cột nghiên cứu của hệ thống là **phân cụm hành vi-không gian-thời gian** và **khai phá luật kết hợp**. Phân cụm cung cấp điểm lệch định lượng so với mẫu bình thường; luật kết hợp cung cấp ngữ cảnh, khả năng giải thích và tín hiệu phát hiện các tổ hợp hành vi hiếm. Trong triển khai, không nên để một luật đơn lẻ quyết định toàn bộ cảnh báo; kết quả cuối nên tổng hợp cả độ lệch cụm, độ hiếm của token và mức vi phạm luật.

---

## 2. Vấn đề cần giải quyết

### 2.1. Vấn đề nghiệp vụ

Người vận hành camera phải theo dõi lượng video lớn trong thời gian dài. Các hành vi bất thường thường hiếm, ngắn và dễ bị bỏ sót. Một hệ thống tự động cần giúp:

- lọc ra các thời điểm đáng chú ý;
- chỉ ra vùng bất thường trong khung hình;
- giảm số cảnh báo giả;
- giải thích lý do cảnh báo ở mức người vận hành có thể hiểu.

### 2.2. Vấn đề kỹ thuật

Phát hiện bất thường trong video không phải bài toán phân loại thông thường vì:

- khó liệt kê toàn bộ loại bất thường có thể xảy ra;
- dữ liệu bất thường ít và không cân bằng;
- cùng một hành vi có thể bình thường ở vùng này nhưng bất thường ở vùng khác;
- điều kiện ánh sáng, bóng đổ, nhiễu và mật độ đám đông làm đặc trưng chuyển động biến động;
- xử lý từng frame bằng mô hình nặng có thể không phù hợp yêu cầu real-time.

### 2.3. Giả định chính

MVP dựa trên các giả định sau:

- camera cố định hoặc gần như cố định;
- cảnh giám sát có cấu trúc không gian ổn định;
- dữ liệu huấn luyện chủ yếu chứa hành vi bình thường;
- hành vi bất thường biểu hiện qua chuyển động, vị trí, mật độ, hướng hoặc tổ hợp ngữ cảnh khác thường;
- không yêu cầu nhận diện danh tính cá nhân.

---

## 3. Người dùng và giá trị

### 3.1. Người dùng mục tiêu

- Nhân viên an ninh;
- người vận hành trung tâm camera;
- quản lý tòa nhà, bãi xe, hành lang, cổng ra vào, khu vực công cộng;
- nhóm nghiên cứu thị giác máy tính và khai phá dữ liệu;
- sinh viên triển khai đề tài phát hiện bất thường trong video.

### 3.2. Giá trị mang lại

- Giảm tải việc quan sát liên tục;
- phát hiện sớm các hành vi đáng chú ý;
- tạo heatmap giúp biết bất thường xảy ra ở đâu;
- có giải thích bằng token/rule thay vì chỉ trả về điểm số;
- có thể học riêng cho từng camera mà không cần bộ nhãn bất thường lớn;
- phù hợp triển khai baseline nhanh trước khi dùng mô hình deep learning nặng hơn.

---

## 4. Phạm vi sản phẩm

### 4.1. Phạm vi MVP

MVP tập trung vào:

- video từ camera cố định;
- xử lý video ghi sẵn trước, sau đó có thể mở rộng sang stream;
- chia frame thành grid hoặc zone cố định;
- tạo spatio-temporal cube ngắn từ 5-10 frame;
- trích xuất đặc trưng chuyển động nhẹ;
- học mô hình phân cụm hành vi bình thường theo từng cell/zone;
- tính anomaly score theo cell và frame;
- hiển thị heatmap bất thường;
- tạo token và rule để giải thích cảnh báo.

### 4.2. Loại bất thường MVP cần bắt

- Chuyển động quá nhanh ở vùng thường chậm;
- chuyển động sai hướng hoặc ngược hướng phổ biến;
- mật độ chuyển động bất thường;
- vùng thường yên tĩnh bỗng có chuyển động lớn;
- đối tượng/chuyển động có kích thước hoặc tốc độ không phù hợp với vùng;
- mẫu chuyển động không thuộc cụm bình thường của vùng đó;
- tổ hợp token hiếm, ví dụ `zone=sidewalk` + `motion=very_fast` + `object_size=large`.

### 4.3. Ngoài phạm vi MVP

- Nhận diện danh tính người;
- nhận diện biển số xe;
- phân loại chi tiết hành vi như đánh nhau, trộm cắp, bỏ quên đồ;
- tracking đa đối tượng chính xác trong đám đông;
- học liên camera;
- suy luận ý định;
- xử lý camera rung mạnh hoặc thay đổi góc nhìn liên tục;
- dashboard vận hành hoàn chỉnh.

---

## 5. Kiến trúc hệ thống

### 5.1. Kiến trúc tổng quát

```mermaid
flowchart LR
    A["Video camera cố định"] --> B["Tiền xử lý frame"]
    B --> C["Chia grid / zone"]
    C --> D["Tạo spatio-temporal cube"]
    D --> E["Trích xuất đặc trưng nhẹ"]
    E --> F["Phân cụm hành vi theo từng cell"]
    F --> G["Anomaly score theo cell"]
    G --> H["Làm mượt theo thời gian / không gian"]
    H --> I["Frame-level alert + heatmap"]
    E --> J["Token hóa đặc trưng"]
    J --> K["Association rules"]
    K --> L["Giải thích / cộng điểm phụ"]
    L --> I
```

### 5.2. Luồng huấn luyện offline

```mermaid
flowchart TD
    A["Training videos bình thường"] --> B["Resize + chuẩn hóa"]
    B --> C["Grid/zone cố định"]
    C --> D["Cube 5-10 frame"]
    D --> E["Feature extraction"]
    E --> F["Chuẩn hóa feature theo cell"]
    F --> G["Huấn luyện mô hình phân cụm từng cell"]
    E --> H["Discretize thành token"]
    H --> I["Mine association rules"]
    G --> J["Lưu model"]
    I --> J
```

### 5.3. Luồng suy luận online/offline

```mermaid
flowchart TD
    A["Video / stream mới"] --> B["Tạo cube"]
    B --> C["Trích xuất feature"]
    C --> D["Tính khoảng cách tới cụm bình thường"]
    C --> E["Sinh token"]
    E --> F["Kiểm tra support/rule violation"]
    D --> G["Tổng hợp anomaly score"]
    F --> G
    G --> H["Temporal smoothing"]
    H --> I["Heatmap + alert + reason"]
```

---

## 6. Thành phần chức năng

### 6.1. Module Input Video

#### Mục tiêu

Đọc video từ dataset hoặc file camera và cung cấp frame tuần tự cho pipeline.

#### Yêu cầu

- Đọc được `.avi`, `.mp4`, chuỗi ảnh `.jpg/.png/.tif`;
- hỗ trợ dataset CUHK Avenue, UCSD Ped1/Ped2, ShanghaiTech nếu có sẵn;
- giữ thông tin `video_id`, `frame_id`, `timestamp`;
- cho phép giới hạn số frame để debug nhanh.

#### Đầu ra

```text
FrameRecord {
  video_id,
  frame_id,
  timestamp,
  frame_rgb,
  frame_gray
}
```

### 6.2. Module Tiền Xử Lý

#### Mục tiêu

Chuẩn hóa frame để giảm chi phí tính toán và giảm nhiễu.

#### Yêu cầu

- Resize về kích thước cấu hình được, ví dụ `320x240` hoặc `160x120`;
- chuyển grayscale;
- tùy chọn cân bằng sáng nhẹ;
- tùy chọn blur nhẹ để giảm nhiễu;
- giữ mapping từ frame resize về frame gốc để vẽ heatmap.

### 6.3. Module Grid/Zone

#### Mục tiêu

Chia frame thành các vùng cố định để học normal pattern riêng theo vị trí.

#### Yêu cầu

- Hỗ trợ grid đều, ví dụ `16x12`, `20x15`;
- hỗ trợ zone thủ công bằng file cấu hình trong giai đoạn sau;
- mỗi cell có `cell_id`, tọa độ, kích thước;
- có thể bỏ qua cell ít thông tin, ví dụ vùng tường/trời.

#### Cấu hình mẫu

```yaml
grid:
  rows: 12
  cols: 16
  ignore_cells: []
```

### 6.4. Module Spatio-Temporal Cube

#### Mục tiêu

Gom thông tin nhiều frame liên tiếp để phát hiện chuyển động thay vì chỉ phân tích frame đơn.

#### Yêu cầu

- Cube gồm `T=5` hoặc `T=10` frame;
- stride cấu hình được, ví dụ `1`, `2`, `5`;
- mỗi cube gắn với một cell/zone cố định;
- hỗ trợ multi-scale trong giai đoạn mở rộng nếu cần nghiên cứu thêm.

### 6.5. Module Feature Extraction

#### Mục tiêu

Tạo vector đặc trưng nhẹ, đủ mô tả chuyển động và ngữ cảnh vùng.

#### Feature bắt buộc cho MVP

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
direction_hist_8bins
motion_density
brightness_mean
brightness_delta
cell_row
cell_col
cell_id
```

#### Feature tùy chọn

```text
object_size_estimate
edge_energy
gradient_3d_energy
flow_consistency
time_bucket
```

#### Phương pháp motion

MVP có thể bắt đầu bằng:

- frame differencing;
- background subtraction MOG2;
- optical flow Farneback nếu cần hướng chuyển động tốt hơn.

Khuyến nghị triển khai:

1. Baseline nhanh: frame differencing + motion density.
2. Baseline tốt hơn: Farneback optical flow + direction histogram.
3. Feature nâng cao: 3D gradient trên cube để khảo sát thêm đặc trưng không gian-thời gian.

### 6.6. Module Phân Cụm Hành Vi-Không Gian-Thời Gian

#### Mục tiêu

Học các cụm hành vi bình thường riêng cho từng cell/zone dựa trên đặc trưng không gian-thời gian.

#### Thuật toán MVP

Ưu tiên **MiniBatchKMeans theo từng cell** vì dễ triển khai, chạy nhanh và dễ giải thích.

Quy trình:

1. Với mỗi cell, gom feature từ training video bình thường.
2. Chuẩn hóa feature bằng mean/std riêng của cell.
3. Huấn luyện KMeans với `K=3..8`.
4. Tính phân phối khoảng cách từ feature training tới centroid gần nhất.
5. Lưu percentile distance làm ngưỡng anomaly.

#### Thuật toán mở rộng để so sánh

- Gaussian Mixture Model;
- Isolation Forest;
- One-Class SVM;
- DBSCAN/HDBSCAN nếu muốn khảo sát cụm tự nhiên và outlier;
- K-Medoids nếu muốn mẫu đại diện là điểm dữ liệu thật, dễ giải thích.

### 6.7. Module Tokenization

#### Mục tiêu

Biến feature liên tục thành token rời rạc để khai phá luật và giải thích.

#### Ví dụ token

```text
cell=08_05
zone=sidewalk
motion=slow
direction=left_to_right
density=low
brightness=day_like
cluster=C3
```

#### Yêu cầu

- Rời rạc hóa tốc độ thành `still`, `slow`, `medium`, `fast`, `very_fast`;
- rời rạc hóa hướng thành 8 hướng hoặc nhóm hướng chính;
- rời rạc hóa mật độ thành `low`, `medium`, `high`;
- rời rạc hóa độ sáng thành `dark`, `normal`, `bright`;
- thêm `cluster=Cx` từ mô hình phân cụm;
- không tạo quá nhiều token khiến luật bị vụn.

### 6.8. Module Association Rules

#### Mục tiêu

Tìm các tổ hợp token phổ biến trong dữ liệu bình thường để hỗ trợ giải thích và cộng điểm bất thường.

#### Thuật toán

- FP-Growth là lựa chọn ưu tiên nếu số transaction lớn;
- Apriori dùng được cho thí nghiệm nhỏ.

#### Chỉ số luật

- `support`;
- `confidence`;
- `lift`;
- số lần xuất hiện theo cell/zone.

#### Ví dụ luật

```text
{cell=08_05, brightness=day_like} -> {motion=slow}
{cell=10_03, density=low} -> {direction=left_to_right}
{zone=entrance, density=high} -> {motion=still_or_slow}
```

#### Vai trò trong MVP

Luật kết hợp là một thành phần nghiên cứu chính của đề tài, dùng để học quan hệ giữa vùng, thời điểm, hướng, mật độ, tốc độ và cụm hành vi. Trong phát hiện bất thường, luật nên được kết hợp với kết quả phân cụm thay vì dùng đơn độc. Chúng dùng để:

- giải thích vì sao một mẫu bị xem là lạ;
- phát hiện tổ hợp token hiếm;
- cộng thêm điểm nếu rule mạnh bị vi phạm;
- hỗ trợ phân tích định tính trong báo cáo.

### 6.9. Module Anomaly Scoring

#### Mục tiêu

Tính điểm bất thường liên tục cho từng cell/cube và tổng hợp thành điểm frame.

#### Thành phần điểm

```text
cluster_distance_score:
  độ xa so với cụm bình thường gần nhất của cell

temporal_change_score:
  độ thay đổi đột ngột so với các cube gần trước đó

rare_token_score:
  mức hiếm của tổ hợp token trong dữ liệu training

rule_violation_score:
  mức vi phạm các luật có confidence/lift cao
```

#### Công thức MVP

```text
score = 0.65 * cluster_distance_score
      + 0.20 * temporal_change_score
      + 0.10 * rare_token_score
      + 0.05 * rule_violation_score
```

Trọng số có thể điều chỉnh qua validation. Nếu chưa có đủ rule ổn định, MVP có thể đặt:

```text
score = 0.80 * cluster_distance_score
      + 0.20 * temporal_change_score
```

#### Tổng hợp frame-level

```text
frame_score = mean(top_k_cell_scores)
```

hoặc:

```text
frame_score = max(cell_scores)
```

Khuyến nghị dùng `top_k mean` để giảm nhiễu từ một cell đơn lẻ.

### 6.10. Module Smoothing và Alert

#### Mục tiêu

Giảm cảnh báo giả do nhiễu từng frame/cube.

#### Yêu cầu

- Làm mượt score theo thời gian bằng moving average hoặc EMA;
- làm mượt nhẹ theo không gian giữa các cell lân cận;
- chỉ alert nếu score vượt ngưỡng trong `N` cube liên tiếp;
- sinh mức độ cảnh báo: `low`, `medium`, `high`.

#### Logic đề xuất

```text
if smoothed_frame_score > threshold_high for >= N frames:
    alert = high
elif smoothed_frame_score > threshold_medium for >= N frames:
    alert = medium
else:
    alert = none
```

### 6.11. Module Output và Visualization

#### Đầu ra bắt buộc

- `frame_score`;
- `cell_scores`;
- heatmap bất thường;
- danh sách cell bất thường;
- lý do cảnh báo dạng text;
- video/frame overlay kết quả.

#### Ví dụ output JSON

```json
{
  "video_id": "avenue_test_01",
  "frame_id": 352,
  "frame_score": 0.87,
  "severity": "high",
  "abnormal_cells": ["08_05", "08_06", "09_05"],
  "reasons": [
    "cell=08_05 has motion=very_fast while normal pattern is slow",
    "token combination {cell=08_05, motion=very_fast, density=high} has low support",
    "nearest normal cluster is C3 but distance is above the 99th percentile"
  ]
}
```

---

## 7. Yêu cầu chức năng

### 7.1. Bắt buộc cho MVP

- Đọc video hoặc chuỗi frame từ dataset;
- resize và chuẩn hóa frame;
- chia frame thành grid cố định;
- tạo cube 5-10 frame;
- trích xuất feature chuyển động nhẹ;
- huấn luyện mô hình phân cụm theo từng cell;
- tính anomaly score theo cell/cube;
- tổng hợp frame-level score;
- làm mượt score để giảm nhiễu;
- xuất heatmap bất thường;
- xuất file kết quả dạng JSON/CSV;
- tạo token từ feature;
- khai phá luật cơ bản từ training data;
- sinh lý do cảnh báo dựa trên cluster distance và token/rule.

### 7.2. Nên có

- CLI train/test rõ ràng;
- cấu hình bằng YAML;
- lưu model bằng pickle/joblib;
- script visualize overlay heatmap lên video;
- báo cáo FPS;
- báo cáo AUC/EER nếu dataset có ground truth;
- ablation giữa score có rule và không rule.

### 7.3. Có thể mở rộng sau

- Zone ngữ nghĩa do người dùng vẽ;
- multi-scale grid/pyramid để so sánh ảnh hưởng của độ phân giải vùng;
- thuật toán phân cụm khác như GMM, DBSCAN, K-Medoids;
- thuật toán khai phá mẫu tuần tự nếu muốn xét thứ tự thời gian mạnh hơn luật kết hợp;
- cập nhật model online;
- dashboard web;
- stream RTSP;
- cảnh báo theo cấp hành vi/đoạn thời gian thay vì chỉ theo frame-level;
- tích hợp detector nhẹ để ước lượng loại đối tượng.

---

## 8. Yêu cầu phi chức năng

### 8.1. Hiệu năng

- MVP nên xử lý tối thiểu 15 FPS ở độ phân giải resize trên CPU phổ thông;
- mục tiêu tốt: 25-30 FPS với frame differencing hoặc MOG2;
- optical flow có thể chậm hơn nhưng vẫn cần đủ nhanh cho thí nghiệm offline;
- huấn luyện offline chấp nhận chậm hơn suy luận.

### 8.2. Khả năng giải thích

Mỗi alert cần có ít nhất một lý do:

- lệch cụm bình thường;
- vượt percentile distance của cell;
- motion/density/direction bất thường;
- tổ hợp token hiếm;
- vi phạm rule mạnh.

### 8.3. Tính ổn định

- Không alert do một frame đơn lẻ nếu chưa qua smoothing;
- xử lý được cell ít chuyển động bằng fallback threshold;
- không crash khi video thiếu frame hoặc frame lỗi;
- log rõ số feature, số cell, số model được huấn luyện.

### 8.4. Tính mở rộng

- Module hóa theo `data`, `features`, `models`, `rules`, `scoring`, `visualization`;
- cấu hình được grid size, cube length, stride, thuật toán feature và thuật toán phân cụm;
- dễ thay KMeans bằng model khác.

---

## 9. Dữ liệu và đánh giá

### 9.1. Dataset ưu tiên trong workspace

Workspace hiện có các dataset phù hợp:

- CUHK Avenue;
- UCSD Ped1/Ped2;
- ShanghaiTech.

Khuyến nghị thứ tự:

1. **UCSD Ped2**: nhỏ hơn, dễ chạy baseline.
2. **CUHK Avenue**: có nhiều hành vi bất thường trực quan.
3. **ShanghaiTech**: đa cảnh, dùng sau khi pipeline ổn định.

### 9.2. Chia dữ liệu

Mặc định:

- training videos: dùng làm dữ liệu normal;
- testing videos: dùng để đánh giá anomaly;
- validation split từ training nếu cần chọn threshold.

### 9.3. Metric định lượng

- Frame-level ROC-AUC;
- frame-level PR-AUC;
- EER nếu cần so sánh với các báo cáo học thuật;
- false positive rate;
- recall tại một mức FPR cố định;
- FPS hoặc ms/frame;
- số cảnh báo/hành vi đúng và sai.

### 9.4. Đánh giá định tính

Cần lưu các ví dụ:

- phát hiện đúng với heatmap rõ;
- cảnh báo sai do ánh sáng/bóng/nhiễu;
- bỏ sót vì chuyển động quá nhỏ;
- rule giải thích tốt;
- rule gây hiểu sai.

---

## 10. Thí nghiệm và Ablation

### 10.1. Baseline bắt buộc

1. Motion magnitude threshold đơn giản.
2. Per-cell KMeans chỉ dùng motion feature.
3. Per-cell KMeans + temporal smoothing.
4. Per-cell KMeans + token/rule explanation.
5. Full score: cluster distance + temporal change + rare token + rule violation.

### 10.2. Ablation

- Không dùng cell-specific model, chỉ dùng model toàn cục;
- không dùng direction histogram;
- không dùng brightness/time token;
- không dùng temporal smoothing;
- không dùng rule score;
- thay `max cell score` bằng `top-k mean`;
- thay grid size `8x6`, `16x12`, `20x15`;
- thay cube length `5`, `10`, `15`.

### 10.3. Kỳ vọng kết quả

- Model theo từng cell phải tốt hơn model toàn cục;
- smoothing phải giảm false positive;
- rule/token có thể không tăng AUC mạnh nhưng phải tăng khả năng giải thích;
- grid quá nhỏ dễ nhiễu, grid quá lớn dễ mất chi tiết;
- optical flow nên phát hiện hướng tốt hơn frame differencing nhưng tốn thời gian hơn.

---

## 11. Thiết kế cấu hình

### 11.1. File cấu hình mẫu

```yaml
data:
  dataset: "ucsd_ped2"
  train_path: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2/Train"
  test_path: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2/Test"

video:
  resize_width: 320
  resize_height: 240
  grayscale: true

grid:
  rows: 12
  cols: 16

cube:
  length: 5
  stride: 1

features:
  motion_method: "farneback"
  direction_bins: 8
  use_brightness: true

model:
  type: "minibatch_kmeans"
  clusters_per_cell: 5
  min_samples_per_cell: 50

rules:
  enabled: true
  algorithm: "fpgrowth"
  min_support: 0.01
  min_confidence: 0.6
  min_lift: 1.1

scoring:
  cluster_weight: 0.65
  temporal_weight: 0.20
  rare_token_weight: 0.10
  rule_weight: 0.05
  top_k_cells: 5
  smoothing_window: 5
```

---

## 12. Giao diện dòng lệnh đề xuất

### 12.1. Train

```bash
python train.py --config configs/ucsd_ped2.yaml
```

Đầu ra:

```text
outputs/models/ucsd_ped2/
  config.yaml
  scalers.joblib
  cell_models.joblib
  thresholds.json
  rules.json
  feature_stats.json
```

### 12.2. Test

```bash
python test.py --config configs/ucsd_ped2.yaml --model outputs/models/ucsd_ped2
```

Đầu ra:

```text
outputs/results/ucsd_ped2/
  frame_scores.csv
  cell_scores.npz
  alerts.json
  metrics.json
```

### 12.3. Visualize

```bash
python visualize.py --video test_001 --results outputs/results/ucsd_ped2
```

Đầu ra:

```text
outputs/visualizations/ucsd_ped2/test_001_overlay.mp4
```

---

## 13. Rủi ro và giảm thiểu

### 13.1. Background subtraction nhiễu

**Rủi ro:** Ánh sáng, bóng đổ, mưa hoặc nén video tạo chuyển động giả.

**Giảm thiểu:** Dùng smoothing, bỏ qua cell có motion nhỏ không ổn định, thử optical flow, chuẩn hóa brightness và thêm threshold tối thiểu.

### 13.2. Rule quá vụn

**Rủi ro:** Quá nhiều token làm support thấp, luật khó tổng quát.

**Giảm thiểu:** Giới hạn số token, rời rạc hóa thô, lọc luật bằng support/confidence/lift, chỉ dùng rule làm tín hiệu phụ.

### 13.3. Support thấp không đồng nghĩa bất thường

**Rủi ro:** Hành vi hợp lệ nhưng hiếm bị báo bất thường.

**Giảm thiểu:** Rare token score chỉ chiếm trọng số nhỏ; alert cần cluster distance hoặc temporal change đủ cao.

### 13.4. Grid không khớp ngữ nghĩa cảnh

**Rủi ro:** Một cell chứa cả đường và vỉa hè, làm mẫu normal bị trộn.

**Giảm thiểu:** Thử grid nhỏ hơn, dùng zone thủ công ở giai đoạn sau, hoặc multi-scale.

### 13.5. Dữ liệu training chứa bất thường

**Rủi ro:** Model học nhầm bất thường thành bình thường.

**Giảm thiểu:** Lọc outlier trong training, dùng percentile robust, kiểm tra top distance trong training.

### 13.6. False positive từ camera noise

**Rủi ro:** Một cell đơn lẻ bị nhiễu gây alert.

**Giảm thiểu:** Dùng top-k mean, spatial smoothing, yêu cầu alert kéo dài nhiều frame.

---

## 14. Kế hoạch triển khai

### Giai đoạn 1 - Baseline xử lý video và feature

- Đọc dataset UCSD Ped2 hoặc Avenue;
- resize frame;
- chia grid;
- tạo cube;
- trích xuất frame difference hoặc optical flow feature;
- lưu feature theo `video_id`, `frame_id`, `cell_id`.

### Giai đoạn 2 - Phân cụm hành vi theo cell

- Chuẩn hóa feature theo cell;
- train MiniBatchKMeans;
- tính distance distribution;
- chọn threshold theo percentile;
- xuất cell score và frame score.

### Giai đoạn 3 - Visualization và đánh giá

- Tạo heatmap cell score;
- overlay lên frame/video;
- tính FPS;
- tính ROC-AUC nếu có ground truth;
- lưu ví dụ đúng/sai.

### Giai đoạn 4 - Token và association rules

- Discretize motion/density/direction/brightness;
- tạo transaction;
- chạy FP-Growth hoặc Apriori;
- lưu rules;
- tạo reason text cho alert.

### Giai đoạn 5 - Tổng hợp score và ablation

- Thêm rare token score;
- thêm rule violation score;
- tối ưu trọng số;
- so sánh với baseline không rule;
- viết báo cáo phân tích.

### Giai đoạn 6 - Mở rộng nghiên cứu sau MVP

- Thêm multi-scale pyramid;
- thử 3D gradient feature;
- thử thêm thuật toán phân cụm khác như GMM, DBSCAN, K-Medoids;
- thử thêm biến thể luật kết hợp và ngưỡng support/confidence/lift;
- so sánh tốc độ, khả năng giải thích và độ chính xác với KMeans baseline.

---

## 15. Tiêu chí hoàn thành MVP

MVP được xem là hoàn thành khi:

- huấn luyện được mô hình phân cụm hành vi trên ít nhất một dataset;
- test được trên video mới và sinh `frame_scores.csv`;
- tạo được heatmap overlay;
- có alert JSON với lý do cảnh báo;
- có ít nhất 3 thí nghiệm baseline/ablation;
- báo cáo được FPS và ít nhất một metric định lượng;
- code chạy lại được bằng config;
- tài liệu mô tả rõ pipeline, feature, model và scoring.

---

## 16. Kết quả bàn giao

- Source code pipeline;
- file cấu hình dataset;
- model đã train thử;
- kết quả score dạng CSV/JSON;
- video/frame visualization;
- báo cáo thực nghiệm;
- phân tích ưu/nhược điểm;
- phần thảo luận về paper Lu et al. như tài liệu tham khảo tư duy, không phải phương pháp cần tái hiện.

---

## 17. Định hướng dài hạn

Sau MVP, hệ thống có thể mở rộng theo các hướng:

- học online để thích nghi thay đổi theo thời gian;
- zone ngữ nghĩa do người dùng định nghĩa;
- kết hợp object detector nhẹ để phân biệt người/xe;
- phát hiện bất thường theo cấp hành vi/đoạn thời gian thay vì chỉ theo frame-level;
- thêm quan hệ giữa nhiều vùng liền kề;
- dùng mô hình deep feature cho cảnh phức tạp;
- triển khai dashboard realtime;
- đánh giá trên nhiều camera và nhiều điều kiện môi trường hơn.
</file>

<file path="doc/spec_1_processed.md">
# SPEC 1 Processed - Trạng thái triển khai tiền xử lý UCSD và CUHK Avenue

Tài liệu này đánh dấu các mục đã thực hiện từ `doc/spec_1.md`.

Ngày cập nhật: 2026-05-26

## 1. Tóm tắt trạng thái

Đã tạo thư mục `src/` và triển khai pipeline tiền xử lý theo SPEC 1 ở mức MVP:

- đọc UCSD Ped1/Ped2 dạng chuỗi frame `.tif`;
- đọc CUHK Avenue dạng video `.avi`;
- resize, grayscale, Gaussian blur tùy config;
- tạo grid cố định;
- tạo spatio-temporal cube;
- trích xuất feature baseline bằng frame differencing;
- xuất manifest, grid, feature CSV, stats JSON;
- xuất ARFF cho WEKA;
- tách entrypoint riêng cho UCSD và CUHK Avenue trong `src/tool/`;
- đưa dataset SPEC 1 vào `src/dataset/`;
- đưa output tiền xử lý mặc định vào `src/outputs/`;
- chạy smoke test trên UCSD Ped1, UCSD Ped2 và CUHK Avenue;
- thêm `.gitignore` để không commit dataset, output, archive và paper PDF ở root.

Chưa chạy full preprocess toàn bộ UCSD Ped2 và toàn bộ Avenue trong bước này. Đã kiểm thử bằng giới hạn `1` video/sequence và `10-20` frame để xác nhận pipeline, schema và output.

## 2. Output đã có

Code chính:

- [X] `src/preprocess.py`
- [X] `src/csv_to_arff.py`
- [X] `src/tool/preprocess_ucsd.py`
- [X] `src/tool/preprocess_avenue.py`
- [X] `src/kpdl_preprocess/`
- [X] `src/configs/ucsd_ped2.yaml`
- [X] `src/configs/ucsd_ped1.yaml`
- [X] `src/configs/avenue.yaml`
- [X] `src/requirements.txt`
- [X] `src/README.md`
- [X] `.gitignore`

Dataset nội bộ `src`:

- [X] `src/dataset/UCSD_Anomaly_Dataset.v1p2/`
- [X] `src/dataset/CUHK-avenue/`

Output smoke test:

- [X] `src/outputs/preprocessed/ucsd_ped2/`
- [X] `src/outputs/preprocessed/avenue/`
- [X] `src/outputs/weka/ucsd_ped2_features_train.arff`
- [X] `src/outputs/weka/ucsd_ped2_features_test.arff`
- [X] `src/outputs/weka/avenue_features_train.arff`
- [X] `src/outputs/weka/avenue_features_test.arff`

Output cũ từng sinh ở project root đã được chuyển vào:

- [X] `src/outputs/legacy_root_outputs/`

## 3. Đánh dấu theo phạm vi SPEC 1

### 3.1. Nằm trong phạm vi

- [X] Đọc frame `.tif` của UCSD Ped1/Ped2.
- [X] Đọc video `.avi` của CUHK Avenue.
- [X] Resize frame về kích thước cấu hình.
- [X] Chuyển grayscale.
- [X] Làm trơn nhẹ bằng Gaussian blur nếu cấu hình bật.
- [X] Chuẩn hóa độ sáng nhẹ nếu cấu hình bật.
- [X] Chia frame thành grid cố định.
- [X] Tạo spatio-temporal cube.
- [X] Trích xuất feature chuyển động và ngữ cảnh cơ bản.
- [X] Lưu feature và metadata.
- [X] Kiểm tra số lượng frame, cube, cell và feature qua `preprocess_stats.json`.

### 3.2. Ngoài phạm vi

Các mục này chưa triển khai vì SPEC 1 xác định là ngoài phạm vi:

- [ ] Train model phân cụm.
- [ ] Tính anomaly score.
- [ ] Khai phá association rules.
- [ ] Vẽ heatmap kết quả bất thường.
- [ ] Đánh giá ROC-AUC/EER.
- [ ] Xử lý semantic object detection.

## 4. Đánh dấu theo cấu hình đề xuất

### 4.1. UCSD Ped2 baseline

- [X] `data.dataset = ucsd_ped2`
- [X] `data.root = src/dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2`
- [X] `video.input_type = frame_sequence`
- [X] `resize_width = 320`
- [X] `resize_height = 240`
- [X] `grayscale = true`
- [X] `blur.enabled = true`
- [X] `blur.kernel_size = 3`
- [X] `brightness_normalization.enabled = false`
- [X] `grid.rows = 12`
- [X] `grid.cols = 16`
- [X] `cube.length = 5`
- [X] `cube.stride = 1`
- [X] `motion_method = frame_diff`
- [X] `frame_diff_threshold = 15`
- [X] `direction_bins = 8`
- [X] `use_brightness = true`
- [X] `output.root = src/outputs/preprocessed`
- [X] `output.weka_root = src/outputs/weka`

File: `src/configs/ucsd_ped2.yaml`

### 4.2. CUHK Avenue baseline

- [X] `data.dataset = avenue`
- [X] `data.root = src/dataset/CUHK-avenue`
- [X] `video.input_type = video`
- [X] `resize_width = 320`
- [X] `resize_height = 240`
- [X] `grayscale = true`
- [X] `blur.enabled = true`
- [X] `blur.kernel_size = 3`
- [X] `brightness_normalization.enabled = false`
- [X] `grid.rows = 12`
- [X] `grid.cols = 16`
- [X] `cube.length = 5`
- [X] `cube.stride = 1`
- [X] `motion_method = frame_diff`
- [X] `frame_diff_threshold = 15`
- [X] `direction_bins = 8`
- [X] `use_brightness = true`
- [X] `output.root = src/outputs/preprocessed`
- [X] `output.weka_root = src/outputs/weka`

File: `src/configs/avenue.yaml`

## 5. Đánh dấu theo pipeline chung

### Bước 1 - Quét dataset

- [X] Tạo danh sách `dataset, split, video_id, source_path, input_type`.
- [X] UCSD: mỗi thư mục `TrainXXX` hoặc `TestXXX` là một sequence.
- [X] Avenue: mỗi file `.avi` là một video.
- [X] Bỏ qua thư mục ground truth `_gt` khi trích xuất feature.

Module: `src/kpdl_preprocess/datasets.py`

### Bước 2 - Đọc frame

UCSD:

- [X] Đọc ảnh `.tif` theo thứ tự tăng dần.
- [X] `frame_id` lấy từ tên file.
- [X] `timestamp` gán theo thứ tự frame khi không có FPS.

Avenue:

- [X] Đọc `.avi` bằng `cv2.VideoCapture`.
- [X] `frame_id` bắt đầu từ 1.
- [X] `timestamp = frame_id / fps` nếu đọc được FPS, ngược lại để trống.

Module: `src/kpdl_preprocess/readers.py`

### Bước 3 - Chuẩn hóa frame

- [X] Đọc frame RGB/BGR hoặc grayscale.
- [X] Resize theo config.
- [X] Chuyển grayscale.
- [X] Blur nhẹ nếu bật.
- [X] Chuẩn hóa độ sáng nếu bật.
- [X] Lưu kích thước gốc và kích thước resize trong manifest.

Module: `src/kpdl_preprocess/readers.py`

### Bước 4 - Tạo grid

- [X] Grid mặc định `rows = 12`, `cols = 16`.
- [X] Với `320x240`, tạo `192` cell.
- [X] Lưu `cell_id` dạng `row_col`, ví dụ `08_05`.
- [X] Xuất `grid.json`.

Module: `src/kpdl_preprocess/grid.py`

### Bước 5 - Tạo cube

- [X] Cube sliding window với `length = 5`, `stride = 1`.
- [X] Công thức số cube đúng: `floor((N - length) / stride) + 1`.
- [X] Không lưu ảnh cube ra đĩa.
- [X] Lưu metadata cube trong từng dòng feature.

Module: `src/kpdl_preprocess/pipeline.py`

### Bước 6 - Trích xuất feature

- [X] Tính chênh lệch tuyệt đối giữa các frame liên tiếp trong cube.
- [X] Threshold để tạo motion mask.
- [X] Tính feature theo từng cell.
- [X] `foreground_ratio`
- [X] `motion_magnitude_mean`
- [X] `motion_magnitude_std`
- [X] `motion_density`
- [X] `direction_hist_0..7`
- [X] `brightness_mean`
- [X] `brightness_delta`

Ghi chú: với `motion_method = frame_diff`, các cột `direction_hist_0..7` hiện được tạo và gán `0.0`, đúng với ghi chú trong SPEC 1 rằng direction histogram chưa đáng tin cậy nếu chưa dùng optical flow.

Module: `src/kpdl_preprocess/features.py`

### Bước 7 - Lưu feature

- [X] `frames_manifest.csv`
- [X] `videos_manifest.csv`
- [X] `grid.json`
- [X] `features_train.csv`
- [X] `features_test.csv`
- [X] `preprocess_stats.json`

Đã xác nhận bằng smoke test tại:

- `src/outputs/preprocessed/ucsd_ped2/`
- `src/outputs/preprocessed/avenue/`

ARFF mặc định lưu tại:

- `src/outputs/weka/`

### Bước 8 - Xuất ARFF cho WEKA

- [X] Convert feature CSV sang ARFF.
- [X] Giữ `cell_id` dạng nominal.
- [X] Cột numeric giữ dạng numeric.
- [X] Cột text dài như `dataset`, `split`, `video_id`, `cube_id` để dạng string.

Module:

- `src/kpdl_preprocess/arff.py`
- `src/csv_to_arff.py`

## 6. Đánh dấu theo kế hoạch riêng cho UCSD

- [X] Ưu tiên UCSD Ped2 trước.
- [X] Có script riêng `src/tool/preprocess_ucsd.py`.
- [X] Hỗ trợ `--ped ped2` và `--ped ped1`.
- [X] Dataset UCSD đã nằm trong `src/dataset/UCSD_Anomaly_Dataset.v1p2/`.
- [X] Đọc các file `.tif` theo thứ tự tên file.
- [X] Tên sequence là tên thư mục, ví dụ `Train001`, `Test001`.
- [X] Không đọc thư mục `*_gt` trong tiền xử lý feature.
- [X] Ground truth mask chưa dùng, đúng phạm vi SPEC 1.
- [X] Lưu `videos_manifest.csv`.
- [X] Lưu `frames_manifest.csv`.
- [X] Không hard-code kích thước frame gốc.
- [X] Smoke test UCSD Ped1 train với `1` sequence và `10` frame.
- [X] Smoke test UCSD Ped2 train/test với `1` sequence mỗi split và `10` frame.
- [X] Chạy full preprocess toàn bộ UCSD Ped2.
- [ ] Mở rộng/chạy full UCSD Ped1.

## 7. Đánh dấu theo kế hoạch riêng cho CUHK Avenue

- [X] Có script riêng `src/tool/preprocess_avenue.py`.
- [X] Dataset Avenue đã nằm trong `src/dataset/CUHK-avenue/`.
- [X] Đọc `training_videos/*.avi`.
- [X] Đọc `testing_videos/*.avi`.
- [X] Bỏ qua `training_vol` và `testing_vol` trong MVP.
- [X] Dùng OpenCV đọc từng frame.
- [X] Lấy FPS, width, height từ `VideoCapture`.
- [X] `video_id` lấy từ tên file không có đuôi, ví dụ `01`.
- [X] Lưu `videos_manifest.csv`.
- [X] Lưu `frames_manifest.csv`.
- [X] Smoke test Avenue train/test với `1` video mỗi split và `10` frame.
- [ ] Chạy full preprocess toàn bộ Avenue.
- [ ] Nghiên cứu file `.mat` để lấy annotation sau.

## 8. Kiểm tra chất lượng đầu ra

Đã có các trường thống kê trong `preprocess_stats.json`:

- [X] `dataset`
- [X] `split`
- [X] `num_videos`
- [X] `num_frames`
- [X] `num_cubes`
- [X] `num_cells`
- [X] `num_feature_rows`
- [X] `missing_frames`
- [X] `failed_videos`
- [X] `avg_motion_density`
- [X] `avg_brightness`

Kết quả smoke test:

- [X] UCSD Ped2 train/test đọc được `1` sequence mỗi split.
- [X] UCSD Ped1 train đọc được `1` sequence.
- [X] Avenue train/test đọc được `1` video mỗi split.
- [X] Với `10` frame, `cube.length = 5`, `stride = 1`: mỗi split có `6` cube.
- [X] Với `12 x 16 = 192` cell: mỗi split có `6 * 192 = 1152` dòng feature.
- [X] `failed_videos = 0`.
- [X] ARFF sinh được cho train/test của UCSD Ped2 và Avenue.
- [X] Header `features_train.csv`, `frames_manifest.csv`, `videos_manifest.csv` của UCSD và Avenue khớp nhau sau khi chuyển output vào `src/outputs`.

Chưa kiểm tra ở quy mô full:

- [ ] Đọc toàn bộ video/sequence trong split.
- [ ] So khớp frame manifest với toàn bộ frame thực tế.
- [ ] Kiểm tra NaN trên toàn bộ feature CSV lớn.
- [ ] Kiểm tra phân phối feature trên toàn bộ dataset.

## 9. Checklist thực hiện từ SPEC 1

- [X] Tạo config riêng cho `ucsd_ped2`.
- [X] Tạo config riêng cho `ucsd_ped1`.
- [X] Tạo config riêng cho `avenue`.
- [X] Tách file chạy riêng cho UCSD.
- [X] Tách file chạy riêng cho Avenue.
- [X] Viết dataset scanner cho frame sequence và video.
- [X] Viết frame reader cho UCSD.
- [X] Viết video reader cho Avenue.
- [X] Viết preprocess transform dùng chung.
- [X] Viết grid generator.
- [X] Viết cube iterator.
- [X] Viết feature extractor baseline bằng frame differencing.
- [X] Lưu manifest, grid và feature CSV.
- [X] Tạo script convert CSV sang ARFF cho WEKA.
- [X] Chạy smoke test trên 1 video/sequence mỗi dataset.
- [X] Chuyển dataset UCSD và Avenue vào `src/dataset`.
- [X] Chuyển output mặc định vào `src/outputs`.
- [X] Thêm `.gitignore` cho dataset/output/PDF/archive/cache.
- [X] Chạy full preprocess cho UCSD Ped2.
- [ ] Chạy full preprocess cho Avenue.
- [X] Ghi `preprocess_stats.json`.

## 10. Tiêu chí hoàn thành SPEC 1

- [ ] UCSD Ped2 có `features_train.csv` và `features_test.csv` full dataset.
- [ ] Avenue có `features_train.csv` và `features_test.csv` full dataset.
- [X] Mỗi feature row truy vết được về video/frame/cell/cube.
- [X] UCSD và Avenue xuất cùng một schema chung qua `src/kpdl_preprocess/schema.py`.
- [X] Có `grid.json` để dùng lại khi vẽ heatmap.
- [X] Có `preprocess_stats.json` cho mỗi dataset trong smoke test.
- [X] Có thể xuất ARFF để thử nghiệm WEKA.
- [X] Pipeline có thể chạy lại bằng config mà không cần sửa code.
- [X] Kết quả tiền xử lý mặc định nằm trong `src/outputs`.
- [X] Dataset và output không bị commit nhờ `.gitignore`.

## 11. Lệnh đã dùng để kiểm thử

```bash
python tool/preprocess_ucsd.py --ped ped2 --limit-videos 1 --limit-frames 10 --export-arff
```

```bash
python tool/preprocess_avenue.py --limit-videos 1 --limit-frames 10 --export-arff
```

```bash
python tool/preprocess_ucsd.py --ped ped1 --split train --limit-videos 1 --limit-frames 10
```

## 12. Lệnh đề xuất để hoàn tất full preprocess

```bash
python tool/preprocess_ucsd.py --ped ped2 --export-arff
```

```bash
python tool/preprocess_avenue.py --export-arff
```

## 13. Quản lý dữ liệu và Git

Đã thêm `.gitignore` để tránh commit các dữ liệu và artifact nặng:

- [X] `/New folder/`
- [X] `/dataset/`
- [X] `/src/dataset/`
- [X] `/outputs/`
- [X] `/src/outputs/`
- [X] `/*.pdf`
- [X] `__pycache__/`
- [X] `*.py[cod]`

Đã kiểm tra bằng `git check-ignore`:

- [X] `New folder`
- [X] `dataset`
- [X] `src/dataset`
- [X] `src/outputs`
- [X] các PDF paper ở project root.

## 14. Cập nhật 2026-05-26 - Avenue full preprocess chạy lâu

Spec gốc: `src/doc/spec_1.md`

- [X] Kiểm tra lại luồng `src/tool/preprocess_avenue.py`: entrypoint chỉ gọi pipeline dùng chung, nguyên nhân nằm ở đọc video/ghi feature và việc thiếu log tiến độ.
- [X] Probe toàn bộ CUHK Avenue: 16 video train, 21 video test, tổng `30652` frame; OpenCV đọc thô toàn bộ video kết thúc bình thường trong khoảng `9.37s`, không thấy file `.avi` bị đọc vô hạn.
- [X] Thêm `--progress-every` cho `src/tool/_common.py` và `src/kpdl_preprocess/cli.py` để in tiến độ theo video/frame khi chạy full preprocess.
- [X] Thêm guard trong `src/kpdl_preprocess/readers.py` dựa trên `CAP_PROP_FRAME_COUNT` cộng tolerance để tránh trường hợp `VideoCapture.read()` không tự kết thúc ở video lỗi.
- [X] Cập nhật `src/kpdl_preprocess/pipeline.py` để log start/progress/done/fail từng video và ghi `missing_frames` khi số frame đọc được thấp hơn metadata.
- [X] Verification: `python -m compileall src\kpdl_preprocess src\tool`
- [X] Verification: `python src\tool\preprocess_avenue.py --limit-videos 1 --limit-frames 10 --output-root src\outputs\preprocessed_fix_smoke --export-arff --progress-every 5`
- [ ] Chưa chạy full Avenue lại trong bước này vì full dataset sẽ tạo khoảng `5.85M` dòng feature trước khi export ARFF.
</file>

<file path="doc/spec_1.md">
# SPEC 1 - Kế hoạch tiền xử lý dữ liệu UCSD và CUHK Avenue

## 1. Mục tiêu

Tài liệu này mô tả kế hoạch tiền xử lý dữ liệu cho hai bộ dataset:

- UCSD Ped1/Ped2;
- CUHK Avenue.

Mục tiêu của giai đoạn này là biến dữ liệu video/frame thô thành tập dữ liệu đặc trưng có cấu trúc, sẵn sàng cho các bước phân cụm hành vi, token hóa và khai phá luật kết hợp trong pipeline phát hiện bất thường.

Kết quả đầu ra không phải là các file ảnh/video đã xử lý đơn lẻ, mà là:

- frame đã được chuẩn hóa để pipeline có thể đọc lại;
- metadata về video/frame/cell/cube;
- feature dạng bảng CSV/Parquet;
- mapping giữa feature và vị trí trên frame để vẽ heatmap sau này.

## 2. Nguyên tắc thiết kế

- Tiền xử lý video/frame bằng Python và OpenCV, không dùng WEKA cho bước xử lý ảnh thô.
- WEKA chỉ được dùng sau khi đã trích xuất feature thành bảng CSV/ARFF.
- Giữ nguyên cấu trúc train/test gốc của dataset.
- Không trộn frame train và test trong quá trình tính thống kê chuẩn hóa.
- Mỗi bản ghi feature phải truy vết được về `dataset`, `split`, `video_id`, `frame_id`, `cube_id` và `cell_id`.
- Ưu tiên pipeline chạy lặp lại được bằng config.
- Lưu các thông số tiền xử lý để bảo đảm train/test dùng cùng một cách xử lý.

## 3. Phạm vi

### 3.1. Nằm trong phạm vi

- Đọc frame `.tif` của UCSD Ped1/Ped2.
- Đọc video `.avi` của CUHK Avenue.
- Resize frame về kích thước cấu hình.
- Chuyển grayscale.
- Làm trơn nhẹ bằng Gaussian blur nếu cấu hình bật.
- Chuẩn hóa độ sáng nhẹ nếu cấu hình bật.
- Chia frame thành grid cố định.
- Tạo spatio-temporal cube.
- Trích xuất feature chuyển động và ngữ cảnh cơ bản.
- Lưu feature và metadata.
- Kiểm tra số lượng frame, cube, cell và feature.

### 3.2. Ngoài phạm vi

- Train model phân cụm.
- Tính anomaly score.
- Khai phá association rules.
- Vẽ heatmap kết quả bất thường.
- Đánh giá ROC-AUC/EER.
- Xử lý semantic object detection.

## 4. Cấu trúc dataset hiện có

### 4.1. UCSD

Thư mục gốc:

```text
dataset/UCSD_Anomaly_Dataset.v1p2/
  UCSDped1/
  UCSDped2/
```

Mỗi tập Ped1/Ped2 có cấu trúc chính:

```text
UCSDpedX/
  Train/
    Train001/
      001.tif
      002.tif
      ...
  Test/
    Test001/
      001.tif
      002.tif
      ...
    Test001_gt/
      frame001.bmp
      ...
```

Đặc điểm:

- dữ liệu đã tách thành chuỗi frame `.tif`;
- train mặc định là normal;
- test có thể có thư mục ground truth mask `_gt`;
- Ped2 nhỏ hơn, nên ưu tiên làm baseline đầu tiên.

### 4.2. CUHK Avenue

Thư mục gốc:

```text
dataset/CUHK-avenue/
  training_videos/
  testing_videos/
  training_vol/
  testing_vol/
```

Đặc điểm:

- video train/test ở dạng `.avi`;
- các file `.mat` trong `training_vol` và `testing_vol` có thể dùng đối chiếu hoặc nghiên cứu sau;
- nên đọc trực tiếp `.avi` bằng OpenCV cho pipeline MVP;
- train mặc định là normal.

## 5. Đầu ra chuẩn hóa

Tất cả dataset sau tiền xử lý cần quy về cùng một schema logic.

### 5.1. FrameRecord

```text
FrameRecord {
  dataset,
  split,
  video_id,
  frame_id,
  timestamp,
  source_path,
  original_width,
  original_height,
  resized_width,
  resized_height,
  frame_gray
}
```

### 5.2. CellRecord

```text
CellRecord {
  cell_id,
  row,
  col,
  x1,
  y1,
  x2,
  y2,
  width,
  height
}
```

### 5.3. CubeRecord

```text
CubeRecord {
  dataset,
  split,
  video_id,
  cube_id,
  start_frame_id,
  end_frame_id,
  center_frame_id,
  cell_id
}
```

### 5.4. FeatureRecord

```text
FeatureRecord {
  dataset,
  split,
  video_id,
  cube_id,
  start_frame_id,
  end_frame_id,
  center_frame_id,
  cell_id,
  cell_row,
  cell_col,
  foreground_ratio,
  motion_magnitude_mean,
  motion_magnitude_std,
  motion_density,
  direction_hist_0,
  direction_hist_1,
  direction_hist_2,
  direction_hist_3,
  direction_hist_4,
  direction_hist_5,
  direction_hist_6,
  direction_hist_7,
  brightness_mean,
  brightness_delta
}
```

## 6. Cấu hình tiền xử lý đề xuất

### 6.1. UCSD Ped2 baseline

```yaml
data:
  dataset: "ucsd_ped2"
  root: "dataset/UCSD_Anomaly_Dataset.v1p2/UCSDped2"
  train_path: "Train"
  test_path: "Test"

video:
  input_type: "frame_sequence"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true
```

### 6.2. CUHK Avenue baseline

```yaml
data:
  dataset: "avenue"
  root: "dataset/CUHK-avenue"
  train_path: "training_videos"
  test_path: "testing_videos"

video:
  input_type: "video"
  resize_width: 320
  resize_height: 240
  grayscale: true
  blur:
    enabled: true
    kernel_size: 3
  brightness_normalization:
    enabled: false

grid:
  rows: 12
  cols: 16
  ignore_cells: []

cube:
  length: 5
  stride: 1

features:
  motion_method: "frame_diff"
  frame_diff_threshold: 15
  direction_bins: 8
  use_brightness: true
```

## 7. Pipeline tiền xử lý chung

### Bước 1 - Quét dataset

Với mỗi dataset, tạo danh sách video/sequence:

```text
dataset, split, video_id, source_path, input_type
```

Quy tắc:

- UCSD: mỗi thư mục `TrainXXX` hoặc `TestXXX` là một sequence.
- Avenue: mỗi file `.avi` là một video.
- Bỏ qua các thư mục ground truth `_gt` trong quá trình trích xuất feature đầu vào.

### Bước 2 - Đọc frame

UCSD:

- đọc ảnh `.tif` theo thứ tự tăng dần;
- `frame_id` lấy từ tên file;
- `timestamp` có thể gán theo thứ tự frame nếu không có FPS.

Avenue:

- đọc `.avi` bằng `cv2.VideoCapture`;
- `frame_id` là chỉ số frame bắt đầu từ 1;
- `timestamp = frame_id / fps` nếu đọc được FPS, ngược lại gán `null`.

### Bước 3 - Chuẩn hóa frame

Mỗi frame cần được xử lý theo thứ tự:

1. đọc frame RGB/BGR;
2. resize về kích thước cấu hình;
3. chuyển grayscale;
4. blur nhẹ nếu bật;
5. chuẩn hóa độ sáng nếu bật;
6. lưu thông tin kích thước gốc và kích thước resize.

Khuyến nghị MVP:

- bật resize và grayscale;
- bật blur kernel `3x3`;
- tắt brightness normalization trong lần đầu để pipeline đơn giản và dễ giải thích.

### Bước 4 - Tạo grid

Grid mặc định:

```text
rows = 12
cols = 16
```

Với frame `320x240`, mỗi cell có kích thước xấp xỉ:

```text
cell_width = 20
cell_height = 20
```

Cần lưu `cell_id` theo dạng:

```text
row_col
```

Ví dụ:

```text
08_05
```

### Bước 5 - Tạo cube

Cube là cửa sổ trượt theo thời gian:

```text
length = 5
stride = 1
```

Với mỗi video/sequence có `N` frame, số cube:

```text
num_cubes = floor((N - length) / stride) + 1
```

Mỗi cube được cắt theo từng cell, nhưng không cần lưu ảnh cube ra đĩa nếu không cần debug. Chỉ cần trích xuất feature và lưu metadata.

### Bước 6 - Trích xuất feature

Baseline dùng frame differencing:

1. tính chênh lệch tuyệt đối giữa các frame liên tiếp trong cube;
2. threshold để tạo motion mask;
3. tính feature theo từng cell.

Feature bắt buộc:

- `foreground_ratio`: tỷ lệ pixel có chuyển động trong cell;
- `motion_magnitude_mean`: trung bình độ lớn chuyển động;
- `motion_magnitude_std`: độ lệch chuẩn độ lớn chuyển động;
- `motion_density`: tỷ lệ pixel vượt ngưỡng chuyển động;
- `direction_hist_8bins`: histogram hướng chuyển động, nếu chưa dùng optical flow thì có thể gán zero hoặc ước lượng sau;
- `brightness_mean`: độ sáng trung bình trong cell;
- `brightness_delta`: chênh lệch độ sáng giữa frame đầu và frame cuối cube.

Ghi chú:

- Nếu `motion_method = frame_diff`, direction histogram có thể chưa đáng tin cậy.
- Khi cần hướng chuyển động tốt hơn, thêm biến thể `motion_method = farneback`.

### Bước 7 - Lưu feature

Đầu ra đề xuất:

```text
outputs/preprocessed/
  ucsd_ped2/
    frames_manifest.csv
    videos_manifest.csv
    grid.json
    features_train.csv
    features_test.csv
    preprocess_stats.json
  avenue/
    frames_manifest.csv
    videos_manifest.csv
    grid.json
    features_train.csv
    features_test.csv
    preprocess_stats.json
```

Nếu dữ liệu lớn, có thể dùng Parquet:

```text
features_train.parquet
features_test.parquet
```

CSV vẫn nên được hỗ trợ để có thể import vào WEKA.

### Bước 8 - Xuất ARFF cho WEKA

Sau khi có CSV feature, tạo thêm file ARFF nếu cần chạy WEKA:

```text
outputs/weka/
  ucsd_ped2_features_train.arff
  ucsd_ped2_features_test.arff
  avenue_features_train.arff
  avenue_features_test.arff
```

Quy tắc:

- bỏ các cột text dài nếu WEKA xử lý chậm;
- giữ `cell_id` dạng nominal nếu cần phân tích theo cell;
- các cột numeric giữ nguyên;
- chưa gán label anomaly trong file train.

## 8. Kế hoạch riêng cho UCSD

### 8.1. Thứ tự ưu tiên

1. Làm UCSD Ped2 trước.
2. Kiểm tra pipeline với 1 sequence train và 1 sequence test.
3. Chạy toàn bộ Ped2.
4. Sau khi ổn định, mở rộng sang Ped1.

### 8.2. Xử lý frame

- Đọc các file `.tif` theo thứ tự tên file.
- Tên sequence là tên thư mục, ví dụ `Train001`, `Test001`.
- Không đọc thư mục `*_gt` trong tiền xử lý feature.
- Ground truth mask chỉ dùng trong giai đoạn đánh giá sau.

### 8.3. Lưu metadata UCSD

`videos_manifest.csv`:

```text
dataset,split,video_id,source_path,input_type,num_frames
```

`frames_manifest.csv`:

```text
dataset,split,video_id,frame_id,source_path,original_width,original_height,resized_width,resized_height
```

### 8.4. Rủi ro UCSD

- Frame `.tif` có thể đọc thành grayscale sẵn, cần xử lý thống nhất.
- Ped1 và Ped2 có kích thước frame khác nhau, không hard-code theo một bộ.
- Ground truth mask có tên file khác frame source, cần tách riêng cho giai đoạn evaluation.

## 9. Kế hoạch riêng cho CUHK Avenue

### 9.1. Thứ tự ưu tiên

1. Đọc `training_videos/*.avi`.
2. Đọc `testing_videos/*.avi`.
3. Bỏ qua `training_vol` và `testing_vol` trong MVP tiền xử lý.
4. Sau khi pipeline feature ổn định, nghiên cứu file `.mat` để lấy thêm annotation nếu cần.

### 9.2. Xử lý video

- Dùng OpenCV đọc từng frame.
- Lấy FPS, width, height từ `VideoCapture`.
- Nếu video lỗi frame, log cảnh báo và tiếp tục nếu có thể.
- `video_id` lấy từ tên file không có đuôi, ví dụ `01`, `02`, `21`.

### 9.3. Lưu metadata Avenue

`videos_manifest.csv`:

```text
dataset,split,video_id,source_path,input_type,fps,num_frames,original_width,original_height
```

`frames_manifest.csv`:

```text
dataset,split,video_id,frame_id,timestamp,original_width,original_height,resized_width,resized_height
```

### 9.4. Rủi ro Avenue

- `.avi` có thể có FPS/metadata không ổn định, cần fallback theo frame index.
- Video dài hơn UCSD, feature CSV có thể lớn.
- File `.mat` không cần dùng trong MVP đọc video, tránh làm chậm tiến độ.

## 10. Kiểm tra chất lượng đầu ra

Sau mỗi lần chạy tiền xử lý, cần có báo cáo:

```text
dataset
split
num_videos
num_frames
num_cubes
num_cells
num_feature_rows
missing_frames
failed_videos
avg_motion_density
avg_brightness
```

Điều kiện chấp nhận:

- đọc được tất cả video/sequence trong split;
- số frame trong manifest khớp với số frame thực tế đọc được;
- số dòng feature xấp xỉ `num_cubes * rows * cols`;
- không có cột feature numeric bị `NaN` hàng loạt;
- `foreground_ratio` và `motion_density` nằm trong khoảng `[0, 1]`;
- `brightness_mean` nằm trong khoảng `[0, 255]`;
- grid mapping đúng kích thước frame resize.

## 11. Checklist thực hiện

- Tạo config riêng cho `ucsd_ped2`.
- Tạo config riêng cho `avenue`.
- Viết dataset scanner cho frame sequence và video.
- Viết frame reader cho UCSD.
- Viết video reader cho Avenue.
- Viết preprocess transform dùng chung.
- Viết grid generator.
- Viết cube iterator.
- Viết feature extractor baseline bằng frame differencing.
- Lưu manifest, grid và feature CSV.
- Tạo script convert CSV sang ARFF cho WEKA.
- Chạy smoke test trên 1 video/sequence mỗi dataset.
- Chạy full preprocess cho UCSD Ped2.
- Chạy full preprocess cho Avenue.
- Ghi `preprocess_stats.json`.

## 12. Tiêu chí hoàn thành

Giai đoạn tiền xử lý được xem là hoàn thành khi:

- UCSD Ped2 có `features_train.csv` và `features_test.csv`;
- Avenue có `features_train.csv` và `features_test.csv`;
- mỗi feature row truy vết được về video/frame/cell/cube;
- có `grid.json` để dùng lại khi vẽ heatmap;
- có `preprocess_stats.json` cho mỗi dataset;
- có thể xuất ARFF để thử nghiệm WEKA;
- pipeline có thể chạy lại bằng config mà không cần sửa code.
</file>

<file path="doc/spec_2.md">
# SPEC 2 - Kế hoạch triển khai phân cụm và khai phá luật trên WEKA

## 1. Mục tiêu

Tài liệu này mô tả kế hoạch dùng WEKA cho giai đoạn sau tiền xử lý của dự án phát hiện hành vi bất thường trên camera giám sát.

SPEC 1 đã tạo dữ liệu đặc trưng dạng bảng và xuất ARFF. SPEC 2 tập trung vào việc dùng các file ARFF đó để:

- kiểm tra nhanh chất lượng feature bằng giao diện WEKA;
- chạy phân cụm hành vi không gian-thời gian trên dữ liệu train normal;
- so sánh một số cấu hình phân cụm cơ bản;
- gán nhãn cụm cho train/test để phục vụ bước anomaly scoring sau này;
- rời rạc hóa feature và thử khai phá luật kết hợp bằng Apriori trong WEKA;
- ghi lại kết quả thực nghiệm sao cho có thể đối chiếu với pipeline Python của dự án.

WEKA trong SPEC 2 được dùng như công cụ thực nghiệm, phân tích và đối chiếu. Runtime chính của hệ thống vẫn nên được triển khai bằng Python ở các SPEC sau để dễ tự động hóa, tính điểm bất thường, vẽ heatmap và đánh giá ROC-AUC/EER.

## 2. Liên hệ với PRD và SPEC 1

Theo PRD, hai trụ cột nghiên cứu của hệ thống là:

- phân cụm hành vi-không gian-thời gian;
- khai phá luật kết hợp để giải thích ngữ cảnh bất thường.

SPEC 1 đã hoàn thành phần tạo feature đầu vào cho hai trụ cột này:

- đọc UCSD Ped1/Ped2 dạng frame `.tif`;
- đọc CUHK Avenue dạng video `.avi`;
- resize, grayscale, blur theo cấu hình;
- chia grid `12 x 16`;
- tạo cube độ dài `5`, stride `1`;
- trích xuất feature bằng frame differencing;
- xuất `features_train.csv`, `features_test.csv`;
- xuất ARFF cho WEKA.

SPEC 2 bắt đầu từ các file ARFF đã có, không xử lý ảnh/video thô trong WEKA.

## 3. Phạm vi

### 3.1. Nằm trong phạm vi

- Import ARFF vào WEKA Explorer.
- Kiểm tra schema, số dòng, kiểu thuộc tính và giá trị thiếu.
- Lọc bỏ các thuộc tính định danh không nên đưa vào mô hình.
- Chuẩn hóa feature numeric trước khi phân cụm.
- Chạy phân cụm bằng `SimpleKMeans` làm baseline chính.
- Thử `EM` để so sánh cụm mềm và số cụm tự ước lượng.
- Thử `Canopy` hoặc `FarthestFirst` nếu cần baseline nhanh trên dữ liệu lớn.
- Chạy per-cell clustering ở mức thử nghiệm cho một số cell đại diện.
- Xuất model, log kết quả, centroid, cluster size và cluster assignment.
- Dùng `Discretize` và `Apriori` trong WEKA để thử khai phá luật kết hợp.
- Ghi lại kết quả vào thư mục thí nghiệm và file processed sau khi triển khai.

### 3.2. Ngoài phạm vi

- Xử lý ảnh/video thô bằng WEKA.
- Vẽ heatmap bất thường.
- Tính anomaly score hoàn chỉnh.
- Tính ROC-AUC/EER hoàn chỉnh.
- Train model production chạy realtime.
- Tối ưu thuật toán phân cụm ngoài các baseline có sẵn trong WEKA.
- Thay thế pipeline Python của dự án.

## 4. Dữ liệu đầu vào

Các file ARFF đầu vào mặc định nằm tại:

```text
src/outputs/weka/
  ucsd_ped2_features_train.arff
  ucsd_ped2_features_test.arff
  ucsd_ped1_features_train.arff
  ucsd_ped1_features_test.arff
  avenue_features_train.arff
  avenue_features_test.arff
```

Thứ tự ưu tiên thực nghiệm:

1. `ucsd_ped2_features_train.arff`: nhỏ hơn Avenue, phù hợp để kiểm tra quy trình.
2. `ucsd_ped2_features_test.arff`: dùng để gán cụm và so sánh phân bố train/test.
3. `ucsd_ped1_features_train.arff`: mở rộng sau khi Ped2 ổn.
4. `avenue_features_train.arff`: dữ liệu lớn hơn, chỉ chạy sau khi quy trình và cấu hình đã rõ.

Kích thước dữ liệu hiện tại theo `preprocess_stats.json`:

```text
UCSD Ped2 train:
  videos: 16
  frames: 2550
  cubes: 2486
  cells: 192
  feature rows: 477312

UCSD Ped2 test:
  videos: 12
  frames: 2010
  cubes: 1962
  cells: 192
  feature rows: 376704

Avenue train:
  videos: 16
  frames: 15328
  cubes: 15264
  cells: 192
  feature rows: 2930688

Avenue test:
  videos: 21
  frames: 15324
  cubes: 15240
  cells: 192
  feature rows: 2926080
```

Với Avenue, nên dùng sample hoặc chạy batch/CLI vì file ARFF có kích thước lớn.

## 5. Schema feature

ARFF từ SPEC 1 có các thuộc tính:

```text
1  dataset
2  split
3  video_id
4  cube_id
5  start_frame_id
6  end_frame_id
7  center_frame_id
8  cell_id
9  cell_row
10 cell_col
11 foreground_ratio
12 motion_magnitude_mean
13 motion_magnitude_std
14 motion_density
15 direction_hist_0
16 direction_hist_1
17 direction_hist_2
18 direction_hist_3
19 direction_hist_4
20 direction_hist_5
21 direction_hist_6
22 direction_hist_7
23 brightness_mean
24 brightness_delta
```

Ghi chú quan trọng:

- `dataset`, `split`, `video_id`, `cube_id` là định danh, không dùng để phân cụm.
- `start_frame_id`, `end_frame_id`, `center_frame_id` có thể tạo nhiễu hoặc làm cụm học theo thứ tự thời gian, không dùng trong baseline đầu tiên.
- `cell_id` là nominal, hữu ích để truy vết, nhưng không nên đưa trực tiếp vào KMeans baseline.
- `cell_row`, `cell_col` có thể dùng cho global spatio-temporal clustering, nhưng không dùng trong per-cell clustering.
- `direction_hist_0..7` hiện bằng `0.0` với `motion_method = frame_diff`, nên bỏ khỏi baseline đầu tiên.
- Các feature numeric chính hiện nên dùng là `foreground_ratio`, `motion_magnitude_mean`, `motion_magnitude_std`, `motion_density`, `brightness_mean`, `brightness_delta`.

## 6. Bộ thuộc tính đề xuất

### 6.1. Baseline A - Behavior-only global clustering

Mục tiêu: kiểm tra xem feature chuyển động và ánh sáng có tạo được cụm hành vi cơ bản hay không.

Giữ các thuộc tính:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Trong ARFF gốc, giữ index:

```text
11-14,23-24
```

Loại bỏ:

```text
1-10,15-22
```

### 6.2. Baseline B - Spatio-behavior global clustering

Mục tiêu: cho phép cụm học thêm vị trí cell trong frame.

Giữ các thuộc tính:

```text
cell_row
cell_col
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Trong ARFF gốc, giữ index:

```text
9-14,23-24
```

Baseline này có thể tạo cụm theo vùng không gian. Cần so sánh với Baseline A để tránh trường hợp model chỉ học vị trí mà không học hành vi.

### 6.3. Baseline C - Per-cell behavior clustering

Mục tiêu: bám sát PRD hơn, vì mỗi cell/zone có normal pattern riêng.

Với mỗi cell được chọn, lọc dữ liệu theo `cell_id`, sau đó giữ:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

Không giữ `cell_row`, `cell_col`, `cell_id` vì tất cả bản ghi đã thuộc cùng một cell.

Trong WEKA Explorer, chỉ nên thử per-cell cho một số cell đại diện. Chạy đủ `192` cell bằng tay sẽ tốn thời gian và dễ sai. Nếu cần chạy toàn bộ cell, nên dùng WEKA CLI hoặc chuyển sang Python ở SPEC sau.

## 7. Quy trình thao tác trong WEKA Explorer

### Bước 1 - Mở dữ liệu

1. Mở `WEKA Explorer`.
2. Vào tab `Preprocess`.
3. Chọn `Open file`.
4. Mở `src/outputs/weka/ucsd_ped2_features_train.arff`.
5. Kiểm tra số instances, số attributes và kiểu dữ liệu.

Điều kiện chấp nhận:

- WEKA đọc được file không lỗi parse ARFF.
- Các feature numeric không bị nhận nhầm thành string.
- `cell_id` là nominal.
- Không có cột numeric bị missing hàng loạt.

### Bước 2 - Lọc thuộc tính

Với Baseline A, dùng filter:

```text
weka.filters.unsupervised.attribute.Remove
```

Cách cấu hình:

```text
attributeIndices: 11-14,23-24
invertSelection: True
```

Sau khi apply, chỉ còn 6 thuộc tính numeric.

Với Baseline B:

```text
attributeIndices: 9-14,23-24
invertSelection: True
```

Sau khi apply, chỉ còn 8 thuộc tính numeric.

### Bước 3 - Xử lý missing value

Nếu WEKA báo có missing value, dùng:

```text
weka.filters.unsupervised.attribute.ReplaceMissingValues
```

Điều kiện mong đợi: SPEC 1 không sinh missing value trong feature numeric. Bước này chủ yếu là guard.

### Bước 4 - Chuẩn hóa feature

Do `brightness_mean` có thang `[0, 255]` còn motion feature thường nhỏ hơn nhiều, cần chuẩn hóa trước KMeans.

Filter đề xuất:

```text
weka.filters.unsupervised.attribute.Standardize
```

Có thể dùng `Normalize`, nhưng `Standardize` phù hợp hơn cho KMeans khi các feature có đơn vị đo khác nhau.

### Bước 5 - Chạy SimpleKMeans

Vào tab `Cluster`, chọn:

```text
weka.clusterers.SimpleKMeans
```

Cấu hình baseline:

```text
numClusters: 5
seed: 10
maxIterations: 500
preserveInstancesOrder: True
```

Chạy các cấu hình K:

```text
K = 3
K = 5
K = 8
```

Lưu lại:

- cluster centroids;
- number of instances per cluster;
- within cluster sum of squared errors;
- thời gian chạy;
- nhận xét xem centroid có ý nghĩa hành vi hay không.

### Bước 6 - Gán cụm cho test set

Sau khi có cấu hình tốt trên train:

1. Lưu model clusterer.
2. Mở hoặc cung cấp `ucsd_ped2_features_test.arff`.
3. Áp dụng đúng chuỗi filter đã dùng cho train.
4. Gán cluster cho test set bằng model đã train.
5. Xuất cluster assignment để đối chiếu với metadata gốc.

Lưu ý: filter train và test phải giống nhau tuyệt đối. Nếu thao tác thủ công trong Explorer dễ lệch, cần ghi rõ cấu hình filter trong log.

### Bước 7 - Lưu kết quả

Kết quả thí nghiệm nên lưu ngoài repo hoặc dưới thư mục output bị `.gitignore`:

```text
src/outputs/weka_experiments/
  spec_2_log.md
  models/
    ucsd_ped2_behavior_kmeans_k3.model
    ucsd_ped2_behavior_kmeans_k5.model
    ucsd_ped2_behavior_kmeans_k8.model
  reports/
    ucsd_ped2_behavior_kmeans_k3.txt
    ucsd_ped2_behavior_kmeans_k5.txt
    ucsd_ped2_behavior_kmeans_k8.txt
  assignments/
    ucsd_ped2_train_behavior_k5_clusters.csv
    ucsd_ped2_test_behavior_k5_clusters.csv
```

Nếu export bằng Explorer không giữ được đầy đủ metadata, cần lưu thêm mapping từ file CSV gốc:

```text
src/outputs/preprocessed/ucsd_ped2/features_train.csv
src/outputs/preprocessed/ucsd_ped2/features_test.csv
```

Mapping tối thiểu cần giữ:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cluster_id
```

## 8. Quy trình per-cell clustering trong WEKA

### 8.1. Lý do cần per-cell

PRD ưu tiên học normal pattern riêng theo từng cell/zone. Một chuyển động có thể bình thường ở vùng này nhưng bất thường ở vùng khác. Vì vậy, per-cell clustering là hướng chính sau khi global baseline chạy ổn.

### 8.2. Chọn cell thử nghiệm

Không chạy ngay toàn bộ `192` cell trong Explorer. Chọn một nhóm nhỏ:

- cell có motion density trung bình cao;
- cell có motion density trung bình thấp;
- cell ở vùng biên;
- cell ở vùng giữa khung hình;
- cell nghi ngờ có nhiều chuyển động bất thường trong test.

Danh sách khởi đầu đề xuất:

```text
04_08
05_08
06_08
07_08
08_05
08_06
09_05
09_06
```

Danh sách này chỉ là điểm bắt đầu, cần điều chỉnh sau khi xem phân bố motion thực tế.

### 8.3. Lọc theo cell trong WEKA

Cách làm thủ công:

1. Mở `ucsd_ped2_features_train.arff`.
2. Dùng filter theo instance để giữ một giá trị `cell_id`.
3. Sau khi chỉ còn một cell, dùng `Remove` để giữ các thuộc tính behavior:

```text
foreground_ratio
motion_magnitude_mean
motion_magnitude_std
motion_density
brightness_mean
brightness_delta
```

4. Dùng `Standardize`.
5. Chạy `SimpleKMeans` với `K = 3`, `5`, `8`.

Nếu thao tác lọc instance trong Explorer quá chậm, cần tạo file ARFF per-cell bằng script ở SPEC sau, rồi dùng WEKA chỉ để chạy clusterer.

### 8.4. Kết quả cần ghi cho mỗi cell

```text
dataset
cell_id
num_train_instances
algorithm
K
cluster_sizes
centroids
within_cluster_sse
nhận xét ý nghĩa cụm
```

Ví dụ nhận xét mong muốn:

```text
cell=08_05, K=5:
  C0: gần như đứng yên, brightness trung bình
  C1: chuyển động nhẹ, density thấp
  C2: chuyển động mạnh, density cao
  C3: brightness thay đổi mạnh
  C4: cụm nhỏ, có thể là nhiễu hoặc hành vi hiếm
```

## 9. Khai phá luật kết hợp trong WEKA

### 9.1. Mục tiêu

Thử dùng WEKA để khai phá luật quan hệ giữa vùng, mức chuyển động, mật độ, độ sáng và cluster. Phần này hỗ trợ trụ cột thứ hai của PRD: association rule mining.

### 9.2. Chuẩn bị dữ liệu

Apriori trong WEKA cần dữ liệu rời rạc/nominal. Với feature numeric, cần dùng:

```text
weka.filters.unsupervised.attribute.Discretize
```

Nguồn dữ liệu đề xuất:

- `ucsd_ped2_features_train.arff`;
- bản đã gắn thêm `cluster_id` từ KMeans, nếu có.

Thuộc tính nên giữ:

```text
cell_id
foreground_ratio
motion_magnitude_mean
motion_density
brightness_mean
brightness_delta
cluster_id
```

Thuộc tính nên bỏ:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
direction_hist_0..7
```

### 9.3. Rời rạc hóa

Cấu hình ban đầu:

```text
Discretize:
  bins: 3 hoặc 5
  useEqualFrequency: True
```

Mục tiêu token:

```text
motion_density = low | medium | high
motion_magnitude_mean = still | slow | medium | fast | very_fast
brightness_mean = dark | normal | bright
brightness_delta = stable | changing
cluster_id = C0 | C1 | C2 | ...
cell_id = 08_05 | ...
```

WEKA có thể tạo tên bin dạng khoảng số. Khi đưa về báo cáo, cần diễn giải lại các khoảng này thành token dễ hiểu.

### 9.4. Chạy Apriori

Vào tab `Associate`, chọn:

```text
weka.associations.Apriori
```

Cấu hình khởi đầu:

```text
lowerBoundMinSupport: 0.01
minMetric: 0.6
metricType: Confidence
numRules: 50
```

Sau đó thử:

```text
support = 0.005, 0.01, 0.02
confidence = 0.6, 0.7, 0.8
```

Kết quả cần lọc lại thủ công:

- bỏ luật quá hiển nhiên hoặc chỉ phản ánh cấu trúc grid;
- ưu tiên luật có `cell_id`, `motion_density`, `motion_magnitude_mean`, `cluster_id`;
- ghi lại luật giúp giải thích hành vi normal;
- ghi lại luật có khả năng dùng để phát hiện vi phạm trong test.

### 9.5. Ví dụ luật mong muốn

```text
cell_id=08_05 brightness_mean=normal -> motion_density=low
cell_id=06_08 motion_magnitude_mean=slow -> cluster_id=C1
cluster_id=C3 motion_density=high -> brightness_delta=stable
```

Các luật này chưa trực tiếp là bất thường. Chúng mô tả pattern phổ biến trong train normal. Bước anomaly scoring sau này sẽ dùng:

- độ hiếm của token combination;
- cluster distance;
- mức vi phạm luật có confidence/lift cao.

## 10. Thí nghiệm bắt buộc

### 10.1. Experiment 1 - UCSD Ped2 global behavior KMeans

Input:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Feature:

```text
11-14,23-24
```

Algorithm:

```text
SimpleKMeans
K = 3, 5, 8
Standardize = yes
```

Kết quả cần có:

- report cho từng K;
- nhận xét cụm;
- chọn một K hợp lý để gán test.

### 10.2. Experiment 2 - UCSD Ped2 global spatio-behavior KMeans

Input:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Feature:

```text
9-14,23-24
```

Algorithm:

```text
SimpleKMeans
K = 3, 5, 8
Standardize = yes
```

Mục tiêu:

- so sánh với behavior-only;
- xem thêm `cell_row`, `cell_col` có làm cụm bị chi phối bởi vị trí hay không.

### 10.3. Experiment 3 - UCSD Ped2 selected per-cell KMeans

Input:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Cell:

```text
04_08
06_08
08_05
09_06
```

Feature:

```text
11-14,23-24
```

Algorithm:

```text
SimpleKMeans
K = 3, 5
Standardize = yes
```

Mục tiêu:

- kiểm tra feasibility của per-cell normal pattern;
- xác định có cần tạo script per-cell ARFF hay không.

### 10.4. Experiment 4 - UCSD Ped2 EM comparison

Input:

```text
src/outputs/weka/ucsd_ped2_features_train.arff
```

Feature:

```text
11-14,23-24
```

Algorithm:

```text
EM
Standardize = yes
```

Mục tiêu:

- so sánh cluster size và ý nghĩa centroid với KMeans;
- xem EM có tạo cụm quá nhỏ hoặc khó giải thích không.

### 10.5. Experiment 5 - Apriori trên feature đã rời rạc hóa

Input:

```text
ucsd_ped2 train, ưu tiên bản sample nếu WEKA chạy chậm
```

Feature:

```text
cell_id
motion_magnitude_mean
motion_density
brightness_mean
brightness_delta
cluster_id nếu có
```

Algorithm:

```text
Discretize + Apriori
```

Mục tiêu:

- sinh tối thiểu 20 luật có thể diễn giải;
- chọn 5-10 luật tốt nhất để đưa vào báo cáo phân tích.

## 11. Đầu ra mong muốn

Sau khi triển khai SPEC 2, cần có:

```text
src/outputs/weka_experiments/
  spec_2_log.md
  models/
  reports/
  assignments/
  rules/
```

File log cần ghi:

```text
experiment_id
dataset
input_file
filter_steps
algorithm
parameters
runtime
num_instances
num_attributes
cluster_sizes
main_centroids
observations
accepted_or_rejected
reason
```

Cluster assignment cần có tối thiểu:

```text
dataset
split
video_id
cube_id
start_frame_id
end_frame_id
center_frame_id
cell_id
cluster_id
```

Rule output cần có:

```text
rule_id
antecedent
consequent
support
confidence
lift nếu có
interpretation
use_for_anomaly_reason
```

## 12. Quy ước đặt tên thí nghiệm

Định dạng:

```text
{dataset}_{scope}_{feature_set}_{algorithm}_{params}
```

Ví dụ:

```text
ucsd_ped2_global_behavior_kmeans_k5
ucsd_ped2_global_spatiobehavior_kmeans_k5
ucsd_ped2_cell_08_05_behavior_kmeans_k3
ucsd_ped2_global_behavior_em_auto
ucsd_ped2_rules_discretize5_apriori_s001_c06
```

## 13. Tiêu chí đánh giá kết quả WEKA

Một kết quả phân cụm được xem là dùng được cho bước sau nếu:

- WEKA train xong không lỗi bộ nhớ;
- cluster không bị lệch hoàn toàn vào một cụm duy nhất;
- centroid có thể diễn giải bằng motion/brightness;
- cụm nhỏ có ý nghĩa như hành vi hiếm hoặc nhiễu, không chỉ là lỗi scale;
- train/test có thể dùng cùng schema và cùng filter;
- cluster assignment truy vết được về `video_id`, `frame_id`, `cube_id`, `cell_id`;
- kết quả có thể chuyển thành token `cluster=Cx`.

Một tập luật được xem là dùng được nếu:

- luật không chỉ lặp lại quan hệ định danh vô nghĩa;
- support không quá thấp;
- confidence đủ cao để diễn giải normal pattern;
- có thể chuyển thành câu giải thích dễ hiểu;
- có thể dùng như tín hiệu phụ, không quyết định alert một mình.

## 14. Rủi ro và giảm thiểu

### 14.1. WEKA thiếu bộ nhớ khi mở ARFF lớn

Rủi ro: Avenue ARFF hơn 500 MB mỗi split, WEKA Explorer có thể chậm hoặc crash.

Giảm thiểu:

- chạy UCSD Ped2 trước;
- tăng JVM heap khi mở WEKA, ví dụ `-Xmx8g` hoặc cao hơn nếu máy đủ RAM;
- tạo sample ARFF cho Avenue;
- dùng WEKA CLI thay vì Explorer khi dữ liệu lớn.

### 14.2. KMeans bị chi phối bởi thang đo brightness

Rủi ro: `brightness_mean` có giá trị lớn hơn motion feature, làm cụm chủ yếu theo độ sáng.

Giảm thiểu:

- luôn dùng `Standardize` trước KMeans;
- so sánh kết quả có và không có brightness;
- ghi rõ filter trong log.

### 14.3. Cụm global không phản ánh normal pattern theo vùng

Rủi ro: global clustering trộn nhiều cell, làm mất ngữ cảnh không gian.

Giảm thiểu:

- chỉ dùng global clustering như baseline kiểm tra nhanh;
- triển khai per-cell clustering cho cell đại diện;
- sau SPEC 2, ưu tiên script Python hoặc WEKA CLI để chạy đủ `192` cell.

### 14.4. Direction histogram hiện chưa có ý nghĩa

Rủi ro: `direction_hist_0..7` đang bằng `0.0` với frame differencing, đưa vào model sẽ thêm cột vô ích.

Giảm thiểu:

- bỏ direction histogram khỏi baseline WEKA;
- chỉ đưa lại khi pipeline feature có optical flow hoặc hướng chuyển động đáng tin cậy.

### 14.5. Apriori sinh quá nhiều luật vụn

Rủi ro: quá nhiều token theo cell làm support thấp và luật khó tổng quát.

Giảm thiểu:

- tăng `min_support`;
- giảm số bin rời rạc hóa;
- chạy luật trên nhóm cell hoặc global trước;
- chỉ dùng luật làm tín hiệu giải thích phụ.

## 15. Checklist triển khai

- [ ] Mở được `ucsd_ped2_features_train.arff` trong WEKA.
- [ ] Kiểm tra schema và kiểu thuộc tính.
- [ ] Tạo filter Baseline A giữ `11-14,23-24`.
- [ ] Standardize feature cho Baseline A.
- [ ] Chạy `SimpleKMeans` với `K=3`.
- [ ] Chạy `SimpleKMeans` với `K=5`.
- [ ] Chạy `SimpleKMeans` với `K=8`.
- [ ] Ghi report và nhận xét centroid cho từng K.
- [ ] Chọn một cấu hình KMeans để gán test set.
- [ ] Xuất cluster assignment cho train/test.
- [ ] Chạy Baseline B giữ `9-14,23-24`.
- [ ] So sánh Baseline A và B.
- [ ] Chạy per-cell KMeans cho ít nhất 4 cell đại diện.
- [ ] Chạy EM để so sánh với KMeans.
- [ ] Rời rạc hóa feature bằng `Discretize`.
- [ ] Chạy Apriori trên train normal.
- [ ] Chọn 5-10 luật dễ giải thích.
- [ ] Ghi toàn bộ kết quả vào `src/outputs/weka_experiments/spec_2_log.md`.
- [ ] Cập nhật `src/doc/spec_2_processed.md` sau khi thực nghiệm.

## 16. Tiêu chí hoàn thành SPEC 2

SPEC 2 được xem là hoàn thành khi:

- có tối thiểu một mô hình KMeans chạy thành công trên UCSD Ped2 train;
- có so sánh `K=3`, `K=5`, `K=8`;
- có report centroid và cluster size;
- có cluster assignment cho UCSD Ped2 train/test;
- có ít nhất một thử nghiệm per-cell;
- có ít nhất một thử nghiệm Apriori trên dữ liệu đã rời rạc hóa;
- có log thực nghiệm đủ để tái hiện thao tác;
- có nhận xét cấu hình nào nên chuyển sang Python trong SPEC tiếp theo.

## 17. Hướng mở rộng sau SPEC 2

Sau khi hoàn thành SPEC 2, các bước tiếp theo nên là:

- viết script Python train per-cell MiniBatchKMeans dựa trên kết quả WEKA;
- tính distance tới centroid gần nhất;
- chọn threshold theo percentile train;
- sinh `cluster=Cx` token cho mỗi feature row;
- kết hợp cluster distance, temporal change, rare token và rule violation thành anomaly score;
- tạo `frame_scores.csv`, `cell_scores`, `alerts.json`;
- vẽ heatmap overlay để kiểm tra định tính;
- đánh giá frame-level ROC-AUC/EER nếu có ground truth.
</file>

<file path="kpdl_preprocess/__init__.py">
"""Preprocessing pipeline for KPDL anomaly detection datasets."""

__version__ = "0.1.0"
</file>

<file path="kpdl_preprocess/arff_cli.py">
from __future__ import annotations

import argparse

from .arff import convert_csv_to_arff


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Convert feature CSV to WEKA ARFF.")
    parser.add_argument("--input", required=True, help="Input feature CSV.")
    parser.add_argument("--output", required=True, help="Output ARFF path.")
    parser.add_argument("--relation", default=None, help="Optional ARFF relation name.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    output = convert_csv_to_arff(args.input, args.output, args.relation)
    print(output)
    return 0
</file>

<file path="kpdl_preprocess/arff.py">
from __future__ import annotations

import csv
from pathlib import Path


STRING_COLUMNS = {"dataset", "split", "video_id", "cube_id"}
NOMINAL_COLUMNS = {"cell_id"}


def convert_csv_to_arff(input_csv: str | Path, output_arff: str | Path, relation: str | None = None) -> Path:
    input_path = Path(input_csv)
    output_path = Path(output_arff)
    if relation is None:
        relation = _safe_relation_name(input_path.stem)

    nominal_values = _collect_nominal_values(input_path)

    with input_path.open("r", newline="", encoding="utf-8") as src:
        reader = csv.DictReader(src)
        if not reader.fieldnames:
            raise ValueError(f"CSV has no header: {input_path}")
        fieldnames = reader.fieldnames

        output_path.parent.mkdir(parents=True, exist_ok=True)
        with output_path.open("w", newline="", encoding="utf-8") as dst:
            dst.write(f"@RELATION {_quote_arff(relation)}\n\n")
            for column in fieldnames:
                if column in STRING_COLUMNS:
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} STRING\n")
                elif column in NOMINAL_COLUMNS:
                    values = nominal_values.get(column) or ["unknown"]
                    joined = ",".join(_quote_arff(value) for value in values)
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} {{{joined}}}\n")
                else:
                    dst.write(f"@ATTRIBUTE {_quote_arff(column)} NUMERIC\n")

            dst.write("\n@DATA\n")
            for row in reader:
                values = [_format_value(column, row.get(column, "")) for column in fieldnames]
                dst.write(",".join(values) + "\n")

    return output_path


def _collect_nominal_values(input_path: Path) -> dict[str, list[str]]:
    values: dict[str, set[str]] = {column: set() for column in NOMINAL_COLUMNS}
    with input_path.open("r", newline="", encoding="utf-8") as src:
        reader = csv.DictReader(src)
        for row in reader:
            for column in NOMINAL_COLUMNS:
                value = row.get(column)
                if value:
                    values[column].add(value)
    return {column: sorted(items) for column, items in values.items()}


def _format_value(column: str, value: str) -> str:
    if value == "" or value is None:
        return "?"
    if column in STRING_COLUMNS or column in NOMINAL_COLUMNS:
        return _quote_arff(value)
    try:
        float(value)
    except ValueError:
        return "?"
    return value


def _quote_arff(value: object) -> str:
    text = str(value).replace("\\", "\\\\").replace("'", "\\'")
    return f"'{text}'"


def _safe_relation_name(name: str) -> str:
    return "".join(ch if ch.isalnum() or ch == "_" else "_" for ch in name)
</file>

<file path="kpdl_preprocess/cli.py">
from __future__ import annotations

import argparse
import json
from pathlib import Path

from .pipeline import run_preprocess


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Run SPEC 1 preprocessing for UCSD/CUHK Avenue.")
    parser.add_argument("--config", required=True, help="Path to a YAML config file.")
    parser.add_argument("--project-root", default=".", help="Project root. Relative dataset paths are resolved here.")
    parser.add_argument("--output-root", default=None, help="Override output root, for example outputs/preprocessed_smoke.")
    parser.add_argument("--split", choices=["train", "test"], default=None, help="Process only one split.")
    parser.add_argument("--limit-videos", type=int, default=None, help="Limit videos/sequences per split for smoke tests.")
    parser.add_argument("--limit-frames", type=int, default=None, help="Limit frames per video/sequence for smoke tests.")
    parser.add_argument(
        "--progress-every",
        type=int,
        default=250,
        help="Print progress every N frames while processing videos/sequences. Use 0 to disable.",
    )
    parser.add_argument("--export-arff", action="store_true", help="Also export WEKA ARFF files from feature CSV outputs.")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    stats = run_preprocess(
        config_path=args.config,
        project_root=Path(args.project_root),
        output_root=args.output_root,
        split=args.split,
        limit_videos=args.limit_videos,
        limit_frames=args.limit_frames,
        progress_every=args.progress_every,
        export_arff=args.export_arff,
    )
    print(json.dumps(_public_stats(stats), indent=2))
    return 0


def _public_stats(stats: dict) -> dict:
    return {
        "dataset": stats.get("dataset"),
        "output_dir": stats.get("output_dir"),
        "splits": stats.get("splits"),
        "arff_outputs": stats.get("arff_outputs", []),
    }
</file>

<file path="kpdl_preprocess/config.py">
from __future__ import annotations

from copy import deepcopy
from pathlib import Path
from typing import Any


class ConfigError(ValueError):
    pass


def load_config(path: str | Path) -> dict[str, Any]:
    config_path = Path(path)
    if not config_path.exists():
        raise ConfigError(f"Config file not found: {config_path}")

    try:
        import yaml
    except ImportError as exc:
        raise ConfigError(
            "PyYAML is required to read YAML config files. "
            "Install dependencies with: python -m pip install -r src/requirements.txt"
        ) from exc

    with config_path.open("r", encoding="utf-8") as handle:
        data = yaml.safe_load(handle) or {}

    if not isinstance(data, dict):
        raise ConfigError(f"Config root must be a mapping: {config_path}")

    _validate_config(data)
    return data


def _validate_config(config: dict[str, Any]) -> None:
    required_sections = ["data", "video", "grid", "cube", "features"]
    missing = [section for section in required_sections if section not in config]
    if missing:
        raise ConfigError(f"Missing config section(s): {', '.join(missing)}")

    data = config["data"]
    for key in ["dataset", "root", "train_path", "test_path"]:
        if key not in data:
            raise ConfigError(f"Missing data.{key}")

    video = config["video"]
    if video.get("input_type") not in {"frame_sequence", "video"}:
        raise ConfigError("video.input_type must be 'frame_sequence' or 'video'")

    for key in ["resize_width", "resize_height"]:
        if int(video.get(key, 0)) <= 0:
            raise ConfigError(f"video.{key} must be a positive integer")

    grid = config["grid"]
    if int(grid.get("rows", 0)) <= 0 or int(grid.get("cols", 0)) <= 0:
        raise ConfigError("grid.rows and grid.cols must be positive integers")

    cube = config["cube"]
    if int(cube.get("length", 0)) <= 1:
        raise ConfigError("cube.length must be greater than 1")
    if int(cube.get("stride", 0)) <= 0:
        raise ConfigError("cube.stride must be a positive integer")


def get_nested(config: dict[str, Any], *keys: str, default: Any = None) -> Any:
    current: Any = config
    for key in keys:
        if not isinstance(current, dict) or key not in current:
            return default
        current = current[key]
    return current


def copy_config(config: dict[str, Any]) -> dict[str, Any]:
    return deepcopy(config)


def resolve_path(path_value: str | Path, project_root: str | Path) -> Path:
    path = Path(path_value)
    if path.is_absolute():
        return path
    return Path(project_root).resolve() / path
</file>

<file path="kpdl_preprocess/datasets.py">
from __future__ import annotations

from pathlib import Path

from .config import ConfigError, resolve_path
from .records import VideoSource
from .utils import sorted_natural


VIDEO_EXTENSIONS = {".avi", ".mp4", ".mov", ".mkv"}


def scan_dataset(
    config: dict,
    project_root: str | Path,
    split_filter: str | None = None,
) -> list[VideoSource]:
    data = config["data"]
    video = config["video"]
    dataset = str(data["dataset"])
    root = resolve_path(data["root"], project_root)
    input_type = str(video["input_type"])

    if not root.exists():
        raise ConfigError(f"Dataset root does not exist: {root}")

    splits = [("train", data["train_path"]), ("test", data["test_path"])]
    if split_filter:
        splits = [item for item in splits if item[0] == split_filter]
        if not splits:
            raise ConfigError(f"Unknown split: {split_filter}")

    sources: list[VideoSource] = []
    for split, split_path in splits:
        split_root = root / str(split_path)
        if not split_root.exists():
            raise ConfigError(f"Split path does not exist: {split_root}")

        if input_type == "frame_sequence":
            sources.extend(_scan_frame_sequences(dataset, split, split_root, input_type))
        elif input_type == "video":
            sources.extend(_scan_videos(dataset, split, split_root, input_type))
        else:
            raise ConfigError(f"Unsupported input_type: {input_type}")

    return sources


def _scan_frame_sequences(
    dataset: str,
    split: str,
    split_root: Path,
    input_type: str,
) -> list[VideoSource]:
    sources: list[VideoSource] = []
    for child in sorted_natural(p for p in split_root.iterdir() if p.is_dir()):
        if child.name.lower().endswith("_gt"):
            continue
        if not any(child.glob("*.tif")):
            continue
        sources.append(
            VideoSource(
                dataset=dataset,
                split=split,
                video_id=child.name,
                source_path=child,
                input_type=input_type,
            )
        )
    return sources


def _scan_videos(
    dataset: str,
    split: str,
    split_root: Path,
    input_type: str,
) -> list[VideoSource]:
    files = [
        path
        for path in split_root.iterdir()
        if path.is_file() and path.suffix.lower() in VIDEO_EXTENSIONS
    ]
    return [
        VideoSource(
            dataset=dataset,
            split=split,
            video_id=path.stem,
            source_path=path,
            input_type=input_type,
        )
        for path in sorted_natural(files)
    ]
</file>

<file path="kpdl_preprocess/features.py">
from __future__ import annotations

import numpy as np

from .config import get_nested
from .records import CellRecord, FrameRecord
from .schema import feature_columns


def extract_cube_features(
    cube_id: str,
    cube: list[FrameRecord],
    cells: list[CellRecord],
    config: dict,
) -> list[dict[str, object]]:
    method = str(get_nested(config, "features", "motion_method", default="frame_diff"))
    if method != "frame_diff":
        raise ValueError("Only features.motion_method='frame_diff' is implemented in SPEC 1 baseline")

    threshold = float(get_nested(config, "features", "frame_diff_threshold", default=15))
    direction_bins = int(get_nested(config, "features", "direction_bins", default=8))

    frames = np.stack([frame.gray for frame in cube]).astype(np.float32)
    diffs = np.abs(np.diff(frames, axis=0))
    masks = diffs > threshold

    first = cube[0]
    last = cube[-1]
    center = cube[len(cube) // 2]

    rows: list[dict[str, object]] = []
    for cell in cells:
        y1, y2, x1, x2 = cell.y1, cell.y2, cell.x1, cell.x2
        cell_diffs = diffs[:, y1:y2, x1:x2]
        cell_masks = masks[:, y1:y2, x1:x2]
        center_crop = center.gray[y1:y2, x1:x2].astype(np.float32)
        first_crop = first.gray[y1:y2, x1:x2].astype(np.float32)
        last_crop = last.gray[y1:y2, x1:x2].astype(np.float32)

        foreground_ratio = _safe_mean(np.any(cell_masks, axis=0))
        motion_density = _safe_mean(cell_masks)
        motion_magnitude_mean = _safe_mean(cell_diffs)
        motion_magnitude_std = _safe_std(cell_diffs)
        brightness_mean = _safe_mean(center_crop)
        brightness_delta = _safe_mean(last_crop) - _safe_mean(first_crop)

        row: dict[str, object] = {
            "dataset": first.dataset,
            "split": first.split,
            "video_id": first.video_id,
            "cube_id": cube_id,
            "start_frame_id": first.frame_id,
            "end_frame_id": last.frame_id,
            "center_frame_id": center.frame_id,
            "cell_id": cell.cell_id,
            "cell_row": cell.row,
            "cell_col": cell.col,
            "foreground_ratio": foreground_ratio,
            "motion_magnitude_mean": motion_magnitude_mean,
            "motion_magnitude_std": motion_magnitude_std,
            "motion_density": motion_density,
            "brightness_mean": brightness_mean,
            "brightness_delta": brightness_delta,
        }
        for bin_index in range(direction_bins):
            row[f"direction_hist_{bin_index}"] = 0.0
        rows.append(row)

    return rows


def _safe_mean(values: np.ndarray) -> float:
    if values.size == 0:
        return 0.0
    result = float(np.mean(values))
    return result if np.isfinite(result) else 0.0


def _safe_std(values: np.ndarray) -> float:
    if values.size == 0:
        return 0.0
    result = float(np.std(values))
    return result if np.isfinite(result) else 0.0
</file>

<file path="kpdl_preprocess/grid.py">
from __future__ import annotations

from .records import CellRecord


def generate_grid(width: int, height: int, rows: int, cols: int, ignore_cells: list[str] | None = None) -> list[CellRecord]:
    ignored = set(ignore_cells or [])
    cells: list[CellRecord] = []
    for row in range(rows):
        y1 = round(row * height / rows)
        y2 = round((row + 1) * height / rows)
        for col in range(cols):
            x1 = round(col * width / cols)
            x2 = round((col + 1) * width / cols)
            cell_id = f"{row:02d}_{col:02d}"
            if cell_id in ignored:
                continue
            cells.append(CellRecord(cell_id=cell_id, row=row, col=col, x1=x1, y1=y1, x2=x2, y2=y2))
    return cells


def grid_to_json(cells: list[CellRecord], width: int, height: int, rows: int, cols: int) -> dict:
    return {
        "resized_width": width,
        "resized_height": height,
        "rows": rows,
        "cols": cols,
        "num_cells": len(cells),
        "cells": [
            {
                "cell_id": cell.cell_id,
                "row": cell.row,
                "col": cell.col,
                "x1": cell.x1,
                "y1": cell.y1,
                "x2": cell.x2,
                "y2": cell.y2,
                "width": cell.width,
                "height": cell.height,
            }
            for cell in cells
        ],
    }
</file>

<file path="kpdl_preprocess/pipeline.py">
from __future__ import annotations

import csv
import json
import sys
from collections import defaultdict, deque
from datetime import datetime, timezone
from pathlib import Path

from .arff import convert_csv_to_arff
from .config import get_nested, load_config, resolve_path
from .datasets import scan_dataset
from .features import extract_cube_features
from .grid import generate_grid, grid_to_json
from .readers import iter_preprocessed_frames, probe_video
from .records import FrameRecord, VideoSource
from .schema import FRAME_MANIFEST_COLUMNS, SCHEMA_VERSION, VIDEO_MANIFEST_COLUMNS, feature_columns
from .utils import ensure_dir


def run_preprocess(
    config_path: str | Path,
    project_root: str | Path,
    output_root: str | Path | None = None,
    split: str | None = None,
    limit_videos: int | None = None,
    limit_frames: int | None = None,
    progress_every: int | None = None,
    export_arff: bool = False,
) -> dict:
    config = load_config(config_path)
    project_root = Path(project_root).resolve()
    dataset = str(get_nested(config, "data", "dataset"))
    direction_bins = int(get_nested(config, "features", "direction_bins", default=8))
    resize_width = int(get_nested(config, "video", "resize_width"))
    resize_height = int(get_nested(config, "video", "resize_height"))
    grid_rows = int(get_nested(config, "grid", "rows"))
    grid_cols = int(get_nested(config, "grid", "cols"))
    ignore_cells = list(get_nested(config, "grid", "ignore_cells", default=[]) or [])
    cube_length = int(get_nested(config, "cube", "length"))
    cube_stride = int(get_nested(config, "cube", "stride"))

    output_root_value = output_root or get_nested(config, "output", "root", default="outputs/preprocessed")
    output_base = ensure_dir(resolve_path(output_root_value, project_root) / dataset)

    cells = generate_grid(resize_width, resize_height, grid_rows, grid_cols, ignore_cells)
    grid_payload = grid_to_json(cells, resize_width, resize_height, grid_rows, grid_cols)
    with (output_base / "grid.json").open("w", encoding="utf-8") as handle:
        json.dump(grid_payload, handle, indent=2)

    sources = scan_dataset(config, project_root, split)
    sources = _limit_sources_per_split(sources, limit_videos)
    active_splits = sorted({source.split for source in sources} or ({split} if split else {"train", "test"}))

    stats = _new_stats(dataset, grid_payload, config_path, active_splits, direction_bins)
    feature_paths: dict[str, Path] = {}

    with (output_base / "frames_manifest.csv").open("w", newline="", encoding="utf-8") as frames_file, (
        output_base / "videos_manifest.csv"
    ).open("w", newline="", encoding="utf-8") as videos_file:
        frame_writer = csv.DictWriter(frames_file, fieldnames=FRAME_MANIFEST_COLUMNS)
        video_writer = csv.DictWriter(videos_file, fieldnames=VIDEO_MANIFEST_COLUMNS)
        frame_writer.writeheader()
        video_writer.writeheader()

        feature_files = {}
        feature_writers = {}
        try:
            columns = feature_columns(direction_bins)
            for split_name in active_splits:
                feature_path = output_base / f"features_{split_name}.csv"
                feature_paths[split_name] = feature_path
                handle = feature_path.open("w", newline="", encoding="utf-8")
                feature_files[split_name] = handle
                writer = csv.DictWriter(handle, fieldnames=columns, extrasaction="ignore")
                writer.writeheader()
                feature_writers[split_name] = writer

            source_total = len(sources)
            for source_index, source in enumerate(sources, start=1):
                _process_source(
                    source=source,
                    config=config,
                    cells=cells,
                    frame_writer=frame_writer,
                    video_writer=video_writer,
                    feature_writer=feature_writers[source.split],
                    stats=stats,
                    source_index=source_index,
                    source_total=source_total,
                    cube_length=cube_length,
                    cube_stride=cube_stride,
                    limit_frames=limit_frames,
                    progress_every=progress_every,
                )
        finally:
            for handle in feature_files.values():
                handle.close()

    _finalize_stats(stats)

    stats["output_dir"] = str(output_base)

    stats_path = output_base / "preprocess_stats.json"
    with stats_path.open("w", encoding="utf-8") as handle:
        json.dump(stats, handle, indent=2)

    arff_paths: list[str] = []
    if export_arff:
        weka_root_value = get_nested(config, "output", "weka_root", default="src/outputs/weka")
        weka_root = ensure_dir(resolve_path(weka_root_value, project_root))
        for split_name, feature_path in feature_paths.items():
            arff_path = weka_root / f"{dataset}_features_{split_name}.arff"
            convert_csv_to_arff(feature_path, arff_path, relation=f"{dataset}_features_{split_name}")
            arff_paths.append(str(arff_path))
        stats["arff_outputs"] = arff_paths
        with stats_path.open("w", encoding="utf-8") as handle:
            json.dump(stats, handle, indent=2)

    return stats


def _process_source(
    source: VideoSource,
    config: dict,
    cells: list,
    frame_writer: csv.DictWriter,
    video_writer: csv.DictWriter,
    feature_writer: csv.DictWriter,
    stats: dict,
    source_index: int,
    source_total: int,
    cube_length: int,
    cube_stride: int,
    limit_frames: int | None,
    progress_every: int | None,
) -> None:
    split_stats = stats["splits"][source.split]
    split_stats["num_videos"] += 1

    video_probe = probe_video(source)
    num_frames = 0
    num_cubes = 0
    original_width = video_probe.get("original_width")
    original_height = video_probe.get("original_height")
    expected_frames = _expected_frame_count(video_probe.get("num_frames_hint"), limit_frames)
    buffer: deque[FrameRecord] = deque(maxlen=cube_length)
    should_log_progress = progress_every is not None and progress_every > 0

    if should_log_progress:
        _progress(
            f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
            f"start, expected_frames={_blank_none(expected_frames)}"
        )

    try:
        for frame in iter_preprocessed_frames(source, config, limit_frames=limit_frames):
            num_frames += 1
            if original_width is None:
                original_width = frame.original_width
            if original_height is None:
                original_height = frame.original_height
            frame_writer.writerow(_frame_manifest_row(frame))
            buffer.append(frame)

            if len(buffer) == cube_length:
                start_index = num_frames - cube_length
                if start_index % cube_stride == 0:
                    num_cubes += 1
                    cube = list(buffer)
                    cube_id = f"{source.video_id}_{cube[0].frame_id:06d}_{cube[-1].frame_id:06d}"
                    rows = extract_cube_features(cube_id, cube, cells, config)
                    for row in rows:
                        feature_writer.writerow(row)
                        split_stats["num_feature_rows"] += 1
                        split_stats["_motion_density_sum"] += float(row["motion_density"])
                        split_stats["_brightness_sum"] += float(row["brightness_mean"])
                        split_stats["_feature_value_count"] += 1
            if should_log_progress and num_frames % progress_every == 0:
                _progress(
                    f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                    f"frames={num_frames}{_progress_total(expected_frames)}, cubes={num_cubes}"
                )

        split_stats["num_frames"] += num_frames
        split_stats["num_cubes"] += num_cubes
        if expected_frames is not None and num_frames < expected_frames:
            split_stats["missing_frames"] += expected_frames - num_frames
        video_writer.writerow(
            {
                "dataset": source.dataset,
                "split": source.split,
                "video_id": source.video_id,
                "source_path": str(source.source_path),
                "input_type": source.input_type,
                "fps": _blank_none(video_probe.get("fps")),
                "num_frames": num_frames,
                "num_cubes": num_cubes,
                "original_width": _blank_none(original_width),
                "original_height": _blank_none(original_height),
                "failed": False,
                "error": "",
            }
        )
        if should_log_progress:
            _progress(
                f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                f"done, frames={num_frames}{_progress_total(expected_frames)}, cubes={num_cubes}"
            )
    except Exception as exc:
        split_stats["failed_videos"] += 1
        video_writer.writerow(
            {
                "dataset": source.dataset,
                "split": source.split,
                "video_id": source.video_id,
                "source_path": str(source.source_path),
                "input_type": source.input_type,
                "fps": _blank_none(video_probe.get("fps")),
                "num_frames": num_frames,
                "num_cubes": num_cubes,
                "original_width": _blank_none(original_width),
                "original_height": _blank_none(original_height),
                "failed": True,
                "error": str(exc),
            }
        )
        if should_log_progress:
            _progress(
                f"[{source_index}/{source_total}] {source.split}/{source.video_id}: "
                f"failed after frames={num_frames}, error={exc}"
            )


def _frame_manifest_row(frame: FrameRecord) -> dict[str, object]:
    return {
        "dataset": frame.dataset,
        "split": frame.split,
        "video_id": frame.video_id,
        "frame_id": frame.frame_id,
        "timestamp": _blank_none(frame.timestamp),
        "source_path": str(frame.source_path),
        "original_width": frame.original_width,
        "original_height": frame.original_height,
        "resized_width": frame.resized_width,
        "resized_height": frame.resized_height,
    }


def _limit_sources_per_split(sources: list[VideoSource], limit_videos: int | None) -> list[VideoSource]:
    if limit_videos is None:
        return sources
    counts: dict[str, int] = defaultdict(int)
    limited: list[VideoSource] = []
    for source in sources:
        if counts[source.split] >= limit_videos:
            continue
        limited.append(source)
        counts[source.split] += 1
    return limited


def _new_stats(
    dataset: str,
    grid_payload: dict,
    config_path: str | Path,
    splits: list[str],
    direction_bins: int,
) -> dict:
    return {
        "dataset": dataset,
        "generated_at": datetime.now(timezone.utc).isoformat(),
        "config_path": str(config_path),
        "schema": {
            "version": SCHEMA_VERSION,
            "frames_manifest_columns": FRAME_MANIFEST_COLUMNS,
            "videos_manifest_columns": VIDEO_MANIFEST_COLUMNS,
            "feature_columns": feature_columns(direction_bins),
        },
        "grid": {
            "rows": grid_payload["rows"],
            "cols": grid_payload["cols"],
            "num_cells": grid_payload["num_cells"],
            "resized_width": grid_payload["resized_width"],
            "resized_height": grid_payload["resized_height"],
        },
        "splits": {split: _empty_split_stats(grid_payload["num_cells"]) for split in splits},
    }


def _empty_split_stats(num_cells: int) -> dict:
    return {
        "num_videos": 0,
        "num_frames": 0,
        "num_cubes": 0,
        "num_cells": num_cells,
        "num_feature_rows": 0,
        "missing_frames": 0,
        "failed_videos": 0,
        "avg_motion_density": 0.0,
        "avg_brightness": 0.0,
        "_motion_density_sum": 0.0,
        "_brightness_sum": 0.0,
        "_feature_value_count": 0,
    }


def _finalize_stats(stats: dict) -> None:
    for split_stats in stats["splits"].values():
        count = split_stats.pop("_feature_value_count", 0)
        motion_sum = split_stats.pop("_motion_density_sum", 0.0)
        brightness_sum = split_stats.pop("_brightness_sum", 0.0)
        if count:
            split_stats["avg_motion_density"] = motion_sum / count
            split_stats["avg_brightness"] = brightness_sum / count


def _blank_none(value: object) -> object:
    return "" if value is None else value


def _expected_frame_count(num_frames_hint: object, limit_frames: int | None) -> int | None:
    try:
        hint = int(num_frames_hint) if num_frames_hint is not None else None
    except (TypeError, ValueError):
        hint = None
    if hint is not None and hint <= 0:
        hint = None
    if limit_frames is not None:
        return min(limit_frames, hint) if hint is not None else limit_frames
    return hint


def _progress_total(expected_frames: int | None) -> str:
    return f"/{expected_frames}" if expected_frames is not None else ""


def _progress(message: str) -> None:
    print(message, file=sys.stderr, flush=True)
</file>

<file path="kpdl_preprocess/readers.py">
from __future__ import annotations

from pathlib import Path
from typing import Iterator

import numpy as np

from .config import ConfigError, get_nested
from .records import FrameRecord, VideoSource
from .utils import bool_config, sorted_natural, stem_to_frame_id


def _cv2():
    try:
        import cv2
    except ImportError as exc:
        raise ConfigError(
            "OpenCV is required for preprocessing. "
            "Install dependencies with: python -m pip install -r src/requirements.txt"
        ) from exc
    return cv2


def iter_preprocessed_frames(
    source: VideoSource,
    config: dict,
    limit_frames: int | None = None,
) -> Iterator[FrameRecord]:
    if source.input_type == "frame_sequence":
        yield from _iter_frame_sequence(source, config, limit_frames)
        return
    if source.input_type == "video":
        yield from _iter_video(source, config, limit_frames)
        return
    raise ConfigError(f"Unsupported source input_type: {source.input_type}")


def probe_video(source: VideoSource) -> dict:
    if source.input_type != "video":
        return {"fps": None, "num_frames_hint": None, "original_width": None, "original_height": None}

    cv2 = _cv2()
    cap = cv2.VideoCapture(str(source.source_path))
    if not cap.isOpened():
        return {"fps": None, "num_frames_hint": None, "original_width": None, "original_height": None}
    try:
        fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0) or None
        count = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) or None
        width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH) or 0) or None
        height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT) or 0) or None
        return {"fps": fps, "num_frames_hint": count, "original_width": width, "original_height": height}
    finally:
        cap.release()


def _iter_frame_sequence(
    source: VideoSource,
    config: dict,
    limit_frames: int | None,
) -> Iterator[FrameRecord]:
    cv2 = _cv2()
    files = sorted_natural(source.source_path.glob("*.tif"))
    if limit_frames is not None:
        files = files[:limit_frames]

    for index, image_path in enumerate(files, start=1):
        raw = cv2.imread(str(image_path), cv2.IMREAD_UNCHANGED)
        if raw is None:
            continue
        gray, original_width, original_height = preprocess_frame(raw, config)
        yield FrameRecord(
            dataset=source.dataset,
            split=source.split,
            video_id=source.video_id,
            frame_id=stem_to_frame_id(image_path, index),
            timestamp=float(index - 1),
            source_path=image_path,
            original_width=original_width,
            original_height=original_height,
            resized_width=gray.shape[1],
            resized_height=gray.shape[0],
            gray=gray,
        )


def _iter_video(
    source: VideoSource,
    config: dict,
    limit_frames: int | None,
) -> Iterator[FrameRecord]:
    cv2 = _cv2()
    cap = cv2.VideoCapture(str(source.source_path))
    if not cap.isOpened():
        raise ConfigError(f"Cannot open video: {source.source_path}")

    fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0)
    frame_count_hint = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) or None
    safety_limit = _video_safety_limit(frame_count_hint, limit_frames)
    frame_index = 0
    try:
        while True:
            if limit_frames is not None and frame_index >= limit_frames:
                break
            if safety_limit is not None and frame_index >= safety_limit:
                break
            ok, raw = cap.read()
            if not ok:
                break
            frame_index += 1
            gray, original_width, original_height = preprocess_frame(raw, config)
            timestamp = (frame_index / fps) if fps > 0 else None
            yield FrameRecord(
                dataset=source.dataset,
                split=source.split,
                video_id=source.video_id,
                frame_id=frame_index,
                timestamp=timestamp,
                source_path=source.source_path,
                original_width=original_width,
                original_height=original_height,
                resized_width=gray.shape[1],
                resized_height=gray.shape[0],
                gray=gray,
            )
    finally:
        cap.release()


def preprocess_frame(raw: np.ndarray, config: dict) -> tuple[np.ndarray, int, int]:
    cv2 = _cv2()
    resize_width = int(get_nested(config, "video", "resize_width"))
    resize_height = int(get_nested(config, "video", "resize_height"))

    original_height, original_width = raw.shape[:2]
    resized = cv2.resize(raw, (resize_width, resize_height), interpolation=cv2.INTER_AREA)

    if resized.ndim == 2:
        gray = resized
    elif resized.shape[2] == 4:
        gray = cv2.cvtColor(resized, cv2.COLOR_BGRA2GRAY)
    else:
        gray = cv2.cvtColor(resized, cv2.COLOR_BGR2GRAY)

    if gray.dtype != np.uint8:
        gray = np.clip(gray, 0, 255).astype(np.uint8)

    if bool_config(get_nested(config, "video", "blur", "enabled"), default=False):
        kernel_size = int(get_nested(config, "video", "blur", "kernel_size", default=3))
        if kernel_size % 2 == 0:
            kernel_size += 1
        kernel_size = max(kernel_size, 1)
        if kernel_size > 1:
            gray = cv2.GaussianBlur(gray, (kernel_size, kernel_size), 0)

    if bool_config(get_nested(config, "video", "brightness_normalization", "enabled"), default=False):
        gray = cv2.equalizeHist(gray)

    return gray, original_width, original_height


def _video_safety_limit(frame_count_hint: int | None, limit_frames: int | None) -> int | None:
    if frame_count_hint is None or frame_count_hint <= 0:
        return limit_frames
    tolerance = max(100, int(frame_count_hint * 0.05))
    guarded_limit = frame_count_hint + tolerance
    return min(guarded_limit, limit_frames) if limit_frames is not None else guarded_limit
</file>

<file path="kpdl_preprocess/records.py">
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Any


@dataclass(frozen=True)
class VideoSource:
    dataset: str
    split: str
    video_id: str
    source_path: Path
    input_type: str


@dataclass(frozen=True)
class FrameRecord:
    dataset: str
    split: str
    video_id: str
    frame_id: int
    timestamp: float | None
    source_path: Path
    original_width: int
    original_height: int
    resized_width: int
    resized_height: int
    gray: Any


@dataclass(frozen=True)
class CellRecord:
    cell_id: str
    row: int
    col: int
    x1: int
    y1: int
    x2: int
    y2: int

    @property
    def width(self) -> int:
        return self.x2 - self.x1

    @property
    def height(self) -> int:
        return self.y2 - self.y1
</file>

<file path="kpdl_preprocess/schema.py">
from __future__ import annotations

SCHEMA_VERSION = "spec_1.common.v1"

FRAME_MANIFEST_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "frame_id",
    "timestamp",
    "source_path",
    "original_width",
    "original_height",
    "resized_width",
    "resized_height",
]

VIDEO_MANIFEST_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "source_path",
    "input_type",
    "fps",
    "num_frames",
    "num_cubes",
    "original_width",
    "original_height",
    "failed",
    "error",
]

BASE_FEATURE_COLUMNS = [
    "dataset",
    "split",
    "video_id",
    "cube_id",
    "start_frame_id",
    "end_frame_id",
    "center_frame_id",
    "cell_id",
    "cell_row",
    "cell_col",
    "foreground_ratio",
    "motion_magnitude_mean",
    "motion_magnitude_std",
    "motion_density",
]

TAIL_FEATURE_COLUMNS = [
    "brightness_mean",
    "brightness_delta",
]


def feature_columns(direction_bins: int = 8) -> list[str]:
    return (
        BASE_FEATURE_COLUMNS
        + [f"direction_hist_{index}" for index in range(direction_bins)]
        + TAIL_FEATURE_COLUMNS
    )
</file>

<file path="kpdl_preprocess/utils.py">
from __future__ import annotations

import re
from pathlib import Path
from typing import Iterable


def ensure_dir(path: str | Path) -> Path:
    resolved = Path(path)
    resolved.mkdir(parents=True, exist_ok=True)
    return resolved


def natural_key(value: str | Path) -> list[object]:
    text = Path(value).name if isinstance(value, Path) else str(value)
    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", text)]


def sorted_natural(paths: Iterable[Path]) -> list[Path]:
    return sorted(paths, key=natural_key)


def stem_to_frame_id(path: Path, fallback: int) -> int:
    match = re.search(r"\d+", path.stem)
    return int(match.group(0)) if match else fallback


def bool_config(value: object, default: bool = False) -> bool:
    if value is None:
        return default
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        return value.strip().lower() in {"1", "true", "yes", "y", "on"}
    return bool(value)
</file>

<file path="preprocess.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from kpdl_preprocess.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="requirements.txt">
numpy>=1.24
opencv-python-headless>=4.8
PyYAML>=6.0
</file>

<file path="tool/__init__.py">
"""Dataset-specific preprocessing entrypoints."""
</file>

<file path="tool/_common.py">
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

SRC_ROOT = Path(__file__).resolve().parents[1]
PROJECT_ROOT = SRC_ROOT.parent

if str(SRC_ROOT) not in sys.path:
    sys.path.insert(0, str(SRC_ROOT))

from kpdl_preprocess.pipeline import run_preprocess


def add_common_args(parser: argparse.ArgumentParser) -> None:
    parser.add_argument("--project-root", default=str(PROJECT_ROOT), help="Project root for resolving dataset paths.")
    parser.add_argument("--output-root", default=None, help="Override output root, for example outputs/preprocessed.")
    parser.add_argument("--split", choices=["train", "test"], default=None, help="Process only one split.")
    parser.add_argument("--limit-videos", type=int, default=None, help="Limit videos/sequences per split for smoke tests.")
    parser.add_argument("--limit-frames", type=int, default=None, help="Limit frames per video/sequence for smoke tests.")
    parser.add_argument(
        "--progress-every",
        type=int,
        default=250,
        help="Print progress every N frames while processing videos/sequences. Use 0 to disable.",
    )
    parser.add_argument("--export-arff", action="store_true", help="Also export WEKA ARFF files from feature CSV outputs.")


def run_dataset(args: argparse.Namespace, config_path: Path) -> int:
    stats = run_preprocess(
        config_path=config_path,
        project_root=Path(args.project_root),
        output_root=args.output_root,
        split=args.split,
        limit_videos=args.limit_videos,
        limit_frames=args.limit_frames,
        progress_every=args.progress_every,
        export_arff=args.export_arff,
    )
    print(json.dumps(_summary(stats), indent=2, ensure_ascii=False))
    return 0


def _summary(stats: dict) -> dict:
    schema = stats.get("schema", {})
    return {
        "dataset": stats.get("dataset"),
        "output_dir": stats.get("output_dir"),
        "schema_version": schema.get("version"),
        "feature_columns": schema.get("feature_columns"),
        "splits": stats.get("splits"),
        "arff_outputs": stats.get("arff_outputs", []),
    }
</file>

<file path="tool/preprocess_avenue.py">
from __future__ import annotations

import argparse
from pathlib import Path

from _common import PROJECT_ROOT, add_common_args, run_dataset


DEFAULT_CONFIG = PROJECT_ROOT / "src" / "configs" / "avenue.yaml"


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Preprocess CUHK Avenue into the common SPEC 1 schema.")
    parser.add_argument("--config", default=None, help="Optional config override. Defaults to src/configs/avenue.yaml.")
    add_common_args(parser)
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    config_path = Path(args.config) if args.config else DEFAULT_CONFIG
    return run_dataset(args, config_path)


if __name__ == "__main__":
    raise SystemExit(main())
</file>

<file path="tool/preprocess_ucsd.py">
from __future__ import annotations

import argparse
from pathlib import Path

from _common import PROJECT_ROOT, add_common_args, run_dataset


CONFIGS = {
    "ped2": PROJECT_ROOT / "src" / "configs" / "ucsd_ped2.yaml",
    "ped1": PROJECT_ROOT / "src" / "configs" / "ucsd_ped1.yaml",
}


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Preprocess UCSD Ped1/Ped2 into the common SPEC 1 schema.")
    parser.add_argument("--ped", choices=sorted(CONFIGS), default="ped2", help="UCSD subset to process.")
    parser.add_argument("--config", default=None, help="Optional config override. If omitted, --ped selects a config.")
    add_common_args(parser)
    return parser


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    config_path = Path(args.config) if args.config else CONFIGS[args.ped]
    return run_dataset(args, config_path)


if __name__ == "__main__":
    raise SystemExit(main())
</file>

</files>
</file>

<file path="src/requirements.txt">
# Runtime dependencies for the KPDL anomaly detection pipeline.
#
# Install from the repository root with:
#   python -m pip install -r src/requirements.txt

# Numeric arrays and feature processing.
numpy>=1.24

# Video/image IO, frame preprocessing, heatmap rendering, and MP4 export.
opencv-python-headless>=4.8

# YAML configuration files in src/configs/.
PyYAML>=6.0

# Per-cell MiniBatchKMeans and StandardScaler models.
scikit-learn>=1.3

# Model/scaler serialization.
joblib>=1.3
</file>

</files>
